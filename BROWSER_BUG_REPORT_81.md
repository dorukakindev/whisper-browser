# BROWSER_BUG_REPORT_81 — Yeni raporların güncel kodda ayıklanması

Tarih: 2026-09-19 21:34 (Europe/Istanbul). Repo `dorukakindev/whisper-browser`, dal `master`, incelenen HEAD `cc81280533834d6f4c3fe1d1880dc80f9c36bd14`. **Salt denetim: ürün kodu değiştirilmedi.** Bu belge diğer AI'ya düzeltme listesi olarak verilebilir; raporların iddiaları talimat değil, doğrulanacak aday olarak ele alındı.

## Kapsam ve karar ölçütü

Yeni tarihli `BROWSER_BUG_REPORT_76–80.md` ile `PROGRAM_BUG_REPORT_79–82.md` tarandı. `BROWSER_BUG_REPORT_107_196.md` 7 Eylül tarihli eski bir toplu dosya; içindeki “90 doğrulanmış hata” ifadesi bugünkü kodun kanıtı sayılmadı. Aşağıdaki **en fazla 15 bağımsız kök neden** için güncel kaynak yolu ve sentetik yeniden üretim / doğrudan davranış kanıtı var. Başka bulguların burada olmaması yanlış oldukları anlamına gelmez; bu turda kapatma kararı verilmedi. P seviyeleri eski raporlardaki P0/P1 etiketlerinden bağımsız, bugünkü kullanıcı etkisine göre yeniden değerlendirildi.

## Düzeltmeye verilebilecek doğrulanmış bulgular

| # | Eski rapor | Güncel karar ve kanıt | Öncelik / kabul koşulu |
|---|---|---|---|
| 1 | `PROGRAM_BUG_REPORT_80/81/82` B80-01 | **Gerçek gizlilik hatası.** `src/settings-security.js:166-178` `translateBaseUrl` sorgusunu denetlemiyor; `:505-523` yalnız bilinen sır *değerlerini* redakte ediyor. `createBackupPayload({ui:{translateBaseUrl:'https://example.test/v1?api_key=SYNTHETIC_SECRET'}},...)` → `secretsExcluded:true` iken URL ve değer aynen çıktı. | **P2.** Yedekte URL-sırrı bulunmamalı; sır içeren endpoint reddi veya redaksiyonu, güvenli query'lerin korunması ve import/export testi. |
| 2 | `BROWSER_BUG_REPORT_78` R78-01 | **Gerçek gizlilik açığı.** `src/browser-sensitive-keys.js:22-58` listesinde `auth_token` ve `PHPSESSID` yok; `src/browser-place-url.js:24-41,48+` iki kalıcılık yolunda da bunları saklıyor. Sentetik URL `?auth_token=x&page=2` ve `?PHPSESSID=x&page=2` iki fonksiyondan da değişmeden geçti. | **P2.** Gizli sorgu anahtarlarının snake/kebab/camel varyantlarını test matrisine al; güvenli `page` kalsın. Gerçek token kullanma. |
| 3 | `BROWSER_BUG_REPORT_78` R78-02 | **Gerçek, daha dar gizlilik açığı.** `src/browser-place-url.js:24-41,48+` yalnız query/hash temizliyor. `https://example.test/a;jsessionid=123/b` çıktısı aynı kaldı. | **P2.** Bilinen path-parametre oturum belirteçlerini yalnız ilgili biçimde temizle; sıradan path'i bozma. |
| 4 | `BROWSER_BUG_REPORT_77/78` B1/R78-04 | **Gerçek veri kaybı davranışı, çözüm politikası dikkat ister.** `src/browser-sensitive-keys.js:22-58` `code`, `state`, `session`, `client_id`, `exp` gibi çok anlamlı adları koşulsuz hassas sayıyor. `safePlaceUrl('https://example.test/watch?code=7&page=2')` → yalnız `page=2` kaldı. Gizlilik adına tüm bu adları körlemesine serbest bırakmak da yanlış olur. | **P2.** Sırla kamuya açık parametreyi bağlama göre ayır; OAuth/oturum testleri ve normal site URL'si testleri birlikte geçmeli. |
| 5 | `PROGRAM_BUG_REPORT_82` B82-01 | **Gerçek yerel sır maruziyeti.** `src/main.js:1539-1542` Invidious parolasını `--password` argv'sine koyuyor; `backend/invidious.py:930` bunu alıyor. Süreç komut satırı yerel süreç gözlemcisince okunabilir. | **P3.** Parola env veya stdin üzerinden geçmeli, argv/log'da görünmemeli; mevcut giriş davranışı korunmalı. |
| 6 | `PROGRAM_BUG_REPORT_80/81` B80-02 | **Mekanizma gerçek; üretim etkisi latent.** `src/main.js:15571-15600` üretim `probeCommand` yalnız stdout okuyor, stderr tüketmiyor ve stdout'u sınırsız biriktiriyor; `src/process-io.js:5-36` ikinci kopya da stderr okumuyor. Büyük stderr çocuk süreci kilitleyebilir. Mevcut normal `ffmpeg -version` çıktısı için çökme gösterilmedi. | **P3.** Kontrollü sahte child ile büyük stderr/sınırsız stdout koşuları; beklenen hızlı, sınırlı, deterministik başarısızlık. |
| 7 | `BROWSER_BUG_REPORT_76` K2 | **Gerçek uç durum veri kaybı.** `src/browser-session-store.js:239-246` ilk 24 sekmeyi alıp aktif 25. sekmeyi 24.'nün üstüne yazıyor. 25 sentetik sekme/aktif `t24` denemesinde çıktı 24 sekme; `t23` yok, son üçlü `t21,t22,t24`. | **P3.** 24 sınırı korunurken hangi sekmenin atıldığı kullanıcıya doğru bildirilmeli; aktif sekme korunmalı, sessiz kayıp olmamalı. |
| 8 | `BROWSER_BUG_REPORT_77` B2 | **Gerçek sıralama kusuru.** `src/browser-chapters.js:20-35` son sıralayıcı eşit başlangıçlı iki native öğede de `-1` döndürüyor; `mergeBrowserChapters([{start:10,title:'A'},{start:10,title:'B'}],[])` sonucu `B,A`. | **P3.** Eşit native kayıtlar için karşılaştırıcı 0 dönmeli, kararlı girdi sırası korunmalı. |
| 9 | `BROWSER_BUG_REPORT_78` R78-03 | **Gerçek biçim kaybı.** `src/browser-subtitles.js:12-47` adlı HTML entity sözlüğü eksik. `cleanCueText('El ni&ntilde;o comi&oacute; &eacute;')` → `El ni&ntilde;o comi&oacute; é`. Sayısal entity yolu ayrı ve burada suçlanmıyor. | **P2.** Yaygın çok dilli adlı entity örnekleri VTT/TTML→SRT boyunca düzgün çözülmeli; tek-geçiş ve kontrol karakteri savunması korunmalı. |
| 10 | `BROWSER_BUG_REPORT_76` F1/F2 | **Gerçek arama kusuru, tek kök neden.** `src/browser-transcript-search.js:6-8` NFKD'den *sonra* `tr-TR` küçültme kullanıyor. `foldTranscriptText('İstanbul')='ıstanbul'`, ASCII `istanbul` farklı; İngilizce `I` de `ı` oluyor. | **P2.** Türkçe `İ/i` ve `I/ı` ile İngilizce `I/i` aramasını fixture'larla birlikte sınayıp katlama politikasını düzelt. |
| 11 | `BROWSER_BUG_REPORT_76` F3 | **Gerçek kapsam boşluğu.** Aynı fonksiyondaki ASCII-ağırlıklı regex `foldTranscriptText('日本語')=''` üretiyor; CJK sorgu tokenları kayboluyor. | **P2** (ürünün hedef dilleri için). CJK metin/sorgu korunmalı ve en az bir ayırt edici isabet dönmeli. |
| 12 | `BROWSER_BUG_REPORT_76` F4 | **Gerçek altyazı arama sıralaması kusuru.** `src/browser-subtitle-search.js:103-119` `İ` harfinin birleşik noktası kelimeyi bölüyor. `scoreCandidate(title='İyi Kötü ve Çirkin',query='iyi kotu ve cirkin')` → 0. Eski rapordaki “aynı başlık da eşleşmez” alt iddiası **yanlış**; aynı Unicode girdisi iki tarafta aynı biçimde bozuluyor. | **P2.** ASCII yazımlı Türkçe sorgu başlığı eşlemeli; birebir Unicode sorgu regresyona uğramamalı. |
| 13 | `PROGRAM_BUG_REPORT_79/81` P79-01 | **Gerçek ölçülebilir verimsizlik, veri hatası değil.** `backend/subtitle_sdh.py:31-56` her çağrıda `_SDH` üzerindeki normalize kümesini yeniden kuruyor (yaklaşık 65 `_key` tekrar). | **P3/perf.** Ön-hesapla; mevcut SDH fixture'larının çıktısı değişmesin, hedefli mikro benchmark önce/sonra raporlansın. |
| 14 | `PROGRAM_BUG_REPORT_79/81` P79-04 | **Gerçek mimari maliyet, ürün hatası koşullu.** `src/browser-media-tools.js:50-60` semantik aramada her isteğe Python child açıyor; `backend/browser_media_tools.py:67+` modeli `semantic_search` içinde yüklüyor, `main()` tek istek tüketiyor. | **P3/perf.** Aynı oturumda iki aramanın süre/RAM ölçümü alınmadan kalıcı işçi mimarisi dayatılmamalı; önbellek/işçi yaşam döngüsü güvenli kapanmalı. |
| 15 | `PROGRAM_BUG_REPORT_81` D81-01 | **Gerçek yerelleştirme boşluğu; “262” sayısı doğrulanmış kesin adet değil.** `src/renderer/renderer.js:149,582,3262` gibi kullanıcı metinleri Türkçe; `src/renderer/ui-locale.js` içinde bu örnekler yok. Önceki statik tarama 262 aday bulmuş ama interpolasyon/veri metni ayrımını yapmamış. | **P3.** İngilizce varsayılan modda seçilmiş ekranların gerçek görünümünü doğrula; yalnız kullanıcı arayüzü metinlerini iki dilli yap, altyazı/başlık/veriyi çevirmeye sokma. |

## Yanlış pozitif, kapanmış veya henüz “düzelt” denmemesi gerekenler

- `BROWSER_BUG_REPORT_78` R78-05 (iki okuyucu seçme uygulaması), R78-06 (üç modül testte adı geçmiyor), `PROGRAM_BUG_REPORT_81` SL1/SL4 (kopya kod): **bakım/test borcu; tek başına kullanıcıya ulaşan bir hata kanıtı değil.** Bunu P0/P1 “bug” diye vermeyin.
- `BROWSER_BUG_REPORT_78` içinde `sanitizePlaces` zoom'u siliyor tarzı eski iddia bugün **yanlış**: `src/browser-session-package.js:61-67` `siteZooms = normalizeBrowserSiteZooms(raw.siteZooms)` ile koruyor.
- `BROWSER_BUG_REPORT_79` OAuth/TV görünüm bulguları `59174a7` ile; `BROWSER_BUG_REPORT_80` SmartTube kontrol/akış/yeniden deneme bulguları `801e75f` ve `856c0c2` ile teslim edilmiş. Bu raporlar **bugünkü açık işler listesi değil**. Gerçek hesapla canlı kabul hâlâ ayrı bir sınır; mock test bunu kapatmaz.
- `PROGRAM_BUG_REPORT_79` P79-02 (TM `SequenceMatcher`) ve P79-03 (DP 50–90 ms): kodda pahalı yol mevcut ama rapordaki rakamlar bu HEAD üzerinde bağımsız yeniden ölçülmedi; **performans adayları**, doğrudan düzeltme emri değil. Semantik TM kapısını gevşetmeyin.
- `BROWSER_BUG_REPORT_77` A1/A2/A3 (`mediaKeySystem`, `mediaTypes`, `ask` izin semantiği): kodda olası uyumsuzluk işaretleri mevcut (`src/browser-site-permissions.js:1-5`, `src/main.js:11103,11150-11167`), fakat bu turda gerçek Electron izin olayı veya DRM akışı çalıştırılmadı. **Önemli, ancak runtime/izolasyonlu Electron testi bekleyen aday** olarak verin; “DRM kesin kırık” demeyin.
- `BROWSER_BUG_REPORT_76` F5–F8, K1; `BROWSER_BUG_REPORT_77` diğer B/C/D/SL maddeleri; `BROWSER_BUG_REPORT_78` R78-07 ve `PROGRAM_BUG_REPORT_81` localStorage notu: bu turda tüm erişilebilir yolları ve kullanıcı etkisini yeniden sınamadım. Eski raporlar incelenmeli; bu belge bunları ne doğrular ne reddeder.

## Diğer AI için çalışma sözleşmesi

Önce güncel HEAD'i ve eşzamanlı değişiklikleri kontrol et. Her maddeye **önce kırmızı test, sonra dar düzeltme** yap; aynı kökenli #1–4'ü tek URL-gizlilik tasarımı altında ama ayrı kabul fixture'larıyla ele al. Gerçek anahtar, hesap, kullanıcı profili veya canlı altyazı kullanma. Testi sırf geçirmek için eşik gevşetme. Sonuçta her maddenin `düzeldi / kısmen / ertelendi` durumunu, test komutunu, eski rapordaki yanlış alt iddiayı ve canlı Electron/servis sınırını ayrı yaz. Bu rapor ürün kodunu değiştirmedi; tam `npm test` bu denetim turunda çalıştırılmadı.
