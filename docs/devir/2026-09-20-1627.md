# Devir — 2026-09-20 16:27 (Europe/Istanbul)

Repo: `dorukakindev/whisper-browser` — dal `master`.
Başlangıç kod commit'i: `4460097` (Parti 6 devir) → bitiş kod commit'i: `a6c58f4`.
Kapsam: FEATURE_RECOMMENDATION_CATALOG_2026-09-20.md E maddelerinden Parti 7 —
A23 + A17 (7ab, commit `4b065de`) ve A06 + A07 (7c, commit `a6c58f4`).

## Teslim edilen maddeler

### A23 — SmartTube arama önerileri (commit 4b065de)
- `backend/invidious.py`: `suggest(query, instance)` — `GET /api/v1/search/suggestions?q=` → `emit("suggestions", ...)`; CLI'ya `suggest` komutu.
- `src/main.js`: `INVIDIOUS_RESULT_TYPES`'a `'suggestions'`; `invidious:suggest` IPC —
  ayrı `mediaJobs['invidious-suggest']` anahtarı (canlı arama önerisi ana `invidious`
  işini kilitlemez), 12 sn timeout.
- `src/preload.js`: `invidiousSuggest`.
- `src/renderer/renderer.js` (~24441): `stSuggestSeq`/`stSuggestTimer`/`stHideSuggest`/
  `stRenderSuggest`/`stFetchSuggest` — 300 ms debounce, seq + input-eşleşme bekçisiyle
  bayat yanıtlar düşer, yanıttaki `instance` alanı kutunun altında görünür
  ("istek uzak instance'a gitti" koşulu).
- `index.html`: `#stSuggestBox` (`.st-search-wrap` içinde, `position:relative` — overflow
  clip kaldırıldı); `styles.css` `.st-suggest*` stilleri.
- Klavye: ArrowDown kutuya, ArrowUp/Esc geri; Enter anında arama (mevcut Y3 debounce
  sözleşmesi korundu — `if (searchInp)` bloğu vm-extract testlerinde çalışması için
  `typeof === 'function'` gardları kullanıldı).
- Testler: `backend/test_invidious.py` `InvidiousSuggestions` (3 test), `tests/browser-parti7.test.js`.

### A17 — mpv/VLC'ye devir (commit 4b065de)
- `src/main.js` (~14724): `EXTERNAL_PLAYER_SPECS` {mpv, vlc} +
  `resolveExternalPlayerPath(name)` — `WHISPER_MPV_PATH`/`WHISPER_VLC_PATH` env,
  platform aday yolları, `where.exe`/`which` taraması; kabul edilen yol
  `basename(p).toLowerCase().startsWith(stem)` doğrulamasından geçer (rastgele exe
  yönlendirmesi kapalı). `player:external` IPC: `authorizedBrowserSender`, URL için
  `decideUrlPolicy` (yalnız http/https), yerel dosya için `authorizeMediaFile` —
  imzalı/DRM akış URL'si veya keyfile asla dışarı verilmez; yalnız kanonik watch URL'si.
- `preload.js`: `openInExternalPlayer`.
- `renderer.js`: `stPlayExternal` (~24105) + kart menüsüne "mpv/VLC ile oynat" +
  tarayıcı sekme menüsüne aynı öğeler (yalnız http/https sayfada etkin).
- Bulunan yol kayıtlı tutulur; oynatıcı `spawn` wrapper ile detached başlatılır
  (sırlar env'den sıyrılır; `settings-security.test.js` rawSpawn sayımı buna bağlı).

### A06 — yerel abonelikler + gruplar (commit a6c58f4)
- `renderer.js` (~24545): `stSubscriptions` localStorage kaydı (maks 500), kimlik =
  UCID veya `@handle` (tekilleştirme `stSubKey`); `stSubToggle`, `stSubGroups`,
  `renderStSubsPanel(grid)`.
- "Abonelikler" bölümü YouTube oturumu YOKSA uzak akış yerine yerel paneli çizer
  (`section === 'subscriptions' && !youtubeLoggedIn` erken dönüşü).
- Panel: "Kanal ekle" (UCID/@handle/kanal URL'si → `SubscriptionIO.normSub`),
  "İçe aktar", "Dışa aktar" düğmeleri; grup `.st-chip` satırı (`grid.dataset.stSubGroup`);
  satırda isim → kanal sayfası (UCID) veya youtube.com/@handle tarayıcı sayfası;
  "Grup" düzenle (boş = grupsuz; iptal `false` döner — temizleme için boş string);
  "✕" kaldır. İpucu satırı: liste cihazda tutulur, uzak hesaba yüklenmez.
- Giriş noktaları: SmartTube kart menüsü "Kanala abone ol/çık" (authorId'li kartlarda),
  kanal sayfası başlığında aynı durum düğmesi.

### A07 — OPML/CSV/JSON/NewPipe içe-dışa aktarma (commit a6c58f4)
- Yeni modül `src/subscriptions-io.js` (UMD — hem renderer `window.SubscriptionIO`
  hem Node `require` için):
  - `channelRefFromUrl` — `UC[A-Za-z0-9_-]{20,24}` veya `/@handle`; YouTube dışı servis
    URL'leri (örn. NewPipe'daki PeerTube) düşer.
  - `parseSubscriptions(text, fileName)` → `{ok, format, subs, groups}`: NewPipe
    `newpipe_subscriptions.json`, kendi JSON'u (`{channels, groups}` ya da dizi),
    OPML `<outline>` (`xmlUrl`/`htmlUrl`/`channel_id=`), CSV `Kanal,URL`.
  - `exportSubscriptions(subs, groups, format)` → `{fileName, text}`:
    `whisper-abonelikler.{json,opml,csv}` veya `newpipe_subscriptions.json`.
    CSV alanları tırnak içinde (`"` → `""`); OPML attribute'ları XML-escape.
    Asla parola/token/`sid` içermez — yalnız kimlik + isim + grup.
- `main.js`: `subscriptions:import`/`subscriptions:export` IPC — `authorizedBrowserSender`,
  `showOpenDialog`/`showSaveDialog`, ≤ 2 MB metin, dışa aktarımda dosya adı whitelist
  (`/^whisper-abonelikler\.(json|opml|csv)$|^newpipe_subscriptions\.json$/`), JSON'da
  fs.writeFileSync UTF-8.
- `preload.js`: `importSubscriptions`/`exportSubscriptions`; `index.html` script etiketi.
- `styles.css`: `.st-subs-head/.st-sub-list/.st-sub-row/.st-sub-name/.st-sub-group/.st-subs-hint`.
- `ui-locale.js`: Kanal ekle / İçe aktar / Grup / Ekle / Abonelikten çık / Kanala abone ol.

## Doğrulama
- `node tests/run-all.js` → "Tüm testler geçti" (7c sonrası tam koşu).
- Ara koşu sırasında 2 hata yakalanıp düzeltildi: `report67` Y3 vm-extract testi için
  searchInp bloğundaki `stSuggest*` erişimleri `typeof` gardlı hale getirildi;
  `settings-security` rawSpawn sayımı için `spawn`/`spawnSync` wrapper'a geçildi.
- `node --check` değişen tüm dosyalarda temiz; Python backend testleri yeşil.
- Electron/E2E UI doğrulaması bu ortamda koşulmadı (bilinen 4 smoke sınırı gibi).

## Bilinen sınırlar / notlar
- A06 yerel liste uzak YouTube/Invidious abonelikleriyle senkronize değildir — kasıtlı
  (katalog maddesi: "Invidious oturumu olmasa çalışır").
- `@handle` aboneleri kanal sayfası yerine tarayıcıda youtube.com/@handle açar
  (UCID'siz kayıtlar Invidious kanal endpoint'iyle çözümlenmez).
- Sıradaki E paketi: A28 (playlist detay sayfası), A04 (bölüm/sekme düzeni), A27 (kanal sekmeleri).
