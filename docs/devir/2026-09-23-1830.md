# Devir Notu — 2026-09-23 18:30 UTC — R115 UI/UX denetim düzeltmeleri

- **Dal:** `codex/r115-ui-audit-1790173960` (master `7173d42` üzerine)
- **Konu:** Kullanıcının gönderdiği iki listeli UI/UX denetim raporunun doğrulanması ve doğrulanan kusurların düzeltilmesi.

## Değişen dosyalar
- `src/renderer/index.html` — kontrol çubuğu yeniden yerleşimi (seek üst satır, pc-group sol/sağ, `playerMore` ⋯ menüsü, sleepTimer menüye, fullscreen en sağa); `player-head-actions` 3 grup + `ph-sep` + `ph-more`; `playerHeadFullscreen` kaldırıldı; browserPageTranslate menü yinelenmesi kaldırıldı; zoom/diagnostics/signal araç çubuğundan gizlendi; sayfa-çevirisi menüsüne "Gelişmiş ayarlar" details'i; `playerEmpty` CTA'ları; kısayol yardımında Backspace satırı.
- `src/renderer/styles.css` — `.player-controls .btn-icon` 4 kural → tek canonical blok (temadan bağımsız koyu-cam: `rgba(0,0,0,.45)` + `backdrop-filter:blur(8px)` + `#fff`); 46→38px (dar ekran 34px); pasif kare artefaktı kaldırıldı; `.player-stage` zemin `#000` (açık temada `--bg-0` bejiyordu); `html[data-theme="light"] .player-body{background:#15181d}`; `.player-side.no-cues` gizleme + `.cue-empty-rich/cta` kartı; `ph-sep/ph-more` stilleri; `menuFadeIn` 120ms + reduced-motion; çeviri menüsü sola yaslama; `.mini-toggle` 12px.
- `src/renderer/renderer.js` — Esc zinciri (⋯ menüleri → tam ekran; oyuncudan çıkış yalnız Backspace/geri); `wireDetailsMenuNav` ok/Home/End klavye gezinmesi + Esc odak iadesi (6 details menüsü); `updatePlayerMeta()` (metaBase + süre + SRT/TR rozetleri + CANLI, UiLocale.t ile); `renderCueList` boş durum kartı (locale-aware, CTA'lı); `syncPlayerEmpty()`; interfaceChoice locale düzeltmeleri (updatePresetDiff/updateSignalDesk); adres çubuğu focus→select-all; ui-locale-change dinleyicisi; `playerHeadFullscreen` dinleyicisi kaldırıldı.
- `src/renderer/ui-locale.js` — ~85 yeni TR→EN anahtar (boş durum kartı, ⋯ menü öğeleri, meta dizgileri, sistematik taramayla bulunan 83 eksik); cueEmpty rewrite'ı `.cue-empty-rich` korumalı.
- `tests/player-ui.test.js`, `tests/player-escape-dialog.test.js`, `tests/report67-smarttube-wiring.test.js` — yeni yapıya uyarlanan sözleşmeler (davranış eşdeğer veya kasıtlı değişiklik; ayrıntı raporda).
- `docs/raporlar/BROWSER_BUG_REPORT_115.md` + `r115-evidence/` (6 PNG).

## Kanıt (gerçek Electron, ayrı profil, CDP trusted input)
- Açık tema: kontrol ikonları bej→görünmez → koyu-cam + beyaz ikon (before/after PNG).
- Tek satır kontrol çubuğu; ⋯ menüsünde ArrowDown/Up/Esc odak iadesi doğrulandı; Esc→fs çıkışı (oyuncu açık kalır); Backspace→`playerLayer` gizlenir.
- Boş durum kartı EN: "No subtitles for this video / Generate with Whisper / Choose subtitle file"; TR: Türkçe — locale doğru.
- Tarayıcı ⋯ menüsü sağ kenar içinde (right=793 < 1180).

## Test
Hedefli: player-ui 148/148, ui-locale 14, player-escape-dialog PASS, player-extras-parti3, report67 84/84, browser-experience 7, reading-design, controls-behavior, dark-mode 9, audit-tur5, browser-subtitles 80, subtitle-selection, subtitle-actions, crash-integrity — hepsi yeşil. `node --check` renderer.js + ui-locale.js OK. Tam `npm test` kasıtlı koşulmadı (hedefli set yeterli görüldü; geniş şüphe yok).

## Kalan / ertelenen
Öncelikli toolbar collapse, adres alan-adı koyulaştırma (input sınırı), "Altyazı işlemleri ▾" birleşimi, up-next geri sayım kartı, 335-hex token konsolidasyonu, hız select→liste butonu. Detay: `docs/raporlar/BROWSER_BUG_REPORT_115.md`.
