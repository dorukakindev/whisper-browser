# 2026-09-20 09:55 — R88 SmartTube testleri davranış testine çevrildi

## Bağlam
Başka bir oturumda yazılan `BROWSER_BUG_REPORT_88.md` (commit 1055592),
`tests/report67-smarttube-wiring.test.js`'teki iki iddianın ürün kusuru
değil kırılgan kaynak-sözleşmesi yanlış pozitifi olduğunu belgelemiş ve
davranış testine çevrilmesini önermişti. Bu oturumda iddialar doğrulandı
ve önerilen dönüşüm uygulandı.

- Repo / dal: https://github.com/dorukakindev/whisper-browser — `master`
- Başlangıç HEAD: `105559216d8c27ab61f0ce895dac6e565949d7a4` (docs)
- Önceki ürün+test commit'i: `44e6b42455a480ebedf3ce5b57f55ce7f192db31`

## Yapılan iş
`tests/report67-smarttube-wiring.test.js` (tek değişen kod dosyası):

1. **Kanal linki stopPropagation** — `buildSmartTubeCard` artık `vm` sandbox'ta
   mini-DOM üzerinde çalıştırılıyor; `dispatchBubbling` ile gerçek click/Enter
   olayları gönderiliyor. Beklentiler (sıkı): kanal click/Enter →
   `openInvidiousChannelPage(authorId)` çağrılır ama kart `open()`'ı
   (probe/hide sayaçları 0) çalışmaz; ok tuşları yayılır (grid gezinmesi);
   kart click/Space oynatmayı tetikler ve preventDefault uygular.
   Eski `\n}` bayt-beklentisi CRLF'te kırılıyordu; çıkarım artık `\s*` toleranslı.
2. **Y3 debounce** — `if (searchInp)` dinleyici bloğu saatli sahte zamanlayıcıya
   bağlanıyor: <450 ms sorgu yok; yeniden yazma bekleyeni iptal eder; bayat
   sorgu bekçisi (value değişimi) düşürür; boş input sıfırlar ve sorgu atmaz;
   Enter anında sorgu atar ve bekleyen timer'ı iptal eder.

Ürün kodu değişmedi — sözleşme aynı, kanıt şekli sağlamlaştırıldı.

## Doğrulama
- `node tests/report67-smarttube-wiring.test.js` → **66/66** (öncesi 64/66 Windows'ta)
- `npm test` → 1 başarısız dosya: `tests/watch-index-assets.test.js` —
  `node:sqlite` `fts5` modülü bu Linux Node 22.14.0 build'inde derli değil;
  daha önce kabul edilmiş ortam sınırı, ürün regresyonu değil.
- `BROWSER_BUG_REPORT_88.md` → Follow-up/RESOLVED bölümü eklendi.

## Hâlâ geçerli sınırlar (R88'in diğer maddeleri — doğrulandı, değişmedi)
- Gerçek YouTube OAuth akışı ve bazı Electron hata-enjeksiyon senaryoları
  canlı/ayrı doğrulama sınırı — bu VM'de gerçek kimlik bilgisi ve Windows
  Electron hedefi yok.
- Büyük oturumlarda seyrek performans takılması (PF2): belgeli ve kabul
  edilmiş sınır — schema-max'te ~312 ms, ≥10 s kısıtlamalı.

## Kalan açıklar
- fts5 ortam engeli: Windows hedefli kurulumda Node'un sqlite'ı fts5 ile
  derlenmişse sorun yok; bu VM'de koşulamıyor.
