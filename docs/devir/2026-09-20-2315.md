# Devir Notu — 2026-09-20 23:15 (İstanbul)

## Kapsam

`FULL_REVIEW_2026-09-20.md`'deki açık **F1 Major** bulgunun düzeltmesi:
SmartTube kuyruğu ve medya geçişlerinde "Sonraki" düğmesinin bayat
`disabled` durumu.

- Repo: `dorukakindev/whisper-browser`, dal: `master` → PR dalı `devin/fix-smarttube-next-stale`
- Başlangıç kod commit'i: `d523405c5dc6df8b2799d3234e9e79a125ed3a94`
- Bitiş kod commit'i: `8e2d4ce` (PR dalı)

## Kök neden

- `stQueueToggle` (`renderer.js`): `stQueue`'yu değiştirip `saveStQueue()`
  çağırıyor ama `updatePlaylistButtons()` hiç çağırmıyordu — kart kuyruk
  düğmesiyle ekleme/çıkarma Sonraki'nin durumunu bayat bırakıyordu.
- `setMediaKey`: `updatePlaylistButtons()` çağrısı `player.mediaKey`
  atamasından ÖNCE koşuyordu; `updatePlaylistButtons` `ytQueued`'ı
  `player.mediaKey`'den hesapladığı için file→youtube geçişinde kuyruk
  dolu olsa bile Sonraki kapalı kalıyordu.

## Değişen dosyalar

- `src/renderer/renderer.js` — `stQueueToggle` her iki dönüşünde
  `updatePlaylistButtons()`; `setMediaKey`'de çağrı `player.mediaKey =
  nextKey` satırının sonrasına taşındı (playlist sıfırlama mantığı aynı).
- `tests/report67-smarttube-wiring.test.js` — iki davranış testi:
  toggle'ın düğmeleri tazelediği (ekle/çıkar sayaçları) ve
  `setMediaKey('youtube:…')` sonrası kuyruk doluyken Sonraki'nin açık,
  `setMediaKey('file:…')` sonrası kapalı kaldığı vm-sandbox doğrulaması.

## Koşulan testler

- `node tests/report67-smarttube-wiring.test.js` — 74/74 geçti (2 yeni test).
- `node --check src/renderer/renderer.js` — temiz.
- `npm test` (tam paket, Node 22.14.0): ilk koşuda 10 dosya yalnız eksik
  ortam bağımlılığından düştü (`jsdom`/`mux.js`/`mp4box`/`darkreader`,
  `guessit`/`genanki`/`yt_dlp`/`faster_whisper`/`ffsubsync`/`rapidocr`/
  `scipy`, `node:sqlite` fts5 derleme sınırı). `npm install` +
  `backend/venv` kurulumu sonrası ilgili dosyalar tek tek yeniden koşup
  geçti; kalan tek sınır `watch-index-assets.test.js`'in bilinen
  fts5/`node:sqlite` ortam notu.
- Electron UI koşusu yapılmadı (Linux ana makine; davranış testleri
  gerçek fonksiyonları vm'de çalıştırır).

## Bağımlılık / ortam notu

Bu oturumda `backend/venv` oluşturuldu ve `srt guessit genanki yt_dlp
faster-whisper ffsubsync rapidocr_onnxruntime scipy` kuruldu; Node 22.14.0
kurulumu ve `npm install` yapıldı. Hepsi test koşusu içindir; ürün kodu
bağımlılığı değişmedi.

## Sonraki adımlar

- PR incelemesi ve merge. FULL_REVIEW'daki F6–F15 minör/nitpick maddeleri
  ayrı tura bırakılabilir.
