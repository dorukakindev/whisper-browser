# Devir notu — 2026-09-23 03:58 (UTC)

## Kapsam

R112'de "doğrulanabilir sınır" olarak açık bırakılan dört madde — kullanıcı onayıyla "hepsi detaylı": legacy smoke tamiratı, T2 hız/görünürlük genişlemesi, T5 çapraz-sekme sızma kanıtı, uzatılmış birleşik soak.

## Dal ve commit'ler

- Dal: `codex/r113-hardening-1790133270` — taban: master `b5dfc35` (PR #35 merge sonrası, R110+R111+R112 içerir)
- Ürün kodu commit'i: **yok** (yalnız tests/ + docs/)
- `4d710a0` test(electron): subtitle-output smoke rename → `.smoke.js` (runner envanterine giriş)
- `6aedff2` test(electron): subtitle-output — `stopElectron()` gövdesi (askı düzeltmesi)
- `86be66c` test(electron): tools-design — narrow yerleşim ölçümünü oturt (batch flake)
- `12b62df` test(electron): overlay-timing — 2×/0.5×/pause/resume/hidden/reshow senaryoları
- `cb8cb8a` test(electron): **yeni** `electron-browser-tab-leak.smoke.js` (T5)

## Bulgular ve kök nedenler

1. **BUG-R113-01 (test):** `electron-subtitle-output-smoke` sonsuz askı → `taskkill.exe` Windows-only, Linux'ta ENOENT ile sessiz başarısızlık → canlı child handle event loop'u açık tutuyor. İkincil: soketler zarif-quit'ten önce kapatılınca CDP promise asla resolve olmuyor. Düzeltme: zarif quit (soketler açık, 5 sn) → soket kapat → SIGKILL/taskkill → exit bekle.
2. **BUG-R113-02 (test):** `tools-design` batch flake `{width:0}` → asenkron X11 resize'dan önce açılan panel, geç gelen `syncResponsivePlayerLayout` tarafından `display:none`'a çekiliyordu. Düzeltme: panel açmadan önce `player.narrowViewport` oturt.
3. **T2:** 0.5×/2×/pause/resume'da 69+12 örnek 0 uyumsuz. `document.hidden` iken overlay donması **bilinçli tasarım** (`document.hidden` korumalı queueFrame/boundaryTimer/render + `visibilitychange→render` reshow toparlanması); bayat örnekler veri olarak kayıtlı, hata sayılmadı.
4. **T5:** split görünümde iki sekme eşzamanlı — 24/24 örnek 0 sızma; kapatmada main+renderer tutarlı; navigasyonda overlay temizleniyor; dinleyici/observer delta 0. **Ölçülen tasarım sınırı:** sekme mutasyonları `tabs-changed` event'i yerine invoke yanıtıyla senkronlanır (probe ile doğrulandı); ürün helper'ları yanıtı uyguladığı için sızma yüzeyi yok.
5. **Soak:** 400 döngü + 50 hibernasyon ~7 dk GEÇTİ (%100 yakalama, listener/timer delta 0, heap/RSS delta negatif~küçük); 8-döngülük smoke'daki +108 listener'ın ısınma olduğu kanıtlandı. 1300 döngü + 60 hibernasyon (~20 dk) GEÇTİ — %100 yakalama (1300/1300), hibernasyon 60/60, listener/timer delta 0, heap eğimi ~400 B/döngü (bütçe 49 KB). Kanıt `docs/raporlar/r113-evidence/soak-1300cycle.json`.

## Testler

- `node tests/run-electron-smokes.js subtitle-output` → 4/4 `ok:true`, süreç kendi başına çıkıyor (önceden sonsuz askı)
- `node tests/run-electron-smokes.js tools-design` → PASS ×3
- `node tests/run-electron-smokes.js overlay-timing` → PASS
- `node tests/run-electron-smokes.js tab-leak` → PASS
- `node tests/run-resource-soak.js --cycles=400 --hibernation-cycles=50` → GEÇTİ (21/21 bütçe)
- `node tests/run-resource-soak.js --cycles=1300 --hibernation-cycles=60` → GEÇTİ
- Tam `npm test` koşulmadı — ürün kodu değişmedi.

## Kanıt dosyaları

`docs/raporlar/BROWSER_BUG_REPORT_113.md` + `docs/raporlar/r113-evidence/` (tab-leak 3 PNG + report.json + soak-400cycle.json + soak-1300cycle.json).

## Sınırlar / yapılmayanlar

- Windows `taskkill.exe` yolu kod düzeyinde — bu makinede doğrulanamaz (Windows CI).
- Gerçek YouTube hesabı / ücretli model kalitesi — kapsam dışı, fixture ile ölçüldü.
- Ürün kodu değişmedi: bulunan iki kusur da test altyapısındaydı; ürün tarafında kanıtlanabilir bug çıkmadı.
