# Devir notu — 2026-09-15 11:12 — Browser derin denetim turu (rapor 27)

## Özet

Üçüncü browser denetim turu. `src/` altındaki kalan ~30 browser modülü tam okundu; üç bulgu gerçek node deneyleriyle doğrulandı. Ürün kodu değiştirilmedi. Rapor: `BROWSER_BUG_REPORT_27.md` (yerel — seri gibi .gitignore'da).

## Bağlam

- Repo: https://github.com/dorukakindev/whisper-browser · Dal: `master`
- Başlangıç HEAD: `9afc877da1053d2785301e432a09454a98f71f7a`
- Bitiş ürün kodu HEAD: `9afc877` — **ürün kodu değişmedi**, yalnız bu not + indeks eklendi.
- Ağaçta kullanıcıya ait ~12 dosyalık commit'lenmemiş çalışma var (capture-provenance, dash-capture, subtitles, integrity, main, preload, renderer + testleri) — **dokunulmadı, commit'e dahil edilmedi**.

## Çalıştırılan deneyler

1. **B9 arşiv yetimi:** temp dizinde `archive.store` 3 revizyon → 4 yetim dosya gözlendi. Doğrulandı.
2. **B8 recovery-flush:** `fs.renameSync` hata fırlatacak şekilde patch'lenip `browser-note-store` yüklendi → constructor throw, örnek oluşmadı. Doğrulandı.
3. **B12 kapı zehirlenmesi:** gerçek `src/browser-tabs.js` modülü ile `open → accept(geçerli) → accept(op çakışmalı, reddedilir) → accept(önceki bağlam)` akışı → reddedilen olay bağlamı ezdiği için hem yeni hem eski bağlam `false` döndü. Doğrulandı.

## Bu turun bulguları

- **B12 P3 (yeni, deneyli):** `browser-tabs.js:71-75` — `accept()` `mediaId`/`acquisitionId`'yi `operationId` reddinden önce yazıyor; reddedilen olay kapıyı zehirliyor, sonraki geçerli olaylar da reddediliyor (`establishesContext` olayı gelene dek kilitli).
- **B13 P4 (yeni):** `browser-page-translate.js` — Alt-önizlemede `hideTools()` `hoveredRef`'i null'lıyor; keyup `restoreView(null)` çağırıp `previewOriginal`'i takılı bırakıyor → çeviri görünümünde kaynak metin.
- **Yanlış pozitif düzeltmeleri:** B5 (asset-store — içerik-adresli, kendini onarıyor), extras-cancel kapısı (valid() öncesi çalışıyor), `moveBefore` (Electron 43'te var), gate.open-per-sync (FIFO tutarlı).
- **Doğrulanan gözlemler:** OpenSubtitles per-request login (`browser-subtitle-search.js:81-91`), `unifiedLibrarySearch` sekme sıralama çarpıklığı (`browser-library-tools.js:78`), `ceaUrlKey` sorgu atma, mini-player `loadFile` yakalanmıyor, atomik olmayan feature yazıları.
- **Tam okunup sağlam çıkanlar:** translation-cache, session-package, series-context, workflow-recorder, subtitle-search, sponsorblock, tab-history, subtitle-files, `syncBrowserTabs`.

## Testler

Çalıştırılan: yukarıdaki 3 hedefli node deneyi (geçici script'ler, repo dışı). `npm test` bu turda çalıştırılmadı — ürün kodu değişmedi; mevcut test paketi denetim konusu değildi.

## Sonraki adımlar

- Düzeltme önceliği: B7 → B1 → B12 → B8 → B9 (rapor 27'deki tablo).
- Kalan az-taranan bölgeler: renderer.js'in kalan bakiye kısımları, main.js bakiye, `backend/`.
