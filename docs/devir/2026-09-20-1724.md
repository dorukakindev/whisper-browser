# Devir Notu — 2026-09-20 17:24 (Europe/Istanbul)

Repo: dorukakindev/whisper-browser · Dal: master
Başlangıç kod commit'i: `f2ec595` (Parti 11)
Bitiş kod commit'i: `9006102` — Parti 12: aynı klipte A/B ASR benchmark'ı (F17)

## Kapsam

- **F17 — iki ASR/model ayarını aynı 30–120 sn klipte ölçen kalite
  karşılaştırması:**
  - `backend/model_benchmark.py`: mevcut tek-çalıştırma akışı korunarak
    `measure()` fonksiyonuna ayrıldı; `--compare-model`, `--compare-device`,
    `--compare-compute-type` argümanları aynı çıkarılan wav üzerinde ikinci
    koşuyu yapar. `clipHash` = wav'ın sha256'sı (kaynak kapsayıcıdan
    bağımsız → aynı klip aynı anahtar). `diff`: eşlenen cue'ların başlangıç
    sapması (ort/maks ms) + birleşik metnin SequenceMatcher sapması
    (`textDeviation`). CUDA'da `torch.cuda.max_memory_allocated` → `vramMb`.
    Klip süresi 30–120 sn'ye kilitlendi (önceden 5–90).
  - `src/main.js`: `options.compare` whitelist doğrulamalı (KNOWN_MODELS,
    cpu/cuda); başarılı sonuç `userData/model-benchmarks.json`'a clipHash
    anahtarıyla atomik (`writeJsonAtomic`) saklanır — klip başına son 20,
    toplam 40 klip. Yeni `models:benchmark:history` IPC'si (yetki kapılı).
  - `src/preload.js`: `modelBenchmarkHistory` köprüsü.
  - `src/renderer/index.html`: benchmark satırına `modelBenchmarkCompare`
    seçici ("A/B: yok" varsayılan).
  - `src/renderer/renderer.js`: seçiliyse `compare:{model}` gönderilir;
    sonuçta A/B özeti (speedX karşılaştırması, cue sapması ort/maks, metin
    farkı %, klip kayıt sayısı) loglanır.
  - `src/renderer/ui-locale.js`: yeni TR/EN çiftleri.

## Testler

- `npm test` → Tüm testler geçti (200 IPC kanalı × 3 yetkisiz gönderici dahil)
- `backend/test_model_benchmark.py` (yeni): sahte `faster_whisper` modülüyle
  compare emit'i, clipHash determinizmi, tek-mod anahtar sözleşmesi
- `node --check` main.js / renderer.js / preload.js; `py_compile` ✅
- Gerçek model koşusu ortamda yapılmadı (faster-whisper kurulu değil).

## Kalan E maddeleri

- F18 (model/bağımlılık yöneticisi — E/K sınırı; boyut/disk/VRAM/devam/
  checksum/temizlik/capability registry — aşamalı; model silme açık seçim)
- F20 (tarihli gerçek-site kabul matrisi belgesi)

K/B/R maddelerine dokunulmadı.
