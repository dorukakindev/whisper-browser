# Yerel ve GitHub kaynaklarının güvenli eşitlenmesi

Repo: dorukakindev/whisper-browser; dal: master.
Başlangıç: bf0d246e7b33ea9d87e8d3c0b2042bfa4b5a83d8.
Alınan uzak kod: 242ae062582ca4d29049a3c2b879b9d4cc315e4f (37 commit, fast-forward).
Bitiş kodu: 491a0c07947e5d51312265d2db023549804489af.

## Ayrıntılı bulgu, düzeltme, ret gerekçesi ve doğrulama dökümü

- backend/series_memory.py: eşzamanlı Windows okuyucusu atomik replace sırasında WinError 5 oluşturuyordu. Başlangıç okuması ortak kilide alındı, yalnız Windows 5/32/33 için altı denemeyle sınırlı tekrar eklendi. backend/test_series_memory.py geçici ve kalıcı kilitte eski dosyanın korunmasını sınar; 6/6 geçti.
- src/persistent-schema-inventory.js: gerçek page-memory-v3 anahtarı ile eski v2 envanter uyuşmazlığı giderildi. tests/browser-page-translate.test.js eski anahtarın yeniden kullanılmadığını doğrular.
- tests/adversarial-ipc.test.js: yeni ASS kayıt bağımlılıkları VM fixture'a eklendi.
- tests/browser-background-navigation.test.js: yeni browserExtras/terminoloji bağımlılıkları eklendi.
- tests/browser-subtitles.test.js: before-quit doğrulamasındaki keyfi 180 karakter sınırı yerine callback kapsamı incelendi.
- tests/player-ui.test.js: ayrıştırılmış UI modülleri HTML'nin gerçekten yüklediği scriptler üzerinden doğrulanıyor.
- tests/subtitle-format.test.js: çıkarılmış script için window olay fixture'ı eklendi.

Son npm test çıkış 0, Tüm testler geçti (yerel scratch/sync-browser-complete.log). npm run test:electron-bridge geçti; gerçek Electron köprü testi, tüm UI'nin görsel kabulü değildir. Main/preload/renderer node --check ve backend transcribe/series_memory/test_series_memory py_compile geçti. Testler silinmedi; yeşil sonuç için ürün eşikleri gevşetilmedi.

## Bağımlılıklar ve kalanlar

npm install --ignore-scripts --no-audit --no-fund: esbuild 0.28.2 ve jassub 2.5.16 dahil eksikler kuruldu, lock değişmedi; mevcut Electron korundu. requirements-browser-align.txt, requirements-catalog.txt, requirements-browser-tools.txt kuruldu: ffsubsync 0.5.1, guessit 3.8.0, rapidocr-onnxruntime 1.4.4, sentence-transformers 5.1.2 ve repo pin'i onnxruntime 1.20.1. Mevcut ffmpeg/ffprobe backend/bin'e hash doğrulamalı kopyalandı.

pip check: eski torch 2.6.0+cu124 / torchaudio 2.6 / torchvision .21 ile pyannote-audio 4.0.4 / whisperx 3.8.6'nın 2.8 gereksinimi uyuşmuyor. GPU yığını körlemesine yükseltilmedi; WhisperX/diarization canlı çalışma kabulü AÇIK. Yeni ürün şeması bu düzeltmede eklenmedi, envanter mevcut v3'e eşitlendi.

## Altı proje kapsamı

Bilgi codex/workspace-features dalına alındı, 1014 pytest geçti / 3 atlandı ve ruff geçti. Whisper Live main'e alındı, smoke ve 25 ek test geçti. nMDB main'e alındı, 40 regresyon geçti. openai-altyazi-cevirisi master 1f690e7d909c6b27a292c610d78893cefdd2d965 ve mitoloji master f3d7047b88a30d1187d7b935b18574aabd4749dc zaten uzakla aynıydı; bu ikisinde ürün testi tekrarlanmadı.

nMDB aktif 289 film/650 analiz, uzak snapshot 386/808. Eski analizlerin 431 içerik imzası yeniyle birebir eşleşmiyor ve iki kişisel puan farklı. Veritabanı DEĞİŞTİRİLMEDİ; veri çatışması kararı bekliyor. Kaynak eşitlemesi veri birleşiminin tamamlandığı anlamına gelmez.

## Koruma ve teslim sınırı

Güncelleme öncesi HEAD, codex/backup-before-sync-20260915-022852 dalında tutuldu. Yerel değişiklik ve izlenmeyen dosya kopyaları Whisper Local scratch/sync-backup-20260915-022852 altında doğrulandı. Kullanıcının raporları, anahtarları ve çalışma verileri bu commitlere katılmadı. Force push/reset/clean kullanılmadı. Eski EXE dosyaları yeniden derlenmedi: güncel kaynak ile eski EXE aynı sürüm sayılmaz. Gerçek ücretli sağlayıcı, tüm siteler ve uzun canlı kullanım bu tur doğrulanmadı.

Bu not kod commitinden sonra ayrı dokümantasyon commitidir. Push sonucu not yazıldıktan sonra uzak dal SHA'sıyla ayrıca doğrulanır; bu metin tek başına push kanıtı değildir.
