# 2026-09-16 07:10 — medya tarama onayı ve önizleme token kapanışı

## Kimlikler

- Depo: `https://github.com/dorukakindev/whisper-browser`
- Dal: `master`
- Başlangıç ürün kodu: `073ac36b02572eef4b60a1068d10d7c9e6895c94`
- Bitiş ürün kodu: `b03ae5e2ca3589eed608f7093d80c6beaf81a1d7`
- Bug raporu: `BROWSER_BUG_REPORT_38.md`

## Amaç ve sonuç

Önceki kapanış notunda açık gösterilen B60, B64, B100 spec alt maddesi ve `paths:scanMedia` yeniden güncel kaynakta doğrulandı. İki somut kusur düzeltildi; B64 test matrisi kapatıldı. B60'ın kalan toplu protokol göçü ve B100 reload bağlanması, kanıtlanmış bug yerine sırasıyla mimari sertleştirme ve yeni özellik olduğundan uygulanmadı. Ret gerekçeleri 38. raporda ayrıntılıdır.

## Değişiklikler

### Ürün kodu

- `src/main.js`
  - Bırakılan medya tarama köklerini ana süreçte kanonikleştirip tür/boyut bakımından doğrulayan `inspectMediaScanRoots` eklendi.
  - Tarama öncesi Türkçe native kullanıcı onayı eklendi; varsayılan ve Escape sonucu İptal.
  - Diyalog açıkken junction/symlink hedefinin değişmesine karşı onay sonrası kökler yeniden doğrulanıyor.
  - `paths:scanMedia` yalnız onaylanmış kökleri tarıyor.
- `src/media-catalog-service.js`
  - `import-cancel`, yalnız önizlemeyi oluşturan sender'ın token'ını siliyor.

### Testler

- `tests/adversarial-ipc.test.js`: kullanıcı onayı olmadan tarama yapılmadığını sınar.
- `tests/audit-report-regressions.test.js`: üretim `paths:scanMedia` handler'ının onay kapısına bağlı olduğunu doğrular.
- `tests/media-catalog-service.test.js`: yanlış sender apply/cancel, yanlış token, tüketilmiş token ve TTL.
- `tests/workspace-restore-regressions.test.js`: paket token sender/tür/tek-kullanım/TTL.
- `tests/browser-feature-services.test.js`: kodlama önizlemesi yanlış token/tek-kullanım/TTL.

### Belgeleme

- `BROWSER_BUG_REPORT_38.md`: ayrıntılı bulgu, düzeltme, ret gerekçesi, doğrulama ve açık kapsam dökümü.
- Bu tarihli devir notu ve `DEVIR-NOTU.md` indeksi.

## Kök nedenler

1. Preload katmanı bırakılan `File` nesnesinden gerçek yolu çıkarsa da ana süreç renderer'ın gönderdiği yollar için bağımsız kullanıcı niyeti/provenans kapısı uygulamıyordu.
2. Katalog token'ında apply sahipliği denetlenirken cancel yolu aynı sender kontrolünü taşımıyordu.
3. B64'ün önceki testleri altyapı davranışını parça parça doğruluyor, üç token ailesinin TTL ve tek-kullanım sınırlarını birlikte görünür kılmıyordu.

## Doğrulama kanıtı

- `npm test` — **GEÇTİ**, çıkış kodu 0, son satır `Tüm testler geçti`.
  - adversarial IPC: 14/14
  - browser subtitle parser: 79/79
  - parser fuzz: 54.024 vaka
  - watch-library fuzz: 6.000 örnek
  - Python `backend/test_transcribe.py`: 180/180
- `npm run test:electron-bridge` — **GEÇTİ**, çıkış kodu 0.
- `node --check src\main.js` — **GEÇTİ**.
- `node --check src\media-catalog-service.js` — **GEÇTİ**.
- `backend\venv\Scripts\python.exe -m py_compile backend\transcribe.py` — **GEÇTİ**.
- `git diff --cached --check` — commit öncesi **GEÇTİ**.

## Çalıştırılmayanlar ve sınırlar

- `npm run test:soak` yeniden çalıştırılmadı; bu tur kaynak yaşam döngüsü/uzun oturum bütçesini değiştirmedi.
- Native onay diyaloğu gerçek Windows ekranında manuel görüntüyle doğrulanmadı; handler davranışı ve güvenli varsayılan otomatik test/kaynak ile doğrulandı.
- Gerçek sağlayıcı veya ücretli API çağrısı yapılmadı.
- Bu çalışma tam kaynak satır-satır denetimi değildir. `transcribe.py`, tüm capture dalları ve bütün Node yardımcılarının eksiksiz yeniden denetlendiği iddia edilmez.
- B60'ın kalan immutable-context toplu göçü, yeni deterministik bayat olay kanıtı olmadan yapılmadı.
- B100 kuyruksuz reload reattach yeni özellik olarak bırakıldı.

## Bağımlılık, kurulum ve şema

- Yeni bağımlılık yok.
- Kurulum adımı yok.
- Kalıcı veri şeması veya migration değişikliği yok.

## Commit ve teslim durumu

- Ürün kodu, testler ve bug raporu `b03ae5e2ca3589eed608f7093d80c6beaf81a1d7` commit'inde.
- Bu not ayrı dokümantasyon commit'ine alınacaktır; not kendi dokümantasyon commit hash'ini kendisine yazmaz.
- Push ve uzak dal eşitliği dokümantasyon commit'inden sonra ayrıca doğrulanacaktır.
