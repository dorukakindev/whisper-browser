# Devir — 2026-09-25 19:28 — R126

## Repo / dal / commit

- Repo: `https://github.com/dorukakindev/whisper-browser` · Dal: `codex/r126-bug-verify-1790363187`
- Başlangıç HEAD: `7ae4e30` (master — R124 stack merge sonrası)
- Bitiş kod HEAD: commit sonrası ayrıca yazılacak (bu notta bitiş kod id'si kod commit'inin hash'i olacak; notun kendi hash'i yazılmaz)

## Yapılan iş

Kullanıcı talimatı: GitHub'daki bug dosyalarına bak → gerçek bug'ları düzelt+test et, gerçek değilse "false" yaz; repo önerilerinden işe yarayanları uygula.

### Bulgu doğrulaması (kaynak: `haze/bug-report-2026-09-24` dalı)

- **GERÇEK → düzeltildi (10):** N1 (`youtube:poll` `_ytDevice` null yarışı → hayalet hesap + TypeError),
  N2 (`accountIdFor` Date.now tohumu → kopya hesaplar; şimdi kanal-ID tohumu `acct-UC…`/`acct-unknown`),
  N3 (`accountRemove` revoke'u poll slotuna takılıyordu → terminate+3sn bekleme+`remoteOk`),
  N4 (`upsertAccount` ölü `activeId` + `expiresIn>=0` semantiği), N6 (`youtube:browse` 150ms busy-wait →
  olay güdümlü `close` yarışı), K1 (`MAX_MEDIA_TIME_SECONDS` ×1000 ms hatası → `7*24*60*60`;
  master'daki report89 sözleşmesi 72 saatlik yayının korunmasını ister, bu yüzden 24 saat değil 7 gün),
  K2 (boş altyazı doğrulaması `ok:true` → erken ret), K3 (`splitBrowserBounds({})` → null),
  K4 (`setPath` ara düğüm dizisini eziyordu → koruma), R124-01 (`_vtt_to_srt` boş-satırsız cue kaybı →
  flush-on-boundary).
- **FALSE / ertelendi:** K5 = kasıtlı tasarım (bellek-içi grant'lar güvenlik tercihi; raporda dokümante edildi),
  K6 = sınır semantiği kararı (ertelendi), N5 = bu depoda uygulanamaz (`tests/r92-deep/` uzakta yok),
  R124-03 = P3 kozmetik (ertelendi).
- Karar raporu: `docs/raporlar/BROWSER_BUG_REPORT_126.md` (haze'nin dosyası `BROWSER_BUG_REPORT_125.md`
  olarak yeniden adlandırılıp korundu — master'da 124 dolu).

### Öneri uygulamaları (kaynak: `devin/repo-research-2026-09-25` → `docs/raporlar/GITHUB_REPO_ONERILERI_2026-09-25.md`)

1. **`Network.enable maxPostDataSize: 1 MB`** (iki çağrı yeri) + `requestWillBeSent`'te `method`/`postData`
   saklama (256 KB tavan) → `normalizeBrowserNetworkRecord.requestPostData` — protobuf-POST manifest
   isteyen servislerde istek gövdesi artık kayıtlı.
2. **Stremio OS-v3 anahtarsız sağlayıcı basamağı** — `target.imdbId` doğrulama (`tt\d{5,10}`),
   MISSING_API_KEY veya OS-sıfır-sonuç durumunda `GET /subtitles/{movie|series}/….json`;
   `downloadSubtitle({url, provider:'stremio'})` `approvedStremioUrl` allowlist'li; `bfImdb` form alanı +
   `· Stremio/OS` rozeti.
3. **ffsubsync ses-referans modu** — `browser_align.py` `payload.audio` → `ffsubsync <medya> -i target.srt`
   (teşhis `ffsubsync-audio-vad`, overlap null, drift<30sn → medium); `browser-alignment.js` `audioPath`
   doğrulaması; feature-services `'alignment-audio-preview'` (480sn tavan); renderer "Videonun sesiyle
   senkron bul" düğmesi — ikinci altyazı olmadan fps/sürüm düzeltme önizlemesi.

## Değişen dosyalar (kod)

`src/main.js` (N1,N3,N6 + postData), `src/youtube-accounts.js` (N2,N4), `backend/youtube.py` (user_id),
`src/browser-session-store.js`, `src/browser-tab-history.js` (K1), `src/browser-subtitle-output.js` (K2),
`src/browser-tab-layout.js` (K3), `src/secret-store.js` (K4), `backend/invidious.py` (R124-01),
`src/browser-subtitle-search.js` (Stremio+imdb), `src/browser-network-capture.js` (requestPostData),
`src/browser-alignment.js` + `backend/browser_align.py` (ses kipi), `src/browser-feature-services.js`
(alignment-audio-preview + stremio download passthrough), `src/renderer/browser-analysis-tools.js`
(düğme), `src/renderer/browser-features.js` (imdbId + stremio passthrough + rozet),
`src/renderer/index.html` (bfImdb + baAlignAudio).

## Testler

- `tests/youtube-accounts.test.js` 9/9 · `tests/youtube-auth-flow.test.js` 4/4 ·
  `tests/browser-report126-regressions.test.js` 4/4 · `backend/test_invidious_vtt.py` 5/5 ·
  `backend/test_browser_align_audio.py` 3/3 · `tests/browser-subtitle-search.test.js` tam (+6) ·
  `tests/browser-alignment.test.js` gerçek ffsubsync geçti · `tests/browser-network-capture.test.js` 7/7.
- `node --check` tüm değişen JS + `py_compile` iki Python — temiz.
- Koşulmadı: tam `npm test`, Electron smoke seti, gerçek Stremio/Google canlı uçları, Windows el kontrolü.

## Windows etkisi / doğrulanmamış sınırlar

- Değişiklikler Windows yolu/encoding/CUDA dokunmuyor; `install.bat`/`start.bat` etkilenmez.
- Yeni ağ ucu `opensubtitles-v3.strem.io` — Windows makinede gerçek imdb_id araması + indirme el kontrolü önerilir.
- ffsubsync ses kipi gerçek VAD kalitesi yalnız stub-routing düzeyinde testli; uzun sessiz intro'larda
  `confidence:low` + `autoApply:false` beklenir.
- N1–N3 gerçek Google hesabıyla canlı doğrulanmadı (politika gereği) — vm-harness + birim testiyle.
- `requestPostData` CDP zinciri uçtan-uca Electron smoke dışı (birim seviyesinde normalize doğrulandı).

## Sonraki adımlar / backlog

- Öneri dokümanının Etap 2+: youtubei.js sağlayıcı katmanı (eval-shim izolasyon tasarımı ilk iş),
  kanal RSS abonelikleri (fast-xml-parser + UI yüzeyi), subliminal merdiveni, RYD/DeArrow istemcileri,
  MSE appendBuffer hook, cue deposu kararı.
- K6 sınır semantiği ve R124-03 tek-gramer hizalaması backlog'da.
