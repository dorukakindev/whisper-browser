# Devir Notu — 2026-09-22 01:10 (Europe/Istanbul)

Repo: `dorukakindev/whisper-browser`. PR #16 (`devin/subtitle-reliability-gauntlet`) kullanıcı isteğiyle komut satırından `master` dalına **normal fast-forward** olarak birleştirildi; squash/force push yapılmadı. Başlangıç `master`: `b203a2d54bcb054a18498f3770eebb16214f2d92`. Bitiş ürün kodu ve PR başı: `354daf9af1d137b63e3d05dd1376732d531fb909`. Bu turda ürün/test kodu değiştirilmedi.

Önce `git fetch origin master` ve `git merge-base --is-ancestor origin/master HEAD` ile PR dalının uzak `master`ı içerdiği doğrulandı. PR'ın Windows ve Ubuntu CI kontrolleri `success`, GitHub API'sinde `mergeable_state=clean` idi. `git push origin HEAD:refs/heads/master` fast-forward başarılı oldu. Sonrasında GitHub PR API'si `state=closed`, `merged=true` döndürdü; `git ls-remote` uzak `master` ve PR dalında aynı `354daf9...` kimliğini gösterdi.

Bu not ve `DEVIR-NOTU.md` indeks güncellemesi dışında dosya değişikliği yok. Yeni bağımlılık, kurulum veya şema değişikliği yok. Önceki doğrulama ve açık test sınırları [2026-09-22-0048.md](2026-09-22-0048.md) ile `BROWSER_BUG_REPORT_97.md` içindedir. Bu birleştirme turunda testler yeniden çalıştırılmadı; yalnız Git/GitHub durum doğrulaması yapıldı. Kullanıcının ana çalışma ağacındaki değişikliklere dokunulmadı.
