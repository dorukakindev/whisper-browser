# Devir Notu — 2026-09-22 05:13 (Europe/Istanbul)

## Görev
Gece denetimi + düzeltme turu: master üstünde A–G kuyruğu (kullanım matrisi, yakalama/zaman, çeviri maliyeti, crash bütünlüğü, UI/a11y, performans, temiz kurulum/CI). Sınıflandırma: doğrulandı/zaten kapalı/yanlış pozitif/ortam-hesap gerektiriyor.

## Repo / Dal / Kimlikler
- Repo: `dorukakindev/whisper-browser`, dal: `devin/night-audit`
- Başlangıç: `93f27d3` (origin/master — PR #23 birleşimi)
- Bitiş kod commit'i: `7fdaa2d`
- Bu not docs commit'indedir; kendi hash'i içerilmez.

## Yapılanlar (dört gerçek bug, hepsi kırmızı→yeşil)
- **F-104-1:** `searchInvidious` 5xx/ağ hatasında `[]` dönüyordu → "Sonuç yok" sahte boş state'i. Artık neden loglanıp throw; `showError` + Tekrar dene. `tests/smarttube-search-error.test.js` (5 test).
- **F-104-2:** `renderSmartTubeSection` 'channels' dalında `.catch(() => null)` ağ/5xx'i yutup giriş-tavsiye boş ekranı basıyordu — kaldırıldı; login-sınıfı hatalar içeride `null`'a indirgeniyor (korundu). `tests/smarttube-channels-error.test.js` (9 test).
- **F-104-3:** 'live' dalı aynı yutma + `stSearchLoadMore` geçici hatada `hasMore`'u öldürüyordu — hata yakalanıp buton "Tekrar dene: <hata>" gösteriyor, `hasMore` korunur.
- **F-104-4:** Player katmanı açıkken global keydown Escape kolu koşulsuz `preventDefault()` → `<dialog>` native cancel ölü (mediaCatalogDialog player'dan Esc ile kapanmıyordu — ajanın xdotool/CDP kanıtı). `e.target?.closest?.('dialog[open]')` kontrolü eklendi. `tests/player-escape-dialog.test.js` (3 hücre) + gerçek Electron `sendInputEvent` doğrulaması: dialog artık `open:false`.

## Yeni test altyapısı
- `tests/electron-smarttube-usage-matrix.smoke.js`: gerçek Electron + mock IPC ile kullanım matrisi — klavye çapraz-tetik (kanal vs kart Enter/Space), ok-tuşu odak kaydırma, 401/403/429/ENOTFOUND durum metinleri + retry, feed hatası→fallback→kurtarma, restart kuyruk kalıcılığı (write/verify faz). Koşu sonu yeşil.

## Zaten kapalı / tekrar doğrulanan
- B: overlay-timing, capture-completeness, subtitle-gauntlet smokes'ları dalda tekrar yeşil — yeni yakalama/zaman bug'ı yok.
- C: translation-reliability (seek/iptal/offline/retry/429/circuit-breaker) + R100 sayaçları; perf koşusu 10k cue'da providerCalls=13 (toplu işlem) — yeni maliyet bug'ı yok.
- D: crash-integrity (fs enjeksiyon, .bak, .tmp süpürme, EIO, destroyed-wc) tekrar yeşil; idempotence R102'de kanıtlı.
- F: `electron-perf-gauntlet` PASS — 10k cue render 15ms, longtask p95/p99 0ms, ΔRAM +125MB, Δdisk +49KB.
- G: sıfır klon `night-clean` install→npm test→bridge yeşil; master CI windows+ubuntu yeşil.

## Yasaklı alanlar
YouTube logout/revocation + browse kuyruğu, izin-isteği gerçek frame kökeni, ASS export ölçeği, çeviri sayı/saat koruması — kod dokunulmadı.

## Ortam/ürün ayrımı
- Temiz klon `npm test` `requirements-ci.txt` bağımlılıkları ister (R103'te belgeli — ürün bug'ı değil).
- Sentetik KeyboardEvent `<dialog>` native cancel tetiklemez — green kanıtı `sendInputEvent` (trusted) ile alındı.
- dbus/gpu stderr satırları Linux gürültüsü.

## Sınırlar / erişim sorunu
- 2026-09-21 DOCX/Markdown denetim ekleri bu oturuma ulaşmadı — repo içi raporlarla gidildi.
- Gerçek YouTube hesabı, gerçek ücretli sağlayıcı, CEA-708 fixture'ı, gerçek Windows makine — erişim gerektiriyor; mock/CI başarısı gerçek-hesap başarısı diye sunulmadı.

## Dosyalar
- `src/renderer/renderer.js` — dört düzeltme (arama/channels/live hata görünürlüğü + dialog Escape).
- `tests/smarttube-search-error.test.js`, `tests/smarttube-channels-error.test.js`, `tests/player-escape-dialog.test.js`, `tests/electron-smarttube-usage-matrix.smoke.js` — yeni.
- `BROWSER_BUG_REPORT_104.md` — tam bulgu raporu (kuyruk matrisi + nicel özet).

## Test komutları
`node tests/smarttube-search-error.test.js` (5 PASS), `node tests/smarttube-channels-error.test.js` (9 PASS), `node tests/player-escape-dialog.test.js` (PASS), `node tests/player-ui.test.js` (145 PASS, regresyonsuz), `DISPLAY=:0 npx electron tests/electron-smarttube-usage-matrix.smoke.js --no-sandbox` (PASS), `electron-perf-gauntlet` (PASS), temiz-klon `npm test` (PASS).

## Sonraki adımlar
- PR açık — kullanıcı inceleyip merge edecek (merge edilmedi).
- DOCX ekleri gelirse karşılaştırma bu rapora eklenebilir.
- `/tmp/e-matrix/skills/testing-whisper-browser-cdp/SKILL.md` taslak skill — suggest_skill_pr adayı.
