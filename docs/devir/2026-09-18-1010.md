# Devir notu — 2026-09-18 10:10

## Konu

Kullanıcı talimatıyla 30 ajanlık kapsam taraması: 2 dalga × 10 salt-okunur
audit ajanı tamamlandı (3. dalga kullanıcı tarafından durduruldu —
`main.js` pencere/oturum politikası dilimi taranmadı). Ürün koduna bu
turda **hiç dokunulmadı**. Bulgular: `BROWSER_BUG_REPORT_64.md`
(rapor-serisi devamı; 62/63 paralel ajanın raporları).

## Repo / dal / commit kimlikleri

- Repo: `https://github.com/dorukakindev/whisper-browser` — dal `master`
- Denetim bazı: `7a14b96` + ağaçtaki commit'lenmemiş paralel-ağan
  değişiklikleri (rapor 62/63 işleri: `browser-place-url.js`,
  `browser-sensitive-keys.js`, `browser-translation-*`, `browser-manga.js`,
  `browser-overlay-controller.js`, `browser-tab-history.js`,
  `subtitle-sentence-layout.js`, `_repro/`, `tests/report62-*/report63-*`)
- Bitiş kod commit'i: kod değişmedi — başlangıç ürün kodu = bitiş ürün kodu
- Bu notun commit'i: yalnız `BROWSER_BUG_REPORT_64.md` + bu not +
  `DEVIR-NOTU.md` indeks satırı; paralel ajanın dosyalarına dokunulmadı

## Sonuç özeti (ayrıntı rapor 64'te)

- **7 P1** (2 koşullu): yt-dlp güncellemesinin transcribe'a ulaşmaması;
  komut paleti bağlam uyumsuzluğu (ölü UI); `browser-place-url.js`'in
  renderer'da `require` çökmesi; `SENSITIVE_PARAM_RE=/./` ile tüm
  YouTube videolarının tek mediaId'ye çökmesi (foundation testi şu an
  kırmızı — devam eden refactor); dark-mode `cssOrigin:'user'` yenilgisi;
  sayfa çevirisinin kendi çıktısını kaynak sanması (orijinal kayıp +
  sınırsız API); iframe izin kontrolünün üst-site grant'ini miras alması
  (koşullu).
- **26 P2** doğrulandı (backend boru hattı, sekme/oturum/yakalama,
  altyazı deposu LRU, OpenSubtitles UTF-8, indirme grant'i, ASS ayrıştırma,
  reader/dark-mode, medya kimliği, sayfa çevirisi).
- ~50 P3 alan özetlendi; iki bulgu iki bağımsız ajanca tekrar bulundu
  (memory alanı crash'i, NDJSON print interleave).
- Paralel ajanın zaten düzelttiği örtüşmeler işaretlendi (manga IPv6,
  overlay LRU, translation-cache flush yarışı).

## Doğrulama (orchestratör yürütmesi)

- `sentence_ended("I don't know... maybe he's right.")` → `False`
- `is_structural_sdh_cue('♪\n♪')` / `('(♪)')` → `False`
- `parse_ass` tag-only Dialogue → `(1.0,2.0,'')`
- `node tests/browser-foundation.test.js` → **RED** (`?lang=en`
  kırpılıyor — /./ regresyonu)
- ~25 bulgu kaynak-satır düzeyinde yeniden doğrulandı.

## Sınırlar / kalan iş

- Read-only ajanlar kod yürütemedi; P1/P2 tetikleyicileri deterministik
  kod-izleriyle kanıtlı, DOM/Electron bulguları çalışma-zamanı
  doğrulaması bekliyor.
- `mediaKeySystem` izin gönderimi ve GHSA-9pf5 iframe kontrolü gerçek
  Electron build'de ölçülmeli.
- Dalga-3 kapsamı (main.js politika dilimi, renderer dilimleri,
  watch/kütüphane, güvenlik yüzeyi, öğrenme/notlar, medya ekstraları,
  altyapı misc) **taranmadı** — istersen sonra tamamlanır.
- Paralel ajanın commit'lenmemiş değişiklikleri ağaçta duruyor; bu not
  onları commit'lemedi.
