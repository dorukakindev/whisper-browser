# Devir Notu — 2026-09-15 13:59

## Oturum özeti

Derin browser denetiminin devamı. Bu turun odağı kullanıcının isteğiyle **son eklenen browser özellikleri** (workflow kayıt/oynatma, video-analiz paneli, atlama aralıkları, referans medya, OCR, altyazı kodlama önizleme, çeviri iş akışları, komut paleti, mini oynatıcı) ve **medya kataloğu** (catalog store/service/extensions/renderer + IPC) idi. Ürün kodu değiştirilmedi; rapor serisi `BROWSER_BUG_REPORT_30.md` ile devam etti (yerel, gitignore'da).

## Bulgular (rapor 30)

Doğrulanmış:

- **B27 (P2)** Workflow son-adım iptali başarı sayılıyor — `browser-workflow-recorder.js` `play()` iptali yalnız adım öncesi kontrol ediyor. Üretim kodu harness'i: `{ok:true, completed:1/1}`.
- **B28 (P2)** Video-analiz panelinden kaydedilen jenerik aralığı atlama panelinin `skipRecords` listesine ulaşmıyor → "kaydedildi" görünmesine rağmen listede yok, aday/otomatik atlama üretmiyor; manuel yenilemeye kadar ölü. Electron harness (değiştirilmemiş üretim modülleri): `VERDICT BUG REPRODUCED`, 10/10 assertion.
- **B29 (P3)** Workflow adımı iç hatasını yutuyor (`completeSelectedBrowserTranslation` catch → `undefined`) → player "tamamlandı" raporluyor.
- **B30 (P3)** `media-catalog-store` `.bak` kurtarması yok — bozuk JSON'da katalog tamamen ölüyor; kardeş `watch-library-store` kendini iyileştiriyor.
- **B31 (P3)** Yetim dosya birikimi: `remove`/`poster-file`/`metadata-apply` eski posteri silmiyor; her `package-apply` `before-restore-complete-*.wbp` yedeği biriktiriyor.
- **B32 (P4)** `list` tüm izleme geçmişini (oturum/altyazı yolları/tercihler) renderer'a gönderiyor + O(items×history) `canonicalWatchKey` parse'ları; payload renderer'da hiç okunmuyor.
- **B33 (P4)** `show(id)` `seasonData`/`metadata` temizlemiyor — B dizisinin sayfasında A'nın sezon önizlemesi görünüyor (token bağlı, veri bozulmuyor).
- **B34 (P4)** 100-adım kayıt sınırında `record()` hatası başarılı komutu "çalıştırılamadı" gösteriyor.

Extras-servis ajanı (geç dönüş, tümü satır-doğrulandı):

- **B35 (P2)** Bozuk `browser-series-context.json` tüm browser çevirilerini kırıyor — `main.js:6246` `translationContext` korumasız, handler try/catch'siz.
- **B36 (P3)** Sekme geneli `cancel` (`browser-feature-services.js:76-77`) diğer panellerin işlerini de öldürüyor + yanıltıcı "Video değişti" etiketi.
- **B37 (P3)** `alignment-preview` referansı ham UTF-8 okuyor (:171) — cp1254'te mojibake.
- **B38 (P3)** `browser-alignment` subprocess timeoutsuz (abort-only).
- **B39 (P3)** UUID altyazı dosyaları süpürge dışı (`^web-*.srt` deseni) + atomik olmayan yazımlar.
- **B40 (P3)** `skip-save`/`skip-list` `seriesName` alanına göre medyayı sessizce başka diziye bağlıyor/çözüyor.
- **B41 (P4 spec)** `skip-save` sahiplik kontrolü normalize-edilmemiş id ile (128-kırpma baypası, güvenilir renderer gerekir).
- **B42 (P4)** Küçük kusurlar grubu: ass-clear bayat frame, TTL-geçmiş önizleme girdisi, aşılan-referans hesap israfı, timeoutsuz executeJavaScript → `hasJobs()`/`canRestore` kilidi, scenes rmSync maskeleme, bozuk skip dosyasının sessiz silinmesi, >2048-anahtar bağlama kaybı.

İkinci katalog ajanı (geç dönüş, doğrulandı):

- **B43 (P3)** Kurcalanmış `.wbp` `rewrite()` substring'iyle tüm geri yüklenen yolları bozabiliyor — `sourceRoot`/`mappings[0]`/`videos[i].source` yalnız `length>=3` denetimli; `'C:\'` girdisi her `C:\` geçişini ezer.
- **B44 (P4)** nMDB dostu hata stdout'ta basılıyor ama exit≠0'da parse edilmiyor → ölü kod, genel mesaj.
- **B45 (P4 spec)** Kurcalanmış nMDB `poster_yolu`'na UNC yolu → giden SMB.
- **B46 (P4)** metadata/season/package token'ları kapanışta iptal edilmiyor + subprocess'lere abort iletilmiyor.
- **B47 (P4)** Store öğeleri yüklemede normalize etmiyor (bozuk-şema çöküyor) + tombstone yok → silinen eser yeniden-içe-aktarımla diriliyor.
- **B48 (P4)** `canRestore` `browserHlsCeaFullCaptureJob` saymıyor → paket-geri-yükleme yakalamayı öldürebilir.
- **B49 (P4)** `season-apply` yerel `airDate`'i boş TMDB değeriyle eziyor.
- **B50 (P4)** Küçükler: token'lar çocuk env'ine sızıyor, arrayBuffer ön-bellekleme, import-cancel sender tutarsızlığı, avif uyumsuzluğu, sınırsız posterCache, LIMIT uyumsuzluğu, ölü kod.

Düşük/spekülatif ilk-tur katalog maddeleri de raporda ayrı bölümde.

## Deneyler

- `.uiprev/browser-audit-30-workflow.cjs` — gerçek `BrowserWorkflowPlayer` + ayıklanmış renderer fonksiyonları.
- `.uiprev/browser-audit-30/` — Electron BrowserWindow'da 4 üretim modülü (skip-segments, browser-features, analysis-tools, video-analysis) + stub köprü.
- Baseline: `browser-workflow-recorder.test.js`, `browser-workflow-core.test.js`, nine-features testleri yeşil; tur başında `npm test` tam paket yeşil.

## Temiz doğrulananlar

Katalog tarafında: tüm eylemler `authorized` arkasında; önizleme token'ları gönderen+tip bağlı/TTL'li/tek kullanımlık; TMDB token bellekte + Bearer; `save` whitelist; nMDB salt-okur + sınırlı; klasör taraması symlink güvenli; paket geri yükleme allowlist + rollback; `subtitle-encoding-preview.js` saf + whitelist. Yeni özelliklerde: workflow ara-adım iptali doğru; kayıtlar `settings-security.js` ile temizleniyor; panel ID/script bağlantıları tutarlı.

## Açık işler / sınırlar

- 8 denetim ajanından 6'sı model hız sınırına takıldı; `browser-feature-services.js` eşzamanlılığı, referans-medya subprocess sınırları, OCR state invalidation, diyalog aralık işleme rapor 31'e açık kaldı.
- Extras-servis ajanı ve ikinci katalog ajanı not yazılırken çalışıyordu; dönerlerse bulgular rapor 31'e eklenir.
- Önerilen düzeltme sırası (tüm seri): B18 → B17 → B28/B27 → B19 → B30 → B31 → gerisi.

## Git

- Başlangıç/bitiş kod: `3a74e998fa5ad01d3d437b5f16c22aa4007efaa0` (ürün kodu değişmedi).
- Dokümantasyon commit'i: bu not + `DEVIR-NOTU.md` indeks güncellemesi.
- Uzak doğrulama: `git ls-remote origin refs/heads/master`.
