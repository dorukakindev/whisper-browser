# 2026-09-19 15:38 — Sağlayıcı model listeleri ve gerçek bağlantı testi

## Kimlik

- Repo: `https://github.com/dorukakindev/whisper-browser`
- Dal: `master`
- Başlangıç ürün commit'i: `f586e28617d7fa23c3af5479e3e84731d3d0273a`
- Çalışma sırasında aynı paylaşılan ağaçta eklenen R82 dokümantasyon commit'i / kod commit tabanı: `15318e9962eb25c7f7d1ca07465a174b647c9efc`
- Bitiş ürün commit'i: `7d1092fa1a7613a55eca0f02222cfe132dcde4df`

## Uygulanan özellikler

- Çeviri model alanının altına sağlayıcıya özel kayıtlı model seçicisi, `Save model / Modeli kaydet` ve `Remove / Kaldır` eylemleri eklendi.
- Model listeleri sağlayıcı kimliğine göre ayrı tutuluyor. Örneğin 4SAPI için kaydedilen model CodeCraftAPI listesinde görünmüyor; sağlayıcıya dönüldüğünde yeniden seçilebiliyor.
- Yeni `Send test request / Küçük test isteği gönder` düğmesi seçili endpoint, model ve API anahtarıyla küçük bir gerçek `chat/completions` isteği yapıyor.
- Başarı yeşil; kimlik doğrulama, model bulunamaması, rate limit, endpoint, timeout, ağ, sağlayıcı ve bozuk yanıt sınıfları kırmızı ve açıklayıcı gösteriliyor. HTTP durumu ve gecikme de kullanıcıya gösteriliyor.
- Varsayılan İngilizce arayüz ile EN/TR geçişi için bütün yeni metinler iki dilli eklendi.

## Güvenlik ve kalıcılık

- Probe yalnız HTTPS endpoint'lerine; yerel geliştirme için yalnız loopback HTTP endpoint'lerine izin veriyor.
- Redirect izlenmiyor. Model adı ve API anahtarı sınırlandırılıyor; anahtar sonuç, log veya hata ayrıntısına geri taşınmıyor.
- Sağlayıcı yanıtı 64 KiB ile sınırlandı; hem `Content-Length` hem akış sırasında sınır uygulanıyor.
- Model profilleri en fazla 32 sağlayıcı ve sağlayıcı başına 64 modelle sınırlı. Kontrol karakterleri, aşırı uzun adlar ve prototype-pollution anahtarları reddediliyor.
- Yeni ayar alanı `translate.modelProfiles`; mevcut settings v3 içinde geriye uyumlu, opsiyonel bir alandır. Şema sürümü artırılmadı.

## Değişen dosyalar

- `src/provider-model-profiles.js`: sağlayıcı kapsamlı model listesi normalizasyonu, ekleme, silme ve serileştirme.
- `src/translation-provider-probe.js`: küçük üretim isteği, güvenli endpoint/yanıt sınırı ve kullanıcıya yönelik hata sınıfları.
- `src/main.js`, `src/preload.js`: yetkili `translation:probe` IPC hattı.
- `src/renderer/index.html`, `renderer.js`, `styles.css`, `ui-locale.js`: model yöneticisi, test düğmesi, yeşil/kırmızı durum ve EN/TR arayüz.
- `src/settings-security.js`: `modelProfiles` içe aktarma ve yerel kayıt doğrulaması.
- `tests/provider-model-profiles.test.js`, `provider-model-ui.test.js`, `translation-provider-probe.test.js`: yeni saf modül, UI/IPC ve güvenlik regresyonları.
- `tests/settings-security.test.js`: model profili kalıcılığı ve bozuk kayıt reddi.
- `tests/electron-foursapi-provider-ui.smoke.js`: geniş/dar görsel düzen, model kaydı ve yerel HTTP 200/404 uçtan uca probe kabulü.

## Doğrulama

- `npm test`: **geçti**; Node test dosyaları ve Python backend testleri, son toplam `192 geçti, 0 başarısız` backend ana paketi dahil tamamen yeşil.
- `npm run test:electron-bridge`: **geçti**.
- `node tests/run-electron-smokes.js foursapi-provider-ui`: **geçti**.
  - Geniş ve 940 px dar görünümde yatay taşma yok.
  - Özel model sağlayıcı listesine kaydolup seçildi.
  - Yerel kontrollü sağlayıcı: HTTP 200 → yeşil `Connection and model are working`; bilinmeyen model HTTP 404 → kırmızı `selected model is not available`.
- `node --check` (main, preload, renderer ve yeni modüller), Python `py_compile`, `git diff --check`: **geçti**.
- Görsel çıktı `.uiprev/foursapi-provider-ui/` altında üretildi; test artefaktıdır ve Git'e alınmadı.

## Kurulum ve sınırlar

- Yeni bağımlılık veya kurulum adımı yok.
- Kullanıcı anahtarıyla canlı ücretli sağlayıcı çağrısı yapılmadı. Canlı kalite/hesap kotası iddiası yok; gerçek istek davranışı izole yerel OpenAI-uyumlu sunucuyla sınandı.
- Sağlayıcının model kataloğu otomatik keşfedilmiyor; kullanıcı model adını yazarak kaydediyor. Bu, erişilemeyen veya güvenilmez `/models` uçlarına bağımlılık yaratmıyor.
- Çalışma ağacındaki başka çalışmalara ait `.freebuff/`, `_repro/`, tarihli devir taslakları ve `m[1])` artefaktına dokunulmadı ve commit'e alınmadı.
