# Devir Notu — 2026-09-23 01:54 UTC — R112 birleşik dayanıklılık gauntlet'i

## Özet

`whisper-browser` güncel master (`850e927`, PR #34 sonrası) üzerinde 12 hatlı
birleşik dayanıklılık çalışması. İki ürün bug'ı bulundu + kırmızı→yeşil düzeltildi
(BUG-R112-01 eşzamanlı-browse supersede, BUG-R112-02 sarkan TR parça kapısı),
iki legacy smoke ortam/beklenti düzeltmesi yapıldı. Kanıtlar PR'da:
`docs/raporlar/r112-evidence/` (gerçek Electron PNG'leri, smoke rapor JSON'ları,
anonim SRT korpusu 1–10k cue, fixture sunucu + koşturucu, sonuç logu).
Rapor: `docs/raporlar/BROWSER_BUG_REPORT_112.md`.

## Dal / commit'ler

- Dal: `codex/r112-resilience-1790126261` (master `850e927`'den)
- Kod commit'leri ve dokümantasyon commit'i ayrı (sırayla):
  1. `truncated_fragment` kapısı — `backend/sentence_translation.py` +
     `backend/transcribe.py` + `backend/test_translation_quality_corpus.py`
  2. per-bid supersede — `src/main.js` `ytBrowseSerialized`
  3. smoke düzeltmeleri — `tests/electron-youtube-oauth.smoke.js` +
     `tests/electron-subtitle-output-smoke.js`
  4. doküman — rapor + devir + evidence + DEVIR-NOTU indeksi

## Bulunan ve düzeltilen bug'lar

### BUG-R112-01 — `ytBrowseSerialized` global supersede (src/main.js)

- Kök neden: tek `_ytBrowseGeneration` sayacı — herhangi bir yeni
  non-continuation browse HER kuyrukta bekleyen browse'u geçersiz kılıyordu.
  Eşzamanlı `FEsubscriptions` + `FEwhat_to_watch` → ilki `{ok:false,
  superseded:true}` döndürür → IPC sözleşmesi ihlali.
- Tek-renderer uygulamada gizliydi (supersede ≈ stale-render çakışır).
- Düzeltme: `_ytBrowseGenerations: Map<browseId, gen>` — aynı bid dedup'u
  korunur, farklı bid'ler serileşir; continuation'lar supersedable değil.
- Kanıt: `electron-youtube-oauth` smoke (sahte OAuth sunucusu) — düzeltmeden
  önce eşzamanlı browse[0] `{ok:false}`; sonra TÜM SENARYOLAR GEÇTİ
  (deviceCalls:5, device:3+code:1+refresh:1, revoke:1, browseCalls:7, meCalls:2).

### BUG-R112-02 — sarkan TR parçalar otomatik kapıyı geçiyordu (backend)

- Kök neden: sağlayıcının döndürdüğü `"...ve"`, `"misin"`, `"gör-"` gibi
  kesik parçalar `translation_meaning_issues`/`_blocking_issues` listesinde
  yakalanmıyordu → ekrana aynıyla yazılıyordu.
- Düzeltme: `_TR_TRAILING_CONJUNCTION` (≥2 hedef kelime; "Ama." muaf),
  `_TR_LONE_QUESTION_PARTICLE` (yalın `mısın?`; "Mi." notası periyotla muaf;
  kaynak ≥3 kelime — "Are you?"→"Misin?" muaf), `_TR_TRUNCATED_HYPHEN`
  (ASCII `-`, `—` muaf) → `truncated_fragment` anlam + blocking sorunu
  (number_mismatch ile aynı reddet→kurtarma yolu). `transcribe.py`:
  `truncated_fragment_indices` sayacı + `issue_indices` + rapor anahtarları
  (`translation_truncated_fragment`, `_indices`) + sert-kapı sayacı.
- Kanıt: `run_r112.py frag` öncesi 4 parça ekranda → sonrası 4 istek
  (1 batch + 3 kurtarma), ekran temiz. FP taraması: 13 meşru form korunuyor
  (quality+golden korpusunda 0 çakışma).

## Smoke (test altyapısı) düzeltmeleri — ürün bug'ı değil

- `electron-youtube-oauth.smoke.js`: Faz 0 PR #26 sonrası otomatik TVHTML5
  cihaz akışına uyarlandı (eski "client formu varsayılan" beklentisi bayattı).
- `electron-subtitle-output-smoke.js`: iç-Electron spawn'ına
  `--no-sandbox --no-zygote` (bu VM'de zygote FATAL Invalid argument(22)) +
  renderer hazır olunca `UiLocale.set('tr')` (EN varsayılan; TR statusText
  assert'i için). NOT: dosya `--all` listesinde değil; attach senaryoları
  yeşil, kuyruk/done-exit fazı bu oturumda ilerlemedi — diagnostics-redaction
  uçtan uca adımı `browser-diagnostics-export.test.js` (yeşil) ile kapatıldı.
- `electron-browser-tools-design`: `--all` batch'inde `{"width":0}` flake;
  standalone'da `{"width":872}` PASS — ürün değil, batch izolasyonu.

## Koşulan testler

- Backend: quality corpus 5/5 · transcribe 192/192 · sentence_distribution
  36/36 · golden 6/6 (venv `python` ile doğrudan dosya koşusu; pytest yok)
- Node: `browser-diagnostics-export` PASS; `node --check` main.js + değişen
  smoke'lar; `py_compile` 3 backend dosyası
- Electron `--all`: 30/32 (2 istisna yukarıda); `youtube-oauth` düzeltme sonrası
  tam PASS; `subtitle-output` kısmi (attach aşaması yeşil)
- `run_r112.py`: 24/24 (10k dahil: 500 istek, 34.9 s) + matrix/anom 18/18
- Koşulmadı: tam `npm test` (talimat), `--network`/`--live` (dış ağ/gerçek
  sağlayıcı — yasak), Windows kabulü (CI'ya bırakıldı)

## Ortam / bağımlılık

- Yeni bağımlılık yok. Fixture sunucu `/home/ubuntu/qa-109/fixture_server.py`
  (16 mod) + `qa-112/run_r112.py` koşturucusu — PR'da `corpus/` altında.
- Test profili izole: `WHISPER_RESOURCE_SOAK_USER_DATA` / mkdtemp profiller;
  kullanıcı verisi/gerçek anahtar yok.
- Legacy not: önceki oturumdan kalan Electron süreçleri (pid 10692, 17613)
  kapatıldı — tek-örnek kilidi/CDP portu karışmasın diye.

## Kalan riskler / sınırlar

- Gerçek YouTube hesabı, ücretli model kalitesi, Windows UI, disk-dolu canlı
  enjeksiyonu, saatlik soak — doğrulanmadı, açık sınır (rapor hat bazında yazar).
- TTML canlı-iz kanıtı yok (parser+birim testiyle kapalı).
- `electron-subtitle-output` kuyruk/done-exit fazı ortamda askıda kaldı —
  smoke'un kendisi legacy; takip konusu olarak açık.

## Sonraki adımlar

- Kullanıcı PR incelemesi + merge kararı (kendim merge etmiyorum).
- CI ubuntu+windows yeşilse dokümantasyon dışında ek iş yok.
