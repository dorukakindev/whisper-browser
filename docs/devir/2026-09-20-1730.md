# Devir Notu — 2026-09-20 17:30 (Europe/Istanbul)

Repo: dorukakindev/whisper-browser · Dal: master
Başlangıç kod commit'i: `9006102` (Parti 12)
Bitiş kod commit'i: `93cd535` — Parti 13: model önbelleği temizliği + capability registry (F18 aşamalı dilim)

## Kapsam

- **F18 (aşamalı ilk dilim — indirme akışı dokunulmadı):**
  - `src/model-manager.js`:
    - `MODEL_CATALOG`: her KNOWN_MODEL için yaklaşık indirme boyutu
      (downloadMb) ve VRAM ihtiyacı (vramMb) — capability registry.
    - `scanModelCache` artık model başına `sizeBytes` (önbellek deposu dizin
      toplamı) ve genel `freeBytes` (statfs `bavail*bsize`) raporluyor.
    - `deleteCachedModel(appPath, model)`: whitelist + kullanılabilir anlık
      görüntü şartı + depo yolunun bilinen önbellek kökü içinde olduğu
      doğrulaması; silinen baytı `freedBytes` olarak döndürür.
  - `src/main.js`: `models:delete` IPC — `authorizedBrowserSender` +
    native `dialog.showMessageBox` onayı (açık kullanıcı seçimi; renderer
    tek başına silemez) + çalışan model/gömme/benchmark işi varken ret.
  - `src/preload.js`: `deleteModel` köprüsü.
  - `src/renderer/index.html` + `renderer.js`: model satırına
    `Önbellekten sil` düğmesi (kurulu değilse disabled); durum satırı
    kuruluysa boyutu, değilse tahmini indirme boyutunu gösterir.
  - `ui-locale.js`: `Önbellekten sil` TR/EN çifti.

## Testler

- `npm test` → Tüm testler geçti (201 IPC kanalı yetki taraması dahil).
- `tests/model-manager.test.js` +2 davranış testi (boyut/katalog/freeBytes;
  deleteCachedModel yalnız hedefi siler, sibling korunur, bilinmeyen
  kimlik ret).
- `tests/browser-parti7.test.js` F18 blok asserts.
- Saptanan hata: `statfsSync().avail` alanı yok → `bavail*bsize` ile düzeltildi.
- Gerçek silme yolu Electron'da denenmedi (native dialog CI'da açılamaz) —
  IPC katmanı unit-test ile sınırlandı.

## Kalan E maddeleri

- F18 geri kalanı (bağımlılık tarafı: yt-dlp/venv sürüm panosu, devam/
  checksum — ayrı parti gerektirir; mevcut kilit sürümler korunacak)
- F20 (tarihli gerçek-site kabul matrisi belgesi)

K/B/R maddelerine dokunulmadı.
