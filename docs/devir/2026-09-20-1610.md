# Devir — 2026-09-20 16:10 (Europe/Istanbul)

## Kapsam
Katalog Parti 6 — sekme ve sayfa araçları paketi (E maddeleri B06, B10, B14).
K/B/R maddelerine dokunulmadı.

## Commit'ler
- Kod: `f31775b` — "Parti 6: sekme menüsü+hover önizleme (B06), element picker (B10), otomatik PiP (B14)"
- Başlangıç HEAD: `27caa54` (Parti 5b belge commit'i)

## Değişen dosyalar
- `src/renderer/renderer.js` — B06: sekme şeridi bağlam menüsü (`openBrowserTabMenu`:
  Sekmeye geç, Kopyasını aç, Sabitle/Sabitlemeyi kaldır, Bağlantıyı kopyala,
  Diğer sekmeleri kapat, Sağdaki sekmeleri kapat, Grubu düzenle…, Sekmeyi kapat
  [sabit sekmede kapalı]) ve 450ms gecikmeli hover/focus önizleme kartı
  (`showBrowserTabPreview` — başlık+URL+Sabit/Sessiz rozetleri, pointer-events:none).
  B10: araç menüsüne `browserElementPick`/`browserElementClear` işleyicileri —
  seçim → geçici gizleme → `openAppDialog` onayı → `save` veya `undo`.
  B14: `browserAutoPip` kalıcı onayı; `activateBrowserTab` içinde ses çalan
  sekmeden ayrılırken `browser:extras mini-open` otomatik tetiklenir.
- `src/browser-element-rules.js` (yeni) — `BrowserElementRules` kaynak-bazlı
  kural deposu: `userData/element-rules.json`, atomik tmp+rename, kaynak başına
  40 seçici / toplam 60 kaynak sınırı; `validSelector` `{ } @ ; <` karakterlerini
  yasaklar (CSS kırılımı engeli; `>` ve `:` meşru seçici parçası olarak serbest).
- `src/main.js` — `browser:elementRules` IPC (`list|pick|save|undo|clear`,
  `authorizedBrowserSender` + aktif sekme doğrulaması); `BROWSER_ELEMENT_PICKER_SCRIPT`
  sayfa içi seçici (outline işaretleme, Esc iptal, 6 seviye `#id`/`tag.cls:nth-of-type`
  yolu); `applyBrowserElementRules` her `did-stop-loading`'de `insertCSS(cssOrigin:'user')`
  ile kuralları uygular, sekme kapanışında anahtar temizlenir; seçim sırasında
  geçici gizleme `elementPickCssKey`'de tutulur (`undo` geri alır).
- `src/preload.js` — `browserElementRules(payload)` köprüsü.
- `src/renderer/index.html` — tarayıcı menüsüne "Sayfada öğe gizle…" ve
  "Bu sitede gizlenenleri geri yükle…" öğeleri; görünüm anahtarları arasına
  `browserAutoPip` ("Sekme değişince videoyu küçük oynatıcıda sürdür").
- `src/renderer/styles.css` — `.browser-tab-preview*` kartı ve `.browser-tab-menu` genişliği.
- `src/renderer/ui-locale.js` — 29 yeni TR→EN çift.
- `src/settings-security.js` — `PERSIST_CHECKBOX_CONTROLS`'e `browserAutoPip`.
- `tests/browser-parti6.test.js` (yeni) — kural deposu sözleşmesi (CRUD, tekilleşme,
  sınırlar, kalıcılık), `originOf`/`validSelector` güvenlik sınırları, IPC/renderer
  bağlantı regex'leri: ~40 kontrol.

## Testler
- `node --check` değişen tüm .js dosyaları: temiz.
- `node tests/run-all.js`: **tüm testler geçti** (Electron smoke'lar ayrı koşulur;
  önceki partilerdeki bilinen ortam hataları koşulmadı, regresyon değil).
- Ara düzeltme: `applyBrowserElementRules` çağrısı `browser-overlay-readiness.test.js`'in
  vm diliminde tanımsız kaldı → `typeof === 'function'` koruma eklendi.
  `validSelector` başta `>`'yi de yasaklıyordu; meşru CSS birleştirici olduğundan
  yalnız `<` kaldı — testte yakalandı ve düzeltildi.

## Bilinen durum / sonraki
- Otomatik PiP tetik noktası sekme değişimiyle sınırlı (ses çalan sekme için);
  tarayıcı yüzeyini gizleme yolunda tetik yok — kasıtlı, yanlış pozitifi önler.
- Element kuralları kaynak (origin) bazlıdır; alt yol ayrımı yok.
- Parti 7 bekliyor: A17 (mpv/VLC harici oynatıcı), A23 (arama önerileri),
  A28 (playlist sayfası), A04 (bölümler), A06/A07 (abonelikler).
- Yeni bağımlılık yok.
