# Devir Notu — 2026-09-18 17:30

## Konu
R67 doğrulanmış bulgularının onarımı + SmartTube/Invidious oynatıcı yüzeyinin uçtan uca çalışır hale getirilmesi (kullanıcı: "player tarafında tam bir smarttube olsun").

## Başlangıç
- Dal: `master` · başlangıç HEAD: `7cf5d568fff6879f2704a4d7c199c75117eb0c1e` (rapor 67 push'u)
- Ağaç: paralel SmartTube çalışması commit'lenmiş (`1c17629`+); izlenmemiş başka-iş-akışı WIP dosyaları mevcut (`tests/report62/63/64-*`, `src/browser-sensitive-keys.js`, `_repro/`) — dokunulmadı.

## Değişen dosyalar
- `src/renderer/renderer.js` — R67-01 TDZ (bildirimler init'ten öne); `invCall` IPC serileştirmesi; `stSectionSeq`/`stSearchSeq` yarış korumaları; tek-tık kart oynatma (`pendingAutoOpen`); kanal sayfası başlık+geri; login modal Esc/backdrop/şifre-temizleme; `refreshSmartTubeAuthUI` (sidebar+panel senkron); oturum geri yükleme; `stSearchResults`/`stGrid` görünürlük mantığı; thumbnail mutlaklaştırma; CANLI rozeti/görüntülenme/tarih; HLS kurtarma `probeWithActiveSource`; İndir kaynak-dallı (`invidious:downloadStream`); `invidious:event` dinleyicisi; `hideHomeOnVideoLoad` HLS yolunda; `openPlayer` medya yokken tarayıcıyı gösterir.
- `src/renderer/index.html` — CSP `img-src|media-src|connect-src https:`; `invidiousLoginModal` script'ten öne; ölü `invidiousHomePage` nesli kaldırıldı; `Oturum aç/kapat` etiketleri.
- `src/renderer/styles.css` — `.st-card-live`, `.st-channel-*`; `.st-card:focus-visible`'den `outline:none` kaldırıldı.
- `src/renderer/ui-locale.js` — SmartTube dizgileri + 4 eksik statik çeviri; çift `Ara...` anahtarı düzeltildi.
- `src/main.js` — `runInvidiousCommand` sonuç beyaz listesi (feed/search/channel/login/logout), per-kind timeout, NDJSON taşma, `invidious:event` iletimi, SID env (`WHISPER_INVIDIOUS_SID`) + sonuçtan `sid` soyma, `validateInvidiousInstance`/`resolveInvidiousInstance`/`invidiousSessionInstance`, `invidiousSessions` haritası, `invidious:downloadStream` → `runMediaCommand(media.py)` (download slot), `invidious:cancel` sahiplik korumalı, spawn hatası sınıflı fallback (err.message yok).
- `src/preload.js` — login'e `instance` opsiyonu.
- `backend/invidious.py` — `email`+`email_or_user` login varyantları; `/api/v1/auth/feed`; `set_session`/`_auth_headers` (SID Cookie); `_fetch_with_failover`/`_iter_instances`; `_to_int`/`_abs_url`/`_pick_thumbnail`; `extract_video_id` genişletildi (nocookie/live/v-param-sırası/instance); probe şeması (`dashUrl`, `videoThumbnails`, `resolution`, `liveNow`, `audioTrack` → `heights`/`stream`/`audioLangs`/`isLive`); `fetch_subs` instance-proxied + `kind=asr` + `language_code` + `url`; `_convert_timedtext_to_srt` srv1+srv3+VTT, boş/tanınamayan → RuntimeError; çıktı `Başlık [vid].lang.srt`.
- `backend/media.py` — `download_stream`: `--url` opsiyonel, `--video-id`, mime→container, ffmpeg önce kontrol, birleşik %100 progress, `.invtmp`/kısmi dosya temizliği; `urllib_parse` importu.
- `backend/test_invidious.py` — 48 test (orijinal 307 satır korundu; boş-içerik/srv1/VTT davranışları yeni doğru semantiğe uyarıldı; R67 URL varyantları + `_to_int`/`_abs_url`/`_parse_video_item` guard sınıfları eklendi).
- `tests/report67-smarttube-wiring.test.js` — YENİ: 24 kaynak-sözleşme testi.
- `tests/electron-smarttube-boot.smoke.js` — YENİ: gerçek Electron boot → TDZ regresyonu uçtan uca.
- `tests/report65-invidious-bridge.test.js` — bayat iddialar güncellendi (yeni extract_video_id pattern'leri, cancel sahiplik semantiği).

## Doğrulama
- `report67-smarttube-wiring` 24/24 · `test_invidious.py` 48/48 · `electron-smarttube-boot.smoke` GEÇTİ (`{"ok":true}` — tüm problar canlı, `initSmartTube`→`invidious:feed` invoke)
- `node --check` renderer/main/preload/ui-locale + `py_compile` invidious/media — temiz
- `design-system`, `ui-locale`, `ytdlp-runtime`, `report65` (21), `player-ui` (145) — yeşil
- `npm test` — kalan kırmızılar yalnız izlenmemiş WIP test dosyaları (başka iş akışının yarım düzeltmelerini bekliyor): `report62-translation-deep-fix`, `report63-secret-redaction`, `report63-ssrf-observer`, `report64-address-listbox`, `report64-contrast` — bu teslimde dokunulmadı.

## Kanıtlanan kullanıcı senaryosu
"İçerik yükleniyor…"da takılma = R67-01 TDZ çökmesiydi; boot smoke artık renderer'ın sıfır uncaught-hatayla `invidious:feed` invoke'una ulaştığını gösteriyor.

## Kalan sınırlar
- Canlı Invidious ağı/login/HLS bu ortamda doğrulanmadı (ağ erişimi yok) — kullanıcı ortamında `start.bat` ile doğrulanmalı.
- `email`/`email_or_user` login varyantları gerçek hesapla denenmedi.
- İzlenmemiş WIP testleri kırmızı kalıyor — sahibi iş akışı tamamlamalı.
