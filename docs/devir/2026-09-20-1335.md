# 2026-09-20 13:35 — Feature research decision review

- Repository: https://github.com/dorukakindev/whisper-browser, branch `master`.
- Start/end product-code commit: `1dcb2d5b34fc768957b9dcee99e9a8b129dae5c5` (product code unchanged).
- Decision report: [FEATURE_RECOMMENDATION_REVIEW_2026-09-20.md](../../FEATURE_RECOMMENDATION_REVIEW_2026-09-20.md).

## Scope and evidence

Read R75/R76 feature research, all seven `FEATURE_BLUEPRINTS.md` items, the September 14 streaming-subtitle study, the September 12 implemented-proposal ledger, the cosmetic backlog and the historical integration brief. Compared highest-value candidate claims with current SmartTube renderer, Invidious subtitle backend, page index, subtitle/DASH parser, thumbnail service and tab lifecycle. Checked relevant current Invidious API documentation, Electron documentation, Remote Playback specification and third-party licensing references. No new feature or bug fix was implemented.

Main corrections: TTML/stpp and static DASH template handling already exist; full-text page history exists but semantic page search does not; browser-reference thumbnail support is not automatically available to the local player; SmartTube has a recommendation rail and watch-library history but not a user-managed play queue or progress on every SmartTube card. The dated research reports are therefore not an implementation checklist.

Recommended sequence: (1) SmartTube card progress/history, then explicit play-next queue; (2) bounded local and YouTube preview paths; (3) local subscriptions with import/export; (4) narrower subtitle moviehash and service acquisition proven by authorized structural fixtures; (5) MediaSession/search suggestions and small UI follow-ups. The report separates conditional, deferred and rejected families and names acceptance gates.

## Verification and limits

- Documentation/source-only change. `git diff --check` and report path/content checks performed. Product tests and Electron UI were intentionally not run; no product behavior changed.
- No authenticated YouTube/streaming trial, provider request, actual model run, dependency install or third-party source copying.
- Existing untracked `.freebuff/`, `_repro/` and older `docs/devir/2026-09-19-*.md` were not touched.
- `git ls-remote` initially failed because this sandbox's shell could not reach GitHub via its configured proxy. Push and remote equality are **not yet verified**; do not claim them until a later successful check.

## Next step

Pick one proposed milestone; re-check the then-current branch and write behavioral tests before implementation. For protected-service subtitles, obtain a consented structural fixture and a coverage contract before any adapter work.
