# Devir Notu — 2026-09-26 00:42

## Repo / dal

- Repo: `https://github.com/dorukakindev/whisper-browser`
- Dal: `devin/repo-research-2026-09-25`
- Başlangıç HEAD (ürün kodu): `e6d83c00b2bc29d58c6c302d84f60472c44d4d55` (R124 raporu)
- Bitiş HEAD (ürün kodu): `e6d83c00b2bc29d58c6c302d84f60472c44d4d55` — **ürün kodu değişmedi; yalnız dokümantasyon**

## Yapılan iş

Kullanıcı isteği: depoda ve klasördeki tüm tarihsel bug raporlarının false
positive'lerini ve gerçek bulgularını doğrulayıp dosyada işaretlemek.

~172 rapor-benzeri dosya envanterlendi (`docs/raporlar/` ≈90, kök ≈45,
`docs/` stray'leri, izlenmeyenler, `haze/bug-report-2026-09-24` dalındaki
R124/R125). Kanonik doğrulama dosyası yazıldı:

- `docs/raporlar/BUG-RAPORLARI-DOGRULAMA-MATRISI-2026-09-26.md` — tüm rapor
  aileleri için madde/dosya bazında karar tablosu: GERÇEK→DÜZELTİLDİ /
  GERÇEK→AÇIK / FALSE POSITIVE / KISMEN / TEORİK-MANUEL / ÖNERİ / BAYAT.

## Bu oturumda bağımsız doğrulananlar (canlı probe + fix-commit kanıtı)

- PROGRAM_BUG_REPORT_83/84/85 hakemliği → `b112a03` fix commit'i ile
  birebir: 83-02/03/04/05 ve R95-13 GERÇEK→DÜZELTİLDİ; R95-03/08/09/10/
  12/14/15/16/17 FALSE POSITIVE (kararlar R89 ile %100 aynı).
- haze R124/R125: `_vtt_to_srt` kompakt VTT cue kaybı **AÇIK** (canlı repro);
  `safePageIndexUrl` sorgu kesme **AÇIK**; `canonicalWatchKey` file:
  lowercase **AÇIK** (case-sensitive FS); `StatusCopy.clock(Infinity)` →
  `"Infinity:NaN"` **AÇIK**; `process-io.js` stderr latent (yalnız test).
- R78 kalıntıları: `PHPSESSID`/`JSESSIONID`/`AWSAccessKeyId`/`api_token`/
  `pwd`/`x-auth-token`/`secret_key` hâlâ redakte edilmiyor; `;jsessionid=`
  yol-içi sızıntı sürüyor → **AÇIK-7**.
- B-108_LOCAL denetimi: B-108-03 (iç ağ SSRF) **FALSE POSITIVE** — LAN
  gezinmesi kasıtlı (`main.js:4728` yorumu); B-108-11 bozuk skip-segments
  JSON sessiz ezmеsi **AÇIK P3**; B-108-12 bayat (refactor); B-108-16/31
  ve B-108-32 düzeltilmiş.
- Kök "Hata N" serisi (57–355): `AUDIT-KAPANIS-MATRISI` + `TUM-BULGULAR`
  kapanışı kanonik; örneklem (340/341) bu oturumda teyit.
- Eski `resolvePython` "py hiç denenmiyor" iddiası **FALSE POSITIVE
  (bayat)** — güncel `main.js` üç komutu da `spawnSync` ile yokluyor.
- `security-audit-report-2026-09` başlık SSRF iddiası **FP**: `200.2.x.x`
  public IPv4; gömülü-v4 blokları zaten `browser-manga.js`'te.

## Konsolide AÇIK bulgular (matriste §6)

AÇIK-1 preview_refresh koşul boşluğu (R124-01) · AÇIK-2 boş `--sync-srt`
(R124-02) · AÇIK-3 `_vtt_to_srt` kompakt cue kaybı · AÇIK-4
`safePageIndexUrl` sorgu kesme · AÇIK-5 `canonicalWatchKey` lowercase ·
AÇIK-6 `StatusCopy.clock` Infinity · AÇIK-7 URL redaksiyon eksikleri +
path-sessionid · AÇIK-8 bozuk skip-segments JSON ezme (P3).

## Değişen dosyalar

- `docs/raporlar/BUG-RAPORLARI-DOGRULAMA-MATRISI-2026-09-26.md` (yeni)
- `DEVIR-NOTU.md` (indeks güncellemesi)
- `docs/devir/2026-09-26-0042.md` (bu not)

## Testler

- Ürün kodu değişmedi; `npm test` bu turda tekrar koşulmadı (önceki turda
  `e6d83c0` üzerinde yeşildi).
- Doğrulama kanıtları: `node -e` probe'ları (translation-scheduler gap,
  IDN normalizasyonu, secret-store merge, redaksiyon listesi,
  `_vtt_to_srt`, `clock(Infinity)`), `git log` fix-commit eşleşmeleri,
  kaynak okumaları (`browser-navigation-policy.js`, `secret-store.js`,
  `browser-feature-services.js`, `browser-translation-scheduler.js`).

## Yapılmayanlar / sınırlar

- Kökteki tarihsel rapor dosyaları **taşınmadı/silinmedi** (R121 kararı:
  kullanıcıya ait artefaktlar); kararlar matriste işaretlendi.
- Gerçek YouTube hesabı, ücretli sağlayıcı, GPU, gerçek Windows makine
  koşuları yapılmadı — ilgili maddeler matriste TEORİK-MANUEL.
- AÇIK-1…8 için ürün kodu düzeltmesi bu turda yapılmadı; sonraki iş
  bunların düzeltmesi olabilir (öncelik: AÇIK-1, AÇIK-3, AÇIK-4, AÇIK-7).
- R36–R50 spekülatif turları dosya-bazında gruplandı; madde-madde yeniden
  doğrulama gerekirse kapanış matrisleri kanoniktir.

## Sonraki adımlar

1. AÇIK listesini tek tek düzeltme turu (her biri için kırmızı test → fix).
2. R78-01 redaksiyon listesini R66-08 önerisiyle birlikte genişletme kararı
   (imzalı-URL parametreleri + `x-goog-*`/`x-amz-*` önekleri).
3. Kök raporlarını arşivlemek istersen ayrı `docs/arsiv/` taşıma PR'ı.
