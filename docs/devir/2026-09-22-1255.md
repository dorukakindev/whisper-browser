# Devir Notu — 2026-09-22 12:55 (Europe/Istanbul)

## Kapsam ve kimlik

- Depo: `dorukakindev/whisper-browser`; teslim dalı: `codex/report-audit-20260922`.
- Başlangıç ürün kodu: `93f27d34a6b11633dc64d80556e01b2a9d352d58` (`origin/master`, #23 birleşik gauntlet).
- Bitiş ürün kodu: `4f9713ade3d1e4707cc417b28d8b7672391bb1a5` (`fix: audit subtitle translation and browser reliability paths`).
- Kullanıcının kirli `codex/catalog-sync` çalışma ağacı korunmuştur. Düzeltme ayrı `scratch/report-audit-20260922` worktree'sinde yapılmıştır. Ekli DOCX/Markdown raporları iddia kaynağı olarak okundu, talimat sayılmadı. Bulgu tablosu `BROWSER_BUG_REPORT_105.md` içindedir; `BROWSER_BUG_REPORT_104.md` birleşen PR #24'e aittir.

## Değişiklikler

- `src/subtitle-sentence-layout.js`, `backend/sentence_translation.py`: Türkçe sayı kapıları saat `5:30` ↔ `5.30` / `beş buçuk`, `3.5` ↔ `üç buçuk`, `10 bin` / `2 milyon` ve çekimli küçük sayı sözcüklerini tanır. Farklı değerler hâlâ `number_mismatch`. Genel dilsel sayı çözümlemesi değildir.
- `src/main.js`, `src/subtitle-sentence-layout.js`: parça yokluğu ve parça uyumsuzluğu için hata kodları; geçerli tam metni yeniden dağıtma. Bilinen `Çeviri:`/`Translation:` JSON sarmalını açma, bozuk sarmalı ekrana basmama; Türkçe dışı hedeflerde Türkçe altyazı istemi göndermeme.
- `src/browser-translation-scheduler.js`: açık yeniden denemede ve iptalde devre kesici hata serisini sıfırlama.
- `backend/youtube.py`, `src/main.js`, `src/renderer/renderer.js`, `src/renderer/ui-locale.js`: uzak Google revoke HTTP sonucunu doğrulama, önce refresh token'ı seçme, yerel çıkış başarılı ama uzak revoke belirsizse EN/TR görünür uyarı. Türkçe/İngilizce kısaltılmış izlenme sayısı ayrıştırması. Kuyruktaki bayat non-continuation YouTube browse isteğini atlama.
- `src/browser-site-permissions.js`, `src/main.js`: izin kökenini istek yapan frame'den alma; alt frame belirsizse üst sayfanın iznini miras bırakmadan reddetme.
- `src/browser-subtitle-output.js`: ASS için açık 1920×1080 çözünürlük ve ölçekli, kenar boşluklu varsayılan stil.
- `backend/test_translation_quality_corpus.py`, `backend/test_youtube.py`, `tests/subtitle-sentence-layout.test.js`, `tests/browser-translation-reliability.test.js`, `tests/browser-subtitle-output.test.js`, `tests/report104-permission-regressions.test.js`, `tests/report104-youtube-browse.test.js`: karşı örnekli regresyonlar.
- `BROWSER_BUG_REPORT_105.md`: doğrulanan/kısmen doğrulanan ve açık kalan bulguların sınırları.

## Doğrulama

- `npm test`: tüm JS/Python testleri geçti; son remote-revoke `remote_ok` değişikliğinden sonra tam paket yeniden çalıştırıldı ve `Tüm testler geçti` sonucuyla bitti. Ayrıca hedefli `backend/test_youtube.py` revoke testi geçti.
- `npm run test:electron-bridge`: gerçek Electron trusted bridge smoke geçti.
- `node --check src/main.js`, `node --check src/renderer/renderer.js`, `py_compile` (sentence translation, youtube, transcribe), `git diff --check`: geçti.
- Varsayılan izole `node --test` çağrısı sandbox child-process `spawn EPERM` verdi; aynı hedefli dosyalar `--test-isolation=none` ile, tam paket ise izinli ortamda geçti. Bu ürün assertion hatası değildi.

## Açık sınırlar

- Gerçek Google hesabı/revoke, canlı ücretli çeviri sağlayıcısı, gerçek alt-frame izin diyaloğu ve libass ekran görüntüsü doğrulanmadı. Mock/ünite başarısı bunların yerine geçmez.
- Ekli denetimlerdeki tüm iddialar kapatılmadı. T-03, T-06/T-07, sayfa çevirisindeki T-08, büyük UI/UX önerileri, DRM/upstream ve gerçek-site kabulü ayrı kapsam olarak açık. Rapor 104 bunları tamamlama iddiasında bulunmaz.
- Yeni bağımlılık, şema veya kurulum değişikliği yok.
- Bu notun commit kimliği kendi içinde yer almaz. Push ve uzak kimlik kontrolü teslim sonunda ayrıca yapılır.
