# Devir Notu — 2026-09-15 10:36 — Browser derin denetim turu (rapor 26)

## Özet

Kullanıcı isteğiyle kod değiştirmeden browser tarafında derin bug avı sürdürüldü. Yeni bulgular `BROWSER_BUG_REPORT_26.md`'ye yazıldı (dosya `.gitignore` kapsamında, serinin öncekileri gibi yerel). Ürün koduna bu turda dokunulmadı.

## Başlangıç/bitiş

- Başlangıç HEAD: `ac5dda11acaae9bd6fa81b71ad429b2b7f3f89a7` (`ac5dda1`, `docs: hand off browser audit report 25`)
- Bitiş kod kimliği: aynı — kod değişmedi; yalnız bu not + indeks commit'i eklendi.
- Dal: `master`, remote: `https://github.com/dorukakindev/whisper-browser`

## Önemli: eşzamanlı kullanıcı çalışması

Denetim sırasında çalışma ağacında kullanıcının commit'lenmemiş ~420 satırlık başka işi vardı (`browser-capture-provenance`, `browser-dash-capture`, `browser-subtitles`, `browser-translation-integrity`, `main.js`, `preload.js`, `renderer.js` + ilgili testler). Bu değişikliklere dokunulmadı, commit'e dahil edilmedi. Bulgular ağacın o anki durumuna göre yazıldı.

## Yeni bulgular (rapor 26)

- **B7 (P2):** `browser-site-permissions.js` — `withBrowserPermission` 'ask' seçiminde kaydı siliyor; `fullscreen`/`clipboard-sanitized-write` varsayılanı `allow` olduğu için "Sor" tercihi sessiz izne dönüşüyor. UI `stored || 'ask'` gösterirken motor allow uyguluyor.
- **B8 (P3):** `browser-note-store.js:79` — yedekten kurtarma sonrası `flush` korumasız; fırlatırsa konstruktör patlar, `browserNoteStore()` örneksiz kalır, tüm not IPC'leri tekrar tekrar hata verir (yedek veri bellekte hazır olmasına rağmen).
- **B9 (P3):** `browser-translation-archive.js` — stem'deki `contentDigest` her revizyonda yeni dosya çifti üretir; eski stem'ler hiç silinmiyor. Sayfa düzenleme/dışlama başına arşiv yazımı birikimi hızlandırıyor. Çökme arası yetimler de var (index tutarlı kalıyor).
- **B10 (P4):** `browser-tab-resources.js:23` `tab.downloadActive` hiç atanmıyor — indirme unload koruması ölü bayrak; karar gerekiyor (kaldır ya da yaşam döngüsüne bağla).
- **B11 (P4):** `main.js:8793` sayfa-içi `exclude` `pageTranslateJob` kontrolsüz çalışıyor; panel yolu (:12506) reddediyor. `acceptsResult` doğruluğu koruyor ama kuyruktaki dışlanmış bloklar API kotası harcıyor; politika tutarsız.

## Yeniden doğrulananlar (rapor 25, hepsi açık)

B1 retry kapsam genişlemesi, B2 bfcache ölü gözlemciler, B3 ikincil tam ekran, B4 ölü olaylar (kapıdan geçip işleyicisiz kalıyor), B5 atomik olmayan varlık çifti, B6 Enter busy atlama (`call()` içinde gerçekten guard yok — kesinleşti).

## Gözlemler

`unifiedLibrarySearch` zaman-damgası sıralama çarpıklığı, `ceaUrlKey` sorgu atma, `splitSecondaryTabId` ham/normalize karşılaştırması (aşağı akış koruyor — kozmetik), OpenSubtitles token önbelleksizliği, hls-recovery bayat `playing` token temizliği.

## Testler

- Çalıştırılan: yok (salt statik denetim + rapor).
- Rapor 26'da 7 öncelikli eksik test listelendi (izin ask eşlemesi, note-store recovery-flush, arşiv yetim GC, downloadActive, sayfa exclude, retryFailed kapsamı, Enter/busy).

## Sonraki adımlar

- Düzeltme fazı istenirse önerilen sıra raporda: B7 → B1 → B8 → B9 → B2 → B4+B6 → B3 → B5 → B10/B11.
- Kullanıcının ağaçtaki ~420 satırlık işi commit/push edilmeden önce kendi doğrulamasını yapmalı; bu raporun bulgularıyla çakışma yok.
