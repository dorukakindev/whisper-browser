# 2026-09-23 14:05 — R114 gerçek uçtan-uca doğrulama (tiny E2E + canlı HLS + Windows)

## Kapsam ve karar

Kullanıcının "Hepsi sırayla (önce 1, sonra 3, 2 onaylı)" onayıyla üç hatta gerçek koşu doğrulaması yapıldı — R110–R113'te fixture/mock ile ölçülen yolların ilk gerçek kanıtları:

| Hat | Karar |
|-----|-------|
| R114-A Gerçek tiny-model E2E transkripsiyon | **PASS** — indirme→ffmpeg→motor→SRT→oynatıcı zinciri ilk kez gerçek çalıştı; ürün bug'ı yok |
| R114-B Canlı HLS rolling-playlist gauntlet | **PASS** — kayan pencere + discontinuity + gecikme + 404-retry + ENDLIST + canlı altyazı yakalama hepsi temiz; 1 fixture artefaktı belgelendi |
| R114-C Gerçek Windows doğrulama | **PASS + ortam bulguları** — Electron açıldı, gerçek transkripsiyon çalıştı, kapanış süreç ağacını temizledi; install.bat sürüm kapıları + bağımlılık notları |

Detay: `docs/raporlar/BROWSER_BUG_REPORT_114.md` · kanıtlar: `docs/raporlar/r114-evidence/`

## Üretilen ortam/dosya durumu

- Repo/dal: `codex/r114-real-e2e-1790133270` (master `0c7533b`'den ff; R112+R113 dahil)
- Başlangıç ürün kodu: `0c7533b` — bitiş ürün kodu: **aynı** (ürün kodu değişmedi, bug bulunmadı)
- Çalışma dizini: `/home/ubuntu/qa-114` (fixture'lar, loglar, ekran görüntüleri — kanıtlar repoya kopyalandı)
- Windows çocuk oturumu: https://app.devin.ai/sessions/ed0f551b773b4abfa09883e4d99ec370 (platform=windows, kendi VM'i)

## R114-A — gerçek tiny transkripsiyon kanıtı

- `gen_clip.py`: ffmpeg `flite` TTS 8 cümle + `anullsrc` sessizlikler → 30.9 sn `clip.mp4`
- `tiny` model ~75MB HF'den gerçekten indirildi (cache zamanı 13:06, model.bin 75,538,270 B)
- `backend/venv/bin/python backend/transcribe.py --input clip.mp4 --model tiny --device cpu --formats srt,json --vad-filter true --split-mode smart` → 7 blok SRT + JSON + düşük-güven raporu; RTF 12.4x
- Cümle birleşimi (0.7s boşluk) → tek cue'da gerçek `\n`; sayı/özel ad sayısal; `--clip-start/end` mutlak geri hizalama; `--reexport` JSON→VTT/TXT; geçersiz `--output-name-suffix` doğru reddedildi
- Oynatıcı kanıtı: ayrı Electron profili + gerçek GTK dosya diyaloğu (`api.selectVideo` + xdotool Ctrl+L + dizin yolu + Open) → kardeş SRT otomatik bağlandı → overlay'de gösterim (`overlay-t10.png`)
- Not: `--output-name-suffix` yalnız `-whisper-*` aşamalı kimliği kabul eder; medya yolu yalnızca gerçek kullanıcı seçimi veya MessageBox izniyle yetkilenir (JS çağrısı yetkilendirmez — tasarım)

## R114-B — canlı HLS kanıtı

- `live-server.js`: gerçek HTTPS (CSP `connect-src https:` gereği; self-signed + `--ignore-certificate-errors`); `produced()` gerçek-zamanlı, 4 segmentlik kayan pencere (`media-sequence=n-4`), DISCONTINUITY seg-008, +250ms seg-010, geçici-404 seg-012, `--endlist-at`, `/stats` sayaç
- Segmentler: kesintisiz PTS'li MPEG-TS (`-reset_timestamps 0` — önceki `-reset_timestamps 1` denemesi DISCONTINUITY'de ~21s yapay stall üretti; fixture artefaktı, raporda belgelendi)
- Sonuç: canlı kenara katılım t≈2.57, 2.57→31.24 sıfır stall, tüm adversity noktaları sırayla aşıldı; seg-012 404→retry→200; altyazı playlist'i 16× poll + cue büyümesi 1→7, activeCues doğru; ENDLIST mid-play → içerik sonuna oynatıp temiz `ended`; istek ekonomisi 16/16+16/16 tek kez
- Canlı altyazı yüzeyi: **native TextTrack** video üstünde render (`live-a.png`); `player.cues`/`#subtitleOverlay` boş kalır — live iz side-file overlay yolundan değil (tasarım)
- Kenar: medya stoğu bitip ENDLIST yokken pencere donarsa hls.js erken `ended` verir (`run-frozen-window.json`) — gerçek canlıda oluşmaz; belgelendi

## R114-C — Windows bulguları (çocuk oturum raporu)

PASS: start.bat→Electron açılışı; `app:getEnvInfo` GPU'suzlukta fallback; CLI + uygulama-içi gerçek transkripsiyon (97.7sn→SRT); transkripsiyon ORTASINDA pencere kapanışında taskkill /T /F süreç ağacını temizledi (0 orphan); `package:win` paketlenmiş exe açıldı/kapandı; node --check 0; npm test python suitleri OK.

Ortam bulguları (ürün bug'ı değil):
1. install.bat preflight: Node≥22.13 + Python 3.10/3.11 kapıları VM sürümlerinde durdu (açık hata — doğru davranış)
2. ctranslate2 4.8.2 pin'siz kurulumda `0xC0000005` çöküyor; repo pin'i 4.4.0 ile çalışıyor
3. onnxruntime 1.30.0 `vcruntime140_1.dll` gerektiriyor → vc_redist.x64 (VAD'sizlik uyarısı log'da)
4. start.bat DRM verify-pkg `castlabs_evs` yokken interaktif [E/H] soruyor
5. GPU'suz ortamda UI rozeti hâlâ `CUDA · float16` (ayar varsayılanı; fallback doğru raporlanıyor) — iyileştirme adayı
6. npm test 5 dosya ortam-bağımlı başarısız (undici/webidl Node≥22.13, srt, PIL, guessit, genanki) — Windows'a özel kod hatası yok

Sınır: GPU'lu Windows (cuDNN PATH + CUDA) ve DRM imzalı Castlabs akışı doğrulanamadı.

## Çalıştırılan testler / çalıştırılmayanlar

- Çalıştırılanlar: gerçek transcribe CLI (Linux + Windows), Electron CDP ölçümleri (live ×3, oynatıcı ×2), sunucu `/stats` doğrulamaları, `node --check`×3 (Windows'ta), Windows `npm test` tam (çocuk oturumda)
- Çalıştırılmayanlar: Ubuntu `npm test` — ürün kodu değişmedi (yalnız docs/evidence); regresyon riski yok. Kanıtlanamayan iddia raporlanmadı.

## Bağımlılık/kurulum etkisi

Yok — repoya bağımlılık eklenmedi; fixture'lar yalnız rapor/evidence altında.

## Commit'ler

- Kod commit'i: yok (ürün kodu değişmedi)
- Docs commit'i: rapor + `r114-evidence/` + bu not + indeks güncellemesi tek docs commit'inde
