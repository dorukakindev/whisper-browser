# Devir — 2026-09-25 17:15 (R124 Adım 4 / Faz C)

## Özet
`docs/raporlar/BROWSER_PLAN_R123.md` §5.4'teki dört madde tamamlandı: yan panel ikon rayı, yeni sekme sayfası, token birleştirme (browser-chrome.css ayrımı + sabit renk sınırı <470), açık tema eşitliği (kontrast taraması CI'a eklendi). Dal: `codex/r124-step4-fazc-1790354583` (Adım 3 dalı `codex/r124-step3-fazb-1790353830` üzerine kurulu).

## Hash'ler
- Başlangıç (Adım 3 sonu): `0b91b86` (menu araması foldSearch CI düzeltmesi dahil)
- Kod bitiş: `b728f7b` — feat(browser): R124 Adım 4 Faz C

## Yapılanlar
1. **Yan panel ikon rayı** (4.1):
   - `.player-side.side-rail` + `.player-layer.side-rail-mode`: grid `minmax(0,1fr) 0 48px`, resizer gizli, sekmeler 36px dikey ikon şeridi (`.side-tab-ico` yalnız `.workspace-browser`'da gösterilir; TR/EN metinler korunur).
   - Tercih site başına: `localStorage.browserSideRail` = `{host: 'rail'|'panel'}`; adres yokken (NTP) `player.browserSideRailNoHost` oturum-içi fallback.
   - Sekme tıklama → `setBrowserSideRailPref('panel')` + `setSideTab`; `#sideRailCollapse` → `'rail'`. `updateBrowserSideRail()` çağrı noktaları: `updateBrowserSignalQuiet`, `setPlayerSidebarCollapsed`, `setWorkspaceMode`, `restoreActiveBrowserTabWorkspace`.
   - Kural: `rail = browser modu && !sidebar-collapsed && (stored ? stored==='rail' : !browserMediaSeen)`.
2. **Yeni sekme sayfası** (4.2):
   - `.browser-ntp-core`: ortada 40px hap arama (`#browserNtpSearch`, Enter → omnibox navigasyonu, Esc temizler), `.browser-quick-places` (8'e kadar, veri yoksa gizli), `.browser-ntp-resume` "İzlemeye devam et" satırı.
   - Slogan/kartlar `.browser-ntp-intro`'ya sarıldı; `browserNtpSeen` localStorage bayrağıyla yalnız ilk açılışta görünür (`.browser-ntp-first` kapsı).
   - Veri besleme: `rememberBrowserVisit(url, title, media)` — media bayrağı `browserWatchMediaId(tab)`/`identity.key` üzerinden did-navigate, did-navigate-in-page ve page-title-updated'de işaretlenir; `normalizeBrowserPlaces` clean() zincirinden geçer. Resume kartları 220px, tıklama sekmeyi o sayfaya yönlendirir.
3. **Token birleştirme** (4.3):
   - `/tmp/split_chrome.py` brace-depth parser ile 610 chrome kuralı `src/renderer/browser-chrome.css`'e taşındı (1356 satır); styles.css 8681→6839 satır. Çoklu-küme doğrulaması: taşınan+kalan = orijinal (aynı kural, aynı sıra).
   - `browser-chrome.css` :root header: `--chrome-radius-{chip:6,control:10,card:14,pill:999}`, `--chrome-h-{compact:28,icon:32,field:36}`, `--elev-1/2/3`, `--motion-{fast,base,ease}` — tekrarlı literal'ler bağlandı. `index.html`'e ikinci `<link>` eklendi.
   - Sabit renk sınırı: `--ink-bright:#fff` token'ı + styles.css `#fff` tekrarları bağlandı; `css-hardcoded-colors` artık `src/renderer/*.css` toplamını sayıyor, taban 470→**459**.
4. **Açık tema eşitliği** (4.4): `report64-contrast.test.js`'e `resolveChromeVar` (var()-zinciri çözümleyen scoped okuyucu) + T-06..T-13: light `.player-layer` kapsamı (--player-muted/text/amber × panel/panel-2/ink) ve yan panel (--text-muted/dim/accent × --bg-1) — hepsi ≥4.5:1 ölçüldü.

## Bulunan ve düzeltilen ek bug
Sekme değişiminde omnibox bayat URL tutuyordu (`updateBrowserNavigation` yalnız `data.url` doluyken yazıyordu; odaklı input'ta da kalıyordu). Artık boş URL'yi de senkronluyor + `forceAddressSync` yalnız tab-restore yolunda (odak koruması gezintilerde sürüyor).

## Testler (hepsi geçti, hedefli)
27 dosya: css-hardcoded-colors **459/459**, report64-contrast T-01..T-13, player-ui 148, report117/120/123, browser-media-controller 31, browser-parti2/7, subtitle-menu, toolbar-clickability, audit-translate 18/18, reading-design, settings-tab, active-jobs, deep-analysis, report65/67/70, smarttube-filters, subtitle-word-highlight, youtube-tv-mode, ui-locale, design-system, renderer-state-a11y, session-package. `node --check` renderer.js/main.js temiz.
Taşınan selektör pin'leri 9 testte css kaynağına browser-chrome.css eklendi; report117 kaskad pin'i ve report123 pill pin'i yeni dosya yapısına uyarlandı (report123 pin'i master'da zaten kırıktı — qualified kural yazılmıştı).

## Canlı Electron doğrulaması (CDP, /tmp/wb-step4 profili)
- NTP'de rail: `sideRail:true`, grid `1112px 0px 48px` → sekme tıkla → `rail:false`, `794px 6px 360px`; `#sideRailCollapse` → ray geri; `browserSideRailNoHost` pref `panel`/`rail` izleniyor.
- NTP: arama kutusu var, intro yalnız ilk açılış (`browserNtpSeen`), quick-places boşken gizli, youtube.com gezintisinden sonra "İzlemeye devam et" 1 kart gösterdi, `mediaSeen:true` → panel otomatik açıldı.
- Sekme geçişi omnibox temizliği doğrulandı (focus'lu input'ta bile).

## Doğrulanamayan sınırlar
- **Windows etkisi**: yalnız CSS/DOM/localStorage ve main.js string/parametre taşıması — `install.bat`/`start.bat`, Windows yolu, PowerShell, dosya seçici, UTF-8 veya GPU/CUDA'ya dokunmuyor. Windows CI bekleniyor.
- Sayfada-bul "Aranıyor…" donması hâlâ Windows'ta el doğrulaması bekliyor (plan §1.2/Adım 1 devirinde de yazıldı).
- `electron-browser-experience-smoke` bu kutuda koşamıyor (iç içe Electron zygote FATAL — ortam sınırı); eşdeğer kontroller manuel CDP ile yapıldı, CI'da koşar.
- Hızlı erişim kutucukları boşken bölüm gizlenir (plan "8 kutucuk" diyor; veri geldikçe dolar) ve "İzlemeye devam et" satırı gerçek oynatma konumu değil, medya-işaretli gezinti geçmişinden beslenir (depoda konum deposu yoktu).
- Rail sekme ikonları svg'dir; gerçek Windows DPI/font renderı ayrıca bakılmalı.

## Sonraki adım
R124 Adım 5 (test altyapısı): find/error-pages/page-bg/tab-icon/frozen-frame Electron smoke'ları — ayrı PR.
