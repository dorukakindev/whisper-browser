# Devir Notu — 2026-09-20 00:40

## Kapsam

Başka modelin R85 uygulama teslimi (`bee53b8`) salt okunur olarak doğrulandı. Ürün kodu değiştirilmedi. Bulgular [BROWSER_BUG_REPORT_86.md](../raporlar/BROWSER_BUG_REPORT_86.md) içinde kanıt ve kabul koşullarıyla kaydedildi.

- Repo/dal: `https://github.com/dorukakindev/whisper-browser` / `master`
- Başlangıç ürün kodu: `bee53b89717b9597ee8fecc4827bcb0a59abc6a8`
- Bitiş ürün kodu: `bee53b89717b9597ee8fecc4827bcb0a59abc6a8` (salt okunur; aynı)
- Başlangıç dokümantasyon HEAD'i: `80b2ff29741030d5857231df0800447bb5dd81a8`
- Bu not ve rapor ayrı dokümantasyon commit'ine alınacaktır.

## Hüküm

Paket büyük ölçüde yararlı ve tam `npm test` paketini geçiriyor; ancak tamamlandı iddiası fazla geniş. Beş kesin ürün maddesi kaldı/oluştu:

1. Video içeren `.wbp` manifestinde kök dışı mutlak video yolu sızıyor.
2. B83-27'nin `sweepOrphans` kolu düzeldi, fakat `pruneTracks` referans toplamadan önce varlığı silebiliyor.
3. `.bak` mirror kopyası hata verirse bazı store'lar başarı dönüp eski gizli veriyi yedekte bırakıyor; note store disk/bellek ayrışabiliyor; watch-library politikası kapsanmadı.
4. Playback diagnostic exponential backoff 193. saniyeden sonra saniyelik log seline dönüyor.
5. Transcript niyeti `tamam*` kökü nedeniyle “tamamen” ve “tamamla”yı yanlışlıkla tüm-transkript isteği sayıyor.

Ek olarak yeni 12-senaryolu test dosyası değişikliğin yüksek riskli yollarını kapsamıyor. Electron smoke'ları nondeterministik: güncel tam turda menu-visual ve video-e2e düştü; üç hedefli tekrarda sırasıyla 1/2, 1/2, 2/2 sonuç alındı. Önceki nottaki yalnız ASS/catalog UnknownViz açıklaması bugünkü davranışı tam temsil etmiyor.

## Deterministik kanıtlar

- Sentetik dış-kök MP4 paketlenip geri okundu; `data.videos[0].source` mutlak `C:\Users\...` yolu ve `path.isAbsolute(...) === true`.
- Session reset fault probunda `.bak` kopyası `EACCES` attı; fonksiyon `{ok:true}` döndü, primary boşaldı, backup `SECRET-OLD` kaldı.
- 230 saniyelik 1 Hz stall probu 42 tanı üretti; 193. saniyeden sonra her saniye kayıt oluştu.
- Transcript probunda “Bu iddia tamamen yanlış mı?” ve “Bu işi tamamla” `coverage: distributed` döndürdü.

## Doğrulama

- `node tests/browser-report85-regressions.test.js` — 12/12.
- `npm test` — gerçek Windows subprocess izinleriyle exit 0; Node ve Python tamamı geçti.
- `npm run test:electron-bridge` — exit 0.
- `node tests/run-electron-smokes.js --all` — 18/20.
- Hedefli Electron üç tekrar — 1/2, 1/2, 2/2.
- Üç JS dosyası `node --check`; dört Python dosyası `py_compile` — geçti.

## Değişen dosyalar

- `BROWSER_BUG_REPORT_86.md`: R85 uygulama turunun kanıtlı doğrulama raporu ve başka modele verilecek iş listesi.
- `docs/devir/2026-09-20-0040.md`: bu salt okunur turun devir kaydı.
- `DEVIR-NOTU.md`: güncel nota bağlantı.

## Sınırlar

- Gerçek kullanıcı profili, anahtar, sağlayıcı veya kişisel altyazı okunmadı.
- Canlı dış site/API çağrısı yapılmadı.
- Ürün kodu düzeltilmedi; kullanıcı bu turda teslimi doğrulatıyor ve düzeltmeyi başka modele verebilir.
- Önceden var olan untracked `.freebuff/`, `_repro/` ve 2026-09-19 tarihli notlara dokunulmadı.
