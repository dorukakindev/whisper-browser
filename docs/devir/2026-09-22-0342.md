# Devir Notu — 2026-09-22 03:42 (Europe/Istanbul)

## Görev
Altı görevli paketin beşincisi (T5): çökme, veri bütünlüğü ve performans gauntlet'i — kritik write/rename/close aşamalarına deterministik hata enjeksiyonu, hasarlı profilde gerçek Electron yeniden açılışı, 10 bin cue + çok sekme + eşzamanlı çeviri altında p95/p99 takılma, bellek ve dosya büyümesi ölçümü.

## Repo / Dal / Kimlikler
- Repo: `dorukakindev/whisper-browser`, dal: `devin/t5-crash-integrity`
- Başlangıç: `4453ae0` (origin/master)
- Bitiş kod commit'i: `ea7f547`
- Bu not docs commit'indedir; kendi hash'i içerilmez.

## Yapılanlar
- Enjeksiyon dikiği: `browser-cea-checkpoint` (fsImpl), `browser-series-context` (fsImpl), `browser-feature-services` (deps.fsImpl→fsx; save/writeOwnedSubtitle/data + series store'a geçiş), `main.js` üç atomik yazıcıya sonda `io=fs`. `browser-note-store`'da `options.io` tutarsızlığı giderildi (okuma/temizleme yolları da io kullanıyor).
- `tests/crash-integrity.test.js` (yeni): 9 bölümlük aşama matrisi — atomic-json, burnin-output, session-store, asset-store, translation-cache (async promises yolu dahil), translation-archive, note-store, reading-list, element-rules, series-context, cea-checkpoint, secret-store, yanlış-nesil izolasyonu.
- `tests/electron-crash-integrity.smoke.js` (yeni): hasarlı profil → gerçek Electron açılışı → 9/9 check PASS (bayat tmp süpürme, taze tmp koruma, .bak oturum kurtarma, temiz-çıkış-olmadan uyarı, birincil onarım).
- `tests/electron-perf-gauntlet.smoke.js` (yeni): 10k cue render 16ms (500/sayfa), 3 sekme, eşzamanlı 10k-cue çeviri + render + sekme geçişi sırasında longtask 0ms, rAF p95 17ms; bellek Δ+124MB; userData dosya Δ+49KB; 13 sağlayıcı çağrısı.

## Gerçek bulgular (BROWSER_BUG_REPORT_102)
- **F-102-1 FAIL-FIXED:** `browser-reading-list._writeIndex` ve `browser-element-rules._write` rename patlamasında `.tmp` yetim bırakıyordu; kardeş depoların aksine try/finally yoktu. Kırmızı test → düzeltme → yeşil.
- **F-102-2 FAIL-FIXED:** `resumeRestoredBrowserPage` → `requestIsCurrent` destroyed webContents'ta TypeError işlenmeyen rejection üretiyordu; gerçek Electron smoke ilk koşusunda yakalandı → null kontrolü → smoke'ta rejection yok.
- PF1/PF2/PF3 belgelenmiş sınırlar yeniden bug sayılmadı; renderCueList sayfalama davranışı mevcut kodla teyit edildi.

## Test komutları ve sonuçlar
- `npm test` → tüm dosyalar geçti (3 kaynak-slice testi önce kırmızıydı: history/watch-library/validation marker'ları imzaya göre uyarlandı, assertion gevşetilmedi)
- `npm run test:electron-bridge` → PASS
- `node tests/crash-integrity.test.js` → PASS
- `DISPLAY=:0 electron tests/electron-crash-integrity.smoke.js` → PASS 9/9
- `DISPLAY=:0 electron tests/electron-perf-gauntlet.smoke.js` → PASS 6/6

## Sınırlar / yapılmayanlar
- Gerçek kill -9 yerine deterministik fs enjeksiyonu + hasarlı profil yeniden açılışı (yarışsız eşdeğer kapsama).
- Uzun soak R96'da geçti (34 dk/4000 döngü) — tekrarlanmadı.
- safeStorage gerçek platform kasası Linux'ta doğrulanamadı (fake safeStorage kullanıldı).
- main.js io dikiği doğrudan Node testinden çağrılamaz (modül export etmez); sözleşme atomic-json üzerinden kanıtlandı.
- Yeni bağımlılık yok.

## Sonraki adım
T6: temiz Windows+Ubuntu checkout, CI'da açık araç kurulumu ve kritik test koşusu, dürüst platform belgelendirmesi.
