# Devir Notu — 2026-09-18 16:52

## Konu
Rapor 67 iş listesinin uygulanması — SmartTube parite düzeltmeleri: trending kategori chip'leri + backend `--tab`, degraded-fallback kanıtı, roving ok-tuşu grid gezinmesi, up-next rayı, yorumlar paneli, arama sayfalaması, kanal linkleri, dinamik-meta yerelleştirmesi.

Kullanıcı: "Gerekli düzeltmeleri yap" (önceki turda ekran analizi + iş listesi raporlanmıştı, kod'a dokunulmamıştı).

## Başlangıç
- Dal: `master` · başlangıç HEAD: `001a378b24afb741786a1499fc2bdcfa983c557d`
- İzlenmemiş başka-iş-akışı WIP dosyaları mevcut (`tests/report62/63/64-*`, `src/browser-sensitive-keys.js`, `_repro/`) — dokunulmadı, stage edilmedi.

## Değişen dosyalar
- `backend/invidious.py` — `feed` `--tab` (default|music|gaming|news|movies; tek-kategori `?type=`); fallback'ler `degraded:true`+`source:"yt-dlp:<yol>"` zarfı; `feed_*` `{videos,source,degraded,instance}` döndürür; arama fallback'i videoId'siz girdileri eler; probe emit'ine `recommended` (`recommendedVideos` parse); yeni `comments` (`--continuation`, `{videoId,comments,continuation,disabled,instance}`) ve `playlist` (`^[A-Za-z0-9_-]+$`, `--page`) komutları.
- `src/main.js` — sonuç whitelist'e `comments`/`playlist`; `invidious:feed` `opts.tab` doğrulama; `invidious:comments` (düz 11-char ID veya doğrulanmış URL, `decideUrlPolicy` try/catch, yetkili-gönderici); `invidious:playlist`.
- `src/preload.js` — `api.invidiousComments`, `api.invidiousPlaylist`.
- `src/renderer/renderer.js` — `fetchInvidiousFeed`/`fetchInvidiousSubscriptions` zarf tüketimi; `renderSmartTubeSection` chip'ler (5 kategori→`tab`) + degraded statü etiketi; `stGridNav` roving ok-tuşu gezinmesi (←/→/↑/↓/Home/End, sütun hesaplı, `#stGrid`/`#stSearchResults`/`.st-upnext-list` delegation); kart `stopPropagation` yalnız Enter/Space; `author` → `.st-card-author` kanal linki; kanal sayfası iç-içe-gridsiz + tam-satır başlık; arama sayfalaması (`stSearchState`, dedupe, "Daha fazla"); `#playerSidePanel` up-next + `stComments` (continuation, `textContent`, `player.ytInfo` bayat-koruması); `N` kısayolu + `#playerUpNextBtn`; meta dizgileri `UiLocale.t()`.
- `src/renderer/index.html` — `#playerSidePanel` + `#playerUpNextBtn` markup.
- `src/renderer/styles.css` — `.st-chip`, `.st-card-author`, `.st-channel-head`, `.st-upnext-*`, `.st-comments*`, `.player-side-panel`; `.st-chip.is-active` → `var(--accent-contrast)`; `subHiddenHint` konum/z-index.
- `src/renderer/ui-locale.js` — yeni dizgi girdileri; `#playerMeta` `ignored`'a eklendi (kısmi çeviri bug'ı).
- `tests/report67-smarttube-wiring.test.js` — 24 → **39** test (roving, chip, degraded, up-next, yorum textContent, IPC zinciri, locale).
- `tests/electron-smarttube-boot.smoke.js` — feature probları genişletildi (chips, roving, degraded, up-next, yorumlar, kanal, ipucu); `player.ytInfo` mock'u eklendi.
- `backend/test_invidious.py` — 48 → **59** test (comments/playlist parse + doğrulama, tab dispatch, degraded zarf).

## Doğrulama
- `report67-smarttube-wiring` **39/39** · `test_invidious.py` **59 OK**
- `electron-smarttube-boot.smoke` **GEÇTİ** — `{"ok":true, feat:{chipCount:5, musicTabLoaded:true, degradedStatus:true, rovingRight:true, rovingEnd:true, upNextItems:1, commentCount:2, commentNoHtml:true, channelHead:true, channelNoNested:true, channelCards:3, hintInStage:true}}`
- `design-system` yeşil (ilk koşuda `.st-chip.is-active -> #1a1208` ihlali yakaladı → `--accent-contrast` ile düzeltildi) · `ui-locale` 14 assertion
- `node --check` main/preload/renderer · `py_compile` invidious.py · `git diff --check` — temiz
- `npm test` kalan 3 kırmızı: `report62`, `report63-secret`, `report63-ssrf` — başka iş akışının commit'siz WIP'i; beklenen ürün kodu o tarafta yazılmamış, dokunulmadı.

## Kalan sınırlar / sonraki adımlar
- Canlı Invidious davranışı mock/fixture seviyesinde doğrulandı; canlı ağ yok. Kullanıcı ortamında `start.bat` ile chip→`?type=` ve comments continuation'ın gerçek instance'ta çalıştığı görülmeli.
- Bilinçli yapılmayanlar (parite eksikleri): instance seçici UI, arama autocomplete/filtreler, izleme-geçmişi rail'i, like/abone düğmeleri, kanal sekmeleri/banner.
- `publishedText` "2 days ago" sağlayıcı-ham metin — opsiyonel çeviri bırakıldı.
