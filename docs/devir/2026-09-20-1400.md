# 2026-09-20 14:00 — Complete unranked feature recommendation catalogue

- Repository: https://github.com/dorukakindev/whisper-browser; branch: `master`.
- Start/end product-code commit for this documentation turn: `2b67ea34` (no product code changed by this turn; the concurrent SmartTube queue commit was already on remote master).
- Final catalogue synchronization base: `f90a5b4`.
- Deliverable: [FEATURE_RECOMMENDATION_CATALOG_2026-09-20.md](../../FEATURE_RECOMMENDATION_CATALOG_2026-09-20.md). The previous [short decision summary](../../FEATURE_RECOMMENDATION_REVIEW_2026-09-20.md) now points here and is explicitly not a complete inventory.

## Why this follow-up exists

The prior turn collapsed numerous distinct ideas into a five-item sequence and barely exposed the cosmetic recommendations. The user requested every proposal, without priority ranking. This follow-up enumerates R75, R76 and its deep expansion, seven blueprints, the streaming-subtitle research, the separate cosmetic report, and the September 1 strategy report derived from 180 GitHub repositories (38 deep reads). The September 12 completion ledger was checked to avoid recommending already delivered work as new. The historical strategy report's twenty distinct decisions are reclassified against the current tree instead of being discarded as an old brief.

The catalogue records E (recommend adding), K (conditional), B (backlog), R (not recommended for the current product), and V (existing/partial). It contains 153 decision rows: R75 40, R76 first layer 27, R76 deep layer 48, blueprint/service 9, cosmetic 9, and the 180-repository strategy report 20. It includes source links, distinguishes current code evidence from report-derived ideas, and makes no assertion that every external repo or feature has been newly prototype-tested.

## Verification and limits

- Documentation-only work. `git diff --check`, explicit path/diff review and row/section count checked. Product tests and Electron runtime not run because product code did not change.
- No dependency, schema or installation change. No credential, real user subtitle, userData or token inspected or committed.
- Existing untracked `.freebuff/`, `_repro/` and unrelated September 19 devir notes left untouched.
- Code/source spot checks were scoped to SmartTube, Invidious captions, subtitle/DASH parser, thumbnail service, watch index, session/acquisition/scheduler/secret store, embedded tracks, Live ASR/OCR and the official sources linked in the catalogue. Conditional items still need separate implementation-time validation.
- Final commit/push/remote identity must be verified after this note is committed; do not infer them from this text.

## Next step

When the user selects a specific feature set, refresh the then-current code and evaluate its acceptance conditions. This catalogue is a complete idea inventory, **not** authorization to implement all items or to add every suggested third-party dependency.
