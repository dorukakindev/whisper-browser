# Devir notu — 2026-09-21 13:57 (Europe/Istanbul)

## Kimlik ve amaç

Repo: `https://github.com/dorukakindev/whisper-browser`. İzole inceleme dalı `codex/devin-review`; başlangıç ürün kodu `82a45740b8f25b661ae7a643e924f2450a99d8be`, bitiş ürün kodu `9aa376aa25a536e13950a079803d0699df2f23f2`. Devin cloud'un `d523405..82a4574` aralığındaki 10 commit'i incelendi. Kök `D:\Whisper Local` dalı başlangıçta `codex/catalog-sync` / `ee0e0ab` ve kirliydi; mevcut kullanıcı dosyaları korunmak üzere ayrı worktree kullanıldı.

## Kod değişiklikleri

- `src/main.js`: başka uygulamadan alınmış gömülü OAuth istemcisi kaldırıldı; device-code için kayıtlı istemci şartı ve istemci değişince token/hesap temizliği eklendi. Eski istemci token'ı otomatik kullanılmaz.
- `backend/youtube.py`: geniş YouTube yönetim kapsamı salt-okunur kapsama indirildi; compact görüntülenme sayısı ve geçerli kanal endpoint'i ayrıştırması düzeltildi.
- `src/renderer/index.html`, `src/renderer/renderer.js`, `src/renderer/ui-locale.js`: login kurulum kapısı ve EN/TR açıklaması düzeltildi; QR destekli device-code akışı korunuyor.
- `backend/test_youtube.py`, `tests/report67-smarttube-wiring.test.js`, `tests/report70-youtube-oauth.test.js`: dar OAuth sözleşmesi, token geçersizleştirme ve kart ayrıştırma regresyonları.
- `tests/electron-smarttube-boot.smoke.js`, `tests/electron-browser-trusted-bridge.smoke.js`: güncel DOM'a ve taşınabilir FFmpeg bulucusuna uyum.
- `BROWSER_BUG_REPORT_94.md`: doğrulanmış hata, test kusuru ve açık canlı kabul kaydı.
- `CHANGELOG.md`, `DEVIR-NOTU.md`: kullanıcıya görünen değişiklikler ve devir indeksi.

## Doğrulama

Hedefli Python 19/19; SmartTube kablolama 84/84; OAuth 20/20. `npm test` tüm dosyalarla geçti. `npm run test:electron-bridge` geçti. `node tests/run-electron-smokes.js smarttube-boot` geçti. Değişen JS için `node --check` ve Python derleme kontrolleri temiz. Temiz worktree'de bağımlılık kurulumunu tekrarlamamak için mevcut `node_modules` junction'ı ve venv kullanıldı; junction commit edilmedi. Sandbox'ta Python child-spawn EPERM verdiğinden tam paket onaylı dış ortamda yeniden çalıştırıldı ve geçti. Yeni bağımlılık, şema veya kurulum adımı yok.

## Yerel köke aktarım

Yayımlanan `master` kodu, kök `D:\Whisper Local` içindeki `codex/catalog-sync` dalına `98036d723ec29a712fec1e0943ce230e34915125` merge commit'iyle alındı. `DEVIR-NOTU.md` çatışmasında hem yeni hem eski not bağlantıları korundu. Önceden mevcut iki ek indeks satırı stash'ten geri uygulandı; raporlar, repro dosyaları ve diğer izlenmeyen kullanıcı dosyaları yerinde kaldı. Bu dalın kullanıcıya ait dört ek commit'i GitHub `master` dalına gönderilmedi. Kök klasörde `npm test` ilk kez yalnız eksik yerel `jsdom` modülü nedeniyle bir dosyada başarısız oldu; mevcut aynı kilitli `node_modules` yolu `NODE_PATH` ile sağlanarak yeniden çalıştırıldığında tüm paket geçti. Kök klasörde `npm run test:electron-bridge` de geçti.

## Sınırlar ve sonraki kabul

Gerçek Google hesabıyla device-code/QR, kişisel SmartTube akışı, uzun oynatma ve tüm servis varyantları bu turda denenmedi. Kendi OAuth istemcisi olmadan sıfır-kurulum giriş artık yok; bu bilinçli güvenlik sınırı. Daha önce verilmiş geniş kapsamlı Google izinleri uygulamadan otomatik geri alınmadı; hesap ayarlarından kullanıcı denetimi gerekir. Kök çalışma ağacının kendi commit ve untracked dosyaları başka işlere ait; bunlar yayımlanan düzeltme commit'ine alınmadı. Kök dalı ile `master` kimliğinin farklı olması, yerel kullanıcı commit'lerinin korunmasından kaynaklanır; yayımlanan commit'in kök dalının atası olduğu ayrıca doğrulanmalı.
