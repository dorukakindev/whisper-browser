# Devir Notu — 2026-09-22 00:48 (Europe/Istanbul)

Repo: `dorukakindev/whisper-browser`. Yerel dal: `codex/pr16-gauntlet-acceptance`; PR hedefi: `devin/subtitle-reliability-gauntlet` (#16). Başlangıç `f9112f14361b82e5c68ed46d7ba6a059c43d6e57`; bitiş kod commit'i `03d721d89baeec46e3df747f5912b19be4bcc09d`. `master` ve kullanıcının kirli ana çalışma ağacı değiştirilmedi.

## Neden ve değişiklikler

- `tests/electron-browser-subtitle-gauntlet.smoke.js`: R96 smoke'unda CEA, seek, dil ve yenileme ölçülüyor ama çoğu doğrulanmıyordu; koşu yanlış-pozitif PASS verebiliyordu. Kabul kapıları eklendi, otomatik çeviri yüklemesinden sonra kaynak+çeviri açıkça eşleştirildi, gerçek DOM görünürlüğü ölçüldü, Windows sertifika komutu kabuksuz yapıldı, gizli pencere/RAF takılması ve sonsuz beklemeler sınırlandı, test çıktı dizinleri profile alındı.
- `tests/browser-subtitle-gauntlet-acceptance.js`: saf kabul koşulları.
- `tests/browser-subtitle-gauntlet-acceptance.test.js`: pozitif ve negatif mutasyon testleri.
- `BROWSER_BUG_REPORT_97.md`: bulgu, kanıt ve sınırlar.
- `DEVIR-NOTU.md`: bu nota bağlantı.

Kullanım: `node tests/browser-subtitle-gauntlet-acceptance.test.js`; gerçek Electron için kurulu `openssl` veya `OPENSSL_BIN` ve `node_modules/.bin/electron tests/electron-browser-subtitle-gauntlet.smoke.js`. Gerçek kullanıcı profili/anahtarları kullanılmaz. Yeni bağımlılık, şema ve ürün kodu değişikliği yok.

## Doğrulama

- İki hedefli Node gauntlet testi exit 0; `node --check` ve diff check temiz.
- İzole Windows Electron smoke exit 0: CEA complete, 3 çevrilmiş cue / 1 sahte sağlayıcı isteği, kaynak ve çift görünüm, seek p1→p0, EN/TR etiketleri, yenileme sonrası 8 iz.
- `npm test` exit 1: izole worktree'de `jsdom` ve Python venv eksikliği dahil 12 test dosyası başarısız; tam paket yeşil denmiyor.
- `npm run test:electron-bridge`: `mojo platform_channel` erişim engeli. 34 dakikalık soak, gerçek sağlayıcı ve gerçek hesaplı site testi bu turda tekrarlanmadı.

Önceki test koşularının gerçek İndirilenler'e bıraktığı iki sentetik SRT doğrulanıp silinmeden `.uiprev/moved-legacy-test-outputs/` içine taşındı. Son koşu yalnız profile yazdı. Sonraki adım: PR dalına normal fast-forward push, CI ve uzak SHA doğrulaması; merge kullanıcı kararı olmadan yapılmayacak.
