# Devir Notu — 2026-09-20 15:35 (Europe/Istanbul)

## Parti 4: Çevrimdışı okuma listesi (B22)

- Repo/dal: `dorukakindev/whisper-browser` @ `master`
- Başlangıç kod commit'i: `cb378fd` (Parti 3 devir sonrası)
- Bitiş kod commit'i: `46f1d80`

## Katalog maddesi

- **B22** — Çevrimdışı okuma listesi (MHTML + sayfa indeksi). Katalog notundaki "mevcut iki parçayı birleştirme" şöyle yorumlandı: mevcut `Sayfayı arşivle` (kullanıcıya yol soran MHTML dışa aktarımı) ve yerler paneli altyapısı birleştirilip yönetilen bir depoya bağlandı. "Arşiv kapsamı" açık tutuldu: kopya sayfanın **o anki** DOM görüntüsüdür (MHTML), canlı içerik değildir; boş durum metni bunu söylüyor.

## Yapılanlar

- `src/browser-reading-list.js` (yeni): `BrowserReadingList` — `userData/OkumaListesi/` dizini + `index.json` (atomik yazım, 200 kayıt sınırı). `prepare`/`commit` iki aşamalı kayıt: `savePage` başarısızsa indeks kirlenmez. Aynı kanonik URL tekrar eklenirse kayıt yenilenir (tekilleştirme). `_safeFilePath` dizin dışına yazma/silme engeller. `refreshTarget` yalnız sekmenin canlı URL'si kaydın URL'siyle kanonik eşleşirse yenilemeye izin verir.
- `src/main.js`: 5 IPC — `readingList:add` (dosya sorusuz MHTML), `list`, `remove` (dosya+indeks), `refresh` (canlı sayfa adresi eşleşme koşuluyla), `open` (yönetilen depo yolundan `file://` `loadURL`; `safeFilePath` doğrulaması sonrası).
- `src/preload.js`: 5 `window.api` yöntemi.
- `src/renderer/index.html`: İçerik menüsüne `browserReadLater` öğesi; yerler paneline `Çevrimdışı` sekmesi (`browserPlaceTabOffline`).
- `src/renderer/renderer.js`: `browserOfflineList` state, `loadBrowserReadingList` (sıra korumalı), `browserPlaceList`'e offline dal, yerler listesinde aç/yenile/sil eylemleri, panel açılışında ve sekmeye geçişte yükleme, menü öğesi işleyicisi.
- `src/renderer/ui-locale.js`: 9 yeni TR→EN çifti.
- `tests/browser-reading-list.test.js` (yeni): depo sözleşmesi (kanonik URL, güvenli dosya adı, tekilleştirme, silme temizliği, dizin kaçışı reddi, dosyasız kayıt, yenileme URL koşulu, INDEX_LIMIT) + IPC/preload/renderer bağlantı kontrolleri.

## Kapsam notları

- `Sayfayı arşivle` (dışa aktarma) davranışı değişmedi; okuma listesi ayrı ve tamamlayıcı.
- `open` doğrudan `view.webContents.loadURL(file://...)` yapar; `browser:navigate`'in http/https normalizasyonundan bilinçli geçirilmez (file:// reddederdi). Gezinme kilidi/stale-istek zinciri basit tutuldu.
- Yenileme yalnız canlı sayfa açıkken çalışır (URL kanonik eşleşmesi şart) — körlemesine ağ isteği yok.

## Test sonuçları

- `node tests/browser-reading-list.test.js` — OK (depo + bağlantı kontrolleri)
- `node tests/ui-locale.test.js` — OK
- `npm test` (tam süit) — **Tüm testler geçti**
- `node --check` main/preload/renderer/browser-reading-list — temiz

## Çalıştırılmayan kontroller

- Electron smoke'ları bu turda koşulmadı; MHTML save/açma akışı gerçek WebContentsView gerektirdiği için birim testler depo sözleşmesini, bağlantı kontrolleri IPC/UI bağlantısını doğruluyor. Gerçek sayfa arşivleme yalnız Windows/Electron ortamında doğrulanabilir.

## Bilinen sınırlar / sonraki adımlar

- `file://` ile açılan MHTML sekmesinde tarayıcı oturum kaydı/HTTP önbelleği davranışı normal sayfa gibi değildir; kayıtlı oturum geri yüklemesinde file:// adresleri `safePlaceUrl` filtresinden düşer (kabul edilebilir).
- Sıradaki paket: Parti 5 — SmartTube (A01 kart menüsü, A03 gizle filtreleri, A08 içerik gizleme, A22 çalma listeleri + kalan A38 alt maddeleri).
