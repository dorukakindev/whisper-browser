# Devir notu — 2026-09-19 14:30

## R81 doğrulama+denetim+şartname turu (kod değişikliği YOK)

**Repo/dal:** `https://github.com/dorukakindev/whisper-browser` · `master`
**Başlangıç kod:** `6335fb6` · **Bitiş kod:** ürün kodu bu oturumda DEĞİŞMEDİ
(kullanıcı talimatı: değişiklikleri paralel AI yapıyor; bu oturum salt-okunur).

## Ne yapıldı

Kullanıcı dört rapor işi istedi; hepsi `PROGRAM_BUG_REPORT_81.md` ve
`FEATURE_BLUEPRINTS.md` içine yazıldı:

### 1. R79/R80 bağımsız doğrulama (PROGRAM_BUG_REPORT_81 §1)

Paralel oturumların iki raporundaki **8/8 bulgu kaynakta satır satır doğrulandı,
0 yanlış pozitif**:

- P79‑01 `subtitle_sdh.py:35` — 65×`_key()` + modifiers set her çağrıda (66 çağrı) ✓
- P79‑02 `translation_memory.py:192-197` — LIMIT 240 → aday başına `ratio()` ✓
- P79‑03 `subtitle-sentence-layout.js:251-268` — DP `cuts` dizi kopyası; 400/12 sınırı ✓
- P79‑04 `browser-media-tools.js:57` + `browser_media_tools.py:67` — istek başına süreç+model ✓
- C79‑01 — 8 backend modülü testte **0 referans** (grep ölçümü) ✓
- D79‑01 — kapalı-sekme bellek-içi (`main.js:461`); sızıntı yüzeyleri session-store:174 + session-package:44→main.js:13741 ✓
- B80‑01 **P2** — `endpointSetting` sorgu taramaz; `collectSecretValues` URL sırrını görmez; `secretsExcluded:true` ihlali doğrulandı ✓
- B80‑02 **P3** — iki `probeCommand` kopyası da stderr tüketmiyor; üretim kopyası stdout sınırsız ✓

### 2. Paralel AI diff incelemesi (PROGRAM_BUG_REPORT_81 §2)

`59174a7` (YouTube cihaz girişi + TV browse) + çalışma-ağacı diff'i satır satır incelendi:
- OAuth kuşak refaktörü tutarlı (`openingGen`/`_ytFlowGen`/`finally` kapıları; `closeYoutubeLogin` tüm çıkışlarda temizliyor)
- `feed_partial` progressive preview iyi kapılı (requestId+bölüm+arama+görünürlük+dedupe)
- `.browsing` yüzey ayrımı JS↔CSS↔smoke tutarlı
- **4 nit (P3):** N1 geç feed_partial tam grid'i ezebilir (emit→resolve sırasına bağımlı, latent); N2 stCloseBrowser görünürlüğü yalnız toggle anında; N3 istemcisizde device→client görünüm flaşı; N4 youtubeCancel fire-and-forget ikinci poll (main'e bağlı, latent)

### 3. Derin denetim (PROGRAM_BUG_REPORT_81 §3)

- **D81‑01 P2 i18n:** 262 Türkçe UI dizgesi 1205-kayıtlık tabloda yok → EN modunda Türkçe sızar (`_repro/audit-i18n.js` çıktısı hazır liste; 57'si tabloda var)
- **D81‑02 IPC:** 185 handler'ın **0'ı** guard'sız — `authorizedBrowserSender`/`senderFrame` evrensel ✓
- **D81‑03 perf:** P79‑01 deseni 6 backend dosyasında → yalnız raporlanan örnek; yeni isabet yok
- **D81‑04 test:** ~90 browser modülünden yalnız 3'ü testsiz (browser-dialogue, browser-mini-preload, browser-sensitive-keys)
- **D81‑05 kalıcılık:** settings yalnız `writeJsonTransaction` (atomik) ✓; localStorage 61 noktada 27 satır-içi korumasız (P4 hijyen)

### 4. Uygulama şartnameleri (FEATURE_BLUEPRINTS.md)

7 blueprint + hazır-düzeltme tablosu:

- **BP‑01** Sekme dondurma — `Page.setWebLifecycleState` (debugger zaten attach; medya/yakalama-aktif skip kuralları)
- **BP‑02** Seekbar hover önizleme — **altyapı zaten var**: `browser-reference-media.js:58` thumbfast deseni + per-sekme 60-LRU + cancel; eksik yalnız UI bağlantısı → **en hızlı kazanım**
- **BP‑03** Userscript yöneticisi — `executeJavaScriptInIsolatedWorld` + `Page.addScriptToEvaluateOnNewDocument` + GM_* shim (`GM_xmlhttpRequest`→main net, SSRF listesi)
- **BP‑04** TTML/DFXP kolu — tespit var (`browser-subtitles.js:4,846,1143`), ayrıştırıcı yok → minimal `<p begin/end>` çekirdeği
- **BP‑05** Semantik geçmiş — önce P79‑04 işçi düzeltmesi; sonra MiniLM embedding indeksi
- **BP‑06** Remote Playback probe — `'remote' in video` → cast-src; yoksa DLNA
- **BP‑07** Telefon kumandası — LAN HTTP+WS + token (benzersiz özellik)

## Dosyalar

- `PROGRAM_BUG_REPORT_81.md` — **yeni** — doğrulama + diff incelemesi + derin denetim
- `FEATURE_BLUEPRINTS.md` — **yeni** — 7 uygulama şartnamesi + düzeltme tablosu
- `_repro/audit-i18n.js` — i18n ölçüm betiği (untracked, kanıt)
- `docs/devir/2026-09-19-1430.md` — bu not
- `DEVIR-NOTU.md` — indeks güncellendi

## Doğrulama

- Ürün kodu değişmedi → ürün testleri koşulmadı (belge-only).
- Tüm doğrulamalar canlı kaynak satırlarıyla yapıldı; ölçüm betikleri `_repro/` altında.
- Paralel oturumların WIP'i (`main.js`, `preload.js`, `invidious.py`, `test_invidious.py`, renderer dosyaları, smoke test, `.freebuff/`, diğer devir notları) commit'e DAHİL EDİLMEDİ — `feed_partial` implementasyonu hâlâ sürüyor olabilir, dokunulmadı.

## Sınırlar / sonraki adım

- B80‑01 (P2) düzeltmeye hazır — spec R80+R81'de; paralel AI'ya devredilebilir.
- D81‑01'in 262 dizgesi `ui-locale.js`'e toplu eklenebilir (`audit-i18n.js` çıktısı).
- BP‑02 en hızlı kazanım (backend hazır, yalnız UI+IPC).
- N1/N4 latent yarışlar main-tarafı emit/iptal sıralamasına bağlı — ayrı doğrulama ister.
