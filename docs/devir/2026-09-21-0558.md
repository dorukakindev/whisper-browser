# Devir Notu — 2026-09-21 05:58

## Yapılan: SmartTube ana sayfa artık hiçbir durumda boş kalmıyor + lockupViewModel desteği

**Şikayet (kullanıcı):** "oynatıcı tarafında YouTube ana sayfasında hiçbir
şey görünmüyor bile."

**Kök nedenler (üçü birden):**
1. **Uzak akış (Invidious) öldüğünde grid tamamen siliniyordu** — `showError`
   `grid.innerHTML=''` yapıyor; kuyruk/devam/çok-oynatılan gibi YEREL raylar
   da gidiyordu → literal boş ekran. Hem `!videos.length` hem catch dalı için
   `stRenderHomeFallback()` eklendi: yerel raylar + hata satırı + girişsizse
   "YouTube ile giriş" birincil düğmesi + "Tekrar dene" (SmartTube'un
   girişsiz ekranı gibi).
2. **Render aşaması try/catch dışındaydı** — `buildSmartTubeCard` gibi bir
   fonksiyonda tek hata, başarılı fetch'ten SONRA grid'i sessizce boş
   bırakıyordu (unhandled rejection). Ana sayfa render bloğu try/catch'e
   alındı; hata → fallback.
3. **`lockupViewModel` ayrıştırılmıyordu** — YouTube kişisel akışları
   (FEwhat_to_watch vb.) videoRenderer yerine yeni kart formatı
   `lockupViewModel` ile döndürüyor (yt-dlp de aynı uyarlama yaptı). Girişli
   ana sayfa hep 0 kartla dönebilirdi. `_lockup_to_card()` eklendi:
   contentId/watchEndpoint videoId, lockupMetadataViewModel başlık/kanal/
   görüntülenme/tarih, thumbnailViewModel sources, badgeViewModel süre;
   `LOCKUP_CONTENT_TYPE_VIDEO` olmayanlar elenir. `browse()` iki formatı da
   toplar.

**Canlı ağ doğrulaması (bu kutudan):** youtubei/v1/search anonim 200 + 29
videoRenderer; FEwhat_to_watch anonim tasarım gereği boş (visitorData ile
bile) — yani girişsiz ana sayfanın içerik kaynağı gerçekten giriş olmalı,
tıpkı SmartTube'da. FEtrending/FEexplore browseId'leri artık 400 (ölü).

## Test
- `npm test` tam süit temiz (report67 84/84, backend unittest 19/19).
- Yeni testler: `test_browse_parses_lockup_view_model` (backend),
  `F5c` fallback davranış testi (raylar + CTA + retry; girişliykende CTA yok).
- UI doğrulama: test ajanı bu değişikliğin üstünde fallback ekranını gerçek
  Electron'da doğrulamaya aldı (ağ kapalı → akış hatası yolu garantili).

## Sınırlar / sonraki
- Gerçek pairing + girişli FEwhat_to_watch içeriği kullanıcı cihazında
  doğrulanmalı — anonim InnerTube bu IP'den boş dönüyor (tasarım), canlı
  token'lı istek test edilemedi.
- FEtrending ölü olduğundan trend bölümü hâlâ Invidious'a bağlı — ayrı iş.
