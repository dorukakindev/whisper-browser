# Devir notu — 2026-09-18 13:30

## Konu

SmartTube tarzı Invidious ana sayfa, hesap girişi, arama ve kanal
tarama. download_stream ile yt-dlp olmadan reklamsız video oynatma.

## Repo / dal / commit kimlikleri

- Repo: `https://github.com/dorukakindev/whisper-browser` — dal `master`
- Önceki: `cb3b156` (rapor 65 — temel Invidious probe/subs)
- Bitiş: `9f4d3b8` (feat: SmartTube tarzi Invidious ana sayfa, ...)
- Push: `03ac20f..9f4d3b8 master -> master` ✓

## Yapılan değişiklikler

### Faz 1: Invidious Auth
- Backend `invidious.py`: login/logout/set_session/_auth_headers
  SID cookie modül seviyesinde; ana süreçten IPC ile yönetilir.
- Main `invidious:login` / `invidious:logout` / `invidious:session`
- invidiousSessions Map: kullanıcı adı → {sid, instance}

### Faz 2: Ana Sayfa + Arama + Kanal
- Backend `feed_popular`, `feed_trending` (4 kategori),
  `feed_subscriptions` (auth gerekli, 401 → anlamlı hata)
- Backend `search(query, page)`, `channel(ucid)`
- Backend `_parse_video_item`: ortak şema (videoId/title/author/
  lengthSeconds/viewCount/videoThumbnails)
- Main: `invidious:feed` (popular/trending/subscriptions),
  `invidious:search`, `invidious:channel` (UCID regex doğrulama)

### Faz 5: download_stream (Faz 1'den yarım kalan)
- Backend `media.py`: Invidious adaptive format URL'lerini indirip
  ffmpeg ile birleştirir (yt-dlp YOK). Content-Length ilerleme,
  atomik geçici dosya temizliği.
- Main `invidious:downloadStream` (URL politika kontrolü)

### Renderer (SmartTube görünümü)
- `playerSourceSelect`: yt-dlp / Invidious (localStorage kalıcı)
- Toolbar: Yenile, Giriş, Çıkış, Arama
- `renderInvidiousCard`: 16:9 thumbnail + süre etiketi + başlık
  (2 satır clamp) + kanal. role=button, klavye destekli
- `renderInvidiousHome`: Popüler + Trend + Abonelikler bölümleri
  (paralel çek, 24'er kart), 5 dakikalık cache
- `doInvidiousSearch`: anlık sonuç grid (40 kart)
- Login modalı: kullanıcı adı + şifre, "Giriş" / "İptal"

### HTML/CSS
- `index.html`: playerYtUrl altına toolbar + grid + login modal
- `styles.css`: `.inv-card` (content-visibility:auto + hover transform +
  focus-visible), `.inv-grid` (auto-fill 180px), `.inv-card-thumb` (16:9,
  siyah arka plan, süre etiketi), `.inv-card-meta` (2 satır clamp).
  Amber accent kullanır.

## Doğrulama

```
node tests/report65-invidious-bridge.test.js → 21 test PASS
python -m unittest backend/test_invidious.py → 36 test OK (0.193s)
node --check (main, preload, renderer) → OK
python -m py_compile (invidious, media) → OK
Tüm test paketi → yeşil (rapor 56/57/64 dahil)
```

## Tasarım kararları

- **yt-dlp yolu değişmedi**: varsayılan kalır (geriye dönük %100 uyumlu)
- **Invidious opt-in**: playerSource dropdown'ından seçilir
- **Auth bilinçli**: SID cookie modül seviyesinde; subprocess'a
  `Cookie: SID=...` başlığıyla gider. Loglama yapılmaz.
- **Şifre şimdilik argv'de** — gelecek iterasyonda ortam değişkeni
  (`WHISPER_INVIDIOUS_PASSWORD`) eklenmeli (mevcut secret-store kalıbı)
- **URL politika her katmanda**: decideUrlPolicy (main IPC girişi) +
  http/https zorunluluğu + UCID regex (kanal ID)
- **Cache 5 dakika**: ana sayfa her açılışta Invidious'a vurmaz;
  "Yenile" düğmesi zorla yeniden çeker
- **Akıllı UI**: abonelik yoksa giriş önerisi (SmartTube UX)

## Sınırlar / kalan iş

- **Canlı ağ testi yapılmadı** — Invidious test ortamında offline
- **Şifre argv'de** — secret-store entegrasyonu sonraki adım
- **Kanal UI hazır değil** — `invidiousChannel` IPC var ama
  renderer'da "kanal sayfası" görünümü eklenmedi (Faz 4)
- **Playlist desteği yok** — Invidious API'sinde playlist endpoint'i
  var (`/api/v1/playlists/{id}`) ama UI yok (Faz 4)
- **download_stream yalnız video+ses adaptive** — HLS manifest'i
  varsa onu kullanmıyor; o yol yt-dlp'de kalır

## Commit özeti

```
9f4d3b8 feat: SmartTube tarzi Invidious ana sayfa, auth, arama, kanal,
           download_stream
 9 files changed, 1102 insertions(+), 5 deletions(-)
```
