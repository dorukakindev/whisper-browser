# 2026-09-15 10:00 — Browser tarafı sözleşme ve kalan-açık denetim turu (rapor 25)

## Kimlikler

- Depo: `https://github.com/dorukakindev/whisper-browser`
- Dal: `master`
- Başlangıç ürün commit'i: `0dd183f` (değişmedi — bu turda ürün kodu el sürülmedi)
- Bitiş ürün commit'i: `0dd183f`
- Bu not ayrı dokümantasyon commit'inde yer alacaktır.

## Yapılan iş

Kullanıcı browser tarafında bug ve geliştirme önerisi istedi (kod değiştirmeden, önce rapor). IPC/preload/renderer sözleşme eşleşmesi ve hedefli satır okuma yapıldı; bulgular `BROWSER_BUG_REPORT_25.md` dosyasına yazıldı.

## Bulgular (özet — ayrıntı rapor 25'te)

| # | Bulgu | Seviye |
|---|---|---|
| B1 | `retryFailed()` `completeTrack=true` yapıyor; `browser:network:setOnline` ağ-geri-gelmede bunu tüm sekmelere uyguluyor → pencere-modu çeviri sessizce tüm ize genişliyor, istenmeyen API harcaması | P2 |
| B2 | `browser-preload.js`/`browser-ass-renderer.js` `pagehide` once-temizliği bfcache geri dönüşünde observer'ları ölü bırakıyor | P3 (Electron'da doğrulanmalı) |
| B3 | Split-secondary sekmede `enter-html-full-screen` bounds uygulanmıyor; sonraki layout'ta ikincil view aniden tüm pencereyi kaplıyor | P3 |
| B4 | `load-retry` ve `html-full-screen` main tarafından gönderiliyor, renderer'da işleyici yok (ölü olay) | P3 |
| B5 | `browser-asset-store.js` JSON+SRT rename çifti atomik değil; bayat SRT sessizce eski cue dışa aktarabilir | P3 |
| B6 | `browser-features.js` Enter tuşu `busy` korumasını atlıyor → paralel yinelenen istek | P4 |

Sağlam çıkanlar: 166 invoke ↔ handler eşleşmesi, `window.api.*` yüzeyi, script/ID sözleşmeleri, polling interval temizliği, mini-oynatıcı kapatma sırası, `decideSkip` baskılaması.

## Değişen dosyalar

- `BROWSER_BUG_REPORT_25.md` — yeni rapor (bu turun bulguları).
- `docs/devir/2026-09-15-1000.md` — bu not.
- `DEVIR-NOTU.md` — indeks güncellemesi.
- `AGENTS.md` — "bulgular her zaman .md dosyasına kaydedilir" kalıcı kural satırı.

## Doğrulama dökümü

- Ürün kodu değiştirilmedi → `npm test` / `node --check` / smoke çalıştırılmadı.
- Doğrulama: `git status --short` temiz, `node --check` gereksiz (kod dokunuşu yok), statik sözleşme eşleşmeleri komut çıktılarıyla doğrulandı.

## Sınırlar ve sonraki adım

- B1 ve B3 ürün kodu düzeltmesi gerektiriyor; düzeltirken ilgili birim/smoke testleri koşturulmalı.
- B2 Electron'da gerçek ileri-geri gezinme probe'u istiyor.
- Kalan okunmamış bölgeler: `renderer.js` ~17k satır, `main.js` 4112-4453, `backend/`.
- Kullanıcının talimatı: bulgular her zaman .md dosyasına kaydedilsin — AGENTS.md'ye eklendi.
