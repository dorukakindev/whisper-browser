# Devir Notu — 2026-09-22 04:15 (Europe/Istanbul)

## Görev
Altı görevli paketin altıncısı (T6): temiz makinede yeniden üretilebilir kurulum — sıfır checkout'ta bağımlılık kurulumu, `npm test`, Electron bridge, hedefli UI smoke, Python testleri; ortam/ürün ayrımı; CI'da açık araç kurulumu ve kritik test koşusu; dürüst platform belgelendirmesi.

## Repo / Dal / Kimlikler
- Repo: `dorukakindev/whisper-browser`, dal: `devin/t6-clean-install`
- Başlangıç: `4453ae0` (origin/master)
- Bitiş kod commit'i: `b438a7e`
- Bu not docs commit'indedir; kendi hash'i içerilmez.

## Yapılanlar
- Sıfır Ubuntu 22.04 klonu (`~/clean-checkout`): `npm ci` → Castlabs v43.2.0+wvcus; `./install.sh` uçtan uca (orkestratör `core`, py3.10 staged venv, torch cu121); `./start.sh` gerçek pencere açtı; `npm test` tam yeşil; `test:electron-bridge` hem `DISPLAY=:0` hem `xvfb-run -a` ile PASS; `electron-browser-video-e2e` + `electron-ui-scale-matrix` PASS.
- **F-103-1 FAIL-FIXED:** venv takas adımı console script'leri bozuyordu — sahne dizini shebang/gömülü yolda kalıyordu; `repairVenvScripts` (distlib ScriptMaker yeniden üretimi) takas sonrası çalışır, hata rollback'li. Kanıt: onarım öncesi `pip` → `bad interpreter`; sonrası `pip`/`yt-dlp`/`evs-vmp` çalışıyor. install-supply-chain +2 senaryo.
- `requirements-ci.txt`: numpy/scipy pinlerine `python_version` marker'ı (py3.10 → 2.2.6/1.15.3; ≥3.11 → mevcut). py3.10 üretim venv'inde `npm test` tam yeşil doğrulandı.
- `ci.yml`: açık Electron ikili kurulumu (`install.js --no`), Ubuntu'da xvfb+runtime libleri ile gerçek bridge smoke; Windows'ta `test:electron-bridge`. Önceden CI hiç Electron koşmuyordu.
- `install.sh`/`start.sh` yeni; README + INSTALLATION dürüst platform notları (VMP/DRM ve CUDA ayarı Windows-only; macOS yok; safeStorage Secret Service ister); browser-subtitles'a install.sh sözleşme assertion'ı.

## Ortam/ürün ayrımı
- srt/PIL/guessit eksikliği test-bağımlılığı meselesiydi — requirements-ci pin uyumsuzluğu giderilince çözüldü.
- parser-fuzz timeout'u tek seferlik jitter (izole koşuda 2.5 ms) — ürün bug'ı değil.
- dbus/GPU stderr satırları Linux ortam gürültüsü.

## Sınırlar / yapılmayanlar
- Gerçek Windows makine yok — install.bat mantığına dokunulmadı; Windows koşusu CI'a emanet. Script-repair'ın Windows `.exe` kolu aynı distlib yolunu kullanır ama burada doğrulanamadı.
- WhisperX/diarize profilleri Linux'ta koşulmadı.
- Yeni bağımlılık yok.
