# Devir notu — 2026-09-26 00:06

## Repo / dal / commit

- Repo: `D:\Whisper Local` → `https://github.com/dorukakindev/whisper-browser`
- Dal: `devin/repo-research-2026-09-25`
- Başlangıç HEAD: `f8d06b7ef3d2da2a48ae3ee13fa582acd0f10950`
- Bitiş ürün-kodu HEAD'i: aynı (`f8d06b7`) — **bu oturumda ürün kodu değişmedi**; yalnız rapor + devir notu eklendi.
- Çalışma ağacındaki izlenmeyen dosyalar (`.freebuff/`, `PROGRAM_BUG_REPORT_83–85.md`, `_repro/`, `tests/r92-deep/`, eski devir notları vb.) bana ait değil; dokunulmadı.

## Yapılan iş

Kullanıcının "baştan sonra A'dan Z'ye detaylı bug denetimi, false positive olmasın" isteği üzerine tam statik/sözleşme denetimi:

1. **Mekanik sözleşme çapraz-kontrolleri** (hepsi temiz): 217 IPC kanalı preload↔main; `window.api.*` metod eşleşmesi; renderer'ın kullandığı 876 DOM id'sinin `index.html`'de varlığı; backend NDJSON olay tipleri↔renderer switch; argparse↔`args.push` zinciri; tüm `ipcMain.handle`'larda gönderici yetkilendirmesi.
2. **`backend/transcribe.py` derin okuma**: bölme/sarma, halüsinasyon filtreleri, dedupe/merge zinciri, LLM çeviri (batch→rescue→refine, fatal-stop, cache), `translate_existing_subtitle` kısmi-devralma, `OutputTransaction`/`pipeline_control` journal+rollback+PID sahipliği, checkpoint atomik yazı/imza, `reexport_from_json`, `sync_subtitles`, `explain`/`chat` yolları, `main()` dağıtımı.
3. **Renderer/oynatıcı**: olay kapısı (`eventMatchesActiveJob`, `shouldAcceptRunEvent`, `awaitingExit`), `previewSegs` yaşamı, `applyPreviewTranslations`/`renderFinalPreview` eşleme mantığı, `playerJobEvent` (segment/preview_refresh/translation_chunk/translation_refresh), `finishProgressiveJob` dosya yazımı, kuyruk terminal mandalı.
4. **Son commit serisi diff denetimi** (90564c1..HEAD, ~3.8K satır): `browser-translation-scheduler` (provider-failure retry/deferred ayrımı), `browser-page-find` R123-B4, `browser-session-store` çapa koruma, `browser-address-model`, `translation-model-score`, `youtube-tv-mode`, `smarttube-tv`, `pdf-translate` RTL, `browser-omnibox` tekli-eksi düzeltmesi — hepsi tutarlı.
5. **Gizli-anahtar/kalıcılık**: `buildSecretEnv` env ayrımı (chat/explain `opts.translate=true` ile anahtar alıyor), `secret-store`, `settings-security`, `queue-persistence` süzgeci, `local-file-access` kanonikleştirme.

## Kesin bulgu

**R124-01 (Orta)** — `preview_refresh` emit koşulu (`transcribe.py:6285–6292`) bayrak beyaz listesi; `fix_common_errors`, `drop_repeated_hallucinations`, iki koşulsuz `apply_dedupe_policy` geçişi ve `snap_to_speech` kapsam dışı. Bunlar `entries`'i değiştirirken koşul false kalırsa `previewSegs` bayat kalır → çeviri metinleri yanlış önizleme satırına düşer, hayalet satırlar kalır, `copyPreview` bayat SRT üretir, progressive modda `liveSource` içindeki hayalet cue'lar çıktı dosyasına yazılır. Tetik: tüm listelenen işleme seçenekleri kapalı + varsayılan-açık mutatorlardan birinin gerçekten düşürmesi (veya koşulsuz dedupe'un rolling-cue birleştirmesi). Ayrıntı ve öneri: `docs/raporlar/BROWSER_BUG_REPORT_124.md`.

**R124-02 (Düşük, savunma)** — `--sync-srt ""` → `Path('')`=`Path('.')` `exists()`'i geçer, dizin okuma hatası yanıltıcı mesaj basar. UI'dan erişilemez (iki yol da guard'lı); yalnız el-IPC/CLI.

## Elenen hipotezler

- `preview_refresh` 1-bazlı index → renderer kullanmıyor (zaman/sıra eşlemesi).
- `chat_about_video` `messages` gölgelenmesi → döngü sonrası, etkisiz.
- `parse_srt` sayı-satırı sezgiselliği → yalnız bozuk girdi.
- Ayrıntılar raporun "Elenen hipotezler" bölümünde.

## Testler

- `npm test` (`node tests/run-all.js`): **tamamı yeşil**, exit 0.
- `node --check src/main.js`, `node --check src/renderer/renderer.js`: temiz.
- Çalıştırılmayanlar: GPU'lu gerçek transkripsiyon E2E, Electron görsel doğrulama, Windows `start.bat` akışı — bu oturum statik denetimdi; R124-01'in uçtan-uca doğrulaması Windows'ta "tüm işleme seçenekleri kapalı" yapılandırmayla yapılmalı.

## Değişen dosyalar

- `docs/raporlar/BROWSER_BUG_REPORT_124.md` (yeni) — denetim raporu ve bulgular.
- `docs/devir/2026-09-26-0006.md` (yeni) — bu not.
- `DEVIR-NOTU.md` — indeks güncellendi.

Bağımlılık/şema değişikliği yok. Ürün kodu commit'i yok (yalnız dokümantasyon).

## Sonraki adımlar

1. R124-01 için önerilen düzeltme: emit koşulunu "entries değişti mi" gerçeğine bağlamak (ör. akış sonrası `len`/kimlik özeti karşılaştırması) ya da eksik bayrakları koşula eklemek; `applyPreviewTranslations`'a zaman-öncelikli eşleme savunma derinliği.
2. R124-02: `sync_subtitles` başında `srt_path.is_file()` doğrulaması.
3. Windows ortamında E2E yeniden üretim (seçenekler kapalı + çeviri açık + düşürülen blok senaryosu).
