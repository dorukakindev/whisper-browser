# Devir notu — 2026-09-20 14:08 (TSİ)

Repo: `dorukakindev/whisper-browser` · Dal: `master`
Başlangıç ürün commit'i: `fc42b40` (1a tamamlandıktan sonraki çalışma ağacı)
Bitiş ürün commit'i: `2b67ea3`

## Yapılan iş

Karar raporundaki 1. önceliğin ikinci yarısı: **kullanıcı kontrollü SmartTube oynatma sırası**
(FEATURE_RECOMMENDATION_REVIEW_2026-09-20.md — "Deliver progress/history first; queue second").

## Değişen dosyalar (ürün)

- `src/renderer/renderer.js`
  - Kuyruk blok'u (`ST_QUEUE_KEY='stPlayQueue'`, `ST_QUEUE_MAX=50`): `loadStQueue` (videoId doğrular,
    thumbnail'ı `http(s)` şemasına indirger, 50'ye keser), `saveStQueue`, `stQueueHas`, `stQueueToggle`,
    `stQueueDequeueIfPlaying` (yalnızca `stQueue[0].videoId === key.slice(8)` ise düşürür),
    `stQueuePlayNext` (openIntent artırır, `pendingAutoOpen={key:intent}` yazar, probe kuyruğa alır),
    `stQueueAutoNext` (ended kapısı: YouTube medyası + dolu sıra), `stQueueRailVideos`.
  - `buildSmartTubeCard`: küçük resmin üstünde `st-card-queue` düğmesi ('+'/'✓');
    click ve Enter/Space `stopPropagation`+`preventDefault` — kartın kendi oynatmasını tetiklemez
    ve Enter'da native click çift-toggle yaratmaz.
  - `video 'ended'`: `player.autoNext && !stQueueAutoNext() && playlistIndex>=0 → playPlaylistDelta(1)`
    (tek satır — player-ui.test.js'in 300-karakter kaynak penceresi korundu).
  - `$('playerStream')` click: HLS ve progressive başarılı açılışların ikisinde de
    `stQueueDequeueIfPlaying(ytKey)` — **probe başarısızsa sıra korunur** (kabul koşulu).
  - `$('playerNextMedia')` click: YouTube + dolu sıra → `stQueuePlayNext()`, aksi halde `playPlaylistDelta(1)`.
  - `updatePlaylistButtons`: YouTube + sıra varsa Next düğmesi playlist'ten bağımsız etkin.
  - Ana sayfa: "Oynatma sırası" rayı "İzlemeye devam et"in üstüne prepend edilir; iki raydaki
    kimlikler feed dedupe'undan da dışlanır (`railIds`).
- `src/renderer/styles.css`: `.st-card-queue` (hover/focus-within'de görünür, sıradayken amber;
  metin `var(--accent-contrast)` — tema sözleşmesi).
- `src/renderer/ui-locale.js`: `Oynatma sırası`/`Sıraya ekle`/`Sıradan çıkar` EN karşılıkları.

## Değişen dosyalar (test)

- `tests/report67-smarttube-wiring.test.js` — mini-DOM `classList` desteği; sahte `localStorage`;
  `buildQueueHarness()` gerçek kuyruk fonksiyonlarını kaynaktan çıkarıp vm bağlamında çalıştırır
  (kaynak-regex kırılganlığı yerine davranış). Yeni testler: ekle/çıkar/kalıcılık/güvenli-thumb,
  playNext + dequeue-yalnız-açılan-kimlik, kart düğmesi oynatmadan toggle, ended/Next sözleşmesi.

## Kabul koşulları eşlemesi (rapor)

- Video-ID mapping: `mediaKeyFor('youtube', videoId)` → `youtube:<id>` ✓
- Çift kart yok: ray kimlikleri feed dedupe'a girer ✓
- Yeniden başlatmada devam: `pendingLibrarySeek` (1a'da) ✓
- Sıra öngörülebilir: localStorage kalıcı, dequeue yalnızca **başarılı akış açılışında** ✓
- Kart/klavye: click + Enter/Space, baloncuklanma ve native-click çiftliği engelli ✓
- ended/manuel-next yarışı: `openIntent`/`pendingAutoOpen` son-yazan-kazanır ✓
- `player.autoNext` saygısı ✓

## Test sonuçları

- `node --check src/renderer/renderer.js`, `ui-locale.js` — temiz
- `node tests/report67-smarttube-wiring.test.js` — **72/72**
- `node tests/player-ui.test.js` — **145/145**
- `node tests/design-system.test.js` — geçti
- `npm test` — tek başarısız dosya: `watch-index-assets` (node:sqlite `fts5` bu Linux
  build'inde derli değil — belgeli ortam sınırı, ürün regresyonu değil)

## Bilinçli sınırlar / kalan iş

- Sıra görünürlüğü SmartTube ana sayfa rayıyla sınırlı; oynatıcı içi sıra paneli yok (kapsam dışı).
- Canlı Invidious probe gerçek ağ gerektirir — vm-sandbox testler davranışı kanıtlar, ağ yolu
  mevcut probe kanalını yeniden kullanır.
- Sıradaki öncelikler (rapor sırasıyla): seekbar kare önizlemesi (yerel + Invidious storyboard
  iki yol), yerel abonelikler + içe/dışa aktarma, altyazı aramasında yerel dosya eşleştirmesi.
