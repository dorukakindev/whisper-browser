# Devir notu — 2026-09-17 13:31

## İş

Dördüncü büyük denetim turu: **57 paralel salt-okunur ajan** (4 dalga:
A01–A20 + B21–B37 + C01–C20) ile tarayıcı + Python backend + renderer
kapsamının tamamı denetlendi. Tüm P1/P2 iddiaları üst ajan tarafından
güncel kaynakta satır seviyesinde yeniden doğrulandı; raporlar 51/53 ile
tekilleştirildi. Konsolide sonuç **`BROWSER_BUG_REPORT_54.md`**
dosyasındadır (~3.560 satır, ~440 bulgu: P1≈44, P2≈120, P3≈175, P4≈105).

## Repo ve commit kimlikleri

- Repo: `https://github.com/dorukakindev/whisper-browser`
- Dal: `master`
- Başlangıç ve bitiş ürün kodu: `1150f0b` — **ürün kodu bu oturumda
  değiştirilmedi** (salt-okunur denetim turu).
- `BROWSER_BUG_REPORT_*.md` dosyaları `.gitignore`'da (satır 68) —
  rapor yalnız yerel; bu not tek dokümantasyon kaydıdır.

## Bu turda denetlenen alanlar

Önceki turların üzerine kalan tüm bölgeler:

- **Dalga 1 (A01–A11):** altyazı keşif/yükleme, senkron/düzenleme,
  CEA/HLS yakalama, DRM/EME, medya kimliği/SPA, SponsorBlock, manga,
  oturum/workspace, izleme geçmişi, renderer orkestrasyonu, IPC güvenliği.
- **Dalga 2 (A12–A20):** overlay/akıcılık, performans sıcak yolları,
  capture kuyruğu, erişilebilirlik, medya araçları, hata yolları,
  sekme-arası sızıntı, son-commit regresyonu, indirme/mini-oynatıcı.
  (İlk denemede altyapı iptali yaşadı, aynı kapsamla yeniden gönderildi.)
- **Dalga 3 (B21–B37):** oynatıcı kontrolleri, çeviri zamanlayıcısı,
  live-ASR, export, gezinme kenarları, site tercihleri, olay sıralaması,
  preload enjeksiyonları, kuyruk entegrasyonu, ekran görüntüsü/OCR,
  bellek/timer sızıntıları, ağ yakalama, UI durum makinesi,
  belgelenmiş-ama-eksik, bul-çubuğu, site adaptörleri.
- **Dalga 4 (C01–C20):** Python backend çekirdeği (transcribe boru
  hattı, halüsinasyon filtreleri, LLM çeviri/postprocess, senkron
  motoru, diarization/WhisperX, live_asr.py, media.py/yt-dlp), yerel
  dosya oynatıcı, burn-in, kuyruk state machine, settings sanitizer,
  preload köprü yüzeyi, kısayollar, ui-locale derinliği, AI chat,
  YouTube akışları, pencere/tray yaşam döngüsü, adblock/Cloudflare,
  tanı/logging + test kapsamı. (11 ajan rate-limit'e takılıp ikinci
  denemede tamamlandı; C01+C20 üçüncü denemede.)

## Öne çıkan bulgular (rapor 54'te tam liste)

- **DRM `mediaKeySystem` izin reddi** → EME her zaman block (A04).
- **CEA-708 stream uyumsuzluğu** (`cc708_N` ≠ `SERVICEN`) + hata-gövdesi
  zehirlenmesi + canlı-liste matcher ölümü → 708 yakalama tamamen ölü (A03).
- **Medya kimliği:** persist edilmiş iz anahtarı yazma≠okuma + streamMediaId
  oturum-rotasyonu (A05, A02 teyitli).
- **Sekme-arası sponsor atlama** — sekme değişiminde bayat segmentler yeni
  videoya uygulanıyor (A06).
- **Overlay senkronu:** her sync'te tam cue listesi + ~570 satır script
  yeniden enjekte → canlı çeviride O(n²) jank (A12).
- **`browserTrackSourceIdentity` per-cue O(n) hash** → kare başına O(n²)
  (A13); **track publish** segment başına O(n) bookkeeping + sync fs (A13).
- **Settings import'unda zincirleme credential-exfiltration:** endpointIdentity
  kasa-enjeksiyonunda ölü + `ui.*`/`translate.*` çift-kaynak desync →
  sıfır-uyarılı anahtar sızıntısı (C11 — dalganın en ağırı).
- **Kuyruk:** `-whisper-q${id}` soneki kendi regex'ini geçemiyor (A19,
  R51-31 regresyonu); `persistQueueNow` TOCTOU → iptal edilmiş iş yine
  spawn + `state.running` sonsuza dek true (C10).
- **Kısayollar:** Ctrl+C `copyCue`'ya sızıyor; button-odak Esc'i öldürüyor;
  input'ta Esc çalışan işi iptal ediyor (C13 — üç P1).
- **Backend:** WhisperX `--task translate` yok sayılıyor (C05); `explain`
  yetki yönlendirmesinde tamamen ölü (C16); `hotwords` kwarg'ı eski
  faster-whisper'da TypeError (C01); `fix_text_artifacts` boş metin →
  strict-writer tüm çıktıyı düşürüyor (C01); `apply_piecewise` sırasız
  SRT yazıyor (C04); halüsinasyon grup-ortalaması gerçek repliği
  siliyor (C02); burn-in kilidi authorize'dan sonra → çift burn (C09);
  live-ASR non-ASCII path → ölü/sonsuz asılma (C06); yt-dlp güncellemesi
  transkribe'a ulaşmıyor (C07).
- **Yaşam döngüsü:** `mainWindow.destroy()` beforeunload'suz → ayar/kuyruk
  kaybı; `app.exit(0)` relaunch sahte-çökme (C18); `did-navigate-in-page`
  sıralaması ters → eski altyazı yeni medyanın kaydına persist (A14).
- **Ölü kod:** `io_errors.py` ve `queue-lifecycle.js` test ediliyor ama
  üretimde çağrılmıyor (C20 + C10 çapraz-doğrulama).

## Düzeltmeler / reddedilenler

- B26'nın `uiLocale` whitelist iddiası **çürütüldü** — `settings-security.js:32`
  whitelist'te var + enum-validated; C11'in birebir diff'iyle ikinci kez
  teyit. Rapor düzeltildi.
- C12 preload köprüsü **temiz** — ~150 API ↔ 176 handler birebir eşleşme,
  sender-doğrulama tutarlı (P1/P2 yok).
- B35 önceki-tur düzeltmelerin yerinde olduğunu doğruladı (regresyon yok).
- A11 IPC denetimi 176 handler'da bulgu üretmedi.
- A15'in ~15 erişilebilirlik adayı + A18'in 16 sekme-izolasyon adayı
  mevcut korumalarla reddedildi.

## Doğrulama

- Denetim statikti; **ürün kodu bu oturumda değiştirilmedi.** Tüm P1/P2
  iddiaları `1150f0b` HEAD'inde `dosya:satır` doğrulandı.
- Runtime'a bağımlı kalanlar raporun "Runtime'a bağımlı" bölümünde
  (CDP davranışları, nativeImage kenarları, AT repro'ları, yarış
  pencereleri).

## Sınırlar ve sonraki adımlar

- 57 ajan / ~440 bulgu — düzeltme turu için rapor 54'teki öncelik
  sıralaması kullanılmalı; güçlü desen: `workspaceMode` guard'ı hiçbir
  save/restore/event-handler'da yok (dört bulgu aynı kök).
- En acil küme: DRM/CEA (özellik tamamen ölü), credential-exfiltration
  zinciri (SS1+SS2), kuyruk suffix regex'i (kullanıcı-görünür hata),
  kısayol sızıntıları (Ctrl+C kopyalama bozuk).
- `AGENTS.md`'deki "son: 28" rapor-numarası ibaresi bayat — gerçek seri
  54'e ulaştı; bir sonraki bakım turunda güncellenmeli.
- 19 Electron smoke testi yetim (`*.smoke.js` hiç otomatik koşmuyor) —
  `test:smoke` script'i önerilir.
- Commit/push bu not ve `DEVIR-NOTU.md` güncellemesiyle sınırlı;
  rapor dosyası `.gitignore`'da olduğu için push kapsamı dışında.
