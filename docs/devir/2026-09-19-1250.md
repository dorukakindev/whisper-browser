# Devir notu — 2026-09-19 12:50

## R75 GitHub özellik araştırması (kod değişikliği YOK)

**Repo/dal:** `https://github.com/dorukakindev/whisper-browser` · `master`
**Başlangıç kod:** `b3da67a` (R74 docs commit'i) · **Bitiş kod:** `b3da67a` — ürün kodu bu turda DEĞİŞMEDİ (kullanıcı talimatı: "bu turda düzeltme yapma").

## Ne yapıldı

Browser/SmartTube/player tarafına eklenebilecek özellikler için **12+ GitHub reposunun kaynak kodu ve API rotaları** incelendi; her bulgu mevcut kod tabanıyla eşleştirilip `BROWSER_FEATURE_RESEARCH_75.md`'ye yazıldı.

**İncelenen repolar:** yuliskov/SmartTube (MIT) · FreeTubeApp/FreeTube (AGPL-3.0) · TeamNewPipe/NewPipe + NewPipeExtractor PR#6434/#647 (GPL-3.0) · iv-org/invidious `routing.cr` tam rota listesi (AGPL-3.0) · TeamPiped/Piped + piped_dart OpenAPI (AGPL-3.0) · ajayyy/SponsorBlock + DeArrow (MIT) · Anarios/return-youtube-dislike (GPL-3.0) · LuanRT/YouTube.js (MIT) · electron/electron docs + PR#32848 · omersusin/piTube · ViewTube/Materialious/Clipious.

## Öne çıkan bulgular

**Kullanılmayan Invidious endpoint'leri** (`routing.cr`'dan doğrulandı — mevcut backend'e ek wrapper ile kazanılır):
- `/api/v1/storyboards/:id` — seekbar hover önizlemesi için hazır sprite spec (NewPipe PR#6434 implementasyonu referans)
- `/api/v1/search/suggestions` — stSearchInput autocomplete
- `/api/v1/captions/:id?tlang=` — YouTube auto-translate ile bedava altyazı çevirisi
- `/api/v1/transcripts/:id`, `/api/v1/clips/:id`, `/api/v1/hashtag`, `/api/v1/mixes`
- Kanal sekmeleri: `shorts/streams/podcasts/releases/courses/playlists/community/channels/search/latest`
- `/api/v1/auth/{feed,playlists,history}` — mevcut SID session ile kullanılabilir

**SmartTube parite boşlukları** (`VideoMenuPresenter.initMenuMapping` + `BrowseSection` type'larından):
- Kart bağlam menüsü (Play Next / Add to Queue / Watch Later / Not interested / Pin / Share)
- Oynatma kuyruğu (mevcut `queue` transkripsiyon kuyruğu — ayrı isim gerekir)
- Yerel abonelikler + gruplar (anonim deneyim; Invidious session bağımlılığını kaldırır)
- Bölüm seti: History/Playlists/Live/Music-Gaming-News bölümleri yok
- TYPE_ROW yatay raflar, kart boyutu/UI ölçeği, hide shorts/watched

**Diğer repolardan:**
- FreeTube (Electron, en yakın mimari): distraction-free ayarlar, kart ilerleme overlay'i, OPML/CSV abonelik import-export, ekran görüntüsü, external player handoff. **AGPL — kod değil desen adaptasyonu.** SABR notu: built-in download'larını SABR kırdığı için kaldırdılar → yt-dlp seçimimiz doğrulandı.
- RYD `/votes?videoId=` — dislike rozeti; rate limit (100/dk, 10K/gün) → yalnız video detayında.
- DeArrow `/api/branding` — clickbait başlık/thumbnail düzeltme; ayara bağlı.
- YouTube.js (MIT, youtubei.js) — InnerTube: playlist-edit, like/subscribe, history-sync, live chat, comment replies; tek büyük bağımlılık adayı.
- Piped-Backend — Invidious-down senaryosunda ikinci backend sınıfı (`/feed/unauthenticated` hesapsız çok-kanallı feed dahil).
- Electron: `navigator.mediaSession` (donanım tuşları + OS now-playing), `setThumbarButtons`, `setProgressBar` — düşük maliyetli platform paritesi.

## Dosyalar

- `BROWSER_FEATURE_RESEARCH_75.md` — **yeni** — tam envanter: repo bazında lisans, incelenen yüzey, bulgu tabloları, S/A/B/C öncelik matrisi, reddedilenler, uygulama notları.

## Doğrulama

- Ürün kodu değişmedi → `npm test`/smoke koşulmadı (belge-only teslim; AGENTS.md kuralına göre beyan).
- Rapor içeriği kaynak okumasına dayanır: Invidious rota listesi `src/invidious/routing.cr`, SmartTube menü seti `VideoMenuPresenter`, storyboard formatı NewPipeExtractor PR#647 diff'i, RYD rate limit repo issue'ları, Piped uçları OpenAPI şeması.
- Mevcut-envanter doğrulaması `grep` ile yapıldı (storyboard/mediaSession/dearrow/suggest → 0 eşleşme; st-section 5 adet; trending chip'leri mevcut).

## Sınırlar / sonraki adım

- Bulgular **tasarım önerisidir; hiçbiri implemente/test edilmedi.** Öncelik matrisi S→C sıralı; ilk sprint adayı: MediaSession, arama önerileri, RYD rozeti, kart ilerleme overlay'i, yorum yanıtları, hide shorts.
- YouTube.js entegrasyonu OAuth→cookie köprüsü araştırması gerektirir (ayrı karar).
- `_repro/` ve `docs/devir/2026-09-19-10{40,100}.md` paralel oturumun untracked WIP'i — bu commit'e dahil edilmedi.
