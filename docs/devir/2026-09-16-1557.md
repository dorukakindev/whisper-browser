# Devir notu — 2026-09-16 15:57

## İş

Üçüncü büyük denetim turu: **35 paralel salt-okunur ajan** (20 + kullanıcının ara talimatıyla 15 ek) ile daha önce hiç taranmamış bölgeler denetlendi. Tüm kritik bulgular üst ajan tarafından güncel kaynakta satır seviyesinde yeniden doğrulandı; raporlar 42/46/47/48/49 ile tekilleştirildi. Konsolide sonuç **`BROWSER_BUG_REPORT_51.md`** dosyasındadır.

## Repo ve commit kimlikleri

- Repo: `https://github.com/dorukakindev/whisper-browser`
- Dal: `master`
- Başlangıç ürün kodu: `bf6780424cc779efd60cedf4c8f5c19014218c90`
- Bu turda ürün kodu **bu oturum tarafından** değiştirilmedi (denetim turu).
- Dokümantasyon commit'i: `d9f395d17d06f8360e3ee39b56231896c2d28c16` + rapor-51 düzeltme commit'i.

## Önemli açıklama — `d9f395d` commit karışıklığı

`d9f395d` "docs: record 35-agent browser/renderer audit" mesajını taşısa da, commit anında paralel oturum (Sol) kendi çalışmasını stage etmişti. Bu yüzden commit şunları da içerdi:

- Paralel oturumun **ürün kodu değişiklikleri**: `backend/transcribe.py`, `src/main.js`, `src/subtitle-sentence-layout.js` + iki test dosyası — bağlamsal çeviride cue-sahipliği (sayı/özel ad doğru cue'da kalsın) prompt düzeltmesi. Değişiklikler eksiksiz ve testleri commit içinde; paralel oturumun kendi raporu `BROWSER_BUG_REPORT_50.md`'yi yazdı (farklı konu — bu yüzden bu turun denetim raporu **51** numaralıdır).
- Paralel oturumun `BROWSER_BUG_REPORT_50.md` dosyası (76 satır, cue-sahipliği düzeltme raporu).

Push edilmiş geçmiş yeniden yazılmadı; `d9f395d` içeriği olduğu gibi bırakıldı ve bu notta açıkça belgelendi. Paralel oturumun çalışması korunmuştur.

## Bu turda denetlenen alanlar

Tur 1+2'nin (~95 modül) üzerine: paket/oturum içe-dışa aktarım güvenlik zinciri, ayar importunda sır mirası ve izin mirası, `browser:command` medya yolu, canlı-ASR süreç yaşam döngüsü, kuyruk kalıcılık/iptal/yeniden-yükleme, sekme yaşam döngüsü (hibernasyon/crash/import), manga uçtan-uca (görsel indirme→model→overlay→düzenleme), CEA/HLS boru hattı bakiyesi (708 adlandırma, byterange, SKIP, CDP sıralama), yerel SRT/VTT parser, altyazı görüntüleme/overlay, JASSUB, anlamsal arama/sahne/OCR araçları, `backend/transcribe.py` metrik hizası, `export_anki.py`, yardımcı alt süreç env temizliği, test altyapısı, kısayol/erişilebilirlik, indirme/favicon UI.

## Öne çıkan bulgular (rapor 51'de tam liste)

- **R51-01 (P1):** Çalışma paketi `browser-session.json`'u ezer; açılış-restore'u kalıcı `primaryFile`/`secondaryFile` yollarına koşulsuz `subtitleFileAccess.grant()` verir → keyfi `*.json`/altyazı-uzantılı dosya okuma+yazma (`workspace-package.js:6` → `main.js:2421-2424`, `.json` uzantısı `local-file-access.js:5`).
- **R51-02 (P1):** Ayar import'u `customBaseUrl`'i değiştirirken mevcut API anahtarını miras alır → saklı anahtar saldırgan endpoint'ine gider (`settings-security.js:361-390`).
- **R51-03 (P1):** `hideBrowserView` yakalama kuyruğunu flush'tan önce sıfırlar (`main.js:10562-10566` vs `3870`) → işlenmemiş altyazı yanıtları sessizce kaybolur.
- **R51-04 (P1):** Canlı-ASR `stdin`'de 'error' dinleyicisi yok → `EPIPE` ana süreci çökertir (`main.js:13189`; doğru kalıp `13990`'da var).
- **R51-05 (P1):** Tekil cue editörü hedefi açılışta sabitlemiyor; `saveCueEdit` `player.activeIdx`'i kayıt anında okur → yanlış bloğa/dosyaya yazım (`renderer.js:21025-21075, 21206, 21251, 21268`).
- **R51-06 (P1):** Manga WebP/AVIF/GIF kabul ediliyor ama `nativeImage` çözemiyor → modern CDN'lerde tüm sayfalar kalıcı başarısız (`main.js:5438, 5559-5560`).
- ~30 P2: sitePermissions aktif-sekme öncesi sessiz grant (9935-9936), `clearedSecretFields` onaysız, katalog `grantMedia` diyalogsuz, `browser:command` timeout/bağlam/aday kusurları (11971-11989), explain modu ölü (15193-15195), CEA 708/byterange/SKIP/CDP-sıralama boşlukları, ana renderer'da `render-process-gone` yok, `ensureBrowserView` mutasyonda null (9907), `lifecycle` renderer'a hiç kopyalanmıyor (boşalt UI'ı ölü), kuyruk spawn-iptali/reload yetimleri, `watchSource` düşmesi, anlamsal-arama seek ofsetsiz, sahne stderr kesilmesi, Anki keyfi dosya gömme, beş yardımcıda env-scrubbing boşluğu.
- ~45 P3 + ~25 P4 — rapor 51 tablolarında.

## Doğrulanan sağlam alanlar

IPC yetki sınırı (`sender === mainWindow.webContents`), gezinme politikası deny-by-default, atomik kalıcılık + terminal-guard, çıktı transaction'ı, manga indirme güvenlik zinciri (DNS-pin + redirect yeniden-doğrulama), oturum paketi checksum/traversal korumaları, `browser:extras` generation/mediaId kapısı, skip-kayıt normalizasyonu, JASSUB yaşam döngüsü, XSS yüzeyi (tümü textContent/escapeHtml). Ayrıntılar raporda.

## Doğrulama

- Denetim statikti: **test koşulmadı, ürün kodu bu oturumda değiştirilmedi.**
- Tüm P1/P2 iddiaları `bf67804` HEAD'inde `dosya:satır` doğrulandı (grep+read ile üst ajan).
- Paralel oturumun `d9f395d`'ye karışan değişiklikleri kendi raporunda (`BROWSER_BUG_REPORT_50.md`) test-kanıtlıdır; bu turun test-koşusu sorumluluğu onlara aittir.

## Sınırlar ve sonraki adımlar

- Runtime doğrulaması bekleyenler rapor 51'in "Runtime doğrulama gerektirenler" bölümünde (nativeImage WebP, yarış pencereleri, gerçek HLS fixture, renderer crash davranışı).
- Düzeltme planı 5 kademeli olarak raporda; önerilen başlangıç Kademe 1 (veri kaybı/güvenlik sınırı).
- `AGENTS.md`'deki "son: 28" rapor-numarası ibaresi güncel değil — gerçek seri 51'e ulaştı; bir sonraki bakım turunda güncellenmeli.
- Rate-limit ile kesilen 3 ajan yeniden başlatıldı; 35 kapsamın tamamı döndü.
