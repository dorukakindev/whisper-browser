# Devir Notu — 2026-09-20 17:38 (Europe/Istanbul)

Repo: dorukakindev/whisper-browser · Dal: master
Başlangıç kod commit'i: `9d11574` öncesi kod `93cd535` (Parti 13);
belge commit'i `c38d9a8`/`9d11574` (Parti 14) bu arada girildi.
Bitiş kod commit'i: `37e3c18` — Parti 15: E05 + E06 kozmetik dilimi.

## Kapsam

- **E05 (dar dilim):** `browser-features.js` altyazı araması —
  API/işlem hatası (`call` → null) ile "sonuç yok" ayrıldı: hata
  durumunda `bfSubtitleResults` içine "Arama tamamlanamadı — ayrıntı
  üstteki durumda. Yeniden deneyebilirsiniz." notu; `totalCount` >
  gösterilen satırda "toplam N sonucun ilk bölümü" kısmi-liste notu.
- **E06 (dar dilim):** `renderer.js` burn-in —
  `type:'start'` olayı "Gömme hazırlanıyor…" aşaması; progress metni
  `N% · m:ss / m:ss` (toplam yoksa geçen süre); iptal düğmesi tıklanınca
  "İptal ediliyor…" + disabled. FFmpeg argümanları hwaccel içermiyor
  (CPU-only) — doğrulanmamış GPU kullanımı hiçbir yerde gösterilmiyor;
  GPU iddiası eklenmedi.

## Testler

- `npm test` → Tüm testler geçti.
- `tests/browser-parti7.test.js`: E05/E06 marker asserts.
- `node --check` renderer.js + browser-features.js ✅

## Kalan E maddeleri

- E04 (rozet hiyerarşisi — renk dışı işaret), E07 (bakım ekranı
  sadeleştirme), E08 (responsive/tooltip — gerçek ekran görüntüsü
  doğrulaması gerektirir), E09 (belge terminoloji bakımı)
- F18 bakiyesi: bağımlılık panosu (yt-dlp/venv sürüm görünürlüğü,
  devam/checksum — yt-dlp güncelleme motoru `test_ytdlp_update.py`'de
  zaten kapsamlı; eksik olan UI görünürlüğü)

K/B/R maddelerine dokunulmadı.
