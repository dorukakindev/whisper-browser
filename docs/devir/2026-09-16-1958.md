# 2026-09-16 19:58 — Screenshot-led English usage guide

## Identity and scope

- Repository: https://github.com/dorukakindev/whisper-browser
- Branch: `master`
- Starting commit: `a5b020ff15a5e12a32a60f532670034d2b9bcced`
- Product-code commit at start and end of this documentation task: `a5b020ff15a5e12a32a60f532670034d2b9bcced`
- Usage-guide commit: `bc2a366a6dfb8f77cbeb2033ec62a3d5a0244330`
- This task changed documentation and public media only. Concurrent report-53/product fixes remained unstaged and were not included.

## Delivered

- Added `docs/USAGE_GUIDE.md`, an English guide mapping the current Turkish UI labels to the following workflows:
  - local file/folder transcription and export;
  - YouTube source mode for complete local subtitle jobs;
  - embedded browser mode for authorized playback and accessible caption capture;
  - complete-track retrieval versus Live Whisper fallback;
  - contextual subtitle translation and provider-specific saved profiles;
  - page, manga/webtoon, and PDF translation;
  - AI context, subtitle review, synchronization, and final quality checks.
- Added a concise “Ways to use it” matrix and guide link to the root README.
- Expanded `docs/README.md` into a current-documentation index.
- Added `docs/media/translation-providers.png`, produced by the real Electron UI in an isolated profile. It shows separate subtitle and manga provider/model profiles without an API key.
- Updated `docs/media/README.md` with provenance and SHA-256, and updated the changelog.

## Visual evidence and verification

- `tests/electron-foursapi-provider-ui.smoke.js --disable-gpu`: passed. It confirmed the provider/model selections are visible at wide and narrow sizes without horizontal overflow and generated the new screenshot.
- New screenshot: valid PNG, 2049×1350, SHA-256 `415847c2dc1472714a9a42ef61b7508c2f44c40a9157efb80dee4cd8350ebf02`.
- Local Markdown targets in `README.md`, `docs/README.md`, `docs/USAGE_GUIDE.md`, and `docs/media/README.md`: passed.
- Secret-pattern scan of changed Markdown: no key/token literal found. The screenshot is from a new isolated profile and no key was entered.
- `git diff --cached --check`: passed before the guide commit.

## Honest limits

- The Windows Computer Use helper failed twice with `windows sandbox failed: helper_unknown_error: apply deny-read ACLs`; no personal desktop or existing browser profile was inspected.
- A new YouTube-source-form capture was attempted in an isolated Electron profile but the current concurrently modified startup flow did not reach the ready state. The process was stopped, its temporary script/profile was removed, and no product fix was attempted. The guide therefore uses the already published real-video YouTube dual-subtitle capture and tour.
- The browser-extras Electron smoke also timed out waiting for its browser tab in the concurrently modified tree. Its existing verified public browser-workspace capture remains in use. This failure was not investigated because another agent owns the active product-code work.
- No full `npm test`, Electron bridge, real provider call, live website login, or DRM-site validation was run for this documentation-only task. The guide does not claim universal site support or DRM bypass.

## Handoff

After the concurrent product-code work is committed, rerun the browser-extras smoke and optionally add a dedicated English-callout YouTube source-form image. Do not replace the real-video tour with an empty mock screen; keep all public captures free of accounts, private subtitles, keys, paths, and browsing history.
