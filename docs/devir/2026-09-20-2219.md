# Devir Notu — 2026-09-20 22:19 (İstanbul)

## Kapsam
`!!bug_catcher` turu: son ~100 commit'in dif'leri tarandı (Parti 7–15 + androiandot paketi: omnibox, subscriptions-io, opensubtitles-hash, reading-list, element-rules, seekPreview, mpv/VLC devir, R83–85 hardening). Bulunan en yüksek değerli bug düzeltildi; ikincil bir spec sapması da aynı commit'te.

## Commit'ler
- Başlangıç: `3459952319db0f37ee3d2be33f1a4771d13d90bc` (master)
- Ürün+test: `53dabe33e32fdf10acf99d8fe45007bc3d22ace3` (dal: `codex/bug-catcher-queue-next-moviehash`)
- Doküman: bu not + DEVIR-NOTU indeksi (ayrı commit)
- Teslim biçimi: playbook gereği PR (önceki turlar doğrudan master'a idi)

## Düzeltilen buglar
1. **F1 (Major, FULL_REVIEW_2026-09-20)**: `stQueueToggle` hiç `updatePlaylistButtons()` çağırmıyordu ve `setMediaKey` içindeki çağrı `player.mediaKey = nextKey` atamasından ÖNCE koşuyordu (`renderer.js` ~16864). Sonuç: YouTube oynarken sıraya ekleme yapınca veya sıra-dışı videodan YouTube'a geçince "Sonraki" düğmesi `disabled` kalıyordu — manuel sonraki ölü, `ended`/oto-next çalışıyordu (tutarsız davranış). Düzeltme: atama yenileme çağrısından önceye alındı + toggle'ın iki kolu da düğmeleri tazeliyor.
2. **moviehash tail-overlap (B03 takip)**: `opensubtitles-hash.js` 64KiB<dosya<128KiB aralığında kuyruk bloğunu çakışmasız okuyordu; resmi algoritma son 64KiB'ı `size-65536`'dan tam blok okur ve çakışan baytları iki kez sayar. 100000B örnekte eski hash `01d017e6b4ff38f4`, referans `e09f6020dfa1c6a0` — OpenSubtitles dosya-eşleşmesi bu boyut aralığında ıskalıyordu.

## İncelenip bug ÇIKMAYAN yüzeyler (kanıt)
- `subscriptions-io.js`: parse/export sanitizasyonu sağlam (UCID/handle regex, MAX_SUBS, OPML escape, dosya adı whitelist). Bilinen küçük sınırlar: aynı kanal UCID ve @handle ile ayrı kayda düşebilir; virgüllü kanal adı CSV'de bölünür.
- `browser-omnibox.js`: hesaplayıcı eval'siz (charset whitelist + RPN), bang URL'leri encodeURIComponent.
- `browser-reading-list.js` / `browser-element-rules.js`: dizin-kaçışı ve origin/selector whitelist korumaları yerinde; atomik yazım var (repo'nun atomic-json yardımcısını kullanmıyor — Nitpick).
- `player:external` (mpv/VLC): whitelist çözümleme + argv dizisi (shell yok), URL `decideUrlPolicy`den geçiyor, imzalı akış gitmiyor — watch URL'si.
- `media:seekPreview`: yetkili yol + süre sınırı + 90sn iptal + 8MB data-URL üst sınırı. Minor: ana süreç `seekPreviewCache` sınırsız base64 tutuyor (renderer tarafı 8'le sınırlı).
- `b112a03` hardening paketi: event-envelope kaynak koruması, font tmp temizliği hata maskeleme düzeltmesi, secret-store redact sertleştirmesi — hepsi doğru yönde, bozuk yok.

## Testler
- `node --check src/renderer/renderer.js` `src/opensubtitles-hash.js` — temiz
- `tests/report67-smarttube-wiring.test.js` **74/74** (yeni F1a davranış + F1b sıralama testleri)
- `tests/opensubtitles-hash.test.js` — 4/4 vektör (yeni 100000B overlap vektörü dahil)
- `tests/player-ui.test.js` 145/145 · `browser-subtitle-search` · `subscriptions-io` · `browser-parti6/7` · `design-system` — hepsi geçti
- Çalıştırılmadı: tam `npm test` (büyük paket; önceki turlarda tek ortam engeli node:sqlite fts5 — bu VM'de yok), Electron smoke

## Sınırlar / sonraki adımlar
- FULL_REVIEW'da açık kalanlar: F2 (sıradan açılan videoda pendingLibrarySeek beslenmiyor), F3 (sıra dışındaki kaydın manuel açılışta kuyrukta kalması — ürün kararı), F6 (youtube:cancel 3 sn ölü-süreç beklemesi), F4/F5 nit'ler. Kullanıcı kararı bekliyor.
- PR birleşince devir zinciri master üzerinden devam eder.
