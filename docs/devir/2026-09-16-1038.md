# Devir — 2026-09-16 10:38 — 10-ajan derin denetim turu (rapor 42)

## Repo durumu

- Repo: `D:\Whisper Local` → `https://github.com/dorukakindev/whisper-browser`, dal `master`.
- Başlangıç HEAD: `35be4e0` ("docs: record Great Courses CEA site evidence").
- **Çalışma ağacında kullanıcıya ait commit'siz değişiklikler vardı ve korunarak bırakıldı:** `src/main.js`, `src/renderer/renderer.js`, `tests/browser-cea-full-capture.test.js`, `tests/electron-browser-cea-full-ui.smoke.js` (CEA yakalama durumunun sekme snapshot'ında kalıcılaştırılması; `publishBrowserHlsCeaCaptureState`, `normalizeBrowserCeaCaptureState`, `tab.ceaCapture` + terminal-durumda `tabs-changed` yeniden yayını). Bu diff bu oturumun işi değildir; commit'lenmedi.
- `BROWSER_BUG_REPORT_41.md` ve `BROWSER_BUG_REPORT_107_196.md` ağaçta mevcut; seri sıradaki numara 42 idi.

## Yapılan iş

Kullanıcı talimatı: "10 agent gönder, daha önce bakmadığın yerlere bak, raporla; uygulama/düzeltme planı ile birlikte; ana kodu değiştirme."

10 paralel salt-okunur ajan çalıştırıldı: transcribe.py derinliği, backend yardımcı betikler, CEA çözücü içeriği, altyazı parser'ları/ağ yakalama, son düzeltme commit'leri (taze göz), Node yardımcı modülleri, ayarlar/oturum/kalıcılık, player çekirdeği, sekme yaşam döngüsü kenarları, sayfa-enjekte kod. Son ajan (enjekte kod + medya denetleyici) bu not yazılırken hâlâ çalışıyordu; dönüşünde bulguları rapora eklenecek.

Tüm kritik bulgular (P1-P3) üst ajan tarafından güncel kaynakta satır seviyesinde yeniden doğrulandı. Sonuç **`BROWSER_BUG_REPORT_42.md`** içinde: **B106–B210** (yeni numaralandırma; ajanların kullandığı B44-*/B76-* çakışan geçici numaralar düzeltildi).

### Öne çıkan doğrulanmış bulgular

- **B106/B107/B108 (P2)** — sahte `__whisperCaptureQueue` girdisi: aynı-origin'e **credentialed** kör GET (üst-frame origin'iyle karar veriliyor, girdi-karesi değil), `streamMediaId` zehirleme doğrulamadan önce, sınırsız `body` → Buffer bellek amplifikasyonu. B66'nın kalan çekirdek vektörleri.
- **B109 (P2)** — `.wbp` paketi `browser-session.json` yerleştirebiliyor → relaunch'ta `restoreBrowserSessionState` sessiz `subtitleFileAccess.grant` → r/w erişim onaysız (B84'ün kardeş yolu).
- **B110 (P2)** — `transcribe.py` `segment_metrics` paralel dizisi `merge_resumed_entries`/recovery/dedupe/fix mutasyonlarında kayıyor → tekrar-halüsinasyon delete/warn kararları yanlış metriklerle; JSON çıktısına yanlış satırlar yazılıyor.
- **B111 (P2)** — `pipeline_control.py` kısmen yazılan `.bak` sağlam final'in üstüne restore ediliyor (veri kaybı).
- **B112/B113/B114 (P2)** — TTML self-closing `<p/>` cue yutması; tembel-tag regex'lerinde O(n·m) main-process DoS; `$Time$` matcher'da `presentationTimeOffset` eksik.
- **B119-B123 (P1/P2)** — CEA canlı-yakalama doğruluğu: sıfır-olmayan PTS/tfdt kökeni tüm cue'ları kaydırıyor; CDP parçaları ağ sırasıyla çözülüyor (sequence okunmuyor); `EXT-X-BYTERANGE` parçaları son matcher'a çöküp tek aralık çözülüyor; `#EXT-X-SKIP` bilinmiyor → AES implicit IV kayıyor; sıfır-cue parça "completed" sayılıyor.
- **B115-B117 (P2)** — yaşam döngüsü: split-ikincil crash sonrası view görünmez; mini-player sekmesi crash'te yanlış pencerede beliriyor; `browser:hide` kuyruksuz → uçan hide yeni view'i pause'lar.
- **B118 (P3)** — `burnin:start` kilidi onay diyaloğundan sonra kuruluyor → iki ffmpeg aynı temp dosyaya.
- ~50 P3/P4 madde daha (izin prompt'u sekme-değişiminde kalıyor, `tab.loading`/`restore_failed` hiç yazılmıyor, crash bayrakları temizlenmiyor, env-scrub tutarsızlıkları, `.bak` eksik depolar, CEA kenar durumları).

### Test onarımı (tek dosya, ana kod değil)

`tests/electron-browser-extras.smoke.js:64` — B51'in güncel biçimi: `browserFeatures.open=true` yetmiyordu; panel `setSideTab('tools')` ile görünür oluyor. Tek satır eklendi → gerçek Electron'da **tam yeşil** (`.uiprev/browser-extras-smoke/report.json`: skips CRUD, UI kayıt/silme, semantic, OCR kare+okuma, ASS, sahneler, mini-player). Bu test değişikliği ayrı commit'e alınabilir veya kullanıcının diff'inden ayrı tutulabilir.

### Doğrulanan kapanışlar (bu turda yeniden kontrol edildi)

B38, B52 (ana senaryo), B53, B54, B57, B67, B73, B101, B43, B49, B50-büyük-kısmı, B30/B31-büyük-kısmı ve transcribe.py tarihsel serisi (B-6..M-9) — hepsi güncel kodda kapalı.

## Çalıştırılan testler

- `tests/electron-browser-extras.smoke.js` (onarım sonrası) — gerçek Electron, EXIT=0, report.json tam.
- `tests/electron-browser-trusted-bridge.smoke.js` — EXIT=0 (köprü/TextTrack/EME/isolated-world akışları).
- `npm test` bu turda koşulmadı (ürün kodu değişmedi; ajanlar salt-okunur).

## Sınırlar / notlar

- `node_modules`/`backend/venv` bu makinede eksik → mux.js iç mekanizması upstream dokümanla doğrulandı (B154, B146 "spekülatif" işaretli); `npm install` + `node tests/browser-cea-captions.test.js` sonrası kesinleşir.
- Ajan repro'ları statik; dinamik repro taslakları raporda madde madde yazılı.
- Kullanıcı CEA-kalıcılık diff'i incelendi: renderer `normalizeBrowserCeaCaptureState`'in `complete` alanını düşürmesi zararsız (`state==='complete'` kullanılıyor).
- Raporlar `.gitignore`'da; seri kuralı gereği `-f` ile eklenmesi gerekiyorsa o şekilde commit'lendi (29-41 tutarlılığı).

## Sonraki adım önerisi

Rapor 42'deki "uygulama/düzeltme planı" sırası: güvenlik paketi (B106-B109, B134, B144) → transcribe/pipeline/parser doğruluk (B110-B114) → CEA canlı-yakalama (B119-B123, B147-B157) → sekme görünürlük yarışları (B115-B132) → P4 hijyen.
