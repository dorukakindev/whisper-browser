# 2026-09-14 15:53 — Çeviri anlamı ve sağlayıcı yanıtı doğrulaması

## Kimlik ve kapsam
- Repo https://github.com/dorukakindev/whisper-browser; dal master.
- Başlangıç HEAD `7f3ba0fed9fe0fac06be2e403319130fc7ceceac`; temiz çalışma ağacı. Başlangıç ürün kodu `1aeeb8241050a35b47c2c05105a644f0dd7cc9c8`.
- Bitiş ürün kodu `c4e63af22bb11613a78b7dd91081fac80eae65d4`.
- Kullanıcı daha derin çeviri doğruluğu çalışması istedi. Sayı/olumsuzluk incelemesi, konuşmacı değişimi ve sağlayıcı sonlandırma durumu tarandı. Kusursuzluk veya gerçek model kalitesinde ölçülmüş artış iddiası yoktur.
- Yeni bağımlılık, Proxifier, alt ajan, kullanıcı anahtarı veya canlı ücretli sağlayıcı kullanılmadı.

## Düzeltmeler ve dosyalar
1. `src/main.js`: text dolu olsa bile finish_reason=length (veya status=incomplete) kısmi yanıtı reddeder. content_filter, tool_calls, function_call, refusal veya error da çeviri sayılmaz. Önceden token sınırında kesilmiş ama okunabilir metin ekrana/önbelleğe ulaşabiliyordu. Mevcut retry/failover hata yolu kullanılır; kısmi metin otomatik tamamlanmış gibi gösterilmez.
2. `src/subtitle-sentence-layout.js`: sayı denetimi artık yüzde ve negatif işareti korur; 1,234.56 ile 1.234,56 aynı sayı olarak okunur, çoklu binlik ayırıcılar parçalanmaz. 5% → 5 ve -5 → 5 reddedilir. Türkçe %5/yüzde 5/yüzde beş, eksi 5/eksi beş kabul edilir. Ondalık son sıfırları normalize edilir. 2-3 aralığı ve diyalog başındaki '- 5 minutes.' yanlışlıkla negatif sayılmaz. Nokta/virgül tek ayırıcı olduğunda üçlü grupların binlik kabulü mevcut sezgisel kuraldır; tüm yerel sayı yazımları/birim dönüşümleri desteklenmiş değildir.
3. `src/main.js` önceki/sonraki bağlam toplama döngüsü konuşmacı farklıysa duruyordu; soru-cevap sahnesinde sorunun kendisi kaybolabiliyordu. Artık üç komşu replik sınırı korunarak dönüşler de alınır. Tek-cue isteğinde konusmaci_baglari; çok-cue istek hazırlığında (`src/subtitle-sentence-layout.js`) speaker_relation yalnız same/different/unknown ilişkisini taşır. Ham CHAR_A/CHAR_B gibi konuşmacı etiketleri modele gönderilmez. Gruplama farklı konuşmacıları aynı cümlede birleştirmez; komşu replikler yalnız bağlamdır.
4. `src/subtitle-sentence-layout.js`: kıvrık apostroflu don’t gibi İngilizce olumsuzluklar normalize edilir; Türkçe harfleri dışlayan ASCII word boundary/word character kullanımı Unicode harflerle değiştirildi. Düşünmüyorum/Hiç gibi doğru örneklerin yanlış uyarısı giderildi. Negation_missing yalnız inceleme uyarısıdır; dilbilgisel sezgiye dayanarak sert engel uygulanmaz. Tüm olumsuzluklar için anlam ispatı değildir.
5. `src/browser-translation-scheduler.js`: cache hash materyaline responseValidationVersion=1 eklendi. Önceki denetimden geçmiş kesik yanıtların metadata'sı cache'de olmadığından eski kayıtlar bu yeni akışta eşleşmez. Önbellek veya arşiv dosyaları silinmez; yeniden çeviri/API kullanımı gerekebilir.

## Test dosyaları
- `tests/subtitle-sentence-layout.test.js`: yüzdelik/eksi/karmalı ayırıcı kaybı reddi ve geçerli Türkçe karşılıklar; diyalog çizgisi/interval yanlış pozitif koruması; kıvrık apostrof/Unicode olumsuzluk; aynı/farklı konuşmacı ilişkisinin tekli/çoklu sağlayıcı payloadında korunması ve etiket sızmaması. Gerçek main istek fonksiyonu kontrollü fetch ile length/filter/tool/function cevaplarını reddeder; gerçek scheduler bu cevapları sonuç ve cache'e yazmaz. Eski yüzde kaybetmiş cache yeniden istenir; bozuk canlı sayısal yanıt yayımlanmaz.
- `tests/browser-translation-source-revision.test.js`: gerçek main kaynak hazırlama/scheduler akışında farklı konuşmacılı sorunun yanıt bağlamında kalması. Önceki arşiv/source revision kontrolleri korundu.
- Bu not ve DEVIR-NOTU.md ayrı dokümantasyon commit'i; önceki notlar korunur.

## Doğrulama sonuçları
Geçen node tests/<ad>.test.js kontrolleri:
- subtitle-sentence-layout (son değişiklikten sonra tekrar geçti)
- subtitle-pair-quality (quality/subtitle-quality-golden.json içindeki iyi çeviriler yanlış reddedilmedi)
- browser-translation-source-revision
- browser-translation-refresh
- browser-workflow-core — 24 senaryo
- browser-page-translation-main
- browser-page-translate
- browser-series-translation
- browser-translation-job-context
- browser-ai-evaluation — 6 kontrollü sahne
- browser-translation-reliability

Sözdizimi: main/preload/renderer, subtitle-sentence-layout ve scheduler node --check; backend/transcribe.py py_compile; git diff --check geçti.

Electron sınırları:
- Birleşik çalıştırmada `npm run test:electron-bridge` UnknownVizError, ardından altyazı smoke testi main DevTools değerlendirmesinde Promise was collected verdi.
- Köprü testi ayrı Node spawnSync ile tekrar çalıştırıldığında gerçek Electron ipucu tuşu etkinleştirme sayacı 0/1 kaldı; bu turda düzeltilmedi veya başarılı sayılmadı. Odağa ilişkin olası neden kesinleştirilmedi, beklenti gevşetilmedi.
- `node tests/electron-browser-subtitle-smoke.js` tek başına değişikliksiz tekrar çalıştırıldığında geçti: sentetik VTT yakalama/yükleme, gerçek overlay metni/görünürlüğü ve tanı dışa aktarımında metin/test URL sırrının korunması.
- Hiçbir Electron test dosyası bu turda değiştirilmedi. UI DOM/stil tasarımı değişmedi; görsel tasarım denetimi yapılmadı.

Tam npm test tekrar çalıştırılmadı; 2026-09-14-1509 notundaki beş genel paket başarısızlığı çözülmüş sayılmaz. Gerçek film örnekleri üzerinde canlı sağlayıcı/insan çevirisi karşılaştırması yok. Sayısal denetim yüzey biçimine dayalıdır; bütün diller, yazıyla verilmiş tüm sayılar, tarih/birim dönüşümleri veya olumsuzluk grameri için doğruluk garantisi yoktur.

## Teslim
Ürün kodu yukarıdaki hash ile commit edildi. Not/indeks ayrı commit ve normal origin/master push ardından uzak tam hash git ls-remote ile son yanıtta doğrulanır. Yeni kurulum/bağımlılık/kalıcı ayar şeması yok; sadece cache anahtarı materyali genişledi. Yerel geçici test profilleri/kişisel veriler Git'e eklenmedi. Sonraki doğrulama konusu ayrı Electron köprü testinin klavye/odak kararlılığı ve gerçek sağlayıcıyla izinli kalite karşılaştırmasıdır.
