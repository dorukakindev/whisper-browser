# 2026-09-14 15:34 — Browser çeviri kuyruğu hata düzeltmeleri

## Kimlik
- Repo https://github.com/dorukakindev/whisper-browser; dal master.
- Başlangıç HEAD `05690ba18e018fbc3c636da17cfa4e36b5fe6aad`; çalışma ağacı temizdi. Başlangıç ürün kodu `1da1739e821f46e541aa9a3125f947dbb754fd6a`.
- Bitiş ürün kodu `6222e8d13fbbfac79a0d0558d2c326fab0fbb14c`.
- Kullanıcı browser/çeviri bug taramasına devam edilmesini istedi. Alt ajan kullanılmadı; Proxifier açılmadı; bağımlılık kurulmadı.

## Kök nedenler ve dosyalar
1. `src/browser-translation-scheduler.js`: readCache reddedilince normal sağlayıcı hata/retry yoluna giriyordu; çalışan sağlayıcıya hiç çağrı gitmiyordu. Senkron/async önbellek okuma hatası artık cache miss kabul edilir. Sağlayıcı kendi hata bütçesini korur; yazma arızası için mevcut tolerans değişmedi.
2. Aynı dosyada anahtar iş başlangıcındaki this.context ile hesaplanırken translateShared daha sonra güncel this.context kullanıyordu. Önbellek beklerken öğrenilen terimler değişirse istek/anahtar farklı bağlam taşıyordu. İş başlangıcında bağlam kopyası alınır; anahtar, kalite kontrolü ve sağlayıcı bu kopyayı kullanır. terminologyText de hash materyaline eklendi; önceki şemayla hesaplanan anahtarlar eşleşmez, yeniden çeviri gerekebilir. Önbellek dosyaları silinmez.
3. translateShared işi Promise microtask olarak başlatıyordu. Tüketici bu arada iptal edilirse provider yine çağrılıyordu. Microtask içinde ortak AbortController kontrolü eklenerek henüz gönderilmemiş istek engellendi. Başlamış ve birden çok tüketicili isteklerin mevcut paylaşma/iptal sözleşmesi korunur.
- `src/main.js`: altyazı ve sayfa çeviri scheduler bağlamına terminologyText anlık kopyası eklenir; öğrenme sonucunda sürüm ve metin birlikte güncellenir. Sağlayıcı config'ine işteki kopya aktarılır. Boş string bilinçli boş bağlamdır; nullish fallback sayesinde daha sonra öğrenilen terimlere dönüşmez. Scheduler dışındaki eski çağrılarda canlı terminoloji fallback'i korunur.
- `tests/browser-translation-job-context.test.js`: senkron ve asenkron cache arızası, bekleyen cache sırasında terim değişimi, anahtar/metin eşitliği ve mikro görev öncesi iptal için yeni regresyonlar.
- `tests/browser-series-translation.test.js`: gerçek main sağlayıcı istek fonksiyonunun kontrollü fetch gövdesinde sabit terim, boş terim ve eski çağrı fallback'i doğrulandı.
- `tests/browser-translation-refresh.test.js`: mevcut kaynak sözleşmesi regex'i sürüm yanında terminoloji metninin de aktarılmasını zorunlu kılacak şekilde güncellendi; davranış kontrolleri korundu.
- `tests/browser-workflow-core.test.js`: başlamış paylaşılan isteğin iptal senaryosu provider'ın gerçekten başlamasını bir microtask bekleyip çağrı sayısını kontrol ederek garanti eder; hâlâ iki çağrı bekler. Başlamadan iptal yeni testte ayrı sınanır. Beklenti gevşetilmedi.
- Bu not ve DEVIR-NOTU.md ayrı dokümantasyon commit'idir.

## Kanıtlar
Başlangıç HEAD scheduler modülü git show ile belleğe yüklenerek üç hata da ayrı kontrollü senaryoda yeniden üretildi: cache hatasında sıfır sağlayıcı çağrısı ve terminal hata; eski anahtarla yeni terim bağlamı; iptalden sonra bir provider çağrısı. Mevcut kodda yeni regresyonlar geçti.

Geçen testler (node tests/<ad>.test.js):
- browser-translation-job-context
- browser-translation-reliability (uzak seek, geç yanıt, hata/manuel retry)
- browser-translation-refresh
- browser-translation-early-timer
- browser-translation-offline
- browser-translation-cache
- browser-workflow-core: 24 senaryo
- browser-series-translation
- browser-page-translation-main
- subtitle-sentence-layout
- browser-ai-context
- browser-ai-evaluation: 6 kontrollü sahne
- `npm run test:electron-bridge` geçti.
- Değişen ürün JS'leri/yeni test, preload ve renderer sözdizimi; backend/transcribe.py py_compile; git diff --check geçti.

Başarısız/sınırlı:
- `node tests/electron-browser-subtitle-smoke.js`: DevTools WebSocket yanıtında `Error: Promise was collected` (test dosyası satır 54); uçtan uca altyazı başarısı bu koşudan çıkarılamaz. Test değiştirilmedi. Bu hatanın kök nedeni bu turda ayrıştırılmadı.
- browser-workflow-core ilk koşusunda iptal edilmiş henüz başlamamış provider için eski iki çağrı beklentisi başarısızdı; senaryo yukarıdaki gibi başlamış iptali açıkça sınayacak şekilde düzeltildi ve 24 kontrol geçti.
- browser-translation-refresh ilk koşusunda eski tek alanlı setContext kaynak regex'i başarısızdı; yeni iki alanlı sözleşmeye güncellendi ve tüm davranış kontrolleri geçti.
- Gerçek API anahtarı/ücretli canlı sağlayıcı kullanılmadı. electron-page-translation-live testi kullanıcı profili/anahtar kasası kopyaladığından çalıştırılmadı; kontrollü sağlayıcı doğrulaması yeterli tutuldu.
- Tam npm test tekrar çalıştırılmadı; 2026-09-14-1509 notundaki beş başarısız dosya çözülmüş sayılmıyor.
- UI DOM/stil değişikliği yok; görsel tasarım denetimi yapılmadı. Bu çalışma gerçek model çeviri kalitesinin ölçümü değildir.

## Teslim ve sonraki iş
Ürün kodu yukarıdaki hash ile commit edildi; bu not/indeks ayrı commit edilip normal origin/master push yapılır. Son uzak hash git ls-remote ile son yanıtta doğrulanır. Şema dosyası/ayar/bağımlılık değişmedi; cache anahtarı materyali genişledi. Sonraki somut inceleme adayı DevTools tabanlı altyazı smoke testinin gezinme/bağlam yaşam döngüsüdür.
