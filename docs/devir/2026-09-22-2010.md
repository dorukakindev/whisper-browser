# Devir notu — 2026-09-22 20:10 (BROWSER_BUG_REPORT_109 gauntlet)

**Dal:** `codex/gauntlet-109` · **Baz:** `d278296` (master, PR #31 dahil) · **Başlangıç HEAD:** `d278296` · **Bitiş kod commiti:** `a142aa1`

## Yapılan iş
A–N ~150 maddelik doğrulama gauntlet'i: gerçek Electron + ayrı profil (`qa-109/profile`) + kontrollü fixture sunucu (`127.0.0.1:8788`, hata/gecikme modları) + CDP sürüşü. Mock başarısı model kalitesi sayılmadı; sağlayıcı kanıtları fixture + sınırlı canlı `claude-haiku-4-5` koşusuyla.

## Commit'ler
- `6e902c0` — C/D kapı düzeltmeleri (sınır-sayı yarım, ad-öbeği yerelleştirme, sarkık-kuyruk, SDH geri-yükleme, cue-içi `\n` korunması); backend/sentence_translation.py, subtitle_sdh.py, transcribe.py + testler.
- `c05eaaf` — BUG-109-08: `done` görmeden gelen `exit` oynatıcı işini terminal duruma indiriyor; ölü süreçte kart temizliği. renderer.js + 2 regresyon testi.
- `a142aa1` — BUG-109-09: `renderStPlaylistRail` `absThumb`'a thumbnail dizisi veriyordu → TypeError, tüm arama gridini sessizce boşaltıyordu. Ray artık entry `.url` seçiyor; çağrı try/catch'li (ray hatası video sonuçlarını götürmez). Regresyon testi davranışsal (dizi-shaped thumbnail).

## Kanıtlar
- `docs/raporlar/BROWSER_BUG_REPORT_109.md` — madde madde kapsam tablosu + kanıt dosyaları.
- `qa-109/evidence/` — j-search-fixed (düzeltme sonrası 20 kart+12 ray), j-playing (kamu video akışı), g-both (çift altyazı), k-narrow/k-zoom125, h-browser*.
- `qa-109/logs/` — corpus koşuları, f-evidence, soak CSV, electron logları.
- Testler: `tests/player-ui.test.js` 148/148 (kırmızı→yeşil her iki bug için kanıtlı).

## Sınırlar (raporda ayrıntılı)
- J5/J7/J8 (YouTube TV + OAuth): gerçek Google hesabı gerekir — doğrulanamadı.
- H3 iframe cross-frame, H5 HLS discontinuity: kod düzeyinde, canlı kanıt eksik.
- M1 10k-cue, L7 çift-iş aynı-yol: koşulmadı.
- Windows'ta ayrıca: safeStorage kalıcılığı, start.bat cuDNN akışı, subtitles= yol kaçışı.

## Yarım iş
- M6 soak arka planda sürüyordu (heap sabit ~10MB eğilimi); kesin süre-dolu kanıtı rapora "eğilim" olarak işlendi.
- Açık: PR'a merge sende; ayrı PR bölme gereksiz (tek bağımsız alanlık düzeltme kaldı — J2 ve F3/F5 aynı dosyada ama bağımsız hunk'lar).
