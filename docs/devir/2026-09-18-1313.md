# Devir notu — 2026-09-18 13:13

## Konu

Dalga 3 — 10 ajanlık tamamlayıcı kapsam denetimi: 8 ajan tamamlandı, bulgular
`BROWSER_BUG_REPORT_66.md` dosyasında raporlandı. Kod değişikliği yok.

## Repo / dal / commit kimlikleri

- Repo: `https://github.com/dorukakindev/whisper-browser` — dal `master`
- Başlangıç commit: `cb3b156` (önceki devir notu 2026-09-18-1300)
- Ürün kodu bitiş commit: `cb3b156` — değişmedi (salt-okunur audit)
- Dokümantasyon commit'i: bu notun bulunduğu commit

## Yapılanlar

- 10 salt-okunur ajan gönderildi (dalga 3, rapor 64'ün 20 ajanından kalan
  kapsamlar): 8 tamamlandı, 2'si ücretsiz-model hız limitine takıldı —
  `69f9a679` (öğrenme/notlar/AI) ve `de02f0fa` (altyapı+altyazı misc)
  kapsamları **taranmadı**, raporda açıkça işaretli.
- Tüm P1/P2 bulguları güncel ağaçta satır-satır yeniden doğrulandı
  (ajanların satır numaraları paralel workstream nedeniyle ~100 kaymıştı;
  rapor güncel numaraları kullanır).
- `BROWSER_BUG_REPORT_66.md` yazıldı. Numaralandırma notu: "rapor 65"
  etiketi paralel workstream'in Invidious tesliminde kullanıldığı için
  bu dosya 66.
- `DEVIR-NOTU.md` indeksi güncellendi.

## Bulgu özeti (detay: BROWSER_BUG_REPORT_66.md)

**P1 (3):**
- R66-01 — `before-input-event`'te `input.type` hiç `'keyDown'` olamaz
  (Electron yalnız `rawKeyDown`/`keyUp` yayar) → sayfa odaklıyken tüm
  tarayıcı kısayolları ölü.
- R66-02 — `jobLog` WriteStream'de dinleyicisiz `'error'` → uncaughtException,
  ana süreç iş ortasında çöker.
- R66-03 — watch-folder: `-whisper-qNNNN` sonekli çıktı dedektör beyaz
  listesine uymaz → sonsuz yeniden-transkripsiyon döngüsü + dosya saçılımı.

**P2 (12):** `writeTextAtomic` tanımsız (bilinen R46-09, açık); `.bak`
rotasyonu bozuk primary'yi sağlam yedeğe kopyalıyor; `settings.json`
yedeksiz + sessiz sıfırlama; `burnin:start` check-then-act yarışı;
hassas-anahtar sözlüğü `wmsAuthSign`/`auth_key`/`oauth_signature`/`x-goog-*`
kaçırıyor; overlay `observeRoot` LRU eviction ölü kod (128 tavan);
`scanMediaNode` eklenen düğümün `shadowRoot`'unu taramıyor; bekleyen
watch öğesi silinince dosya kalıcı atlanır; paylaşılan outputDir'de
aynı-kök-ad maskelemesi; watch-key canonicalizasyonu `mediaKey` ile
uyuşmuyor (hash'li SPA'larda resume ölü + secret'lı URL'ler diske);
silinen kütüphane öğesi izleyerek diriltilemiyor; HLS kurtarma
duraklatılmış videoyu oynatıyor.

**P3 (21):** terminateProcessTree exit-without-close körleşmesi; `-`
ile başlayan argparse değerleri; handleLine terminal-olay yutması;
backend `exit` taklidi; redactJobLogArgs eksik bayraklar; `flush:true`
ölü seçenek; atomik-olmayan yazımlar; settings:export `.json` şartı;
saveSubtitleCopy cap eksik; downloads.json yedeksiz; `userScrolled`/
öğrenme durumu medya değişiminde taşınıyor; karışık drop yanlış videoya
altyazı; `writingMode` beyaz listede düşüyor (dikey CJK ölü); overlay
tık-yutma; `pickFolderBtn` ölü ID; `stopAfterCurrent` desync;
`.jobs-tab`/`.drawer-page-tab` klavye erişilemez; EN locale eksik JS
dizgileri; "Kuyruğa" sonrası bayat özet; birleşik arama 1000-cap ötesini
görmüyor.

## Doğrulama / sınırlar

- Salt-okunur audit: hiçbir ürün kodu değiştirilmedi, test koşulmadı.
- R66-01 kanıtı Electron kaynak analizi (deterministik emit filtresi);
  gerçek Electron koşusu ek kanıt olur.
- Diğer P1/P2'ler güncel ağaçta elle doğrulandı; çalışma-zamanı repro
  koşulmadı.
- Paralel workstream'in commit'lenmemiş ~22 dosyası ağaçta duruyor;
  bu teslim ona dokunmadı, yalnız `BROWSER_BUG_REPORT_66.md` + bu not +
  indeks satırı commit'lendi.
- Taranmayan 2 kapsam sonraki turda ayrıca gönderilmeli.
