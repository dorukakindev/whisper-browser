# Devir Notu — 2026-09-15 15:48

## Oturum özeti

Derin browser denetiminin devamı (denetim fazı, ürün kodu değiştirilmedi). Bu tur kullanıcı talebiyle **5 ek paralel ajan** + kendi doğrulamalarımla sürdü. Kapsam: backend `browser_*.py` betikleri, çeviri/yakalama yaşam döngüsü, test-kapsamı/persistans yüzeyi, **enjekte-sayfa kodu (düşmanca sayfa tehdit modeli)** ve renderer bakiyesi. Rapor serisi `BROWSER_BUG_REPORT_30.md` ile devam etti (yerel, gitignore'da); yeni bulgular B51–B70 aralığına işlendi.

- Repo: `D:\Whisper Local` · Dal: `master`
- Başlangıç HEAD: `612e10fa` (önceki devir push'u) → oturum sırasında kullanıcının kendi demo notları `cb8a335` + `33e15e1` eklendi; bitiş ürün kodu aynı (kod değişmedi).
- Bu notun commit'i ayrı dokümantasyon commit'i.

## Bu turun yeni bulguları (rapor 30'a eklendi)

### Deneysel doğrulananlar

- **B52 (P2)** Python↔JS kodlama uyumsuzluğu: `browser_video_analysis`/`browser_align`/`catalog_scan` `ensure_ascii=False` JSON'u ANSI kod sayfasına basıyor; JS `toString('utf8')` okuyor. Kanıt: mevcut test çıktısında `eletirme` bozulması + `Şiddetli Diziler` taramasında 0xDE baytı → katalog **bozuk yol** saklıyor; CJK'da encode/decode hatasıyla toptan çöküş.
- **B53 (P3)** `catalog_scan.py` handlersız: malformed input → `KeyError`/`JSONDecodeError` traceback'i stderr'e düşüyor, JS sarmalayıcı ham metni gösteriyor (gerçek koşuyla doğrulandı). Junction döngüsü savunması da yok.
- **B51 (P2, test altyapısı)** `electron-browser-extras.smoke.js` sekme refaktöründen beri kırık (`init` `false` → extras otomasyonu ölü); düzeltilmiş kopya `.uiprev/smoke-fixed` altında tam geçti.

### Yaşam döngüsü (satır-doğrulandı)

- **B57 (P2)** `browser:subtitle:captureFull` `retry-wait` durumunda iptal → `job.state` hiç güncellenmiyor, timer siliniyor ama iş listede `retry-wait` kalıyor → yeni başlatma reddediliyor; özellik o sayfada kalıcı kilitli.
- **B58 (P3)** `browser:translation:start` `sourceComplete`'i düşürüyor → kısmi yakalamalar "tamamlandı" etiketiyle özetleniyor.
- **B59 (P3)** Sekme-kapatma `activeWork` hesabı CEA tam-yakalama işini saymıyor → yakalama sürerken sekme onaysız kapanıyor.
- **B61 (P2 spec)** İlk stream-kimlik evlat edinmesi (`adoptBrowserStreamMediaIdentity`) süren çeviriyi `mediaId` değişimiyle sessizce askıya alabiliyor.
- **B54–B60, B62–B63** katalog hizalama O(n×m), Python deputy boşlukları, paket `file_size` beyanı, unload'da yaşayan scheduler, extras `cancel`'in bayatlık sonrası erişilemezliği (`valid()` iptal yolundan önce), skip deposunun sabit `.tmp` adı + `0o600` eksikliği.

### Düşmanca sayfa tehdit modeli (enjekte-sayfa ajanı, satır-doğrulandı)

- **B65 (P3)** `browser-page-translate.js`'te sıfır `isTrusted` (grep 0 eşleşme) → sentetik pointer/click `page-action` IPC'sine ulaşıyor: `retry` kota yakıyor, `edit-save` keyfi metni **kalıcı çeviri önbelleğine + arşive** yazıyor. Manga/overlay tüm dinleyicilerde `isTrusted` kapılı — tutarsızlık.
- **B66 (P3)** Sahte `__whisperCaptureQueue` girdileri → `session.fetch(credentials:'include')` ile kimlikli kör GET'ler (~24 amplifikasyon) + `streamMediaId` zehirleme. Özel-IP/loopback SSRF DNS kontrolüyle engelli — kör ve sadece public.
- **B67 (P3)** `browser:open-link`'te `isTrusted` (preload:35-48) ve `mainFrame` (main.js:11308) denetimi yok → sentetik auxclick/iframe ile hareketsiz sekme açma; yorum "subframe açamaz" dese de kod denetlemiyor. Aynı boşluk `browser:page-mutated`'de (11320).
- **B68 (P3)** `whisperPageTranslate:v2` sayfa-yazılabilir sessionStorage'da → güvenilir çeviri enjeksiyonu arşive/belleğe kalıcı sızıyor (edit yolundaki `pre===current` kontrolünü baypas).
- **B69 (P4)** `pageContextScript` sayfa indekslemede MAIN dünyada çalışıyor (`browser-page-index.js:18` `mainFrame.executeJavaScript`) — diğer çağrılar izole dünya 999; sayfa global/prototip zehirleyebiliyor.
- **B70 (P4)** Sayfa-eklenebilir global kaçırma (`__whisperMediaController`, `__whisperLinkHints`, `__whisperCapture*`) + shadow-root başına sınırsız MutationObserver ×3 özellik.

### Test-kapsamı boşlukları (B64)

- Üç önizleme token sisteminin TTL/sender/tek-kullanım sözleşmesi hiç test edilmiyor; mini-player/extras/katalog eylem switch'leri adversarial vm-testi kapsamı dışında; renderer localStorage şekil-doğrulamasız okuyor; `browser-note-draft:*` GC'siz; `.wbp` renderer-değer içeriği şemasız.

## Temiz doğrulananlar (ajan)

- Tüm `browser:*` handler'ları `authorizedBrowserSender` arkasında; `trusted-bridge` mainFrame+aktif-sekme+eylem allowlist+jeton; manga/overlay `isTrusted`; `fetchBrowserBuffer` DNS+özel-IP reddi her yönlendirmede; PDF protokolü Origin/Range kapılı; reader kapalı shadow DOM + `safeUrl`; mini-player strict CSP + komut allowlist; paket restore `hasJobs()` dahil; depolarda atomik yazım + `.bak` (katalog hariç — B30).

## Testler

- Yeşil: `browser-video-analysis.test.js` (gerçek RapidOCR!), `browser-feature-services`, `browser-skip-segments`, workflow-recorder (7/7), nine-features, workflow-core (24); Electron smoke: `electron-browser-analysis`, `analysis-ui`, `trusted-bridge`, `browser-mini`, `browser-ass`, `media-catalog`.
- Kırmızı (test kusuru, ürün değil): `electron-browser-extras.smoke.js` — B51.
- Tam `npm test` bu turda çalıştırılmadı (ürün kodu değişmedi).

## Yarım işler / sonraki adımlar

- Renderer-bakiye ajanı (17k satır kapsamı) hâlâ çalışıyor — dönünce bulgular rapor 31'e doğrulanıp işlenecek.
- 6 ajan model hız sınırına takıldı; `browser-feature-services` eşzamanlılık derinliği açık.
- Önerilen düzeltme sırası: B18 → B17 → B35 → B52 → B57 → B27/B28 → B19 → B30 → güvenlik paketi (B65–B68: isTrusted + mainFrame + bellek taşıma).
