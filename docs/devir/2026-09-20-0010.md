# Devir Notu — 2026-09-20 00:10

## Kapsam

BROWSER_BUG_REPORT_85 + resimdeki raporlar + önceki doğrulanmış bulguların **uygulama turu**: 7 kümeli düzeltme paketi (sır sızıntısı, arama doğruluğu, izinler, yaşam döngüsü, gizlilik, taşınabilirlik, l10n) + 14 başarısız test dosyasının onarımı. Repo: `https://github.com/dorukakindev/whisper-browser`, dal `master`.

- Başlangıç kod commit'i: `3038a4ced824ba2e0089a93c650d7d7447883b81` (paralel oturumların docs commit'leri dahil; ürün kodu değişiklikleri commit'siz çalışma ağacındaydı)
- Bitiş kod commit'i: bu commit (aşağıda doğrulanan hash)
- Dokümantasyon commit'i: ayrı (devir notu + indeks)

## Uygulanan düzeltmeler (özet)

### Sır/sızıntı kümesi
- `src/browser-sensitive-keys.js`: snake/kebab/camel varyantları (auth_token, client_secret, api_key, access_token, session_id, id_token, refresh_token, password, passwd, apikey) ortak sözlüğe eklendi.
- `src/browser-place-url.js`, `src/browser-translation-archive.js`, `src/browser-adapters.js`: ortak sözlük + userinfo sıyırma.
- `src/settings-security.js`: `endpointSetting` artık sorgu-sırlarını ayıklıyor ve **temizlenmiş değer kaydediliyor** (B80-01 — `?api_key=` yedeğe düz metin düşüyordu; fonksiyonel parametreler korunur).
- `backend/invidious.py` + `src/main.js`: Invidious login şifresi `--password` argv'den `WHISPER_INVIDIOUS_PASSWORD` env'e taşındı (B82-01).
- `src/main.js openBrowserLinkInNewTab`: adres çubuğuyla aynı `decideUrlPolicy` (userinfo/kontrol karakteri reddi, B83-03).

### Arama kümesi
- `browser-transcript-search.js`: Türkçe İ/I/ı/i + İngilizce I + CJK koruyan fold; `özetle` gibi tam-transkript niyeti; kanıt seçimi skor-öncelikli + gösterim kronolojik.
- `browser-subtitle-search.js`: Unicode fold, release alanı normalize, OpenSubtitles `fileId`/`fileName` eşleşmesi, boş Location tekrar-çözümü kaldırıldı.

### İzin kümesi
- `browser-site-permissions.js` + `main.js`: `mediaKeySystem` işleme, `media` → `mediaTypes` eşlemesi, default-deny, aktif-sekme kapısı, yaşam döngüsü temizliği.

### Doğruluk/yaşam döngüsü kümesi
- `main.js`: `did-start-navigation`'da manga/çeviri/altyazı temizliği erkene alındı (kaynak-yapı test sözleşmesi korunur); crash yolunda `loadRetryTimer`/`crashRecoveryTimer` temizlenir; overlay `off` modu gezinmede korunur; `restoring` takılması giderildi; uzun kapalı-sekme URL'leri origin/path fallback ile saklanır; aktif sekme oturum sınırında korunur; sayfa-çeviri kuşak/oturum kontrolleri.
- `browser-cea-checkpoint.js`: anlamsız `captionMode: undefined` alanı artık eklenmiyor.
- `browser-cue-timeline-calibration.js`: kaydırma sonrası tamamen sıfır-öncesi kalan cue'lar düşer.
- `browser-textutil.js`: Latin-1/Extended-A named entity çözümü (İzlandaca þ ð ý dahil).
- `browser-lifecycle-policy.js`: `net--105` → `net-err-105` doğru hata formatı.
- `subtitle-sentence-layout.js`: DP geri-izleme + önceden hesaplanmış cezalar (~75ms → ~22ms, P79-03).
- `backend/subtitle_sdh.py`, `transcribe.py`, `translation_memory.py`: `_SDH` kümesi tek-kez normalize (P79-01), `SequenceMatcher.quick_ratio` erken-eleme (P79-02) vb.

### Gizlilik/kalıcılık kümesi (hibrit `.bak` modeli)
- **Tasarım kararı:** `.bak` = önceki geçerli ana dosya (bozuk-main koruması korunur — `audit-followup`, `note-store`, `history`, `places` testlerinin sözleşmesi). **Silme/temizleme yazımlarında** `mirrorBackup` ile `.bak` güncel duruma çekilir (R83-34): `history:remove`/`history:clear`, `browser:places:remove`/`clearHistory` (debounce bayrağı `browserPlacesMirrorBackup`), `browser:session:reset`, `BrowserNoteStore.remove`.
- `browser-session-privacy.js`: tek-origin temizliğinden `clearAuthCache` **çıkarıldı** (origin-bazlı değil — diğer sitelerin kimliğini siliyordu); yalnız tam `resetBrowserSessionData`'da kalır.
- `browser-tab-resources.js` + `browser-preload.js`: bilinmeyen metrik `null` kalır; geçersiz sayı (Infinity/NaN) güvenli tabana sıkışır; preload gerçek sayaçlar (timer/observer/listener/overlay/frame) ölçer.
- `workspace-package.js` + `workspace-video-package.js`: mutlak yollar `{{ROOT}}/...` taşınabilir forma çevrildi; video manifest `source` alanları da taşınabilir (B83-33); içe aktarımda `{{ROOT}}` → hedef kök + `videoMappings` eşleşmesi.

### L10n/UI
- `renderer.js`: `toLocaleString` çağrıları `globalThis.UiLocale` üzerinden locale'e bağlandı (İngilizce arayüzde sabit tr-TR kalmaz); VM-extraction testlerinin görebileceği self-contained biçimde.
- `styles.css`: `[hidden]` override — gizli browser önizleme panelleri grid'e rağmen gizli kalır.

## Test/inceleme onarımları
- `tests/browser-report85-regressions.test.js` (yeni, 12 senaryo): sensitive-key varyantları, endpoint sorgu-sırrı, 9000+ char URL, aktif-sekme koruma, IPv6 zoom, zh-hans-cn terminoloji, negatif cue, GİDİYORUM normalize, null metrik, sentence-layout, {{ROOT}} paket, Latin-1 entity.
- Extraction context'leri: `decideUrlPolicy` (controls-behavior, navigation-abort), `fs` + 4-arg `writeBrowserSessionAtomic` stub'ı (session-privacy).
- Sözleşme güncellemeleri: lifecycle `net-err-105`, foundation `.bak` önceki-nesil beklentisi korundu (hibrit modelle uyumlu).

## Doğrulama
- `npm test` — **TAM GEÇTİ** ("Tüm testler geçti"; backend 192/192 dahil).
- `node --check src/main.js src/preload.js src/renderer/renderer.js` — temiz.
- `node tests/run-electron-smokes.js --all` — 18/20; **`browser-ass` ve `catalog-roadmap` `UnknownVizError`** ile düşüyor: `capturePage()` compositor readback çökmesi, `--disable-gpu` ortamında bilinen nondeterministik sorun (2026-09-19-1310 notunda temiz ağaçta aynı imzayla belgelendi; `browser-video-e2e` ve `browser-analysis` tek tek koşunca geçti).
- Cümle düzeni benchmark: ~75ms → ~22ms (yerel ölçüm).

## Kalan işler / sınırlar
- `browser-ass`/`catalog-roadmap` smoke'ları ortam Viz çökmesiyle başarısız — kod kaynaklı değil; GPU'lu ortamda veya Viz bayraklarıyla yeniden denenmeli.
- D81-01 (262 Türkçe dizge tabloda yok) bu turda kapsanmadı — ayrı çeviri işi.
- `.freebuff/`, `_repro/` untracked araç klasörleri commit'e girmedi.
- Paralel oturumların untracked devir notları (1040/1100/1310/1330/1345/1405/1440) commit'e girmedi.
