# Devir notu — 2026-09-22 13:27

Repo: `dorukakindev/whisper-browser` · Dal: `master`
Başlangıç HEAD: `583b15a` · Bitiş kod commit: `b260fdb` (bu notun commit'i ayrıdır)

## Kullanıcı isteği

"oynatıcı kısmının ana ekranında youtube videoları falan hiçbir şey görünmüyor çok uzun sürüyor ayrıca bir şeylerin yüklenmesi ayrıca hala youtube tv auth ile giriş olayı yok bunu düzelt" + "direk normal youtube da olabilir, indivious şart değil". Üç şikâyet: boş ana ekran, yavaş yükleme, YouTube TV auth eksikliği.

## Bulunan kök nedenler (BROWSER_BUG_REPORT_106)

1. **F-106-1a — Kart boyutu üç katmanlı Chromium tuzağı:** `.st-card-thumb` `aspect-ratio:16/9` önce flex item'da (`.st-card` flex-column) cross-size belirsizken 0'a düşüyordu. `display:grid` iç thumb'ı kurtardı ama kart bu kez DIŞ grid'in implicit satırında 57px letterbox'a sıkıştı — asıl derin neden `.st-card`'daki `overflow:hidden`'ın scroll-kabı etkisiyle auto-track katkısını ~min-content'e (25px) düşürmesiydi (E2E ajanı CDP bisect: `overflow:clip`→252px). Üçüncü olarak aspect-ratio grid item'da da auto-track katkısı üretmedi (satır "0px 80px") → thumb için `padding-top:56.25%` kalıcı çözüm. `content-visibility` suçsuz (kaldırılınca da 25px).
2. **F-106-1b — Invidious `/vi/` proxy bozuk:** `invidious.f5.si` `/api/v1/popular` veriyor ama `/vi/<id>/mqdefault.jpg` **200 + text/html** dönüyor; img'ye HTML basılıyordu. Diğer instance'lar 401/403. Stats endpoint sağlık kontrolünü yanıltıyor.
3. **F-106-2 — Seri failover yavaşlığı:** Çalışan tek instance (f5.si) listede sondan bir önceydi → 4 instance'ın 401/403 timeout zinciri.
4. **F-106-3 — TV auth yoktu:** `openYoutubeLogin` client yoksa `ytClientView` (Google Cloud OAuth formu) açıyordu — sıfır-kurulum giriş imkânsızdı.

## Değişiklikler (kod commit `b260fdb`)

- `src/renderer/styles.css`: `.st-card` → `display:grid` + **`overflow:clip`** (köşe kesimi korunur, scroll-kabı etkisi yok); `.st-card-thumb` → `height:0; padding-top:56.25%` + img `position:absolute; inset:0` (her sütun genişliğinde kesin 16:9). Neden açıklamaları Türkçe yorum.
- `backend/invidious.py`: `_ytimg_thumbs(vid)` — tüm kart thumbnail'ları `i.ytimg.com/vi/<id>/{mq,hq,maxres}default.jpg` (instance bağımsız, canonical ID'den deterministik). `DEFAULT_INSTANCES`: f5.si başa.
- `backend/youtube.py`: gömülü TVHTML5 client (`TV_CLIENT_ID/SECRET/SCOPE`, yt-dlp/SmartTube ile aynı); `device_code(cid, scope)`; `poll`/`refresh` `client_id == 'tv'` iken gömülü secret'a düşer; `main()` `_resolve_client('tv')` sentinel'i.
- `src/main.js`: `youtubeSession.authMode` (`'tv'|'custom'`) + persist/restore `auth_mode`; `ensureYoutubeAccessToken` tv modunda client creds gerektirmez (`--client-id tv`); `youtube:deviceCode` client yoksa tv; `youtube:poll` başarıda authMode set; `youtube:setClient`/`authCode` → custom.
- `src/renderer/renderer.js`: history + continue-watching kartları i.ytimg.com'a çevrildi; `openYoutubeLogin` `!hasClient` → `startYoutubeDeviceFlow()` (QR + google.com/device + kod); akış hatası cihaz görünümünde `ytPollStatus` hata ile kalır.
- `tests/report70-youtube-oauth.test.js`: session whitelist korundu (deviceCapable/authMode eklenmedi — response'a dahil etmedim); R70-13'e scope-URI istisnası; R70-14 refresh kapısı tvMode'a güncellendi.

## Doğrulama

- `mocha report70 + report65 + report104` → 25/25; `node --check` ×3, `py_compile` ×2 → temiz.
- Canlı uygulama: HOME + TRENDING thumbnail'ları render (CDP `naturalWidth=320`, ekran görüntüsü alındı). Feed `invidious.py home` ≈ 3.6 s.
- Login modalı: sıfır-config açıldı, gerçek device_code üretildi (QR + `www.google.com/device` + `DKH-YHH-FSHS`), poll "Waiting for approval" döndü, Cancel temiz kapattı.

## Sınırlar / sonraki

- Gerçek Google onayı yapılmadı (kullanıcının google.com/device'a kodu girmesi gerekir; token takası sonra kanıtlanır).
- Feed hâlâ Invidious + yt-dlp zincirinde; f5.si de ölebilir. "Direkt YouTube" görüşüne izin verilmişti — şu an Invidious çalışıyor, değiştirilmedi.
- Lead Brain MCP (önceki iş) kullanıcı isteğiyle devre dışı bırakıldı — bu çalışmada kullanılmadı.
