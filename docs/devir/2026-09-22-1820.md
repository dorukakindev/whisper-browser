# Devir notu — 2026-09-22 18:20

## Repo / dal
- Repo: https://github.com/dorukakindev/whisper-browser · Dal: `codex/tr-sentence-groups-1790094000`
- Başlangıç kod commit'i: `e612a16` (PR #31 ilk hâli) · Bitiş kod commit'i: `5728d1a`
- PR: https://github.com/dorukakindev/whisper-browser/pull/31 (merge edilmedi; CI ilk turda kırıldı, bu tur düzeltildi)

## Yapılan iş
BROWSER_BUG_REPORT_108'in ikinci turu: ilk push'un CI'ında `test_golden_corpus.py` 2 testte kırıldı — golden cevaplar yeni parça kapılarına yanlış-pozitifle takılıyordu (13 ret) ve kusur sözleşmesi yeni sert kapıları hesaba katmıyordu (16 kusur beklenti dışı bloklandı). Kökler:

1. **`insert_sentence_breaks` rakam bölüyordu**: "3.000"/"2.4" içindeki nokta cümle sonu sanılıp `3.\n000` üretiliyordu; demirleme kapısı `sayi_kaydi` ile doğru çeviriyi reddediyordu (num-05). `_numeric_separator` üç `_END_RUN` döngüsüne eklendi.
2. **`_DANGLING_TAIL` edat/soru-eki içeriyordu**: `için/gibi/kadar/ile/beri/göre/dolayı/yüzünden/mi/de/da/ya/ki` sonda doğal cümlecik kapanışıdır ("…kestiği için,", "Doğru mu?") — çıkarıldı; liste yalnız gerçek sarkık bağlaç/birleşik yapılara (`ve/ama/çünkü/değil/emin/zorunda/hâlâ/hatta/bile/ise/sanki/her/bir/bu/şu/o/ne/hem/diye`…) indi (sb-02).
3. **`_proper_names` fazla genişti**: zamir `I` ve gün/ay adları ("Tuesday"→"Salı" meşru yerelleştirme) ad sayılıyordu; satır başı SDH/konuşmacı öneki sonrası ilk kelime ("[distorted] Run!", "MAN: Hold") ad değil — düzeltildi (sb-03, sdh-*).
4. **Ad demirleme birebir-substring'di**: `Lisbon`→`Lizbon'dan` ve `Majesty`→`Majesteleri` reddediliyordu (pn-03, ctx-a4). `_name_preserved`: birebir ∪ `Kök'ek` çekimi (ilk 2 harf + ≤2 lev) ∪ büyük harfli hedef tokenında ≥%70 ortak önek (uzunluk ≥ ad). `Larson`/`Shen`/`İstanbul`/`Winterhaven`/`Madrid` bozmaları yine reddedilir.
5. **SDH demirleme birebir işaret istiyordu**: `[GUNFIRE]`→`[SİLAH SESLERİ]` gibi yerelleştirme düşüyordu → tür-bazlı varlık denetimine çevrildi (köşeli/paren/♪/konuşmacı etiketi); işaretin düşmesi hâlâ bloklanır.
6. **`rescue_group` ölü koddı**: `task_once` tam reddedilen tekil grupta istisna atıyor → `RESCUE_SUFFIX` dalına hiç ulaşılmıyordu; tek-grup paketleri doğrudan `rescue_group`'a bağlandı, `invalid_response/empty_response` (veya kısmi dolma) durumunda ikinci deneme gerçekten koşuyor.

## Değişen dosyalar
- `backend/sentence_translation.py` — `_numeric_separator`, `_WEEKDAY_MONTH`, `_LEADING_MARKERS`, `_SDH_KIND`/`_sdh_issue`, `_edit_distance`, `_common_prefix_len`, `_NAME_INFLECTED`/`_NAME_CAPITALIZED`, `_name_preserved`; `_DANGLING_TAIL` daraltıldı.
- `backend/transcribe.py` — `rescue_group` yeniden yapılandı; tek-grup bundle'lar rescue akışına girdi.
- `backend/test_golden_corpus.py` — `expected_blocked`/`expected_pass` gerçek kapı fonksiyonlarıyla hesaplanıyor; bloklanan grup = 2 istek; danışman-geçer invariant korunuyor.
- `backend/test_transcribe.py` — `test_sentence_translation_failed_groups_never_reach_refine` 3→4 istek (spec'in ikinci kurtarma denemesi); invariant: başarısız grup refine'a gitmez.
- `backend/test_sentence_distribution.py` — +2 test (rakam-ayraç, edat-kapanışı/ad-çekimi): toplam 26.
- `docs/raporlar/BROWSER_BUG_REPORT_108.md` — kök nedenler, önce/sonra, test ve sınırlar güncellendi.

## Test kanıtları
- `venv/bin/python backend/test_sentence_distribution.py` → 26/26
- `venv/bin/python backend/test_transcribe.py` → 192/192
- `venv/bin/python backend/test_golden_corpus.py` → 6/6 (golden 0 ret; kusur tekilleme metriği sdh/names/cur/unt/dat'ı sert kapıda yakalar)
- Canlı sağlayıcı (claude-haiku-4-5 @ shuaiapi, `--translate-only`): `Wait. Are you sure / you want to come with me?` → `Bekle.` / `Benimle gelmek istediğinden emin misin?` — spec'in birebir istenen örneği; `3,000 coins` → `3.000` korundu; `Your Majesty` → `Majesteleri` kabul.

## Sınırlar / notlar
- `npm test` koşulmadı (talimat: tam paket gereksiz tekrarlanmasın); JS'e dokunulmadı, CI'ın kalanı yeşil koşacak.
- Ad korunması kural-tabanlı; apostrofsuz serbest transliterasyonlar (ör. Persephone→Persefone) reddedilir — `Larson` bozmasını yakalamak için kasıtlı muhafazakârlık.
- Semantic retime bilinçli yapılmadı (ayrı seçenek).
- `BROWSER_BUG_REPORT_108.md` gitignore'da → `git add -f` ile eklendi (69 raporun izlediği kalıp).
- master'a merge YOK — kullanıcı kendisi merge'ler.
