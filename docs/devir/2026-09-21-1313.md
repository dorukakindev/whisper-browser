# Devir notu — 2026-09-21 13:13 (Europe/Istanbul)

## Özet
`!!pr_documentation` turu — kullanıcı "repoya kalıcı altyapı: PR template + CHANGELOG + checklist" seçeneğini seçti. Repo dalı `devin/pr-docs-infra` (başlangıç `bbf97ac` = origin/master). Üç dosya zaten vardı; eksikler kapatıldı.

## Değişen dosyalar
- `.github/pull_request_template.md` — 3 başlıklı cılız şablon playbook standardına genişletildi: What / Why / How / Tests (checkbox) / Risks·Rollback / Screenshots·Media / Links / Release Notes + repoya özgü checklist korundu (Türkçe UI, backend sözleşme senkronu, argv/log'a secret yok, lisans bildirimi) ve CHANGELOG kalemi eklendi.
- `CHANGELOG.md` — Keep a Changelog formatına çevrildi (`## [Unreleased]` + Added/Changed/Fixed alt başlıkları). Bu oturumda merge edilmiş PR'lar geriye dönük işlendi: #9 (device-code giriş + kuyruk rayı, Added), #11 (boş ana sayfa + lockupViewModel, Fixed/Added), #8 (kuyruk resume, Fixed), #7 (overlay geri açılma, Fixed), #5 (refine önizleme index, Fixed), #2 (Sonraki düğmesi, Fixed). Eski serbest Unreleased maddeleri Added/Changed/Fixed kategorilerine dağıtıldı; 0.9.0-beta.1 başlığı `[0.9.0-beta.1]` bağlantı formatına alındı.
- `CONTRIBUTING.md` — kullanıcıya görünen değişikliklerde CHANGELOG'a Unreleased kalemi ekleme kuralı yazıldı.

## Doğrulama
Sadece belge değişikliği — ürün testleri çalıştırılmadı (AGENTS.md izni: belge işlerinde diff/içerik kontrolü yeterli). Değişiklikler `git diff` ile gözden geçirildi; bağlantılar ve markdown yapısı doğrulandı. Çalışma ağacındaki `.agents/skills/testing-whisper-browser/SKILL.md` değişikliği (test ajanının) bilinçli olarak commit dışı bırakıldı.

## Sınırlar / sonraki adımlar
- CHANGELOG'da `0.9.0-beta.1`'den sonra yeni sürüm etiketi yok; ilk gerçek sürümde Unreleased bloğu sürüm numarasına taşınacak.
- GitHub'da label taksonomisi (size/risk/area) tanımlı değil; playbook'un label adımı uygulanmadı — istenirse ayrı iş.
