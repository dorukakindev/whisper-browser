# Devir notu — 2026-09-25 15:37 — R124 Adım 2 (Faz A kalanı): hayalet araç çubuğu + site çipi + sekme birleşimi + hata cilası

- **Dal:** `codex/r124-step2-faza-1790350667` · **Taban:** `codex/r124-step1-i18n-1790347020` (stacked — Adım 1 PR #51 merge olunca GitHub base'i master'a döner) · **Başlangıç:** `808cef9` · **Bitiş kod commit'i:** `d2ea455`
- **Kapsam:** [BROWSER_PLAN_R123.md](../raporlar/BROWSER_PLAN_R123.md) §5 Adım 2 — 2.1 hayalet araç çubuğu, 2.2 site çipi (kalkan üst şeritten buraya), 2.3 aktif sekme yüzeyle birleşimi, 2.4 hata ekranı cilası.
- **Plan güncellemesi:** Adım 2 uygulandı; Adım 3–5 açık.

## Değişen dosyalar (kod commit'i `d2ea455`)

- `src/renderer/index.html` — `.browser-address-wrap` ilk çocuğu olarak `browserSiteChipDetails` `<details>` (kilit+host+caret `<summary>` + popover: site bilgisi başlığı, taşınan `#browserAdblockQuick` kalkanı, "Site izinlerini yönet" düğmesi); `.browser-adblock-quick` sekme şeridinden kaldırıldı (üst şeritten 1 kontrol eksildi); hata yüzeyine `.browser-error-icon` SVG + katlanır `.browser-error-details` (teknik ayrıntılar, `browserErrorCode` taşındı).
- `src/renderer/renderer.js` — güvenlik işareti güncellemesi site çipine taşındı (`data-security` secure/insecure/none, host metni, güvenlik durumu, title/aria-label); `browserSitePermissionsOpen` → `openBrowserSettings('site', 'browserPermissionsTitle')`; `browserSiteChipDetails` `browserToolbarMenuIds` kaydına eklendi (Esc/dış-tık/ok-gezinme/occlusion kazan-kazan); `Hata:` öneki locale'e bağlandı.
- `src/renderer/styles.css` — site çipi bloğu (kilit+host popover, secure/insecure/none kilit renkleri); hayalet kurallar: `.browser-nav-button`/`.browser-place-button`/`.browser-manga-button`/`.browser-page-quick-menu > summary`/`.browser-zoom-toolbar`/`.browser-address-bookmark` 32px şeffaf çerçeve (çerçeve yalnız aktif/odak/hover); `.browser-translate-split`+`> summary` `--bg-2` dolgulu çerçevesiz; `.browser-tab.active` `--bg-2` zemin + 10px üst köşe + alt kenar zeminle aynı (alt çizgi `::before` kaldırıldı, `.workspace-browser` türevi dahil); `.browser-error-icon`/`.browser-error-details` tipografi/boşluk token'ları; bayat `margin-left:auto` kalkan kuralı silindi.
- `src/renderer/ui-locale.js` — +7 anahtar (Teknik ayrıntılar, Site bilgisi, Site bilgisi ve izinler, Site izinlerini yönet, Adres bekleniyor, Şifrelenmemiş HTTP bağlantısı, Reklam koruması).
- Testler — `browser-tabs`, `browser-report117-regressions`, `browser-adblock` pinleri kalkanı site-çipi popover'ına sabitleyecek şekilde güncellendi; `electron-browser-experience-smoke` siteChip+addressWrap kutu ölçümleri (çip görünür + adres hapı içinde); `css-hardcoded-color-baseline` 470→467 (net hex azaldı, iyi yön).

## Test sonuçları

- `browser-tabs` 12 · `browser-report117` 15 · `browser-adblock` · `browser-report124` 7 · `ui-locale` 14 · `css-colors` 467/467 yeşil; `electron-browser-chrome-design` + `ui-scale-matrix` Electron smoke temiz; `node --check` renderer temiz.
- **Gerçek Electron doğrulaması (doğrudan lansman + CDP):** site çipi adres hapının içinde görünür (46×26, wrap içi), popover+kalkan açılıyor, yeni sekmede `data-security="none"`; aktif sekme alt kenarı zeminle aynı (birleşim); hayalet düğmeler pasifken `border: transparent`, `[aria-expanded]`/`active` durumda çerçeveli.
- **`electron-browser-experience-smoke` bu makinede koşamıyor (ortam sınırı, kodla ilgisiz):** smoke kendi içinde ikinci bir Electron süreci spawn ediyor; iç içe Electron bu kutuda `FATAL:zygote_host_impl_linux.cc:221` SIGTRAP ile ölüyor (`--no-sandbox` ile zygote aşılıyor ama `GPU process isn't usable` FATAL kalıyor; Chromium bayrakları uygulama yolundan SONRA verilse de sonuç aynı — üreme 7 kez tekrarlandı). Doğrudan lansman (`npx electron . --remote-debugging-port`) sorunsuz çalışıyor. Smoke'un yeni site-çipi assertion'ları aynı ölçümlerle manuel CDP'de doğrulandı; CI/Windows'ta normal koşar. Tam `npm test` koşulmadı (yalnız değişen alanlar); CI bekleniyor.

## Windows etkisi / doğrulanamayan sınırlar

- Salt CSS/HTML/renderer dokunuşları; `install.bat`/`start.bat`, yollar, file picker, GPU/CUDA etkilenmedi. `details`/`summary` popover kalıbı zaten Chromium/Electron'da çalışıyor (aynı düzen translate-menu/more-menu'de var).
- Adım 1.1 açık kalıyor: sayfada bul "Aranıyor…" asılması — Windows'ta Ctrl+F ile elle doğrulanmalı (`1 / N` sayacı).
- Hayalet düğmelerde kontrast: metin/ikon `--text`/`--text-dim` üzerinde kaldığından iki temada da ui-scale-matrix smoke'u geçti; Windows'ta gerçek DPI/font ölçüsü doğrulanmadı.
- Site çipi host metni gezinme sonrası dolar; bu doğrulamada yalnız boş-sekme `none` durumu ölçüldü (http/https kilit renkleri gerçek navigasyonla görülür — aynı kod yolu daha önce security-mark'ta çalışıyordu).

## Sonraki adımlar

- Adım 3 (Faz B): tek-satır üst çerçeve (sekmeler başlık çubuğunda, ~38px titleBarOverlay), birleşik Whisper düğmesi, 32px durum hapı, gruplu ⋯ menüsü — ayrı `codex/` dalı + PR; kabul ölçütü: 1440×900'de içerik y≤90px.
- Adım 4 (Faz C): yan panel ikon rayı, yeni-sekme sayfası, token birleştirme (`browser-chrome.css`), açık-tema paritesi.
- Adım 5: find/error-pages/page-bg/tab-icon/frozen-frame smoke'ları.
