# Devir Notu — 2026-09-21 16:17 (Europe/Istanbul)

## Kapsam

`devin/comprehensive-tour` dalında kapsamlı entegrasyon + regresyon turu (matris A–G). Gerçek Electron masaüstü (`DISPLAY :0`, izole profil `WHISPER_RESOURCE_SOAK_USER_DATA=$HOME/qa-profile-r95`) üzerinde UI doğrulaması; bulunan ürün hataları düzeltildi ve regresyon testi eklendi.

- Başlangıç HEAD: `1fd9792` (master; `9aa376a` güvenlik düzeltmeleri mevcut)
- Rapor: `BROWSER_BUG_REPORT_95.md` (matris + kanıt dosyaları)
- QA raporu (kutu içi, commit'li değil): `~/qa-report-r95-comprehensive.md`

## Ürün düzeltmeleri (kod commit'i)

- **F1 OAuth:** `youtube:authCode` IPC — PKCE (S256) + `http://127.0.0.1` loopback + state doğrulaması; backend `exchange_code` (secret'lar env'de, argv'de asla). Modal artık önce yöntem seçimi sunar; device-code yalnız "TVs and Limited Input" istemcilerine. `youtube:cancel`/`logout` bekleyen loopback'i iptal eder. Scope `youtube.readonly`'de kaldı; üçüncü-taraf istemci geri getirilmedi. `youtube:session` artık `clientId` döner (public id; R70-03 whitelist'e bilinçli eklendi) ve istemci formu bounce sonrası ID'yi geri doldurur.
- **F2 araç kontrolü:** `browser-video-analysis.js` + `browser-media-tools.js` — çıplak PATH adları (`python3`/`ffmpeg`) `fs.existsSync` ile bulunamıyordu → `toolAvailable()` probu (mutlak yol → existsSync, çıplak ad → `--version`/`-version` spawn). Linux'ta intro/OCR/anlamsal arama + sahne şeridi çalışır hale geldi.
- **F3 i18n:** `ui-locale.js`'e 4 eksik girdi; `renderer.js`'te 3 ham `textContent` `t()` sarmasına alındı (abonelik boş durumları + "Cihaz kodu alınamadı").
- **F4 Linux env check:** `app:getEnvInfo` + `resolveFfTool` — `Scripts/python.exe`/`ffmpeg.exe` yalnız win32; POSIX'te `bin/python`/`ffmpeg`.
- **F5 CSP:** `worker-src 'self' blob:` eklendi — hls.js demuxer worker artık engellenmiyor.

## Test harnesi düzeltmeleri (ürün hatası değil)

- `findMediaTool`/`findTestPython`/`process.execPath` taşıması: 9+ smoke dosyası Windows-sabitlerinden kurtarıldı (ffmpeg.exe, electron.exe, venv/Scripts).
- `electron-browser-video-e2e`: eksik `flower.mp4` fixture'ı artık sentetik üretiliyor; codec `mpeg4`→`libx264` (Chromium MPEG-4 Part 2 oynatmaz).
- `electron-smarttube-boot` + `report67`: F1'in yeni seçim ekranına güncellendi.
- `ui-locale.test.js`: R95 bloğu (6 anahtarın EN karşılığı doğrulanır).

## Ortam

- `backend/venv`: `sentence-transformers==5.1.2` kuruldu (requirements-browser-tools.txt); pip 22.0.2→26.2.1. `faster_whisper` import doğrulandı. Bu makine-local değişiklik; kullanıcı makinesinde install.bat/requirements akışı geçerli.

## Doğrulama

- `npm test` → 0 ("Tüm testler geçti")
- `npm run test:electron-bridge` → 0
- `node tests/run-electron-smokes.js --all` → 0, 22/22 smoke
- `node --check` değişen tüm JS → 0
- UI turu kanıtları: `~/screenshots/ss_*`, `/tmp/qa-r95/soak.log`, `~/Downloads/Whisper/ÇIKTI/mxa4HDgfWFs.tr.srt`, `/tmp/qa-r95/dup.tr.srt`

## Sınırlar (açık)

- Gerçek Google hesabı OAuth onayı: BLOCKED (yetkili test hesabı yok). PKCE+loopback+state üretimi ve iptal yolları doğrulandı.
- Canlı Invidious/YouTube probe'ları box'ta engelli; hata/boş/yavaş/retry kontrollü cevaplarla test edildi.
- `data:,WEBVTT` CSP uyarısı: ürün kodunda kaynak bulunamadı; kasıtlı genişletme yapılmadı.
- Bozuk istemcide tarayıcı akışı 600 sn bekler (Google hatası tarayıcıda kalır); "İstemciyi değiştir"/"İptal" mevcut — minor UX notu.

## Commit'ler

- Kod: (bu dosyadaki `git log` kaydına bakınız — `feat:`/`fix:` commit'i)
- Dokümantasyon: bu not + `BROWSER_BUG_REPORT_95.md` + `CHANGELOG.md` + `DEVIR-NOTU.md`

Sonraki adım: PR açık bırakılacak (merge edilmeyecek) — kullanıcı bağımsız inceleme yapacak.
