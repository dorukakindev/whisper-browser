# Devir Notu — 2026-09-20 17:14 (Europe/Istanbul)

Repo: dorukakindev/whisper-browser · Dal: master
Başlangıç kod commit'i: `a6d3296` (Parti 10)
Bitiş kod commit'i: `f2ec595` — Parti 11: OpenSubtitles moviehash (B03)

## Kapsam (FEATURE_RECOMMENDATION_CATALOG_2026-09-20.md, yalnız E maddesi)

- **B03 — OpenSubtitles moviehash + dosya boyutu ile kesin altyazı arama:**
  - `src/opensubtitles-hash.js` (yeni): klasik moviehash — ilk ve son 64 KiB
    little-endian u64 toplamı + dosya boyutu, mod 2^64, 16-hex. Python
    referans portuyla aynı fixture üzerinde birebir doğrulandı
    (`5f9fe02060a3cd40`, `feb1681acd80358f`, sıfır-dosya `…30d40`).
  - `src/browser-subtitle-search.js`: `moviehash` sorgu parametresi (yalnız
    16-hex doğrulanmış), `filesize` doğrulaması; sağlayıcının
    `moviehash_match` bayrağı +100 puan ve sonuçta `hashMatch` alanı;
    hash sorgusu sıfır dönerse aynı oturumda salt-başlık fallback'i;
    geçersiz giriş `INVALID_INPUT`.
  - `src/browser-feature-services.js`: `subtitle-search` case'i
    `payload.mediaPath`'i `authorizeMedia` (mevcut `mediaFileAccess`
    yetkilendiricisi, main.js'ten enjekte) ile doğrular; yetkisiz veya
    okunamayan dosyada arama hashsız sürer. İç yol API'ye hiç gitmez
    (`delete target.mediaPath`).
  - `src/renderer/browser-features.js`: arama `player.localPath`'i gönderir;
    `hashMatch` adaylara "dosya parmak izi ✓" rozeti.

## Değişen dosyalar

- `src/opensubtitles-hash.js` (yeni), `src/browser-subtitle-search.js`,
  `src/browser-feature-services.js`, `src/main.js` (dep enjeksiyonu),
  `src/renderer/browser-features.js`
- `tests/opensubtitles-hash.test.js` (yeni), `tests/browser-subtitle-search.test.js`,
  `tests/browser-parti7.test.js` (wiring assert'leri)

## Testler

- `npm test` → Tüm testler geçti
- `node --check` değişen tüm JS dosyaları
- Not: gerçek OpenSubtitles API çağrısı yapılmadı (API anahtarı ortamda yok);
  sözleşme fixture fetch ile doğrulandı.

## Kalan E maddeleri

- F17 (aynı klip iki ASR ayarı benchmark'ı — RTF/VRAM/cue-farkı, hash'li saklama)
- F18 (model/bağımlılık yöneticisi — E/K sınırı, aşamalı)
- F20 (tarihli kabul matrisi belgesi)

K/B/R maddelerine dokunulmadı.
