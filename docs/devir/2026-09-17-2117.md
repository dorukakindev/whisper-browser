# Devir Notu — 2026-09-17 21:17

## Kapsam

BROWSER_BUG_REPORT_53 + `docs/devir/2026-09-16-2112.md`'de açık bırakılan maddelerin doğrulama/düzeltme turu: A1/A2/A3 kabul açıkları, 26 B sınıfı, 16 D sınıfı madde. C sınıfı 17 reddedilmiş maddeye dokunulmadı. Sonuç raporu: `BROWSER_BUG_REPORT_56.md` (yerel, `.gitignore`'da — öncekiler gibi).

- Repo: https://github.com/dorukakindev/whisper-browser · dal: `master`
- Başlangıç HEAD: `4fe4d4aaf5a388e1dcad52591d3f4539eda38022` (`docs: record subtitle boundary repairs`)
- Bitiş kod commit'i: bu commit (`fix:` mesajıyla, aşağıda)

## Bu turda yapılanlar

### A kabul açıkları
- **A1 (R51-01):** crafted `browser-session.json` içindeki `primaryFile`/`secondaryFile` yolları gerçek içe aktarma hattından (`readPackage`→`restorePackage`→`restoreBrowserSessionState`) geçirilip restart sonrası `SubtitleFileAccess` grant'ı verilmediği kanıtlandı — kod zaten kapalıydı, e2e kilit eklendi (`tests/workspace-restore-regressions.test.js`).
- **A2 (R51-17):** HLS `EXT-X-BYTERANGE` — aynı URL'li iki parçada `Content-Range` yoksa eşleyici son parçayı tahminen seçiyordu. Artık belirsizlikte **fail-closed** reddediyor; ayrıca `browserRequestRanges` haritası CDP `requestWillBeSent`'te istek Range'ini kaydedip `responseReceived`'de kesin eşleştiriyor; gezinme/bitiş/başarısızlık noktalarında temizleniyor. `src/browser-cea-captions.js`, `src/main.js`, `tests/browser-cea-captions.test.js` (11 test, gerçek CDP dilimi dahil).
- **A3:** `tests/electron-a3-acceptance.smoke.js` — gerçek Electron harness'te 7 yol: render-process-gone/unresponsive kurtarma, sekme kapatma koruması, manga stale-edit rollback, sekme başına ofset izolasyonu, playbackRate geri yükleme, cancelTooLate, .wspak içe aktarma+restart. **Gerçek bug bulundu:** manga geri-alma script'i ana dünyada `executeJavaScript` ile koşuyordu ama `__whisperMangaOverlay` izole world 999'da → revert hiç çalışmıyordu; `executeBrowserTrustedMain` ile düzeltildi.

### Düzeltilen B maddeleri
- **R51-07:** içe aktarılan oturum izinleri — `sitePermissions` pakette taşınmıyor, merge mevcut izinleri koruyor.
- **R51-08:** `clearedSecretFields` import yolunda reddediliyor; kasa anahtarı gizlenince fiziksel silinmiyor, özellik çalışmaya devam ediyor.
- **R51-09:** katalog/nMDB kaynakları `play` akışında artık native onayla yeniden grant'lanıyor.
- **R51-15:** `browser:command` mutasyon öncesi `isCurrentBrowserContext` + `docToken`.
- **R51-36:** OSD geri bildirimi browser modda `executeBrowserTrustedMain` ile sayfaya taşınıyor.
- **R51-43:** 5 ham `spawn` noktası `withoutSecretEnv`'e bağlandı.
- **R51-50/71:** `cueSignature` WeakMap cache (10k cue'da updateCueMeta 3.36ms→cache'li); `findCueAt` ofsetli indexOf.
- **R51-60/78:** öğrenilen terminoloji prompt'ta "aday" olarak etiketleniyor, zorunlu sözlük değil.
- **R51-61:** `stopBrowserManga` `mangaFailures`'ı temizliyor.
- **R51-63:** taslak düzenleyici dirty iken Esc/backdrop onay istiyor.
- **R51-64:** sayfa-silinen manga overlay `isConnected` kaybında geri kuruluyor.
- **R51-84:** sayısal ayarlar alan başına fallback; tek bozuk alan tüm kaydı düşürmüyor.
- **R51-87:** live-ASR `chunk_done` yankısında `fs.unlinkSync` öncesi üyelik denetimi.
- **R51-100:** manga `no_regions` negatif sonucu cache'leniyor.
- **R51-105:** manga overlay `!important` stiller + reattach + `getBoundingClientRect` ile sayfa CSS'ine dayanıklı.
- **R51-107:** `requestPinnedMangaImage` çerezleri yalnız `https:` için okuyor; `http:` isteğinde Referer yok.
- **R51-115:** `tests/run-electron-smokes.js` envanter/koşturucu + `npm run test:electron` + `run-all.js` sonuç notu.

### Düzeltilen D maddeleri
- **R51-13:** medya probe'una `docToken`; komut script'i mutasyondan önce token eşler (mini-player dahil).
- **R51-16:** `ceaStreamMatchesInstream` — SERVICE1/`cc708_1` eşleme.
- **R51-19:** decoderKey başına sequence sıralama; ters tamamlanan segmentler sınırlı beklemeyle sıralanıyor.
- **R51-28/29:** kuyruk iptal yarışı — renderer persist-await sonrası canlılık kontrolü; main `jobStarting`/`jobStartSeq`/`jobCancelSeq` ile authorize-await sırasındaki iptali spawn öncesi düşürüyor.
- **R51-47:** `browser:show` view reddinde `createdHere` hayalet sekmesi temizleniyor.
- **R51-57:** `sweepOrphanOutputTransactions` Electron başlangıcında artık journal/backup süpürüyor.
- **R51-58:** `playerJobEvent` dağıtımı `jobId`↔`player.job` süzgeçli; `sendJobEvent` sarmalayıcısı.
- **R51-77:** ASS/JASSUB canvas aktifken HTML overlay bastırılıyor, detach'te geri yükleniyor.
- **R51-91:** mini-player kapanış/reparent yaşam döngüsü korumaları.
- **R51-99:** `done` sonrası nonzero exit ikinci terminal üretmiyor (terminal mandal).

### Zaten kapalı / reddedilen / sınır
- Kapalı: R51-22, 24, 42, 46, 53, 69, 73(kısmi), 86, 93, 104 — hepsine sözleşme/regresyon kilidi eklendi veya mevcut test doğrulandı.
- Reddedildi: R51-103 (tüm wheel/touchmove zaten doğru `passive` bayraklı).
- Sınır: R51-76 font fallback — gerçek görsel fixture gerekiyor; kullanıcı font yolu mevcut.

## Test sonuçları

- `npm test` — tüm dosyalar geçti, 0 FAIL (son tam koşu)
- `npm run test:electron-bridge` — geçti
- `node tests/run-electron-smokes.js a3-acceptance` — geçti (7 kabul + manga rollback)
- `node --check` main/renderer/manga/overlay/ass-renderer/media-controller — OK
- Python `test_transcribe.py` — 185/185

## Altyapı notu

`ELECTRON_RUN_AS_NODE=1` oturumdan sızarsa `electron` binary saf Node gibi davranıyor → `require('electron')` yol string'i dönüyor. `run-electron-smokes.js` env'den siliyor; `electron-browser-trusted-bridge.smoke.js` doğrudan `npm run test:electron-bridge` altında kendini temiz env ile re-exec ediyor.

## Kalan işler / sınırlar

- R51-76 görsel fixture bekliyor; R51-73 kalan IMSC sunum farkları bilinen sınır.
- Tam Electron smoke envanteri `node tests/run-electron-smokes.js --all` ile ayrı koşulur (gerçek süreç/ekran/ağ ister); bu turda yalnız `a3-acceptance` ve `trusted-bridge` koşuldu.
- `BROWSER_BUG_REPORT_56.md` `.gitignore`'da — yerel kalıcı rapor, öncekiler gibi commit'lenmiyor.
