# Devir notu — 2026-09-20 15:57 (Europe/Istanbul)

## Kapsam
Parti 5b — SmartTube, katalog E-maddeleri:
- **A22** Yerel çalma listeleri ve Enqueue
- **A38** kalan alt maddeler: radio/related-video zinciri (varsayılan kapalı), shuffle-all, most-played sıralaması

Bu partiyle birlikte Parti 5 (A01/A03/A08/A22/A38) tamamlandı.

## Commit'ler
- Kod: `67db739` — `SmartTube: yerel calma listeleri + radyo zinciri + karistirma + en-cok-oynatilan (A22/A38)`
- Önceki kod commit'i: `dc0ea54` (Parti 5a)

## Değişen dosyalar
- `src/renderer/renderer.js`
  - `stPlaylists` (`localStorage` `stPlaylists`; 30 liste / 200 öğe tavanı; `stNormPlaylistItem` ortak normalizasyon)
  - `stPlaylistCreate/Remove/Add/RemoveItem` + `stPlaylistEnqueue` (eksikleri sıraya ekleme) + `stPlaylistPlay` (kuyruğu liste içeriğiyle kurup baştan başlatma)
  - `renderStPlaylistPanel` + panelde `data-pl-play|enqueue|remove` delegasyonu, `stPlaylistName` + Enter/`stPlaylistCreate`
  - `openStPlaylistPicker` — kart menüsü kabuğunu yeniden kullanan "Listeye ekle…" seçici (yeni-liste giriş satırı dahil)
  - `stPlayCounts` (`stPlayCounts`; 120 kayıt) + `stBumpPlayCount(info)` — akışın gerçekten açıldığı iki `playerStream` noktasında çağrılır → `stMostPlayedVideos()` → ana sayfa "En çok oynatılan" rayı (ilk 12)
  - `stAutoRelatedNext()` — `stAutoRelated` onayı kapalıyken no-op; sıra dolu / yerel liste devralacak / YouTube dışı medya durumlarında devreye girmez; ilk gizlenmemiş `ytInfo.recommended` öğesini normal probe akışından açar
  - `ended` handler'a `else if (player.sleepTimerMode !== 'end') stAutoRelatedNext();` (ilk satır testinin 300-karakter penceresi korundu; kuyruk oynattıysa iç kapı tekrar ateşlemeyi engeller)
  - `stQueueShuffle()` Fisher-Yates + `stShuffleQueue` düğmesi
  - `PERSIST_CHECKBOX_CONTROLS` += `stAutoRelated`
- `src/renderer/index.html` — görünüm paneline "Oynatma" (radyo onayı + karıştır) ve "Çalma listeleri" (liste + yeni-liste satırı) grupları
- `src/renderer/styles.css` — `.st-mini-btn`, `.st-pl-new`, `.st-pl-actions`
- `src/renderer/ui-locale.js` — 19 yeni EN/TR çifti
- `src/settings-security.js` — `stAutoRelated` persist listesine
- `tests/smarttube-playlists-parti5b.test.js` — yeni: CRUD/sınırlar/enqueue/play, sayaç sıralaması+budama, radyo zincir kapıları, shuffle, bağlantı regex'leri

## Test
- `node tests/smarttube-playlists-parti5b.test.js` — OK
- `node tests/smarttube-filters-parti5.test.js` — OK
- `node tests/settings-security.test.js` — 23/23
- `node tests/player-ui.test.js` — 145/145
- `npm test` — **Tüm testler geçti**
- `node --check src/renderer/renderer.js` — OK

## Sözleşme notları
- A22: kuyruk = oturum-odaklı geçici sıra; çalma listesi = isimli kalıcı koleksiyon. "Oynat" kuyruğun üzerine yazar (kuyruk zaten değiştirilebilir oturum verisi); "Sıraya ekle" mevcut sırayı koruyup eksikleri sona ekler.
- A38 radyo zinciri varsayılan kapalı ve yalnız yerel tercih; gizlenen videolar zincire girmez (`stVideoHidden` süzgeci).
- Bilinen sınır: radyo zinciri probe'un `recommended` alanına bağlı; öneri boşsa sessizce durur (katalog koşulu: otomatik zincir opt-in).

## Sonraki
Parti 6 — B06 sekme menüsü+hover önizleme, B14 otomatik PiP/sinema, B10 element picker.
