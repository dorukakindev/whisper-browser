# Devir notu — 2026-09-19 14:51 — R83 derin tur 2 (kod değişikliği yok)

## Özet

İlk turun ardından kullanıcı isteğiyle ikinci, daha derin bir denetim turu
yapıldı: 6 çapraz-kesim kümesi (yarış durumları, yaşam döngüsü/sızıntı,
i18n/unicode, kalıcılık/dışa-aktarım, girdi-doğrulama/hata yolları,
kozmetik/UI-sözleşmesi). Tüm bulgular kaynakta yeniden doğrulandı; üç bulgu
**gerçek Chromium 137'de CDP** ile ve birkaçı Node repro'suyla ampirik
kanıtlandı. Sonuçlar `BROWSER_BUG_REPORT_83.md` BÖLÜM 2'ye eklendi.

- Repo: https://github.com/dorukakindev/whisper-browser, dal `master`
- Başlangıç ürün commit'i: `cc81280533834d6f4c3fe1d1880dc80f9c36bd14`
- Bitiş ürün kodu: aynı (tur yalnız rapor + not üretir)

## Yeni bulgular (+21 → toplam 39)

- **P1 B83-19:** çeviri arşivi kendi SENSITIVE_QUERY regex'ini kullanıyor,
  `session_id`/`sid`/`id_token`/`refresh_token`/`hdnts`/`hdnea`/`client_*`
  gibi ortak sözlükteki adları kaçırıyor → canlı token'lar arşive ve
  paylaşılabilir export'lara yazılıyor.
- **P2:** credentialed URL'ler `media.url`/`sourceRef`/yedeklere
  kalıcılanıyor (B83-03'ün saklama yarısı); `page:exclusions` ve
  `startBrowserPageTranslation` await'lerinde kuşak/oturum yeniden
  kontrolü yok → bayat oturum yeni sayfaya çeviri basıyor + arşiv yanlış
  URL altında bozuluyor; bayat `loadRetryTimer` kullanıcının yeni
  gezinmesini `wc.reload()` ile eziyor; ui-locale observer'ı sayfa
  başlıklarını/kullanıcı verisini çeviriyor ("Giriş"→"Intro" gibi);
  en-US `İ` katlaması CEA kalibrasyonunu Türkçe'de öldürüyor (Node repro:
  İSTANBUL→"i stanbul", eşleşme 1/3); sponsor "Atla" bitmiş segmente
  geri sarıyor; açılış GC'si session/workspace `trackRefs`'lerini saymadan
  altyazı varlıklarını süpürüyor.
- **P3:** ölü `tab.loading` koruması, yarıda kalan unload'un capture'ı
  kapalı bırakması, takılan `lifecycle='restoring'` (hiç atanmayan
  'restore_failed'), scheduler'da atan onResult → results+failures çift
  kaydı, whenIdle `.catch`'siz devam, `.wbp` export'ta mutlak yol/OS
  kullanıcı adı, `.bak`'ların silinen veriyi gölgelemesi, clearSite'ın
  auth-cache'i atlaması, þ/ð/ý fixup bozulması (repro: İzlandaca satır
  "şağ er şıtt orğ"), eksik `Å`/`Ã¢` mojibake marker'ları (repro: "Åžimdi"
  onarılmadı), ~14 sabit `tr-TR` locale çağrısı, `[hidden]`'ı ezen
  `display:grid` önizleme paneli (Chromium computed-style repro).

## Testler / ampirik kanıtlar

- `npx node --test tests/browser-*.test.js` → **140/143 PASS**; 3 düşüş
  hep ortam: `backend/venv/Scripts/python.exe` ve `backend/bin/ffmpeg.exe`
  yok (browser-alignment, browser-reference-media, browser-video-analysis).
- Modül yük taraması: 89/91 `browser-*.js` Node'da temiz; 2 preload
  modülü DOM API gerektirir (beklenen, hata değil).
- Chromium 137 CDP: `[hidden]`+`display:grid` → computed `grid` (B83-39);
  `pageBlockScanScript`→`pageApplyScript`→SPA `nodeValue` mutasyonu→
  `pageRestoreScript` zinciri canlı site metnini bayat orijinalle ezdi
  (B83-14 mekanizması). Repro scriptleri `/tmp`'de, repo'ya yazılmadı.
- Node: `calibrateCueTimeline` Türkçe-kasa çakışması accepted:false;
  `shiftCueTimeline([{3,5}],-8)` → `[0,0.001]` phantom cue; İzlandaca
  þðý fixup bozulması; ÅŸ mojibake onarım atlaması.

## Sınır notları

- SWE-2 sınırı yine 2 kümenin ilk başlamasını geciktirdi; biten ajanlar
  uyutulup workflow `run_id=wfr-ff0aa68cf50346dc8d53ffece4c6bcef` ile
  devam ettirildi — sonuçta 6/6 küme tamamlandı.
- Uygulama Windows+Electron; gerçek UI burada açılamaz — kozmetik bulgular
  DOM/CSS repro'suyla doğrulandı, görsel ekran görüntüsü yok.

## Sonraki adımlar

- Düzeltme turu: P1 B83-19 öncelikli; ardından P2 kümesi. Rapor her
  bulguya çözüm yönü içeriyor.
- En değerli kalıcı testler: cue-timeline-calibration Türkçe fold ve
  decodeSubtitleBuffer ikiz-parite testleri `tests/` altına yazılabilir.
