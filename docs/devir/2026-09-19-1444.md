# Devir notu — 2026-09-19 14:44

## R81 ikinci tur — feed_partial denetimi + SL1-5 doğrulama + test koşumu (kod değişikliği YOK)

**Repo/dal:** `https://github.com/dorukakindev/whisper-browser` · `master`
**Başlangıç kod:** `5bd852a` · **Bitiş kod:** ürün kodu DEĞİŞMEDİ (salt-okunur rol).

## Ne yapıldı

`PROGRAM_BUG_REPORT_81.md`'ye **§4** eklendi:

- **`npm test` bağımsız koşum:** çalışma ağacında paralel oturumun commit'lenmemiş
  `feed_partial` implementasyonu varken **TAM GEÇTİ** (Node+Python, EXIT:0).
- **feed_partial son hâl incelemesi:** `main.js` requestId threading + renderer
  preview + kısmi-hata yolu doğru kurgulanmış; N1 latent yarış hâlâ açık —
  tek-satır öneri: full render'da `grid.dataset.previewRequestId = seq` işaretle.
- **R77 SL1–SL5 slop doğrulaması:**
  - SL1 `browser-link-intent.js` öksüz **DOĞRU** — yalnız test referanslı; üretim
    kopyası `browser-preload.js:23` inline (test üretimi temsil etmiyor)
  - SL2 `browserPageResourceTelemetry` **DOĞRU** — `measured:true` ama 4 alan sabit-0
  - SL3 diakritik **KISMEN** — `remove_diacritics 2` FTS5 konfigü canlı; "ölü"
    iddiasının kastı raporda belirsiz
  - SL4 normalizeCues/cuesToSrt **DOĞRU + eksik** — çift değil **üçer kopya**
  - SL5 kök artıkları **DOĞRU** — `m[1])` 0-bayt shell artefaktı kökte
- **Genel ders:** preload-içi inline saf fonksiyonlar ile modül kopyaları
  ayrışabiliyor — `browser-mini-preload`/`browser-dialogue` testsizlik listesi
  aynı desenin adayı.

## Dosyalar

- `PROGRAM_BUG_REPORT_81.md` — §4 eklendi (52 satır)
- `docs/devir/2026-09-19-1444.md` — bu not
- `DEVIR-NOTU.md` — indeks güncellendi

## Doğrulama

- `npm test` TAM GEÇTİ (EXIT:0) — çalışma ağacı feed_partial WIP dahil.
- Ürün kodu bu oturumda değiştirilmedi; paralel WIP'e dokunulmadı.

## Sonraki adım adayları

- B80‑01 (P2) ve D81‑01 (262 dizge) paralel AI'ya devredilebilir — spec'ler hazır.
- `m[1])` silinebilir (kullanıcı onayı ile — dosya bana ait değil).
- SL1 düzeltmesi: preload'un modülden türetilmesi veya inline kopyanın modülle
  eşlenmesi.
