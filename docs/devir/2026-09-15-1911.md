# 2026-09-15 19:11 — Bug raporları 25–32 tam kapanış

## Kimlik

- Depo: `https://github.com/dorukakindev/whisper-browser`
- Dal: `master`
- Başlangıç HEAD'i: `fdc8de23302a6a15b3c7f475e9fd84f8b58a4cd2`
- Başlangıç ürün kodu: `0b100736ff5ebcdf91980f654cf750e359d4ac71`
- Bitiş ürün kodu: `4b54cad1fe8c67ffd6c8b86c5e49ba296fcb638a`
- Dokümantasyon commit'i bu nottan sonra oluşturulacağı için kendi hash'i bu dosyaya yazılmadı.

## Kapsam ve envanter

15 Eylül 2026 tarihinde oluşturulan yerel ve GitHub bug raporlarının tamamı envanterlendi: `BROWSER_BUG_REPORT_25.md`–`BROWSER_BUG_REPORT_32.md`.

- Rapor 25–31: 108 numaralı ürün bulgusu. Son dağılım: **99 doğrulandı/düzeltildi**, **5 kısmen doğrulandı ve sertleştirildi**, **4 reddedildi**.
- Rapor 28'de ayrıca bir test varsayımı reddi (`RET-28-01`) bulunuyor.
- Rapor 32, toplu test kapanışında bulunan B61 devam kusurunu `B105` olarak kayda bağladı; bu yeni bağımsız kök neden değil, daha önceki düzeltmenin eksik kalan son koşuludur. Üç test-düzeneği yanlış pozitifi/yarışı `RET-32-01`–`03` olarak gerekçeleriyle kapatıldı.
- Her raporda bulgu başlıklarının yanında sonuç durumu ve sonunda **“Ayrıntılı bulgu, düzeltme, ret gerekçesi ve doğrulama dökümü”** bölümü bulunuyor.

## Bu turda kapatılan ürün yolları

- Başarısız çeviri yeniden denemesi yalnız terminal hata kimliklerini kuyruğa alıyor; bütün izi yeniden API'ye göndermiyor.
- bfcache `pagehide.persisted` dönüşünde preload ve ASS gözlemcileri canlı kalıyor; gerçek unload temizliği korunuyor.
- Split ikincil browser görünümünde HTML tam ekran yerleşimi ve overlay kontrolleri aktif görünümle aynı çalışıyor.
- Renderer `load-retry` ve `html-full-screen` ana süreç olaylarını işliyor.
- Site izinlerinde eksik/`ask` değer artık fullscreen ve clipboard-write için sessiz `allow` olmuyor.
- `BrowserNoteStore` kurtarma yazımı hata verirse constructor çökmüyor; hata görünür kalıp sonraki başarılı flush'ta temizleniyor.
- Çeviri arşivi yeni revizyonu atomik indeksledikten sonra aynı kaydın eski JSON/display çiftini dar dizin ve ad denetimiyle siliyor.
- Oturum kapsamlı indirmeyi sekme kaynağı gibi gösteren ölü `downloadActive` koruması kaldırıldı.
- Çeviri scheduler geçerliliği scheduler sahipliği, sekme kuşağı ve sabit temel medya kimliğiyle korunuyor; ilk stream kimliği evlat edinmesi geçerli işi bayat saymıyor.

## Test düzeneği düzeltmeleri

- `watch:start` testi değişken adını değil kanonikleştirme + gerçek dizin davranışını doğruluyor.
- İzleme kütüphanesi kaynak-parçası testleri gerçek `SubtitleFileAccess` bağımlılığını alıp yalnız kendi sentetik dosyalarını grant ediyor. Kalıcı/içe aktarılmış `subtitlePaths` otomatik grant edilmiyor; B83 güvenlik sınırı korunuyor.
- Electron köprü smoke'u zoom sıfırlamasından sonra gecikmiş resize'ın bitmesini bekliyor. Link-hints başarısızlığında click/katman/etiket durumu tek çağrıda görünür.

## Değişen dosyaların amacı

- `src/browser-translation-scheduler.js`: dar kapsamlı hata yeniden denemesi.
- `src/browser-preload.js`, `src/browser-ass-renderer.js`: bfcache yaşam döngüsü.
- `src/browser-site-permissions.js`: fail-closed `ask` varsayılanı.
- `src/browser-note-store.js`: kurtarma yazımı hata dayanıklılığı.
- `src/browser-translation-archive.js`: eski revizyon temizliği.
- `src/browser-tab-resources.js`: ölü indirme bayrağının kaldırılması.
- `src/main.js`: split tam ekran, scheduler medya geçerliliği ve olay yerleşimi.
- `src/renderer/renderer.js`: eksik ana süreç olay tüketicileri.
- `tests/report-25-28-regressions.test.js` ve güncellenen hedefli testler: davranış sözleşmeleri ve yanlış pozitif düzeltmeleri.
- `BROWSER_BUG_REPORT_25.md`–`32.md`: bulgu yanı statüleri, ayrıntılı düzeltme/ret/doğrulama kayıtları.

## Doğrulama kanıtı

- `npm test` — **GEÇTİ**, exit 0, son satır `Tüm testler geçti`; Python `test_transcribe.py` **178/178**.
- `npm run test:electron-bridge` — zoom yarış düzeltmesinden sonra **3/3 ardışık koşu geçti**.
- `npm run test:soak` — **GEÇTİ**: 400/400 yakalama, 20 ısınma, 50/50 hibernasyon, 0 işlenmemiş Promise reddi; raporlanan 24 kaynak bütçesinin tamamı sınır içinde.
- `backend\\venv\\Scripts\\python.exe -m py_compile backend\\transcribe.py` — **GEÇTİ**.
- `node --check src\\main.js`, `src\\preload.js`, `src\\renderer\\renderer.js` — **GEÇTİ**.
- `git diff --check` — **GEÇTİ**.

## Bağımlılık, kurulum ve şema

- Yeni bağımlılık kurulmadı.
- Kalıcı veri şeması değiştirilmedi.
- Gerçek sağlayıcı/API çağrısı yapılmadı; testler yerel fixture/sentetik veri kullandı.

## Açık ve bilinçli sınırlar

- B46, B60, B64, B66 ve B70 rapor 30'da “kısmen doğrulandı · sertleştirildi” olarak bırakıldı. Bunlar kanıtlanmış mevcut hata diye gizlenmedi; kalanlar daha geniş abort/event-context/mutasyon/izole-dünya mimarisi gerektiriyor.
- Raporlarda reddedilen dört ürün iddiası ve test varsayımı retleri kod değişikliğiyle gizlenmedi; ret gerekçeleri ilgili dosyalarda korunuyor.
- Kullanıcının gerçek profil, gizli anahtar veya kişisel altyazı içeriği testlere/Git'e eklenmedi.
