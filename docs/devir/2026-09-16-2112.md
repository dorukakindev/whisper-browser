# Devir Notu — 2026-09-16 21:12 · Rapor 51 A-sınıfı 56 bulgunun uygulanması

## Özet

`BROWSER_BUG_REPORT_53.md`'nin A sınıfında doğrulanan **56 gerçek bulgu** beş kademede
uygulandı; tam test paketi yeşil. B sınıfı (26 dar-kapsamlı) ve reddedilen 17 bulguya
dokunulmadı; 16 repro-bağımlı bulgu için ürün kodu değişmedi.

- Başlangıç kod commit'i: `091eaf6` ("docs: record usage guide handoff")
- Bitiş kod commit'i: `469724cb919589656ef8810b7c9b9fe39cb0da6d`
  ("fix: apply 56 verified findings from browser audit report 51 (class A)")
- Dal: `master` → `https://github.com/dorukakindev/whisper-browser`

## Kademe 1 — veri kaybı ve güvenlik (P1/P2)

- **R51-01** `workspace-package.js`: içe aktarılan `browser-session.json`'daki
  altyazı yolları paket dışı disk yoluna işaret edebiliyordu; `portableSessionTab`
  kuralının karşılığı olarak paket dışı altyazı referansları import'ta temizleniyor.
- **R51-02** `settings-security.js` + `main.js`: endpoint kimliği değişince API
  anahtarı/miras sessizce yeni uca taşınıyordu. `endpointIdentity` artık preset,
  `customBaseUrl` ve `ui.translate*` fallback'lerini de kapsıyor; kimlik farklıysa
  sır mirası kesiliyor ve `settings:import` onay diyaloğu endpoint farkını bildiriyor.
- **R51-03** `main.js`: yakalama flush sırası — bekleyen parçalar diske inmeden
  sekme kapanıp veri kaybolabiliyordu; kapanış öncesi drain/onay akışı bağlandı.
- **R51-04** `main.js`: live-ASR `stdin`'e `error` dinleyicisi (EPIPE çökmesi).
- **R51-05** `renderer.js`: cue editörü hedefi `cueEditTarget` ile açılışta
  sabitleniyor; sekme/gezinme sırasında yanlış dosyaya yazım engellendi; kapanış
  ve medya reset yollarında temizleniyor.
- **R51-39** `main.js` + `renderer.js`: `media:readSubtitle` mtime/size döndürüyor;
  `media:writeSubtitle` `expect` statı ile disk-tazeliği doğruluyor — okuma ile
  yazma arasında dosya değiştiyse yazım reddedilip uyarılıyor. Bulk yazım yolu
  (`writeSubtitleBulkFiles`) dahil tüm yazıcılar damga taşıyor.

## Kademe 2 — ölü/kırık özellikler

- **R51-06** `main.js`: AVIF/WebP manga görselleri `nativeImage`'in çözemediği
  için sayfa içi canvas decode fallback'ine düşüyor.
- **R51-17** `browser-cea-captions.js` + `main.js`: HLS byte-range CEA segmentleri
  `Content-Range` yanıt başlığıyla eşleştiriliyor (`matchHlsCeaSegmentUrl(url,
  matchers, response.headers)`).
- **R51-18** aynı bölge: `EXT-X-GAP`/`SKIP` parçaları gap bayrağı taşıyor;
  ardışık-gap'li parçalar tamlık hesabına katılmıyor.
- **R51-23** `renderer.js`: sekme durum kopyası `lifecycle`, `unloadedAt`,
  `keepAwake` vb. yaşam-döngüsü alanlarını düşürüyordu → "boşalt" komutu hiç
  aktifleşemiyordu. Kopyalama tamamlandı.
- **R51-26** `main.js`: `explainTranslation` (player.sub2Path) altyazı yetki
  döngüsüne eklendi.
- **R51-27** `browser-subtitles.js`: `listFolder` `maxDepth:0` ile çağrıldığı için
  kardeş taraması hep boş dönüyordu → `maxDepth:1` (sınır korunarak).
- **R51-34/35** `renderer.js`: yerel altyazı ayrıştırıcı VTT blok etiketlerini
  atlıyor, ASS çekme etiketlerini sıyırıyor; SRT tam-serileştirme yolunda
  düzenlenmemiş cue'ların ham metni korunuyor (biçim kaybı yok).
- **R51-37** `renderer.js`: browser modunda G/H ofset tuşları ve kaydırıcı
  `player.offset`/medya durumuna yazmak yerine `browserSyncEnsurePreview`
  üzerinden sekme ofsetine gidiyor — kalıcı medya ofseti kirlenmiyor.

## Kademe 3 — yaşam döngüsü

- **R51-21** `main.js`: ana pencereye `render-process-gone`/`unresponsive`
  kurtarma handler'ları eklendi (tarayıcı wc deseninin ana pencere karşılığı).
- **R51-30** `main.js` + `renderer.js`: meşgul-ret'lerine yapısal `busy: true`
  alanı eklendi; renderer artık Türkçe hata metni regex'ine bel bağlamıyor
  (metin fallback'i daraltıldı).
- **R51-31** `renderer.js`: `addToQueue` aynı kök-adlı farklı dosyaların
  çıktılarını benzersiz sonekle ayırıyor (`name (2).srt` çakışması yok).
- **R51-33** `queue-persistence.js`/`renderer.js`: kuyruk öğelerinde `watchSource`
  normalize edilip korunuyor — watch-folder kaynağı yeniden tarama dedupe'unda
  eşleşiyor.
- **R51-44** `renderer.js` + `main.js`: sekme kapatma, boşaltmanın
  `browserTabProtectionReasons` kararlarını `requiresConfirmation` olarak
  uyguluyor — taslak/form içeren sayfa onaysız kapanmıyor.
- **R51-54** `renderer.js`: `addToQueue` 500 UI sınırı (persistence sınırıyla
  uyumlu); sınır aşımı görünür hata.
- **R51-55** `main.js`: 8 MB üstü kuyruk kaydı sessiz sıfırlanmak yerine
  güvenli şekilde reddediliyor/raporlanıyor.
- **R51-56** `main.js`: `loadHistory` bozuk ana dosyada `history.json.bak`'a
  düşüyor; `saveHistory` yazmadan önce sağlam yedeği koruyor.

## Kademe 4 — doğruluk ve performans

- **R51-12** `main.js`: komut/probe `executeJavaScript` çağrılarına `withTimeout`
  (CEA yakalama, manga canvas decode, DRM probe, `browser:command` üç çağrı).
- **R51-25** `browser-media-tools.js`: `run()` satır-bazlı stderr kancası alıyor;
  `sceneStrip` akan `pts_time:` işaretlerini topluyor — son-100KB kesintisiyle
  sahte `0` sahnesi üretilmiyor. Boş sonuç UI'da güvenli.
- **R51-38** `backend/transcribe.py`: `segment_metrics` chunk başına `start`/`end`
  taşıyor; `compute_quality_report` öncesi mutasyona uğramış entries metrikleriyle
  zaman tabanlı yeniden hizalanıyor (sessiz metrik kaybı kapandı).
- **R51-40** `browser-manga.js`: `extractJsonPayload` aday penceresi sabit sınırlı
  — O(n²) tarama kaldırıldı, gövde boyut sınırı korunuyor.
- **R51-70** `browser-subtitles.js`: transform alanı **var ama bozuksa** overlay
  renderer'la aynı kimlik transform'a düşüyor; yalnız alan yoksa `legacyOffset`
  korunuyor (kalıcı senkron kopması kapandı).
- **R51-72** `renderer.js`: yerel `findCueAt` yarı-açık `[start,end)` + tüm
  örtüşen cue'lar (browser `browserActiveCuesAt` ile aynı semantik; sıralanmamış
  listede erken kesme yok).
- **R51-74** `styles.css`: yerel overlay `max-width`/`overflow-wrap` taşma
  koruması.
- **R51-75** `renderer.js` + `browser-subtitles.js`: bidi/zero-width kontrol
  karakterleri `decodeEntities` sayısal yolunda, iki `cleanCueText`'te ve
  `hashText`'te sıyırılıyor — görünmez enjeksiyon cue kimliğini bölemiyor.

## Kademe 5 — sertleştirme ve kozmetik (25 madde)

- **R51-10** `browser-sponsorblock.js`: tüm videoyu kaplayan (≥%98, ~0'dan başlayan)
  segment auto-skip'ten reddediliyor; `coversWholeVideo` yardımcısı eklendi.
- **R51-41** `browser-manga.js`: manga görsel kimliği `src`'ye bağlı (WeakMap
  src→id geçmişi); `src` değişince eski overlay sökülüyor ve `mangaAttempted`
  yeni görseli engellemiyor.
- **R51-48** `browser-features.js` + `browser-skip-segments.js`: skip kararına
  `player.browserAdPlaying` bağlamı eklendi; reklam sırasında auto-skip kapalı
  (aday gösteriliyor, seek yapılmıyor).
- **R51-49** `backend/live_asr.py`: `stop`/EOF kuyruğu düşürmüyor — kuyruktaki
  parçalar işlenip `chunk_done` yayınlanıyor; son ~9 sn ses kaybolmuyor.
- **R51-51** `browser-media-identity.js`: `normalizeBrowserUrl` `#/...` ve
  `#!/...` route-fragmanlarını kimlikte tutuyor; sıradan fragment'lar hâlâ atılıyor.
- **R51-52/98** `renderer.js`: terminal-guard salma ortak yardımcıya çıkarılıp
  `queue:saveSync` yoluna bağlandı; silinen öğelerin guard'ı temizleniyor.
- **R51-59** `renderer.js`: indirme ve transkripsiyon ETA tabanları ayrıldı
  (`_speedEma`/`_etaBase` resetleri); indirme olayları transkripsiyon ETA'sını
  kirletmiyor.
- **R51-62** `main.js` + `renderer.js`: bayat manga düzenlemesi reddedilince sayfa
  overlay'i otoriter değere geri döndürülüyor ve `manga-edit-rejected` olayı
  kullanıcıya yüzeyleniyor.
- **R51-66** `browser-manga.js`/`main.js`: LQIP placeholder regex'ine `jpe?g`
  eklendi — minik JPEG placeholder artık modele gitmiyor, gerçek görsel deneniyor.
- **R51-80** `main.js`: favicon `session.fetch` ile indirilip `data:` URL'ye
  çevriliyor — CSP `img-src data:` sıkı kalıyor, sayfa-kaynaklı img güvenilmiyor.
- **R51-81** `main.js`: sürükle-bırak girdi-bazlı süzülüyor; tek desteklenmeyen
  dosya tüm grubu reddettirmiyor.
- **R51-85** `renderer.js`: import'ta `playerPositions`/`browserSponsorExemptions`
  belleğe uygulanıyor; workspace açılışında kayıtlı `playbackRate` `browserSavedRate`
  (+`browserSavedRateMediaId` medya-anahtarı bağlı) ile korunuyor — 1x ezmesi yok.
- **R51-88** `renderer.js`: `cancelTooLate` exit/terminal yoluna bağlandı — `done`
  üretildikten sonra gelen iptal "çıktılar yazıldı" bilgisiyle raporlanıyor.
- **R51-89** `pdf-translate.js` + `main.js`: PDF kimliği ilk 1 MiB + son 256 KiB
  kuyruk örneğiyle SHA-256 — aynı ilk megabaytı paylaşan dosyalar ayrışıyor.
- **R51-90** `main.js`: `watch:start` numaralandırma kanonik `target` ile.
- **R51-92** `browser-feature-services.js`: seri-kapsamlı atlama kayıtlarında
  yetim kayıtlar görünür+silinebilir; son üye ayrılınca kayıtlar yeni seriye
  taşınıyor; silme sahiplik denetimi genişletildi.
- **R51-94** `renderer.js`: `queueInputKey` yolu kanonikleştiriliyor
  (`..` çözümü, sürücü-harf normalizasyonu; kök pop korumalı) — aynı fiziksel
  dosya farklı yazımla iki kez kuyruğa giremiyor.
- **R51-97** `renderer.js`: `addToQueue` ret'leri ayırt edilebilir neden taşıyor;
  watch hata sayacı gerçek hataları sayıyor.
- **R51-101** `browser-manga.js`: manga çeviri metin sınırı üç noktada 4000'de
  birleşti (depolanmış çeviri yeniden yüklemede 1200'e kısalmıyor).
- **R51-108** `main.js`/`browser-manga.js`/`styles.css`: overlay ve kartlara
  `dir="auto"` + bilinen `lang`; kartlara `overflow-wrap`.
- **R51-109** `renderer.js`: `renderCue` else kolu ve `copyCue` davranışı
  düzeltildi.
- **R51-111** `renderer.js`: SRT/VTT zaman regex'leri satır başına anchor'landı
  (VTT satır-sonu ayarları serbest); ASS alanı tam eşleşme.
- **R51-113** `tests/run-all.js`: `spawnSync`'e dosya başına 10 dk timeout —
  tek asılı test CI'yı sonsuz kilitlemiyor.
- **R51-114** `CLAUDE.md`: "Python yoksa atlanır" ifadesi gerçek davranışla
  (eksik doğrulama bildirerek başarısız) uyumlu hale getirildi.

## Testler

- Yeni regresyon dosyası: `tests/report51-kademe-fixes.test.js` (13 test —
  endpoint kimliği kapısı, bidi temizliği, transform fallback'i, JSON aday
  sınırı vb.).
- Yeni backend testi: `backend/test_live_asr.py` (stop-drain davranışı).
- Güncellenen testler (yeni sözleşmeye uyarlananlar): `browser-capture-queue`
  (regex penceresi), `browser-direct-media-navigation` (withTimeout stub),
  `browser-sponsorblock` (tam-video reddi), `browser-subtitles` (maxDepth:1 +
  CEA headers argümanı), `history` (.bak reset), `pdf-translate` (kuyruk örneği),
  `report-29-31-regressions` (explain auth), `subtitle-find-replace-integration`
  (stat helper stub'ları), `browser-skip-segments` (reklam bağlamı).
- `tests/run-all.js`: dosya başına 10 dk `spawnSync` timeout — asılı test
  CI'yı kilitlemesin.
- **Sonuç:** `node tests/run-all.js` tam paket — **tüm testler geçti**
  (161+181+144+… JS; Python tarafı 5+3+16+… dahil `test_transcribe` 181/181).
- `node --check` tüm değişen JS dosyalarında temiz; `python -m py_compile`
  `transcribe.py`, `live_asr.py`, `test_live_asr.py` temiz.

## Sınırlar / notlar

- B sınıfı 26 dar-kapsamlı bulgu bilinçli olarak bu tura dahil edilmedi;
  `BROWSER_BUG_REPORT_53.md` kapsam notlarına göre ayrıca ele alınmalı.
- 16 repro-bağımlı bulgu için deterministik Electron repro'su hâlâ gerekli.
- Elektron/E2E doğrulaması yapılmadı; tüm doğrulama kaynak-sözleşme testleri,
  VM harness ve birim testleriyle.

## Git

- Kod commit'i: `469724cb919589656ef8810b7c9b9fe39cb0da6d` — 33 dosya,
  +1292/−167; `backend/test_live_asr.py` ve `tests/report51-kademe-fixes.test.js`
  yeni.
- Dokümantasyon commit'i: bu dosya + `DEVIR-NOTU.md` indeks satırı.
- Push hedefi: `origin` = `https://github.com/dorukakindev/whisper-browser` (master).
