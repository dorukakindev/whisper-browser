# Devir notu — 2026-09-18 19:47 — Gerçek YouTube OAuth (SmartTube cihaz-akışı)

## Özet

Kullanıcının "Invidious değil, SmartTube gibi gerçek YouTube" talebi **A — OAuth cihaz kodu** seçeneğiyle uygulandı. Yeni `backend/youtube.py` (OAuth device flow + InnerTube browse), main.js'te `youtube:*` IPC ailesi + `SafeSecretStore` kalıcılık, preload köprüsü ve SmartTube UI entegrasyonu (sidebar düğmeleri + 3 görünümlü modal + subscriptions'ın gerçek YouTube verisine geçişi).

## Başlangıç/Bitiş

- **Başlangıç commit:** `11d44b688bee3d35a1d4ee7a48d2e4e05e807db1` (R68 push'lu HEAD)
- **Bitiş kod commit:** bu devir notundaki kod commit'ine bakın (aşağıda)
- **Push:** origin/master'a yapıldı

## Değişen dosyalar

| Dosya | Amaç |
|---|---|
| `backend/youtube.py` (yeni) | OAuth device_code/poll/refresh/revoke/me + InnerTube browse; NDJSON; gizli değerler yalnız env |
| `backend/test_youtube.py` (yeni) | 18 mock'lu unittest |
| `src/main.js` | `mediaJobs.youtube` slotu, `YOUTUBE_RESULT_TYPES`, `youtubeSession` + `SafeSecretStore` (`youtube-session.safe.json`), `runYoutubeCommand`, `ensureYoutubeAccessToken`, 7 `youtube:*` handler |
| `src/preload.js` | `youtubeSession/SetClient/DeviceCode/Poll/Browse/Logout/Cancel/onYoutubeEvent` |
| `src/renderer/index.html` | `stYtLoginBtn`/`stYtLogoutBtn` + `youtubeLoginModal` (client/cihaz-kodu/bağlı görünümleri) |
| `src/renderer/renderer.js` | YouTube oturum durumu, modal akışı, subscriptions → `youtube:browse('FEsubscriptions')`, statü `YouTube: <kanal>` |
| `src/renderer/styles.css` | `.yt-user-code`, `.yt-device-code-wrap`, `.st-yt-icon` |
| `tests/report70-youtube-oauth.test.js` (yeni) | 19 wiring/güvenlik testi |
| `tests/adversarial-ipc.test.js` | sandbox mock listesine `restoreYoutubeSession` |
| `tests/electron-smarttube-boot.smoke.js` | `youtube:session`/`youtube:browse` mock + 6 yeni YouTube probu |
| `BROWSER_BUG_REPORT_70.md` | Öz-denetim raporu (R70-K1..K4 commit-öncesi buglar dahil) |

## Commit-öncesi bulunan ve düzeltilen gerçek buglar

1. **R70-K1 (kritik):** `youtube:poll` `onEvent` `login` emit'ini (access+refresh token içerir) `youtube:event` ile renderer'a forward ediyordu → yalnız `log` olayları iletiliyor.
2. **R70-K2:** `verification_url` https'e indirgendi (`javascript:` şeması taşınamaz).
3. **R70-K3:** `proc.on('error')` `err.message` → Türkçe sınıflı mesaj (ytdlp-runtime sözleşmesi).
4. **R70-K4:** `restoreYoutubeSession()` init sırası `restoreBrowserSessionState()` öncesine taşındı — `restoreInvidiousSessions()`'ın da bozduğu `restoreBrowserSessionState→createWindow` bitişikliği birlikte düzeldi (browser-foundation testi yeşillendi).

## Testler

- `report70-youtube-oauth` **19/19** · `backend/test_youtube.py` **18/18** · wiring 66/66 · bridge 21/21 · adversarial 15/15 · design-system OK · backend toplam **143/143**
- Electron smoke **GEÇTİ** — gerçek DOM'da 6 yeni YouTube probu dahil tümü true
- `npm test`: kalan kırmızılar `report62`, `report63-secret`, `report63-ssrf` — başka iş akışının commit'siz WIP'i (önceki notlarda belgeli), dokunulmadı

## Sınırlar / kullanıcı için kurulum

- **Canlı Google akışı mock düzeyinde doğrulandı** — gerçek cihaz kodu onayı ve canlı abonelik feed'i kullanıcı ortamında denenmeli (`start.bat`).
- **Kurulum:** Google Cloud Console → YouTube Data API v3 etkin → OAuth consent → "TV ve sınırlı girişli cihazlar" tipinde client → Client ID + Secret'i modal'a gir → `google.com/device`'ta kodu onayla.
- UI'da yalnız **subscriptions** gerçek YouTube verisine geçti; `FEwhat_to_watch/FElibrary/FEhistory/VLWL/VLLL` backend'de hazır, UI bağlantısı sonraki iş.
- Invidious yolu korunuyor (public feed + fallback).

## Bu notla birlikte teslim edilen önceki-tur artefaktları

- `BROWSER_BUG_REPORT_69.md` + `docs/devir/2026-09-18-1930.md` + `docs/BROWSER_SESSION_BUG_REPORT.md` + `docs/security-audit-report-2026-09.md` + `docs/bug-reports/BROWSER_BUG_REPORT_29_subtitle-pipeline.md` — önceki turun salt-okunur audit çıktıları (kod değişmedi), bu commit'le arşivleniyor.
