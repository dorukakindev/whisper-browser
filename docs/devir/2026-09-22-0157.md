# Devir Notu — 2026-09-22 01:57 (Europe/Istanbul)

## Kapsam

Altı görevli paketin **T1'i**: altyazı–görüntü zaman doğruluğu. Zaman-oraklı yerel fixture kuruldu; kaynak/çeviri SRT, video zamanı ve gerçekten görünen overlay metni aynı çizgide ölçüldü. Üç üretim hatası kırmızı test → düzeltme → gerçek Electron kanıtıyla kapatıldı. Tam rapor: `BROWSER_BUG_REPORT_98.md`.

## Dal / commit kimlikleri

- Repo: `dorukakindev/whisper-browser`, dal `devin/t1-timing-oracle`
- Taban: `origin/master@4453ae0` (PR #16 merge sonrası; `git fetch` ile güncel çekildi)
- Başlangıç HEAD: `4453ae0` — bitiş kod commit'i: (bu notun commit'inden önceki `fix:` commit'i)

## Değişen dosyalar ve amaçları

- `src/browser-overlay-controller.js` — üç düzeltme + teşhis genişletmesi:
  - F-98-1: `queueFrame` artık bekleyen `boundaryTimer`'ı temizleyip sınırı güncel `currentTime`/`playbackRate` ile yeniden planlıyor (seek/ratechange sonrası saniyelerce bayat cue kalıyordu).
  - F-98-2: sınır callback'ine 400 ms `frameFallbackTimer` — rVFC/rAF kare açlığında doğrudan `render()` (`frameKind:'fallback'`); `cancelFrame` ikisini de temizler.
  - F-98-3: sınır seçimindeki `+ .003` epsiloni ve `-12 ms` erken ateşleme payı kaldırıldı; erken düşen çizim artık aynı sınırı yeniden kurar, cue aralığı atlanamaz.
  - `diagnostics()`: `fallbackRenders`, `lastRenderVideoTime`, `armedBoundary/DelayMs/At`, `frameKind`, `mediaTime`, 100 ms `timerProbeDelayMs` ölçümü.
- `tests/browser-overlay-timing.test.js` (yeni) — vm harness: gerçek `buildBrowserOverlayScript` + sahte DOM; 5 senaryo (geri/ileri seek, ratechange, pause, sınır-epsilon) + sanal saatli sapma-dağılımı ölçümü (`wrongCueMs`, `missingMs`, `correctionLatencyMs`).
- `tests/electron-overlay-timing.smoke.js` (yeni) — gerçek Electron: ffmpeg `testsrc2` fixture'ı yerel HTTP'den, izole profilde (`WHISPER_RESOURCE_SOAK_USER_DATA`), oynatma sırasında geriye seek + ~200 ms örnekleme + izole dünya 999 teşhisi.

## Kanıtlar

- Kırmızı→yeşil: epsilon testi eski kodda `delay=3991 ms` ile düşüyordu; düzeltme sonrası ~0 ms. Geriye-seek testi eski kodda 3988 ms bayat gecikme gösteriyordu.
- Gerçek Electron: `electron tests/electron-overlay-timing.smoke.js` — 4 ardışık koşuda 23 örnek 0 uyumsuz; karşıt-kanıt olarak eski kod aynı koşuda 10 uyumsuz (~2000 ms yanlış cue) verdi.
- `npm test` tamamı yeşil; `npm run test:electron-bridge` geçti (dbus bağlantı hataları bu Linux ortamının gürültüsü).

## Çalıştırılmayan / sınırlar

- HLS/DASH/CEA protokol fixture'ları ve 30 dk drift ölçümü T2'nin konusu; bu PR yalnız overlay zamanlama çekirdeğini kapatır.
- Kare üretmeyen sayfada overlay'in son cue'su donuk kalır (tasarım); fallback yalnız sınır callback'i tetiklendikten sonra devrededir.
- Yeni bağımlılık yok; fixture `.uiprev/` altında üretilir (Git'e girmez).

## Sonraki adımlar

- T2–T6 ayrı dallarda: tam-yakalama eksiksizlik kanıtı, TR çeviri kalite/maliyet korpusu, SmartTube OAuth dayanıklılığı, crash/bütünlük/perf enjeksiyonu, temiz-makine repro.
