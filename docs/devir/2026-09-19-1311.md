# Devir notu — 2026-09-19 13:11

## R76 tarayıcı-tarafı GitHub özellik araştırması (kod değişikliği YOK)

**Repo/dal:** `https://github.com/dorukakindev/whisper-browser` · `master`
**Başlangıç kod:** `b3da67a` → çalışma sırasında paralel oturumun commit'leri geldi · **Bitiş kod:** ürün kodu bu oturumda DEĞİŞMEDİ (kullanıcı talimatı: bu turda düzeltme yok; ayrıca `renderer.js/index.html/styles.css/ui-locale.js` başka oturumun WIP'i — commit'e dahil edilmedi).

## Ne yapıldı

Kullanıcı R75'in SmartTube/YouTube odağını daralttı: **tarayıcının kendisine** eklenebilecek özellikler için 15+ repo/platform API'si kaynak düzeyinde incelendi → `BROWSER_FEATURE_RESEARCH_76.md`.

**Kritik envanter bulgusu:** Browser tarafı zaten çok olgun — ~90 `browser-*` modülü (adblock+cosmetic, reader, page-translate, CEA/DASH capture, live-ASR, mini-player, komut paleti, link-hints, sekme grupları, OpenSubtitles, MHTML, full-page screenshot, reopen-closed-tab). `package.json` **castlabs ECS v43.2.0+wvcus** kullanıyor → Widevine zaten entegre (eksik: EVS üretim imzalama).

**İncelenen repolar:** jellyfin/jellyfin (TrickplayManager, PR#9554, #11336 keyframe-hızlandırma) · intro-skipper/intro-skipper (4 algılama yöntemi) · ClearURLs/Addon+Rules (LGPL-3, provider kataloğu) · cavi-au/Consent-O-Matic (200+ CMP kuralı; lisans NOASSERTION) · minbrowser/min (userscripts wiki, passwordManager/keychain+CSV) · thibauts/upnp-mediarenderer-client + @edenware/dlnacasts (DLNA+subtitlesUrl) · GPMDP/electron-chromecast (bakımsız) · WICG/spatial-navigation polyfill · electron extensions.md + issue #39633 (documentPiP bozuk) + #37876 (MV3 scripting) · ghostery/adblocker-electron · Diaoul/subliminal (çok-sağlayıcı altyazı) · castlabs/electron-releases wiki (EVS).

**Doğrulanmış gerçek boşluklar (grep ile yokluğu kanıtlı):** trickplay yerel önizleme, incognito/konteyner sekmeler, partition proxy, ClearURLs, element-picker, lite-mode, moviehash+subliminal, intro/jenerik atlama, DLNA cast, userscript yöneticisi, Whisper sesli giriş, video filtreleri, uyku zamanlayıcısı, sekme ses kaydı, otomatik PiP, printToPDF, QR, turndown-markdown, çevrimdışı okuma listesi, mobil emülasyon, DDG bangs, TTS, spatial nav, electron-updater, SSB, sekme önizleme, duplicate/close-others menüsü.

## Dosyalar

- `BROWSER_FEATURE_RESEARCH_76.md` — **yeni** — repo bazında desen/lisans/uygulanabilirlik + S/A/B/C öncelik matrisi + reddedilenler.

## Doğrulama

- Ürün kodu değişmedi → test koşulmadı (belge-only; AGENTS.md beyanı).
- Yokluk kanıtları: `incognito|sleepTimer|qrcode|chromecast|dlna|mpv|duplicate.*tab|printToPDF` grep → 0 eşleşme; `moviehash` browser-subtitle-search'te yok; `captureBeyondViewport` zaten var (listeden çıkarıldı); `documentPictureInPicture` Electron#39633'te bozuk → reddedildi.

## Sınırlar / sonraki adım

- Öneriler tasarım düzeyi; implementasyon bu turda yok. S-grubu ~12 ucuz özellik ilk sprint adayı.
- Paralel oturumun değişiklikleri (`renderer.js`, `index.html`, `styles.css`, `ui-locale.js`, smoke test, `.freebuff/`, `_repro/`, `2026-09-19-10{40,100}.md`) bu commit'e dahil EDİLMEDİ.
