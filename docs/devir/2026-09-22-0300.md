# Devir Notu — 2026-09-22 03:00 (Europe/Istanbul)

## Konu
Altı-görev paketinin T3'ü: TR çeviri kalitesi ve token maliyeti — sabit altın korpus, deterministik mock sağlayıcı, yeniden-çeviri sayaçları. Bulunan gerçek hata F-100-1 (birleşik sayı kapısı atlaması) kırmızı test → dar düzeltme → yeşil ile kapatıldı.

## Repo / dal / SHA
- Repo: https://github.com/dorukakindev/whisper-browser · dal `devin/t3-translation-quality`
- Başlangıç: `4453ae003ee092b33d06a802535b65b460169e31` (origin/master)
- Bitiş kod SHA: ayrı kod commit'inde; rapor `BROWSER_BUG_REPORT_100.md`.

## Değişen dosyalar ve amaç
- `backend/golden-corpus-tr.json` — 56 girdi/60 cue elde yazılmış sentetik EN→TR korpus (özel ad, sayı, olumsuzluk, zamir, hitap, cümle sınırı, SDH, CPS, bağlam, modal, para, birim, tarih).
- `backend/test_golden_corpus.py` — korpus testi: 0 yanlış-pozitif, katman bazlı kusur metriği, cache-key bağlam ayrımı, `llm_translate` üzerinden sahte OpenAI ile tam boru + kusur geçiş davranışı.
- `backend/sentence_translation.py` — `_number_preserved`: TR sayı kelimesi eşleşmesi artık devam-eden sayı kelimesiyle (ör. `kırk beş`) reddedilir.
- `src/subtitle-sentence-layout.js` — aynı düzeltme JS aynasında (`TR_NUMBER_WORDS` devam kontrolü).
- `src/browser-translation-scheduler.js` — `stats={providerRequests,cacheHits,cacheMisses}`: `translateShared`/`start` içinde artım; `emitState`/`snapshot` taşır.
- `src/browser-translation-integrity.js` — `summarizeTranslationIntegrity` dönüşüne `providerRequests/cacheHits/cacheMisses`.
- `src/renderer/renderer.js` — sinyal satırına `· önbellek H/L · N API çağrısı` (EN `cache`/`API calls`).
- `tests/browser-translation-retranslation-count.test.js` — 5 sayım testi: restart-persistence (gerçek fs cache), reconcile no-op, retryFailed selektifliği, single-flight, stats yüzeyi.
- `tests/electron-browser-subtitle-gauntlet.smoke.js` — gerçek Electron'da `diagnostics.translation` sayaçlarının sahte sağlayıcı isteğiyle eşitliği doğrulandı.

## Kanıtlar
- Kırmızı→yeşil: F-100-1 `Kırk beş` testi düzeltmeden önce `found=[]`/True idi; sonrası `number_mismatch`+False. Ters yönde de sıkı (`12` vs `on iki bin` reddedilir).
- Gerçek Electron: gauntlet smoke PASS; `report.translation.diagStats={providerRequests:1,cacheMisses:1,cacheHits:0}`.
- `npm test` tam paket yeşil; `npm run test:electron-bridge` yeşil; `node --check` + `py_compile` temiz.

## Sınırlar
- Zamir/bağlam kusurları kapılarca yakalanmıyor (bilinçli FAIL-OPEN; raporda sayılandı).
- Canlı ücretli sağlayıcı koşulmadı; kalite iddiası yapılmadı.
- Devir notları T1/T2 dallarında ayrı girildi; indeks birleşim sırası kullanıcıda.

## Sonraki adımlar
- T4 (SmartTube OAuth gauntlet), T5 (çökme/bütünlük/perf), T6 (temiz-makine kurulum) ayrı dallarda.
