# Devir Notu — 2026-09-21 03:08 (Europe/Istanbul)

## Repo / dal / durum
- Repo: dorukakindev/whisper-browser · Dal: `devin/fix-translation-refresh-index` (taban `origin/master`)
- Başlangıç kod commit'i: `7a45133` (master HEAD — F1 düzeltmesi `23f6f22` + skill PR'ları dahil)
- Bitiş kod commit'i: bu notun yazıldığı andaki `backend/transcribe.py` değişikliği commit'lenecek; tam hash `git log` ile doğrulanır.
- Bağlam: Kullanıcı LLM sağlayıcı key'i (shuaiapi, claude-sonnet-5) verdi; YouTube → transcribe → TR çeviri boru hattı uçtan uca test edildi.

## Hata / kök neden / kanıt
- **Bulgu (UI doğrulamasında yakalandı):** 2. geçiş (refine) sonrası canlı önizlemede bayat 1. geçiş metni kalıyordu. Dosya doğruydu (`jfk.tr.srt` → "isteyin"), ekran "isteyeyin" gösteriyordu.
- **Kök neden:** `backend/transcribe.py` içindeki üç `translation_refresh` emit'i `{"start","end","text"}` gönderiyor, `index` yok. Renderer `applyPreviewTranslations` (renderer.js:1368) indeks yoksa zaman anahtarına düşer ve aynı zamanlı blokları (`timeCounts>1`) atlar → revize metin o segmente ulaşmaz.
- **Düzeltme:** üç emit'e de `"index": i` eklendi (entries/previewSegs sırasıyla birebir eşleşir). İlerleyici `translation_chunk` zaten `index` taşıyordu; sözleşme tutarlı hale geldi.
- **Kanıt:** `--translate-only` çalıştırmasında emit edilen `translation_refresh` segments'inde `index: 0..11` görüldü; `npm test` tamamı geçti (son satır: "Tüm testler geçti"). Backend py_compile temiz.

## Bu turda doğrulanan boru hattı (gerçek ağ + gerçek key)
- yt-dlp ile YouTube indirme ÇALIŞIYOR (146 sn JFK clipsi indi; önceki oturumdaki bot-check geçiciydi).
- Backend E2E: `transcribe.py` jfk_yt.wav → tiny/cpu 12 blok → claude-sonnet-5'e TR çeviri 12/12, 20.7 sn. Çeviri kalitesi yüksek; `--translate-refine` ikinci geçişi 10/12 bloğu ölçülü şekilde iyileştirdi.
- UI E2E (test ajanı, kayıtlı): YouTube URL'si yapıştır → app içi yt-dlp indirme → 22 segment → TR çeviri + refine → `.srt`/`.tr.srt`/`.dual.srt` yazıldı.

## Çalıştırılanlar / çalıştırılmayanlar
- Koşuldu: `npm test` (hepsi geçti), `py_compile`, gerçek translate-only NDJSON doğrulaması.
- Koşulmadı: Electron smoke'ları (ayrı koşulur); GPU/Windows yolu bu Linux kutusunda yok.

## Bilinen kalan işler (düzeltilmedi — kayıt altında)
- Log paneline sızan yt-dlp `[download]` ham NDJSON satırları (kozmetik).
- Linux'ta safeStorage backend olmadığı için translate/LLM API key'i yeniden başlatmada kalıcı değil (Windows'ta DPAPI ile sorun beklenmez).
- İlk provider probe 401 dönüp key yeniden yazılınca 200 oldu — alan değeri glitch'i olabilir, kesin kök neden belirsiz.
- 2. geçişte `meaning_model_missing=1` uyarısı ve `1 blokta hata (1. geçiş korundu)` — shadow-evaluation koruması çalışıyor, blok kaybı yok.

## Değişen dosyalar
- `backend/transcribe.py` — üç `translation_refresh` emit'ine `index` alanı eklendi (kod commit'i).
- `.agents/skills/testing-whisper-browser/SKILL.md` — Translate kartı + YouTube yolu bölümleri (dokümantasyon; ayrı commit).
- Bu not + `DEVIR-NOTU.md` indeksi (dokümantasyon; ayrı commit).
