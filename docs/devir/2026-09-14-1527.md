# 2026-09-14 15:27 — Çalışma paketi hata düzeltmeleri

## Kimlik
- Repo: https://github.com/dorukakindev/whisper-browser; dal master.
- Başlangıç HEAD: `febe8ef8cddf1b604091cbd18fdf1c1590f022f4`, çalışma ağacı temiz. Başlangıç ürün kodu `9ceb5c56bc76950042bb7094f5f821315ee5bd41`.
- Bitiş ürün kodu: `1da1739e821f46e541aa9a3125f947dbb754fd6a`.
- Kullanıcı somut bug arama/düzeltme istedi. Katalog ve browser çalışma paketinin geri yükleme akışına odaklanıldı. Yeni özellik/bağımlılık kurulmadı; Proxifier açılmadı; alt ajan kullanılmadı.

## Üç hata ve düzeltme
1. Yol eşlemeleri art arda split/join uyguluyordu. Yeni hedef eski kökün altındaysa önceden dönüştürülmüş hedef ikinci kez dönüştürülüyordu: örneğin profile/restored/workspace-videos yolu profile/restored/restored/workspace-videos olabiliyordu. `src/workspace-package.js` özgün metin üzerinde tek geçiş, en uzun eşleşme ve ilk eşleme önceliği kullanır. JSON anahtarları da aynı şekilde işlenir. Regresyon, eski HEAD modülünü ayrıca yükleyerek önceki hatayı ve yeni sonucu karşılaştırdı.
2. Pakette bulunmayan renderer taslak/tercih anahtarları geri yüklemede atlanıyordu; hedef profildeki eski düzeltmeler yeni çalışma ile karışabiliyordu. Aynı dosyada izinli STORAGE_KEYS için eksik değerler açık null olarak döner; mevcut uygulama işlemi bunları siler. Ön yedek mevcut değerleri korur. Hata halinde önceki localStorage geri alınır. Genel uygulama ayarları bu listenin dışındadır.
3. `src/catalog-extensions.js` video çıkarma sonrasında renderer okuma/ön yedekleme/eşleme hatalarını temizlik try bloğu dışında bırakıyordu. Ayrıca renderer geri alma hatası video temizliğini atlıyordu. Ön hazırlık artık aynı hata kapsamındadır, localStorage yalnız başarıyla değiştirilmişse geri alınır ve çıkarılan video klasörü finally ile temizlenir. Renderer kapanmışsa yerel depolamayı geri yükleme garanti edilemez; video temizliği yine çalışır ve işlem başarı sayılmaz.

## Dosyalar
- `src/workspace-package.js`: tek geçişli dosya yolu eşleme, eksik renderer anahtarlarını temizleme.
- `src/catalog-extensions.js`: geri yükleme hata kapsamı ve çıkarılmış video temizliği.
- `tests/workspace-restore-regressions.test.js`: gerçek geçici dosyalarda yol dönüşümü; gerçek apply handler ile kontrollü renderer okuma, ön yedek, çekirdek restore ve renderer rollback arızaları; başarılı restore sırasında eski taslak temizlenmesi. Hata senaryolarında restart yapılmadığı ve yeni video klasörünün kaldırıldığı doğrulanır.
- Bu not ve `DEVIR-NOTU.md`: ayrı dokümantasyon commit'i, önceki notlar korunur.

## Doğrulama
Geçti:
- `node tests/workspace-restore-regressions.test.js`
- `node tests/catalog-roadmap.test.js` (gerçek video/font/altyazı paket roundtrip dahil)
- `node tests/media-catalog-service.test.js`
- `npm run test:electron-bridge`
- Electron ile `tests/electron-catalog-roadmap.smoke.js --disable-gpu`: metadata koruma, sezon/takvim, paket dışa aktarım/önizleme, belirteç temizleme, hata ve klavye/dar ekran.
- Değişen iki ürün JS'i ve yeni test için node --check; src/main.js, src/preload.js ve src/renderer/renderer.js sözdizimi; backend/transcribe.py py_compile; git diff --check.

Ürün arayüzünün tasarımı/DOM/stili değişmedi; yeniden görsel tasarım incelemesi yapılmadı. Kullanıcı profili ve gerçek medya kullanılmadı. Gerçek uygulama kapanıp yeniden başlatılarak paket restore denenmedi; çekirdek gerçek dosyalarla, hata akışı kontrollü servis testiyle doğrulandı. Canlı sağlayıcı testi yok.

Tam npm test bu turda tekrar çalıştırılmadı. Önceki 2026-09-14-1509 notundaki beş başarısız test dosyası çözülmüş değildir; tam paket yeşil iddiası yoktur. Bunların test bağlamı/kaynak kesiti sorunları ayrı bakım konusudur.

## Teslim
Ürün kodu yukarıdaki hash ile commit edildi. Not/indeks ayrı commit edilip normal origin/master push yapılır; uzak tam hash son yanıtta git ls-remote ile doğrulanır. Yeni bağımlılık, kurulum veya paket sürümü değişikliği yok. Geçici sentetik test dosyaları temizlenir; Electron .uiprev kanıtları Git dışında kalır.
