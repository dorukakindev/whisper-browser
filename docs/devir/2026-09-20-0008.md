# Devir Notu — 2026-09-20 00:07

## Kapsam
R86 raporundaki R86-01–R86-05 bulgularının düzeltilmesi + repodaki diğer bug
raporlarının (BROWSER_BUG_REPORT_* ve PROGRAM_BUG_REPORT_* serisi) R85 kanonik
tablosu üzerinden yeniden doğrulanması; gerçek olanların düzeltilmesi,
yanlış pozitiflerin kanıtla kapatılması.

- Repo/dal: https://github.com/dorukakindev/whisper-browser · master
- Başlangıç ürün kodu: `3af9f7ab14d7cf7c834748b3eef314fb00326771` (denetim dokümanı commit'i)
- Bitiş ürün kodu: `de3f835` (aşağıda)
- Ortam: Linux doğrulama VM'i (D:\Whisper Local hedefi Windows'tur; Windows'a özgü
  yollar burada koşamaz — yerine POSIX eşdeğerleri kullanıldı)

## R86-01–05 sonuçları

| Bulgu | Durum | Düzeltme | Test |
|-------|-------|----------|------|
| R86-01 video paketi mutlak yol sızıntısı | DÜZELTİLDİ | `src/workspace-video-package.js` manifest artık güvenli takma ad taşır; paket dışı video için yeniden bağlama bildirimi | `browser-report86-regressions.test.js` senaryoları 44/44 |
| R86-02 pruneTracks referans silinmesi | DÜZELTİLDİ | prune/sweep öncesi session+workspace `trackRefs` koruma kümesine katılır | yaş eşiği + referanslı/kayıp senaryoları |
| R86-03 .bak sessiz başarı + bellek/disk ayrışması | DÜZELTİLDİ | `browser-session-store.js` ayna-yedek önce-yazılır politikası; mirrorBackup modunda yedek hatası ana dosyayı eskitmeden başarısız döner | EACCES enjeksiyon matrisi |
| R86-04 diagnostic backoff log seli | DÜZELTİLDİ | üst-sınır sonrası emit temeli güncellenir; türler ayrı durumda | sanal saat: 10 dk stall, düzelme-tekrar, çoklu tür |
| R86-05 transcript 'tamam' yanlış pozitifi | DÜZELTİLDİ | geniş startsWith kaldırıldı; dar niyet kalıbı listesi | pozitif 4 + negatif 5 örnek |

## Bu turda düzeltilen R85 kuyruk satırları

- B83-13/16 dinamik-blok eski kimlik birikimi → `pruneReplacedBrowserPageBlocks`
- B83-14 sayfa çevirisi restore eski-orijinal ezmesi → `latestOriginal`
- B83-18 annotation toggle kayıpsız-eşleşme → `missing:true` dönüşü
- B83-21/22 çeviri/exclusion await bayatlığı → `exclusionStale` + kuşak denetimi
- B83-23 `loadRetryTimer` gezinme ezmesi → kuşak+did-start-navigation temizliği
- B83-24 ui-locale site başlığı çevirisi → `ignored` seçici genişletildi
- B83-25 CEA kalibrasyonu TR İ sorunu → NFD+birleşen işaret ayıklama
- B83-26 sponsor bayat "Atla" → segment bitiminde pendingAction sıfırlanır
- B83-27 orphan sweep referans kümesi → workspace/session trackRefs dahil
- B83-28 tab.loading → 'navigation' nedeni korunur
- B83-30 `restoring` takılması → erken dönüşlerde `restore_failed`
- B83-31 scheduler onResult throw → emitResult try/catch
- B83-35/R78-07 site clearData kapsamı → **karar düzeltildi**: `cache`/`backgroundFetch`
  origin filtresiz çalışır; site temizliğinde bilinçle dışarıda (test kontratı korundu)
- B83-36/37 mojibake marker paritesi + U+FFFD/eksik-marker guard
- B80-02 probeCommand stderr tüketimi + 256KB çıkış tavanı
- F4/F8 subtitle-search TR-I katlaması + boş Location reddi
- D2 fonts statSync try sarması
- S2 aynı-kimlikli kaymış cue dedupe (merge revizyon anahtarı)
- N1 feed_partial bayat-sonuç ezmesi → previewRequestId işareti
- N2 SmartTube ölü kapat düğmesi → koşulsuz kapat
- N3 cache `.corrupt-*` birikimi → en yeni 5 sınırı (yeni test senaryosu)
- N4 youtube:cancel → close'u 3 sn tavanla bekler
- D81-01 EN modda kalan TR durum dizgeleri → 7 sözlük girdisi

## Performans ölçümleri/düzeltmeleri (bu VM'de)

| Madde | Ölçüm | Sonuç |
|-------|-------|-------|
| PF1 fingerprint | 20k cue: 45 ms → **8.4 ms** | `cueFingerprint` hızlı yolu (işaretleyici yoksa `cleanCueText` atlanır) |
| PF1b GC platosu | 600 batch dolu buffer: heap +0.0 MB | kaçak yok — plato korunur |
| PF1c SRT+asset | `cuesToSrt` 20k cue: 88 ms | finalize'da tek seferlik — kabul edilebilir |
| PF2 session write | şema-maks (24 sekme×2000 edit = 1.27 MB): 312 ms | ≥10 sn kaydetme kademesi → etki seyrek; memoization adayı |
| PF2b edit normalize | çift `createEditRecord` → tek | `recordFieldsMatch` ayrıştırması |
| P79-04 semantic arama | ilk sorgu ~6.0 sn → sonraki **11-15 ms** | kalıcı Python işçisi (`serve` NDJSON), model tek yükleme |

## Electron smoke kararsızlığı

- Kök neden: Electron-altı-Electron spawn'da child zygote SIGTRAP
  (`zygote_host_impl_linux.cc:221` EINVAL) — Linux-VM'ye özgü.
- Çözüm: `electron-a3-acceptance.smoke.js` child'ına `--no-sandbox --no-zygote
  --disable-gpu` + `ui.browserHardwareAcceleration:false`.
- Karar kuralı: `node tests/run-electron-smokes.js --all` — **4 ardışık tur 20/20**
  (3'ü düzeltmeden hemen sonra, 4.'sü bu dalganın son hâliyle).
- Sabit sleep yerine durum-bekleme korundu; global timeout büyütülmedi.

## Değişen dosyalar (ürün+test commit'i `de3f835`)

- `src/main.js` — exclusions/restore/unload bayatlık kapıları, loadRetryTimer
  kuşak denetimi, annotations toggle, youtube:cancel close-bekleme, probe stderr
- `src/browser-subtitles.js` — S2 revizyon dedupe, cueFingerprint hızlı yolu
- `src/browser-page-translate.js` — latestOriginal restore
- `src/browser-session-privacy.js` — SITE_DATA_TYPES karar düzeltmesi + yorum
- `src/browser-session-store.js` — .bak ayna-yazım politikası (R86-03)
- `src/browser-link-hints.js` — cross-frame ESC köprüsü
- `src/browser-textutil.js` — mojibake marker paritesi + guard
- `src/browser-transcript-search.js` — dar niyet kalıbı (R86-05)
- `src/browser-translation-cache.js` — .corrupt arşiv sınırı (N3)
- `src/browser-subtitle-sync.js` — tek normalize (PF2b)
- `src/browser-fonts.js` — statSync hata sarması (D2)
- `src/browser-media-tools.js` + `backend/browser_media_tools.py` — kalıcı
  semantic işçi (P79-04) — yeni 'serve' modu, FIFO eşleşme, iptal/respawn/idle
- `src/browser-note-store.js`, `src/atomic-json.js`, `src/browser-asset-gc.js`,
  `src/workspace-video-package.js`, `src/workspace-package.js`,
  `src/playback-policy.js`, `src/renderer/renderer.js`, `src/renderer/ui-locale.js`,
  `src/ytdlp-runtime.js`, `src/media-folders.js`, `src/burnin-output.js`,
  `src/watch-index.js`, `backend/workspace_video_package.py` — R86 kapsamı
- `tests/browser-report86-regressions.test.js` (yeni, 44 senaryo),
  `tests/browser-subtitles.test.js` (S2), `tests/browser-translation-cache.test.js`
  (N3), `tests/electron-*.smoke.js` (no-zygote/durum-bekleme)
- `.gitignore` — `C:/` yolu (Linux doğrulama fixture'ı)

## Doğrulama sonuçları

- `node tests/browser-report86-regressions.test.js` — **44/44**
- `node tests/browser-report85-regressions.test.js` — **12/12**
- `npm test` — 2 dosya ortam nedeniyle düşüyor (fts5 node:sqlite modülü eksik,
  Windows `venv` shim'i) — ürün regresyonu yok; diğer tüm dosyalar geçti
- `npm run test:electron-bridge` — **geçti**
- `node --check` main/preload/renderer + değişen tüm modüller — **temiz**
- `python3 -m py_compile` backend/*.py — **temiz**
- `node tests/run-electron-smokes.js --all` — **4 ardışık 20/20**

## Açık sınırlar / çalıştırılamayanlar

- fts5 ve Windows venv shimleri bu VM'de yok → 2 test dosyası ortamdan düşüyor
  (Windows koşusunda yeşil beklenir; R85'te de belgeli sınır).
- YouTube device-flow canlı uç koşusu gerçek Google OAuth istemcisi gerektirir;
  N4 düzeltmesi sözleşme düzeyinde test edildi.
- PF2 (şema-maks oturum yazımı 312 ms) kabul edilen sınırda — büyük oturumlarda
  seyrek jank; memoization ileride düşünülebilir.
- T2 smoke hata-enjeksiyon kolu eksik — açık sınır.

## Kullanıcıya not
- `D:\Whisper Local` bu VM'de yok; Windows'a özgü komutlar yerine POSIX
  eşdeğerleriyle doğrulandı (`/usr/bin/python3`, `node --check` vb.).
- Repo'daki `BROWSER_BUG_REPORT_*.md` dosyaları `.gitignore`'da — bu yüzden
  R87 raporu commit'e girmez, yerel olarak repoda durur.
