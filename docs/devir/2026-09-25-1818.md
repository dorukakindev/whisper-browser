# Devir notu — 2026-09-25 — GitHub repo derin araştırması: browser'a faydalı entegrasyon adayları (kod değişikliği YOK)

- **Dal:** `devin/repo-research-2026-09-25` (taban: `origin/master` = `b6c8fdf`)
- **Başlangıç kod commit'i:** `b6c8fdf` · **Bitiş kod commit'i:** `b6c8fdf` (ürün kodu değişmedi — yalnız doküman)
- **Rapor:** [docs/raporlar/GITHUB_REPO_ONERILERI_2026-09-25.md](../raporlar/GITHUB_REPO_ONERILERI_2026-09-25.md)

## Özet
- Kullanıcı talebi: browser tarafına eklenebilecek GitHub depolarını **kodlarını inceleyerek** derin araştır, raporla, kodu değiştirme.
- ~25 depo/paket GitHub API + ham kaynak dosyalarıyla incelendi; lisans, bakım durumu, paket yüzeyi ve bizdeki entegrasyon noktası (dosya adıyla) kaydedildi.
- **Canlı doğrulama:** `opensubtitles-v3.strem.io` uç noktası gerçek HTTP çağrısıyla test edildi (anahtarsız, IMDB-bazlı, dil alanlı sonuç + UTF-8 indirme URL'leri).
- **Düzeltme/enventory bulgusu:** `ffsubsync`, `guessit`, `rapidocr-onnxruntime`, `sentence-transformers` projede **zaten isteğe bağlı bağımlılık olarak kurulu ve kablolu** (`backend/browser_align.py`, `browser_media_tools.py`, `browser_video_analysis.py`, `catalog_scan.py`) — raporda "yeni ekle" diye değil "kullanılmayan mod/kapasite" olarak işaretlendi (örn. ffsubsync ses-referans modu boşta).
- Kademe A: youtubei.js (InnerTube provider katmanı), kanal-RSS abonelik fallback'i, Stremio OS-v3, subliminal sağlayıcı merdiveni, ffsubsync ses modu, better-sqlite3 FTS, koşullu bgutil POT provider (GPL — lisans incelemesi şart).
- GPL/AGPL depolar (FreeTube, Piped, DeArrow, electron-chrome-extensions, ccextractor, PgsToSrt) kod kopyalama için değil desen/API/harici-araç olarak işaretlendi; `electron-chrome-extensions`'ın çift lisansı (GPL/patron) MIT ürüne linklenemez diye açıkça uyarıldı.
- Kapsam dışı bırakılanlar ve reddedilenler raporda gerekçeli listelendi.

## Değişen dosyalar
- `docs/raporlar/GITHUB_REPO_ONERILERI_2026-09-25.md` — yeni araştırma raporu (öncelik matrisi + etap planı + reddedilenler).
- `docs/devir/2026-09-25-1818.md` — bu not.
- `DEVIR-NOTU.md` — indeks güncellemesi.

## Testler / doğrulama
- Ürün kodu değişmediği için test paketi çalıştırılmadı (kural: yalnız doküman → diff/içerik kontrolü yeterli).
- Doğrulanan gerçekler: GitHub API metadata'sı (25.09.2026), `youtubei.js@18.1.0` package.json export'ları, subliminal `src/subliminal/providers/` ağacı (10 sağlayıcı), FreeTube `subscriptions.js` RSS ayrıştırıcısı, ffsubsync `requirements.txt` + bizim `browser_align.py` argümanları, alass v2.0.0 release asset'leri, m3u8-parser LICENSE (Apache-2.0), DeArrow `/api/branding` dokümantasyonu, Stremio endpoint canlı yanıtı.
- Gizli anahtar/kişisel veri kullanılmadı.

## Yarım kalan / sonraki adımlar
- Rapor yalnızca öneri katmanıdır; herhangi bir entegrasyon kullanıcı onayıyla ayrı iş olarak yapılmalı.
- Etap 1 adayları (RSS subs, Stremio sağlayıcısı, ffsubsync ses modu) küçük bağımsız PR'lar olarak önerildi — sıra kullanıcıya ait.
- `electron-browser-shell`'in `electron-chrome-extensions` paketi lisans nedeniyle ancak ayrı değerlendirmede ele alınabilir.
