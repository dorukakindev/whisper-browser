# Devir Notu — 2026-09-25 18:18

## Bağlam
- Repo: `whisper-browser` · Dal: `devin/repo-research-2026-09-25` · Taban: `origin/master` (`b6c8fdf`)
- Görev: "Browser tarafına gerçekten faydalı GitHub repoları — kodlarına bak, derin araştır, raporla" + iki derinleştirme turu ("daha derin", "browser tarafına odaklan")
- Durum: tamamlandı (rapor v2 — ~45 depo/paket, interception katmanına kadar indi)

## Yapılanlar
- `docs/raporlar/GITHUB_REPO_ONERILERI_2026-09-25.md` — v2 derinleştirme turu ile baştan yazıldı (25.4KB)
- `DEVIR-NOTU.md` kök indeksi bu notu işaret eder
- Ürün kodu DEĞİŞMEDİ — yalnız 2 doküman dosyası
- PR: `devin/repo-research-2026-09-25` dalı push'landı (gh CLI/MCP yok → kullanıcı GitHub'dan açmalı)

## v2 turunda doğrulanan kritik gerçekler
- CDP interception **zaten derin**: `Network.getResponseBody`, `Fetch.enable/fulfillRequest` (YT player-response ad-prune), `Target.setAutoAttach`, sayfa-içi XHR/fetch hook'ları, `textTracks` gözlemcisi
- windows-1254 Türkçe kodlama sezgisi `browser-textutil.js`'de mevcut → iconv-lite yalnız non-TR legacy seti için anlamlı
- `savePage` MHTML, bağlam menüsü, MediaSession, getDisplayMedia loopback zaten var → ilgili depo önerileri elendi
- Gerçek interception boşlukları: `SourceBuffer.appendBuffer` hook'u, WebSocket capture, `requestPostData` okuma
- youtubei.js Electron'da `Platform.shim.eval` köprüsü ister (FreeTube `local.js` deseni); CJS'ten dinamik import gerekir
- `node:sqlite` Electron 43/Node 22'de `--experimental-sqlite` arkasında → güvenilmez; better-sqlite3/Dexie önerilir
- Lisanssız belirsizlikler: `ts-ebml` (lisanssız→elendi), `edge-tts` ve `moonshine` (GitHub "other"→teyit şart)
- Beklenmedik güçlü bulgu: `web-scrobbler` MIT + 500+ site konnektörü → media-identity kalıp kataloğu
- `N_m3u8DL-RE` MIT harici indirici; `mediabunny` MPL-2.0 modern medya toolkit
- Piped API canlı testte 502 → instance kararsızlığı kanıtlı

## Testler
- Ürün testi yok (doküman görevi). Canlı HTTP doğrulamaları: Stremio OS-v3 ✓ (200), Piped trending ✗ (502 — kararsızlık kanıtı)
- npm/github metadata doğrulamaları: youtubei.js 18.1.0, mediabunny, foliate-js (MIT), cat-catch (GPL), stream-detector (MPL, archived), xhook (MIT), vosk-api (Apache-2.0), web-scrobbler (MIT), N_m3u8DL-RE (MIT)

## Deps/env değişikliği
- Yok. Gelecek uygulama kademeleri rapordaki etap planına göre (Etap 1 hızlı kazanımlar → Etap 5 mimari).

## Sonraki adımlar
- Kullanıcı raporu okuyup etap seçerse ilgili maddeler kodlanır
- `aniskip` API şeması ve `edge-tts`/`moonshine` LICENSE dosyaları uygulama öncesi teyit bekler

## Commit'ler
- `81f783d` — rapor + devir notu (v1)
- (v2 düzenlemesi aynı dalda ikinci commit olarak eklenecek)
