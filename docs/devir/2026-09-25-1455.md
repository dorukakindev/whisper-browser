# Devir notu — 2026-09-25 14:55 — R124 Adım 1: dinamik mesaj EN'i (messageKey+params)

- **Dal:** `codex/r124-step1-i18n-1790347020` · **Başlangıç:** `b6c8fdf` · **Bitiş kod commit'i:** `4319cb3`
- **Kapsam:** [BROWSER_PLAN_R123.md](../raporlar/BROWSER_PLAN_R123.md) §5 Adım 1 (1.2–1.4). Adım 1.1 yalnız Windows doğrulaması gerektiriyor.
- **Rapor:** [docs/raporlar/BROWSER_BUG_REPORT_124.md](../raporlar/BROWSER_BUG_REPORT_124.md)

## Değişen dosyalar (kod commit'i `4319cb3`)

- `src/main.js` — `browserLoadErrorMessage` → `browserLoadError` (`{message, messageKey}`); `browserCertificateErrorMessage` → `browserCertificateError` (`{message, detail, messageKey}`); boş-HTTP, did-fail-load, restore, yeni-sekme, load-retry ve sertifika olaylarına `messageKey`/`params` eklendi.
- `src/browser-lifecycle-policy.js` — `crashRecoveryPolicy` dönüşüne `messageKey` (`crash-memory`/`crash-recoverable`/`crash-manual`) + `params.reason`.
- `src/browser-playback-diagnostics.js` — 27 katalog girdisine `labelEn`/`messageEn`; `makeDiagnostic` alanları kopyalar; 401/403 metinleri belge bağlamında genelleştirildi ("Oynatma isteği" öneki kaldırıldı).
- `src/renderer/renderer.js` — `BROWSER_ERROR_EN` şablon haritası + `browserErrorText` + `diagnosticField` + `DIAGNOSTIC_CONFIDENCE_EN`; hata yüzeyi/sinyal/retry/teşhis satırları ve yetenek özeti locale'e göre; "Altyazı · N" çipi `UiLocale.t('Altyazı')` + üç durum başlığı iki dilli; `tab.errorMessageKey/errorParams` taşınır ve temizlenir.
- Testler: yeni `tests/browser-report124-regressions.test.js` (7); güncellenen `browser-report123`, `browser-playback-diagnostics`, `browser-navigation-abort`, `audit-tur5-main`, `browser-controls-behavior` sözleşmeleri.

## Test sonuçları

- `browser-report124` 7/7 · `browser-report123` 6/6 · `browser-playback-diagnostics` 18/18 · `browser-navigation-abort`, `audit-tur5-main`, `browser-controls-behavior`, `browser-lifecycle-policy` yeşil · `player-ui` 148/148 · `ui-locale` 14/14 · `browser-report120` 6/6 · `electron-browser-chrome-design` Electron smoke temiz.
- `node --check` main/renderer/diagnostics/lifecycle temiz. Tam `npm test` koşulmadı (yalnız değişen alanlar); CI bekleniyor.

## Windows etkisi / doğrulanamayan sınırlar

- Yalnız metin katmanı değişti; `install.bat`/`start.bat`, yollar, file picker, GPU/CUDA etkilenmedi.
- **Adım 1.1 açık:** sayfada bul "Aranıyor…" asılması Xvfb'de kök-nedenlenemedi — Windows'ta Ctrl+F ile elle doğrulanmalı (`1 / N` sayacı).
- `notice` kanalındaki diğer parametrik TR mesajlar (ör. "En fazla N sekme") aynı kalıpla sonraki tura aday.
- Gerçek EN arayüz görselliği Ubuntu Electron smoke'unda doğrulandı; Windows DPI/font ölçüsü doğrulanmadı.

## Sonraki adımlar

- Adım 2 (Faz A kalanı): hayalet araç çubuğu ikonları, site çipi (kilit+host, kalkan çubuğa), aktif sekme birleşimi, hata ekranı cilası — ayrı `codex/` dalı + PR.
- Adım 3 (Faz B): tek-satır üst çerçeve (sekmeler başlık çubuğunda, ~38px), birleşik Whisper düğmesi, 32px durum hapı, gruplu ⋯ menüsü.
