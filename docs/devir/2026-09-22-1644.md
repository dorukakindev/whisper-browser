# Devir Notu — 2026-09-22 16:44 (UTC)

## Konu

TR altyazı çevirisinde sentence-group oluşturma → bütün cümle çevirisi → cue'lara geri dağıtım boru hattının derin doğrulaması ve kalite kapısı sertleştirmesi (kullanıcı: 13 zorunlu davranış + test listesi + kabul kriterleri).

## Dal / commit'ler

- Dal: `codex/tr-sentence-groups-1790094000` (origin/master @ `d3cab6b` üzerinden)
- Kod commit'i: bu notta yazılacak (push sonrası hash aşağıya)
- Dokümantasyon commit'i: ayrı

## Ölçüm özeti

Mevcut mekanizmanın ~8/13 davranışı zaten doğruydu (gruplama, bütün gönderim, yapısal sözleşme, join-match, timestamp koruma, cache/dedupe, küçük-paket kurtarma, bütün-seviye anlam kapıları). Eksikler: iç-cümle istisnası olmayan erken-sonu kapısı, parça-seviyesi demirleme, açık bağlantı ve tekrar kapıları, sertleştirilmiş ikinci kurtarma, cue içi `\n` garantisi.

## Değişen dosyalar

- `backend/sentence_translation.py` — `sentence_end_count`, `internal_sentence_end_count`, `part_tail_issue`, `part_repetition_issue`, `part_anchor_issue`, `insert_sentence_breaks` eklendi; `sentence_part_boundary_issue` kota kuralına çevrildi; `validate_sentence_parts` kabul edilen parçalara `\n` ekler.
- `backend/transcribe.py` — `part_gate_issue` birleşik kapısı; cache-hit / batch `task_once` / `refine_task` üç kabul sitesine bağlandı; `task_once(prompt_suffix=)`; `RESCUE_SUFFIX` + `rescue_group` (tekil kurtarmadan sonra son deneme); `build_translate_prompt`'a 4 dağıtım kuralı.
- `backend/test_sentence_distribution.py` — 24 regresyon testi (yeni dosya).
- `docs/raporlar/BROWSER_BUG_REPORT_108.md` — tam rapor (değişenler, kök nedenler, önce/sonra, sınırlar).

## Testler

- `venv/bin/python backend/test_sentence_distribution.py` → 24/24
- `venv/bin/python backend/test_transcribe.py` → 192/192
- Canlı shuaiapi + claude-haiku-4-5 (`--translate-only`, 9 blok): 2 istek, 1 grup tekil kurtarmada kurtarıldı; `Wait. Are you sure / you want to come with me?` → `Bekle.` / `Emin misin benimle gelmek istediğinden?` — önceki kapı bu çıktıyı reddederdi.
- `npm test` koşulmadı (talimat: gereksiz tekrar yok; JS dokunulmadı).

## Sınırlar / yarım işler

- Semantic retime kasıtlı yapılmadı (ayrı seçenek olacak).
- `part_tail_issue` liste tabanlı; ayrıntı raporda.
- Anlam-yumuşaması (kelime-kelime ama yapısal geçerli) mekanik kapı dışında; savunma = bütün-çeviri + anlam kapıları.

## Push hash'leri

- Kod: `<push sonrası>`
- Docs: `<push sonrası>`
