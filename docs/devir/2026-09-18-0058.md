# Devir notu — 2026-09-18 00:58

## Konu

Rapor 58 (`BROWSER_BUG_REPORT_58.md`, commit `a9597e9`) envanterindeki **15
kesin bulgunun tamamının onarımı** — her madde için önce kırmızı regresyon
testi, sonra en küçük sahip modül değişikliği, sonra hedefli + tam paket
doğrulama. Onarım kaydı: `BROWSER_BUG_REPORT_59.md`.

## Repo / dal / commit kimlikleri

- Repo: `https://github.com/dorukakindev/whisper-browser` — dal `master`
- Başlangıç HEAD: `a9597e937d22a8f1d1fd268a5b711882664109c0` (rapor 58
  doğrulama kaydı; denetlenen ürün kodu `0d9dcc7` ile aynı)
- Bitiş kod commit'i: bu commit (aşağıdaki kod + test değişiklikleri)
- Dokümantasyon commit'i: rapor 59 + bu not + kök indeks güncellemesi

## Değişen dosyalar ve amaçları

### Ürün kodu

- `src/main.js`
  - `stopBrowserLiveAsr()`: `browserLiveAsr`'ı artık `close` olayına kadar
    drain sahibi tutar — Python'ın `stop` sonrası boşalttığı son segmentler
    kaybolmuyor (R58-01).
  - `render-process-gone` işleyicisi: `destroyBrowserTab` ile aynı temizlik
    sözleşmesi — `stopBrowserManga`, `stopBrowserPageTranslation`,
    `translationScheduler.cancelAll()` + null, sekmeye ait
    `stopBrowserLiveAsr()` (R58-02/03).
  - `captureBrowserResponse()`: CEA adayları için non-2xx gövdeler
    çözümleyiciye verilmiyor; fetched/completed işaretlenmiyor (R58-04).
  - Manifest işleme: canlı media-playlist yenilemelerinde CEA matcher'lar
    `buildHlsCeaSegmentMatchers` + `hlsCeaTracksForVariant` +
    `registerBrowserHlsCeaMatchers` + `mergeCeaCaptureSegments` ile
    güncelleniyor; tam-yakalama işi plan/total/completeness tazeleniyor
    (R58-05).
  - `transcribe:start`: `options.explain` de altyazı-yetkilendirici
    dalına alındı (R58-08); `isValidOutputNameSuffix` ortak doğrulayıcıya
    bağlandı (R58-09).
- `src/browser-cea-full-capture.js` + `src/main.js` plan yolu:
  `EXT-X-GAP` parçaları zorunlu/fetchable işten çıkarıldı, süreleri
  zaman çizelgesinde korunuyor (R58-06).
- `src/browser-media-identity.js`: `ROUTE_KEYWORD_RE` + son rota anahtar
  kelimesinden sonraki kuyruk kimliği — `udemy:lecture`, `max:watch`,
  `discovery:show`, `raiplay:2024` çöküşleri giderildi (R58-07).
- `src/renderer/queue-lifecycle.js` (UMD, main+renderer ortak):
  `OUTPUT_NAME_SUFFIX_RE`, `isValidOutputNameSuffix`,
  `queueOutputNameSuffix(id)` → `-whisper-q0002` biçimi (R58-09).
- `src/renderer/renderer.js`
  - `computeQueueCollisionSuffix` ortak yardımcı; ekleme anında ve
    `processNextQueueItem`'da her başlatmada yeniden üretim;
    `next.error` kalıcı yazımı (R58-09).
  - Oynatıcı `keydown` işleyicisi öncelik sözleşmesine göre yeniden
    sıralandı: katman/Escape → düzenlenebilir hedef → tarayıcı kısayolu →
    oynatıcı kısayolu; eşleşmeyen Ctrl/Meta/Alt kombinasyonu tek-harf
    eylemlerine düşmüyor (R58-15).
  - Ana-ekran Escape: düzenlenebilir hedefte iş iptali dalı atlanıyor,
    açık modal hâlâ kapanıyor (R58-15).
- `backend/transcribe.py` (önceki oturum diliminde uygulanmıştı, bu turda
  doğrulandı): WhisperX `task` iletimi (R58-10), temizlikte boşalan blok
  düşürme (R58-11), karışık-güvenli grup başına tekil düşürme kararı
  (R58-12), sıfır-sınırlı metrik hizalaması (R58-13), parçalı çıktıda
  kronolojik sıra + boşluk sözleşmesi (R58-14).

### Testler

- `tests/browser-live-audio.test.js` — R58-01 drain sahipliği davranış testi
- `tests/browser-lifecycle-policy.test.js` — R58-02/03 crash işleyicisi
  vm davranış testi
- `tests/browser-cea-captions.test.js` — R58-04 (304/4xx/5xx) + R58-05
  (kayan pencere matcher yenileme)
- `tests/browser-cea-full-capture.test.js` — R58-06 gap fixture
- `tests/browser-foundation.test.js` — R58-07 dört-servis bölüm matrisi
- `tests/adversarial-ipc.test.js` — R58-08 gerçek `transcribe:start`
  işleyicisiyle explain yetkilendirme
- `tests/queue-lifecycle.test.js` — R58-09 ortak sözleşme + davranışsal
  çakışma + yeniden üretim/hata kalıcılığı
- `tests/player-ui.test.js` — R58-15 sahte-DOM klavye öncelik testleri +
  mevcut kısayol testinin kesim penceresi genişletildi
- `tests/main-args.test.js` — çıkarılan argv bloğuna ortak doğrulayıcı
  enjeksiyonu (R58-09 takip düzeltmesi)
- `backend/test_transcribe.py` — R58-10/11/12/13/14 fixture'ları
  (önceki dilimde eklendi, bu turda yeşil doğrulandı)

## Kök nedenler ve kanıtlar

Her maddenin kök nedeni, düzeltmesi ve test kanıtı
`BROWSER_BUG_REPORT_59.md` içinde madde madde belgelendi. Özet:

- R58-01: drain sahipliği `stop`'ta erken null → `close`'a taşındı.
- R58-02/03: crash yolunda sekme-işi temizliği yoktu → destroy sözleşmesi.
- R58-04: HTTP durumu decode öncesi denetlenmiyordu → non-2xx reddi.
- R58-05: master track bildirimi yenilenen media playlist'te yoktu →
  matcher tazeleme.
- R58-06: `gap: true` ledger'da okunmuyordu → zorunlu-işten çıkarma.
- R58-07: ilk rota kelimesi sonrası ilk parça kimlikti → son kelime + kuyruk.
- R58-08: explain girdisi medya yetkilendiricisine gidiyordu → altyazı dalı.
- R58-09: `-whisper-q2` ≥4 karakter kuralını kırıyordu → ortak üretici/
  doğrulayıcı + başlatmada yeniden üretim + `next.error`.
- R58-10..14: backend beşlisi (task iletimi, boş cue, grup ortalaması,
  0.0 metrik, parçalı sıra) — fixture'lı düzeltmeler.
- R58-15: öncelik sözleşmesi eksikti → dört katmanlı sıralama.

## Test komutları ve sonuçlar

- `npm test` — **Tüm testler geçti** (tam paket; tek düzeltme gereken
  `main-args.test.js` harness enjeksiyonu yapıldı)
- `npm run test:electron-bridge` — geçti
- `node tests/run-electron-smokes.js a3-acceptance` — 7 kabul + manga
  stale-edit rollback, temiz çıkış (exit 0)
- `backend/venv/Scripts/python.exe backend/test_transcribe.py` — 190/190
- `node --check` main/preload/renderer + `py_compile transcribe.py` — temiz
- Hedefli: browser-live-audio 10, browser-cea-captions 15,
  browser-foundation 24, adversarial-ipc 15, queue-lifecycle 19,
  player-ui 145, main-args 16

## Sınırlar / sentetik-olmayan kanıt gerektirenler

- R58-02/03: gerçek işleyici harness'te çalıştırıldı; canlı model
  süreciyle gerçek Electron `render-process-gone` koşusu ek kanıt olur.
- R58-05: sentetik CDP yenilemesi; gerçek kayan-pencereli canlı HLS
  yayınıyla doğrulanmadı.
- R58-10: sahte model; gerçek WhisperX çıktısı koşulmadı.
- R58-15: DOM harness; AltGr düzenleri/zengin contenteditable gibi gerçek
  odak kenarları koşulmadı.

## Yarım işler / sonraki adımlar

- Rapor 58'in "kesin olmayan" bölümüne dokunulmadı (stale/reddedilen/
  kanıtsız maddeler aynı durumda).
- `BROWSER_BUG_REPORT_55.md`/`56.md` korunuyor; rapor 59 seri
  adlandırmayla eklendi.
- Olası takip: tam Electron smoke envanteri
  (`node tests/run-electron-smokes.js --all`) ayrı koşulabilir.

## Güvenlik/gizlilik

Sağlayıcı anahtarı, gerçek profil, imzalı URL, çerez veya kişisel veri
okunmadı/günlüklenmedi.
