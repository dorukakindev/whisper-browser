# Devir notu — 2026-09-19 11:18

**Kapsam:** R73 devam turu — paralel oturumun salt-okunur audit raporundaki
(`BROWSER_BUG_REPORT_73.md`) 36 bulgunun kaynakta tek tek doğrulanması + aynı
oturumda bağımsız denetimle bulunan 4 ek bug. Gerçekler düzeltildi, yanlış
pozitifler rapora işlendi.

**Başlangıç:** `master @ f9eee65` (R72 docs commit'i)
**Bitiş:** `master @ 3d4a7d7` (kod+test commit'i)

**Bağlantı:** [BROWSER_BUG_REPORT_73.md](../raporlar/BROWSER_BUG_REPORT_73.md) —
doğrulama turu bölümü eklendi (hangi bulgu GERÇEK / yanlış pozitif / by-design).

## Doğrulanan ve düzeltilen bulgular (7)

| # | Bulgu | Düzeltme |
|---|-------|----------|
| K5 | `browser:command` `zoom-set` geçersiz `value`'da else dalına düşüp `current - 0.1` uyguluyor — "set" komutu zoom-OUT gibi davranıyordu | `main.js:13309` — geçersiz değerde `{ok:false,error}` |
| K4 | `browserQuickHistory` dirty editörde undo/redo'yu sessiz no-op yapıyordu (kullanıcıya geri bildirim yok) | `renderer.js:21690` — dirty'de status mesajı |
| Y1 | `decideSkip` çakışan kayıtlarda yalnız end-sıralı — kısa autoSkip'siz kayıt geniş autoSkip bölgesini gölgeliyordu | `browser-skip-segments.js` — `autoSkip` önce, sonra end |
| E1 | Native A-B döngüsü: seek tamamlanan `timeupdate` `seeked`'den ÖNCE gelir — B ötesi atlama "kesiş" sanılıp A'ya sarılıyordu | `renderer.js:20589` — `video.seeking` koruması; `seeked`'de `_abPrevT` güncellemesi |
| E2 | Browser A-B döngüsü: kullanıcı/site seek'i B ötesine inerken döngüleniyordu; ayrıca sabit 4.5s eşiği 4× hızda doğal ilerlemeyi (~4 sn/poll) seek sanıp döngüyü kırabiliyordu | `renderer.js:10076` — `_ownSeekAt` seek işareti + hıza ölçekli `naturalStep` + `browserAdPlaying` koruması + `resetMediaBoundState`'te sıfırlama |
| E3 | `player.lastT` altyazı-zamanında saklanıp `+player.offset` geri çevriliyordu — offset değişiminde "önceki konum" delta kadar kayıyordu | `renderer.js:12700` — `lastT` video-zamanında |
| E4 | `stSelectSection` uçuştaki arama isteğini düşürmüyordu — bölüm değişince geç dönen sonuç gizli arama grid'ine yazabiliyordu | `renderer.js:22406` — `stSearchSeq++` kuşak düşürme |

## Yanlış pozitifler (16 — düzeltme yapılmadı)

K1/Y10 (`loadedmetadata` listener — `staleGeneration`+hls kimliği koruyor),
K2 (assetId hash-formatlı, traversal yok; watch-index yetkili), K3
(backward-iter+removeItem güvenli kalıp), Y2 (`paused` koruması video sonunu
yakalıyor), Y3 (ilk-çağrı skip bilinçli — mevcut test bekliyor), Y4
(`manualSeek` geri-sarma içindir; ileri seek'te skip istenen davranış), Y5
(`prev.queued` sonsuz kuyruğu engelliyor), Y6 (has→set senkron atomik), Y7
(`setSentences` ilk satırında `cancelAll` var), Y8 (exemption 100 anahtarla
sınırlı), Y9 (`MAX_TRACK_REFS` sınırı var), Y11 (`domain=` sır taşımaz),
O5 (64 MB dosya sınırı), O6 (24-char normalize yeterli), O11 (`isCurrent`
`!!token` koruyor). Ayrıntılar raporda.

## Değişen dosyalar

- `src/main.js` — zoom-set geçersiz değer erken dönüşü
- `src/renderer/renderer.js` — A-B native+browser seek kaçışı, hız ölçekli
  eşik, reklam koruması, `_ownSeekAt` işaret/sıfırlama, `lastT` video-zamanı,
  `stSearchSeq` düşürme, quick-history geri bildirimi
- `src/browser-skip-segments.js` — autoSkip öncelikli sıralama
- `tests/report73-player-ab-search.test.js` — **yeni**, 14 davranışsal test
- `tests/browser-skip-segments.test.js` — Y1 overlap regresyonları
- `BROWSER_BUG_REPORT_73.md` — doğrulama turu bölümü (raporun sonuna eklendi)

## Test kanıtı

- `node tests/report73-player-ab-search.test.js` — **14/14** (browser A-B
  doğal kesiş/kendi seek'i/site seek'i/4× hız/reklam; native A-B
  seeking/seeked/kaçış; lastT; stSelectSection; quick-history; zoom-set)
- `node tests/browser-skip-segments.test.js` — geçti (overlap regresyonları)
- `node --check` renderer.js, main.js, browser-skip-segments.js — temiz
- `npm test` — **TAM GEÇTİ** (backend 192/192 dahil)
- `node tests/run-electron-smokes.js smarttube-boot` — **geçti**
- `node tests/run-electron-smokes.js bridge` — **geçti**

## Sınırlar / dürüst notlar

- Bu oturumda 14 denetim ajanı görevlendirildi; 9'u oturum yenilenmesiyle,
  5'i rate-limit ile kaybedildi. Doğrulama tamamen kaynak okuması +
  davranışsal regresyon testleriyle yapıldı.
- R73 raporunun O1-O12/D1-D8 maddeleri polish düzeyi; çoğunda dosya:satır
  yoktu, doğrulanabilir olanlar (O5, O6, O11) yanlış pozitif çıktı.
- Paralel oturumun iz bıraktığı untracked dosyalara
  (`docs/devir/2026-09-19-1040.md`, `2026-09-19-1100.md`,
  `tests/report6x-*.test.js`, `_repro/`) dokunulmadı — ayrı WIP.
