# Devir — 2026-09-23 18:19 — R117 UI polish paketi

- **Repo/dal:** whisper-browser · dal `codex/r117-ui-polish-1790185285` · taban master `f0ecb1c`
- **Başlangıç kod:** `f0ecb1c` · **Bitiş kod:** (bu nottan ÖNCE gelen kod commit'i — nota gömme; aşağıdaki "commit" bölümünde kesin)
- **Çalışma:** R115 raporunda ertelenen UI polish paketinin turu — 5 madde + token değerlendirmesi.

## Yapılanlar (dosya → amaç)

- `src/renderer/index.html`
  - `.sentence-tools.tool-row-main` → `details#subtitleActionsMenu` + `.subtitle-actions-pop` ("Altyazı işlemleri ▾", 4 düğme menüye taşındı).
  - `#playerStage` içine `#stUpNextCount` up-next geri sayım kartı (thumb/başlık/5 sn/Şimdi oynat/İptal).
  - `.browser-address-field` sarmalayıcısı + `#browserAddressMirror` overlay (şema/host/kalan üç span).
  - `.player-speed-wrap` → `#playerSpeedMenu` details+summary ("1×" pill) + `.player-speed-pop`; `#playerSpeed` select gizli durum deposu (opacity 0, tabindex -1, aria-hidden).
- `src/renderer/styles.css` — `.subtitle-actions-pop` (yukarı açılır), `.st-upnext-count` (sağ-alt koyu-cam kart), `.browser-address-mirror` + `input.mir-mode`, `.player-speed-menu/pop` (eski glass pill birebir kopyalandı + menuitemradio satırları).
- `src/renderer/renderer.js`
  - `playerDetailsMenuIds` → `subtitleActionsMenu` + `playerSpeedMenu` eklendi (Esc/dış-tık/ok-nav/tek-açık kazan-kazan); dış-tık ve öğe-kapama closest selector'leri genişletildi.
  - `stUpNextCountdown`/`cancelStUpNextCountdown`/`stUpNextCountdownStart` — 1 sn interval; iptal noktaları: İptal düğmesi, hideUpNextPanel, queuePlayerProbeFromCard, closePlayer, video 'play', Esc zinciri — hepsi `typeof` korumalı (sliced-test sözleşmesi).
  - `updateBrowserAddressMirror` — odakta değilken `new URL(value)` → scheme dim + host bold + rest dim; tüm programatik `.value=` atamaları senkronlanır; http dışı şemada gizli.
  - `playerSpeedPopupSync` — select'ten popup aynası; popup tıklama → `select.value` + `change` dispatch; `syncPlayerSpeedControl`/`loadedmetadata`/menü 'toggle' tetikleyicileri korundu.
- `src/renderer/ui-locale.js` — 5 yeni EN↔TR çifti (Altyazı işlemleri, menü açıklaması, Sıradaki, sn içinde oynatılacak, Şimdi oynat, Okuma görünümü).
- `tests/browser-parti7.test.js` — A/B/D/E sözleşme blokları (DOM id'leri, fonksiyonlar, iptal noktaları, locale çiftleri, CSS kuralları).

## Değerlendirilip REDDEDİLEN: öncelikli araç çubuğu collapse'ı

Gerçek ölçümle premise yanlışlandı: pencere `minWidth: 940px` (main.js:12941); eylem düğmelerinin tamamı `.browser-action-source` kalıcı gizli (styles.css:2601) → görünür eylem yüzeyi ~307px; 940px'de overflow fiziksel oluşamaz → mekanizma hiç tetiklenmeyecek ölü kod olurdu. Tüm collapse öznitelikleri/CSS/JS tamamen geri çıkarıldı; gerekçe BROWSER_BUG_REPORT_117'de.

## Ertelenen: 335 hex → token konsolidasyonu

Ölçüm: 440 hex / 334 tekil vs ~1302 `var(--)`; iki `:root` paleti; video yüzeyindeki 28× `#fff` kasıtlı tema-bağımsız. Aşamalı plan raporda (medya-krom token grubu → gri ölçekleri → tek-kullanımlıklar).

## Testler

- `node --check` renderer.js + ui-locale.js — temiz.
- Hedefli: browser-parti7 OK; player-ui 148/148; ui-locale 14; browser-reading-design; design-system; browser-subtitle-menu; player-escape-dialog; audit-tur5; browser-subtitle-session — yeşil.
- **Tam `npm test`: tüm dosyalar geçti.** İlk koşuda iki sliced-test ReferenceError (`cancelStUpNextCountdown` closePlayer'da, `updateBrowserAddressMirror` nav-handler'da) yakalandı → tüm çağrı noktalarına `typeof` koruması; ikinci koşu temiz.
- Electron kanıtı: `docs/raporlar/r117-evidence/` — 01 kontroller, 02 subtitle-actions açık, 03 speed popup (menuitemradio 0.5–2×), 04 up-next kart (EN), 05 adres mirror (host bold; DOM `{host:"127.0.0.1"}` doğrulandı).

## Sınırlar

- Geri sayım yalnız related-autoplay akışında devreye girer; 5 sn sayım ve iptal/şimdi-oynat gerçek uygulamada görüldü.
- Mirror yalnız http/https; diğer şemalarda input düz (kasıtlı).
- Token konsolidasyonu bu turda yok — ölçüm + plan.
- Rapor: `docs/raporlar/BROWSER_BUG_REPORT_117.md`.

## Commit'ler

- Kod: (aşağıdaki kod commit hash'i — push sonrası doğrulanacak)
- Docs: (docs commit hash'i)
- Uzak doğrulama: `git ls-remote origin refs/heads/codex/r117-ui-polish-1790185285`
