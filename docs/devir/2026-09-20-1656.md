# Devir Notu — 2026-09-20 16:56 (Europe/Istanbul)

## Repo / dal
- Repo: https://github.com/dorukakindev/whisper-browser · Dal: `master`
- Başlangıç kod commit'i: `44cb9c5` (devir) / `0b03929` (son kod)
- Bitiş kod commit'i: **`284b2b3`**

## Kapsam — Parti 9 (katalog E maddeleri C03, C22)

### C03 — Nyxt tarzı site otomatik kuralları: `readerAuto`
Mevcut site profili altyapısı (origin/path/tab katmanlı, en özgül kural kazanır — `resolveEffectiveBrowserSettings`) zaten zoom/darkMode/pageAuto gibi deklaratif eylemleri taşıyor; katalogdaki birleşik listeden eksik olan **reader** eylemi eklendi:
- `src/browser-site-profiles.js`: `PROFILE_FIELDS`'a `readerAuto: boolean`.
- `index.html`: tarayıcı görünüm ayarlarına `browserReaderAuto` kutusu ("Bu sitede okuma görünümünü otomatik aç"); site profili panelindeki `browserProfileControls` listesine de girer (genel/site/yol/sekme kapsamları).
- `renderer.js`: `updateBrowserNavigation` sonunda `loading===false` iken etkin profil `readerAuto===true` ise 700 ms debounce ile `setBrowserReader(tabId,'open')`. Döngü/çakışma sözleşmesi: sekme kimliği + nesil + URL eşleşmesi, `readerActive`/`compatibilityMode` kontrolü; kullanıcı kapatırsa aynı sayfada tekrar tetiklenmez (yalnız yeni yüklemede).
- `settings-security.js` + `PERSIST_CHECKBOX_CONTROLS`: kalıcı kontrol olarak iki listede de kayıtlı.

### C22 — axe-core erişilebilirlik regresyon kapısı
- Yeni dev bağımlılıkları (sabit sürüm, ≥7 gün önce yayımlanmış): `jsdom 30.0.1`, `axe-core 4.13.0`.
- `tests/accessibility.test.js`: `index.html`'i script çalıştırmadan jsdom'a yükler, axe-core'u `resultTypes:['violations']` ile çalıştırır. `region` dışında hiçbir ihlal kuralı kabul edilmez; `region` düğüm sayısı 35 tabanını aşamaz. jsdom'un hesaplayamadığı `color-contrast` kuralları kapalı.
- İlk taramada çıkan 3 gerçek ihlal düzeltildi:
  - `#browserAddress`: `aria-autocomplete="list"` için gerekli `role="combobox"` eklendi (listbox/expanded sözleşmesi zaten vardı).
  - `#browserFeatures`: `<details>` üzerinde izinli olmayan `role="tabpanel"` kaldırıldı (`aria-labelledby` korundu; JS `.open` kullanıyor).
  - `#browserSplitDivider`: focusable `role="separator"` için zorunlu `aria-valuemin/max/now` statik eklendi (sürükleme sırasında JS zaten güncelliyordu).

## Testler
- `npm test` → **tüm testler geçti** (yeni a11y testi dahil; Electron smoke ayrı/env).
- `browser-site-profiles`: 15 test (yeni C03 katman testi dahil).
- `settings-security`: 23/23 · `ui-locale`: 14/14 · `browser-parti7`: OK.
- `node --check`: renderer.js, browser-site-profiles.js temiz.

## Kalan E maddeleri (sonraki partiler)
A20+B01 (seekbar kare önizleme — YT storyboard + yerel ffmpeg tile; IPC/önbellek/iptal gerekir), B03 (OpenSubtitles moviehash — harici API doğrulaması gerekir), F17 (klip A/B ASR karşılaştırması), F18 (model/bağımlılık yöneticisi — E/K), F20 (kabul matrisi belgesi).
