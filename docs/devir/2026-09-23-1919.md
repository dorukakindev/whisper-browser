# Devir Notu — 2026-09-23 19:19 — R118 gauntlet T1/T3/T4

## Dal / bağlam

- Depo: `dorukakindev/whisper-browser`, dal `codex/r118-gauntlet-1790190179`
- Başlangıç kodu: `f0ecb1c` (origin/master)
- Bitiş kodu: `1ddd52d` — `fix(translate): R118 — orta-koşu kalıcı sağlayıcı hatası kuyruğu durdurur + ölçek/kurtarma regresyon testleri`
- Dokümantasyon commit'i bu nottan ayrı tutulur (aşağıdaki git log'a bak).

## Ne yapıldı

Kalan gauntlet hatlarının doğrulama turu:

- **T1 — HLS/DASH altyazı yakalama:** kod haritası çıkarıldı (`src/browser-subtitles.js`, `src/browser-dash-capture.js`). Kapsam zaten geniş: `EXT-X-MEDIA` SUBTITLES+CLOSED-CAPTIONS, rolling playlist mediaSequence, byte-range; DASH `SegmentTemplate` $Number$/$Time$/$SubNumber$ + `%0Nd`, `SegmentTimeline`, `SegmentList`, `SegmentBase indexRange`, dinamik-MPD sınırlı-zaman kapısı, init URL, 10k parça sınırı, 6'lı indirme, 64 MB limit, 64'lü LRU. Doğrulanmış boşluk YOK → düzeltme yok.
- **T3 — çeviri bütünlüğü + R116 `rescue_pool` regresyon kanıtı:** iki yeni test.
  - `test_translate_rescue_scales_across_chunks_parallel`: 400 blok/20 parça; her parçada son cümle grubu düşürülür → 20 tekil kurtarma paketi paylaşımlı `rescue_pool`'a akar; eksiksizlik + `max_inflight ≥ 2` + deterministik istek sayısı (40).
  - `test_translate_thousand_cues_invariance_order_and_count`: 1000 blok; sıra + zaman damgası + tam çeviri + tam 50 toplu istek.
- **T4 — sağlayıcı maliyet/dayanıklılık:** bir doğrulama + bir gerçek düzeltme.
  - `test_translate_rescue_bundle_server_error_marks_only_missing`: tekil kurtarma isteği 503 dönerse `call_api_with_retry` içinde toparlanır; diğer parçalar etkilenmez.
  - **BUG:** prob-sonrası kalıcı hata (kota/kimlik/model) kalan parçaları durdurmuyordu — her parça tüm rotaları dolaşıp boşa istek yakıyordu. `fatal_stop` bayrağı eklendi: ilk FATAL sınıflı hata bayrağı kurar; `call_api_with` rota döngüsü başında kontrol eder → sonraki parçalar/kurtarma istekleri API'ye gitmeden aynı nedenle düşer.
  - `test_translate_midrun_fatal_error_stops_queued_chunks`: 10 parçada 2. sinde kota → toplam 2 API isteği; `failedReasons` tamamı `quota`.

## Değişen dosyalar

- `backend/transcribe.py` (+12): `fatal_stop` bayrağı, `call_api_with` rota-kontrol noktası, `as_completed` döngüsünde FATAL tespiti + log.
- `backend/test_transcribe.py` (+199): 4 yeni test (yukarıda).

## Test sonuçları

- `python3 backend/test_transcribe.py` → **188 geçti / 10 başarısız**; başarısızların tamamı ortamsal (`numpy`, `faster-whisper` kurulu değil) — master'da da aynı 10.
- `python3 -m py_compile backend/transcribe.py backend/test_transcribe.py` ✓
- `node tests/browser-dash-capture.test.js` ✓ · `hls-recovery` 13/13 ✓ · `browser-capture-completeness` ✓ · `browser-capture-queue` (20k interleaving) ✓ · `browser-subtitle-gauntlet` ✓

## Sınırlar / sonraki adımlar

- Gerçek sağlayıcı anahtarı bu ortamda yok; T4 maliyet senaryoları sahte istemcide deterministik koştu. Gerçek API davranışı Windows tarafında bir sonraki çeviride gözlenebilir (beklenen: kota hatasında günlüğe "Kalıcı sağlayıcı hatası [quota]…" satırı + kalan parçaların anında düşmesi).
- T1 canlı-yayın uçtan-uca kanıtı bu turda tekrar koşulmadı; R114'teki live-server doğrulaması geçerlidir.
- `llm_refine` ikinci geçişi `fatal_stop`'u paylaşır — kalıcı hatada ikinci geçiş de API'ye gitmez (kasıtlı).

Rapor: `docs/raporlar/BROWSER_BUG_REPORT_118.md`
