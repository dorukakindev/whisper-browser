# Devir Notu — 2026-09-15 12:59 — Paralel denetim turu (rapor 29): üç P1

## Özet

Bu tur üç paralel salt-okunur alt ajan (renderer.js bakiye, main.js bakiye, backend) + üst ajan satır-doğrulaması ile çalışıldı. Üç P1 doğrulandı: **kuyruk iptalinde oturum kilidi (B17)**, **transcribe girdi kapısının üç özelliği kırması (B18, 8b9a526 regresyonu)**, **izleme klasörü dosyalarının grant'siz kalması (B19)**. Ürün koduna dokunulmadı; tek teslim `BROWSER_BUG_REPORT_29.md`.

## Repo ve dal

- Repo: https://github.com/dorukakindev/whisper-browser · dal `master`
- Başlangıç commit'i: `cf69b208663246b7486c914f45f747ab3d5815a7`
- Tur ortasında kullanıcı/paralel oturum kendi çalışmasını commit'ledi: `8d8179e` (ürün) + `d3ad6d5` (belge) — denetim bulguları bu güncel kod üzerinde satır-seviyesinde doğrulandı.
- Bitiş kod commit'i: `d3ad6d5` (ürün kodu bu turda değişmedi; yalnız rapor + bu not eklendi)

## Değişen dosyalar (yalnız belge)

- `BROWSER_BUG_REPORT_29.md` — yeni rapor (yerel, `.gitignore` deseninde — seri 1-28 gibi commit'e girmez)
- `docs/devir/2026-09-15-1259.md` — bu not
- `DEVIR-NOTU.md` — indeks güncellemesi

## Doğrulanmış bulgular (rapor 29'da tam kanıt zinciri)

- **B17 · P1** — Kuyruk iptalinde `exit` olayı `queueItemId` filtresine takılıyor (`renderer.js:3728` ← iptal 2952'de `currentQueueId=null`) → `activeJobId` kalıcı takılı, sonraki her başlatma "hâlâ kapanıyor" döndürüyor. Tekil iş güvenli; yalnız kuyruk iptali.
- **B18 · P1** — `main.js:14661` koşulsuz `authorizeLocalMediaPath(options.input)` → `.json`/`.srt`/`.vtt` her zaman reddediliyor → JSON re-export (4265), AI açıkla (14516), yalnız-çeviri (18814) **ölü**. `git show 8b9a526` ile regresyon kanıtlı (sertleştirme 2026-09-12'de geldi; özellikler daha eski). Bonus: `syncSrt`/`translateExisting`/`explainTranslation` hiç yetkilendirilmiyor.
- **B19 · P1** — `scanWatchFolder` (986-1025) + `watch:start` + `dialog:openFolder` hiçbir yerde `mediaFileAccess.grant` çağırmıyor → yeni izlenen dosya auth hatası alıyor → `error` → 5 dk cooldown → `addToQueue` dedupe'u `error`'u eşleştirmediği için her döngüde yinelenen satır.
- **B20 · P2** — `importSettings` `state.watchDir`'i geri yüklemiyor; `formats`/`langSuffix`/`watchEnabled` dinleyicileri `_applyingSettings` korumasını atlayıp apply ortasında yarım anlık görüntü yazıyor.
- **B21 · P3** — `mainWindow.on('close')` async IIFE'sinde dış catch yok; korumasız adımlardan biri fırlatırsa `mainWindowClosing` true kalır → pencere kalıcı kapanamaz.
- **B22 · P3** — `mergeCont` açıkken SRT düzenleme `cuesRaw`'ı bayat bırakıyor (koruma yalnız uzunluk farkına bakıyor; klonlar nedeniyle nesne kimliği hep farklı).
- **B23 · P3** — `mergeCont` açıkken birleşmiş VTT cue'sunu düzenlemek dosyada içerik tekrarı yaratıyor; ASS'te yanıltıcı hata.
- **B24 · P4** — `loadSelectedEmbeddedSubtitle` await sonrası kuşak/medya kontrolü yok → A'nın altyazısı B'ye bağlanabilir.
- **B25 · P4** — Alt-önizleme `hoveredRef` null'lanınca `restoreView(null)` erken dönüyor → `previewOriginal` takılı kalıyor.
- **B26 · P4** — spawn-hatası erken dönüşleri `job-*` temp dizini + job-log akışını sızdırıyor; `job-*` başlangıç süpürmesi yok.
- **B16 · P2** — (önceki turun deneyi, rapora burada girdi) secret-store kısmi çözümü sonraki save'de çözülemeyen alanları kalıcı siliyor.
- Alt ajanların ek düşük bulguları (renderer 6-10; main: burnin grant açığı, waveform timeoutsuzluğu, burnin:cancel preflight penceresi, browser-alignment timeoutsuzluğu, `child.kill()` torun-süreç açığı, POSIX env-info, pdf:export atomik olmayan yazım, getEnvInfo GPU hang, second-instance flaşı) raporda "bildirildi" statüsünde — üst ajan satır-doğrulaması bekliyor.

## Testler

- `npm test` tam paket tur başında **yeşil** (231 JS dosyası + backend; "Tüm testler geçti").
- B17/B18/B19/B20/B22/B23: satır-zinciri doğrulaması; B18 ek olarak git bisect kanıtlı.
- Ürün kodu değişmedi → ürün testleri yeniden koşturulmadı.

## Bilinen sınırlar / yarım işler

- Backend alt ajanı (`transcribe.py` ve yardımcıları) bu not yazılırken hâlâ koşuyordu; bulguları gelirse rapor 30'a girer.
- Alt ajanların "düşük" listesindeki maddeler üst-ajan satır doğrulaması bekliyor; raporda ayrı işaretlendi.
- Önerilen düzeltme sırası: B18 → B17 → B19 → B20 → B21/B22/B23/B16 → B24-26.

## Commit/push

- Yalnız belge commit'i yapılacak; rapor `.gitignore`'da (yerel).
