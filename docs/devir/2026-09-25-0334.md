# Devir — 2026-09-25 03:34

## Repo / dal / commit

- Repo: `D:\Whisper Local` → `https://github.com/dorukakindev/whisper-browser`
- Dal: `haze/bug-report-2026-09-24`
- Başlangıç HEAD: `1fbc03f380fba6692078ef8a8490b30f469a559b`
- Bitiş kod HEAD: **aynı** — bu turda ürün kodu değiştirilmedi (kullanıcı: "sen kodu değiştirme").
- Yalnız dokümantasyon eklendi: `docs/raporlar/BROWSER_BUG_REPORT_124.md` + bu not + `DEVIR-NOTU.md` indeks satırı.
- Kullanıcının önceden var olan izlenmeyen dosyalarına (`.freebuff/`, `PROGRAM_BUG_REPORT_*`, `_repro/`, `docs/BROWSER_BUG_REPORT_*.md`, eski `docs/devir/2026-09-19/20-*`, `tests/r92-deep/` vb.) dokunulmadı, commit'e alınmadı.

## Yapılan iş

Kullanıcının "A'dan Z'ye durmadan bug araştır, detaylı test yap, raporla, GitHub'a koy; kodu değiştirme" talebi kapsamında derin denetim sürdürüldü ve `BROWSER_BUG_REPORT_124.md` yazıldı.

### Kesin bulgular (raporda)

- **R124-01 (P2):** `backend/invidious.py:356-386` `_vtt_to_srt` — boş satırsız ardışık VTT cue'larında önceki cue sessizce düşüyor (`-->` satırı `cur`'ı flush etmeden eziyor; `cur is not None` ölü koşul). Aynı girdi `src/browser-subtitles.js::parseTimedBlocks` ile 2 cue üretirken bu dönüştürücü 1 cue üretiyor. Etki: `invidious:subs` yoluyla indirilen SRT'de koşullu, sessiz altyazı kaybı.
- **R124-02 (P3):** `MAX_MEDIA_TIME_SECONDS = 60 * 60 * 1000` (`src/browser-session-store.js:18`, `src/browser-tab-history.js:7`) — saniye alanında ms formülü; etkin sınır ~1000 saat. Zararsız ama yanlış sabit.
- **R124-03 (P3):** Renderer `parseClipInput` (`renderer.js:1617`) `1e3` bilimsel gösterimi reddederken backend `parse_timecode` kabul ediyor — sözleşme asimetrisi, UI'dan erişilemez.

### İncelenip reddedilen adaylar

- `browser-subtitles.js:386` U+2060 WJ regex'i: kasıtlı kaçış mekanizması (commit `8b9a526`); probe ile `\{`,`\}`,`\\` davranışı doğru.
- Arg push/declare uyumsuzluğu: ayrı parser'lar + env-taşımalı sırlar — mimari, bug değil.
- `wrap_text` sarmaması: `sentence` modu tasarımı. `srt_time` AttributeError: probe'da yanlış isim (`format_srt_time` sağlam).
- `transcribe:start` yetkilendirme/iptal yarışı ve kuyruk olay süzgeci: sağlam bulundu.

## Testler / kanıtlar

- `npm test` — **tüm paket geçti** (son satır "Tüm testler geçti"; 198/198 çeviri testi, Python unittest'ler dahil).
- Node probeları: `parseTimedBlocks` boş satırsız/saatli/settings'li/identifier'lı/BOM'lu VTT varyantlarının hepsinde doğru.
- Python probeları (`backend/venv`): `parse_timecode` sınır haritası; `_vtt_to_srt` kompakt-VTT kaybı deterministik olarak gösterildi.
- Geçici probe dosyaları (`tmp-scan-invisible.js`, `tmp-ass-probe.js`, `backend/tmp_vtt_probe.py`, `backend/tmp_probe2.py`) temizlendi; `git status` bunlardan artakalan iz göstermiyor.

## Çalıştırılmayanlar / sınırlar

- Gerçek YouTube hesabı ve canlı Invidious instance'ı doğrulaması yok; fixture/mock kanıtları canlı sağlayıcı başarısı sayılmaz.
- Widevine/DRM, GPU transkripsiyon, gerçek Windows `start.bat` kabulü koşulmadı.
- `tests/run-electron-smokes.js` tam-suite ayrı koşulur; bu turda hedefli bridge smoke + birim/entegrasyon paketi.

## Bağımlılık / şema değişikliği

Yok.

## Sonraki adımlar

- R124-01 için `_vtt_to_srt`'ye flush-on-boundary düzeltmesi + fixture regresyonu (kullanıcı onayıyla kod değişikliği gerektirir).
- R124-02 sabit düzeltmesi ve R124-03 sözleşme hizalaması (düşük öncelik).
