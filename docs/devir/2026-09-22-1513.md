# Devir notu — 2026-09-22 15:13

## Repo ve commitler

- Repo: `https://github.com/dorukakindev/whisper-browser`
- Dal: `codex/catalog-sync`
- Başlangıç ürün commit'i: `b7fe94046ffa6d5281f7ec466b7c8be0b19a978e`
- Paket ucu: `dcbdb95d2289839eaf730ce781fb413549bce74f`
- Ek R106-01 düzeltmesi: `274e492`
- Bütünleştirme sonrası ürün commit'i: `24a3ef0206e09b341647500f271326fc6d8838ed`

## Yapılanlar

1. Kullanıcının verdiği `whisper-browser-paket-2026-09-22` bundle olarak doğrulandı ve ayrı
   inceleme dalında açıldı.
2. 17 paket commit'i; ürün kodu, CI, testler, UI audit aracı, rapor taşıması ve kanıt
   görüntüleri açısından incelendi.
3. Yeniden çeviri fark penceresinde bayat cue ile daha yeni kullanıcı düzenlemesini ezme riski
   bulundu ve `selectedChangesStillMatch()` korumasıyla kapatıldı.
4. Paket `codex/catalog-sync` dalına çakışmasız birleştirildi. Kullanıcının önceden var olan
   `DEVIR-NOTU.md` değişikliği, `PROGRAM_BUG_REPORT_83-85.md`, `_repro`, `.freebuff`, tarihsel
   devir notları ve `tests/r92-deep` dosyaları değiştirilmedi veya commit'e alınmadı.

## Değişen alanlar

- `backend/`: model yanıt kimliği doğrulama, çeviri kalitesi olayı, yalnız-çeviri adlandırma,
  ASS etiketleri ve RTL PDF sırası.
- `src/browser-*`, `src/main.js`: adres çubuğu, arama katlama, sekme sırası, yerel HTTP,
  oturum çapası, görsel türü/PNG dönüşümü, sayfa çevirisi ve sağlayıcı yeniden denemesi.
- `src/renderer/`: EN/TR metinleri, çevrilmemiş filtre, model karnesi, yeniden çeviri farkı,
  açık/koyu tema ve erişilebilirlik düzeltmeleri.
- `src/translation-diff.js`: R106-01 bayat inceleme koruması.
- `tools/audit-ui.mjs`, `.github/workflows/ci.yml`: UI kontrast matrisi ve CI bağlantısı.
- `docs/raporlar/`: tarihsel raporların tek dizinde düzenlenmesi ve R106 raporu.

## Testler

Dar paket testleri: Node 18/18 + 6/6 + 3/3 + 6/6; i18n ve CSS sınır testleri geçti;
Python denetimi 11/11; değişen ana JS dosyalarının `node --check` ve `git diff --check`
sonuçları temiz. Paket inceleme dalında Electron bridge geçti; UI strict denetimi 0 hata verdi.

`pytest` venv'de kurulu değildi; yeni bağımlılık eklemek yerine aynı test dosyası standart
`unittest` giriş noktasıyla başarıyla çalıştırıldı.

## Açık sınırlar

`electron-media-catalog.smoke.js` seçili birleşik smoke sırasında bekledi; paket regresyonu
kanıtlanmadı. Gerçek ücretli sağlayıcı kalitesi ve kişisel kullanıcı verisi test edilmedi.
