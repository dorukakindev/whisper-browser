# Devir Notu — Great Courses gerçek-site CEA kabulü ve doğrudan tam yakalama

Tarih: 2026-09-16 08:31 (Europe/Istanbul)

## Repo ve kimlikler

- Repo: `https://github.com/dorukakindev/whisper-browser`
- Dal: `master`
- Başlangıç ürün kodu: `2b4e4a326f400c171f974feaf8e3115f713f5a97`
- Bitiş ürün kodu: `c974cedb314debbee88c2754f6c8f184aefe2a18`
- Kod commit'i: `c974ced` — `fix: expose full CEA capture from subtitle status`

## Gerçek-site kanıtı

Kullanıcının Great Courses tanı paketi `master.m3u8`, bildirimli `CC1`, 906 video segmentlik plan ve CMAF parçalarından üretilen 5+4 CEA cue gösterdi. Bu kanıtla pending körlüğü, DASH-CEA ve bildirimsiz CEA adayları bu akış için elendi. `92f8f7f` manifest/iframe düzeltmesi gerçek sitede doğrulandı.

## Değişen dosyalar

- `src/renderer/renderer.js`
  - CEA hazır/çalışıyor/kısmi/tamam durumları üst altyazı sağlık şeridine bağlandı.
  - Hazır planda `Tüm altyazıyı getir`, kısmi planda `Eksikleri yeniden dene` gösteriliyor.
  - `capture-full` doğrudan mevcut güvenli tam-yakalama IPC yolunu çağırıyor.
  - CEA ilerleme olayları sağlık şeridini anında yeniliyor.
- `tests/browser-subtitle-health.test.js`
  - Dört CEA yaşam döngüsü ve doğrudan eylem bağlantısı regresyonları.
- `BROWSER_BUG_REPORT_41.md`
  - gerçek-site kanıtı, kök neden, düzeltme, retler ve doğrulama.

## Güvenlik ve davranış kararı

906 video parçası otomatik indirilmez. Açık kullanıcı tıklaması korunur; tıklamadan sonra mevcut düşük bant genişlikli varyant, sıralı decoder, iptal, retry, süre kapsamı ve tamlık kapıları çalışır. `Çeviri oluştur` akışı da mevcut davranışla önce tam kaynağı getirir, sonra çeviriyi başlatır.

Ekrandaki `Video karesi üretilemiyor` mesajı video kare analizi/OCR tanısıdır; CEA ağ yakalamasını engellemediği gerçek tanıda görüldü. Ayrı kanıt olmadan bu turda değiştirilmedi.

## Doğrulama

- `node tests/browser-subtitle-health.test.js` — geçti.
- `node tests/browser-cea-full-capture.test.js` — geçti.
- Electron `tests/electron-browser-cea-full-ui.smoke.js --disable-gpu` — geçti; geniş/dar görünüm, ilerleme ve taşma doğrulandı.
- `npm test` — exit 0, son satır `Tüm testler geçti`.
- `npm run test:electron-bridge` — exit 0.
- `node --check` üç JavaScript giriş noktası ve `python -m py_compile backend/transcribe.py` — geçti.

## Bağımlılık, şema ve kurulum

Yeni bağımlılık, kalıcı şema veya kurulum değişikliği yoktur.

## Açık gerçek-site kabulü

Yeni sürümde Great Courses sayfasını yenileyince üst şeritte `Tüm altyazıyı getir` görünmeli. Tıklanınca 0/906 segment ilerlemesi başlamalı; bitişte tam cue sayısı gösterilmeli ve kaynak SRT GİRDİ klasörüne kaydedilmelidir. Kullanıcı hesabında gerçek 906-parça indirme bu kod oturumunda çalıştırılmadı.
