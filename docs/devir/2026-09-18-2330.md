# Devir Notu — 2026-09-18 23:30 — R71 çalışma zamanı log bug'ları

## Özet

Kullanıcının verdiği canlı oturum logu (20:25–21:03) analiz edildi; 10 bulgu çıkarıldı, 7 gerçek bug/iyileştirme uygulandı ve push edildi. En kritikler: çeviri hata sınıflandırıcısının request-id rakamlarını HTTP kodu sanması (rastgele `authentication` → FATAL abort riski) ve canlı web çevirisinin düşen sağlayıcıya ~800 cümle × ≤3 deneme fırtına atması (kendi 429'unu üretmesi).

- Repo: `https://github.com/dorukakindev/whisper-browser` · Dal: `master`
- Başlangıç HEAD: `087aadccd7b0f953298e68f66cf81bfdb8148f3b`
- Bitiş HEAD: <push sonrası doldurulacak>
- Rapor: `BROWSER_BUG_REPORT_71.md`

## Değişen dosyalar

| Dosya | Amaç |
|---|---|
| `src/renderer/renderer.js` | `openAppDialog`: `maxLength=-1` DOM istisnası → `removeAttribute`; `logLiveTranslationError` özdeş-hata dedup (`×N tekrar`); `player.browserDrmVerdict` ile Widevine karar dedup'ı |
| `backend/transcribe.py` | `classify_translation_error`: yapısal `api_error_status` öncelikli + `model_unavailable` sınıfı + `\b` sınırlı regex (request-id çakışması fix); `FATAL_TRANSLATION_ERRORS` += `model_unavailable`; `call_api_with` rota-kesme kontrolü yapısal status; kullanıcı mesajına `model_unavailable` |
| `src/main.js` | `requestBrowserSentenceTranslationAtEndpoint`: `retry-after` başlığı → `error.retryAfterMs` (≤120 sn); fetch düşmesi → `transportError` bayrağı |
| `src/browser-translation-scheduler.js` | `providerFailureThreshold` (vars. 8) + `consecutiveProviderFailures`; eşikte `tripProviderFailure` ("arka arkaya N isteği reddetti… durduruldu"); retry gecikmesi `max(expBackoff, retryAfterMs)`; başarı sayacı sıfırlar |
| `backend/test_transcribe.py` | request-id rakam çakışması, `model_unavailable` FATAL, yapısal status testleri |
| `tests/browser-translation-reliability.test.js` | devre-kesici, kesikli-hata-sıfırlama, `Retry-After` regresyonları |

## Kök nedenler (log kanıtı)

1. `input.maxLength = -1` → DOM `Failed to set 'maxLength'` — her girişsiz diyalogda (logda 6 kez).
2. `classify_translation_error` düz substring: `request id: ...92540358...` içinde `"403"` → `authentication` (FATAL); `...429832...` → `rate_limit`.
3. `model_not_found`/`No available channel` sınıfı yok → `server_error` → probe abort yok → 33 parça 52 sn boşa.
4. Canlı-çeviri scheduler: Retry-After okunmuyor + devre-kesici yok → ~2,5 dk 502 seli → 120 istek/dk sınırı → 429 seli.
5. Her cümle hatası ayrı `logLine` → 200+ satır sel.

## Test kanıtı

- `backend/test_transcribe.py`: **192/192 geçti**
- `tests/browser-translation-reliability.test.js`: devre-kesici + Retry-After geçti
- `browser-translation-integrity` 10/10 · `translation-endpoints` 20/20
- `npm test`: **EXIT:0 — Tüm testler geçti**
- `node --check` 3 JS dosyası · `py_compile` transcribe.py — temiz
- Electron smoke: bu turda UI davranışı değişmedi (yalnız log/diyalog iç mantığı) — koşulmadı

## Kalan iş / sınırlar

- R71-9: `t0` parça etiketi kozmetik (raporda).
- Dedup özet satırı mesaj değişiminde düşer; son tekrar sayısı ayrıca görünmez.
- `gpt-5.4` kullanıcı ayarında sağlayıcıda yok — artık net `model_unavailable` mesajı veriyor.
- Devre-kesici paylaşımlı isteklerde cümle-catch bazlı sayar → dedup'lı cümlelerde daha erken keser (muhafazakâr, bilinçli).
- `_repro/` ve `tests/report6x-*.test.js` başka akışın WIP'i — commit'siz bırakıldı.
