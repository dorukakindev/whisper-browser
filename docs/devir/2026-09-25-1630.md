# Devir Notu — 2026-09-25 16:30

## Kapsam
`docs/raporlar/BROWSER_PLAN_R123.md` §5 **Adım 3 (Faz B)** — stacked PR serisi, `codex/r124-step2-faza-1790350667` üstünden dallandı.

## Yapılanlar (plan maddeleri 3.1–3.4)

### 3.1 Tek satır üst çerçeve
- `main.js` `titleBarOverlay.height: 36 → 38` (win32); `--window-controls-safe-height: 38px` token güncellendi.
- Browser modunda `.player-head` artık **38px tek satır**: `[W logo menüsü | sekmeler | uygulama menüsü]`. Geri düğmesi, başlık-kopya, `.player-workspace-switch` ve `.player-head-actions` bu modda gizli.
- `#appModeMenu` (sol, `W` işaretli summary): Oynatıcı / Browser (role=menuitemradio, `aria-checked` senkron) + Medya kitaplığı — hepsi `data-browser-proxy` ile mevcut gerçek düğmelere basar (`workspacePlayerMode`/`workspaceBrowserMode`/`playerCatalogEntry`); hiçbir davranış yeniden yazılmadı.
- `#appMenu` (sağ, dişli): uygulama dili select (`.player-locale-control` head'den buraya taşındı) + Ayarlar (`playerHeadSettings`) + Görev merkezi (`playerTaskCenterToggle`) + PDF kitap aç (`pdfReaderOpen`) proxy'leri.
- Sekme şeridi başlık çubuğunda: `button, summary, [role=button]` zaten global `no-drag`; ayrıca `.browser-tab`, `.browser-tab-strip`, `.browser-tabbar` için de eklendi. Sağ `padding-right: calc(10px + safe-width)` pencere düğmeleriyle çakışmayı önler.
- Klavye odak sırası değişti: appModeMenu → sekmeler → appMenu → araç çubuğu.

### 3.2 Birleşik Whisper düğmesi
- `.browser-translate-split` artık `<details>`'in kendisi; summary, `browserSubtitleSettingsToggle` kimliğini/dataset.state/aria-busy'i koruyan tek bağlamsal düğme (ikon + `#browserSubtitleLabel` + caret).
- Medya yok + iz yok + işlem yok → `hidden` (updateBrowserSubtitleSummary). İz var → "Altyazı · N". Çeviri sürüyor → `data-state="busy"` nabız noktası.
- Ayarlar girişi popover'ın ilk öğesi: `id="browserSubtitleSettingsMenu"` → `closeBrowserToolbarMenus(); toggleSettingsPage('browser-subtitles')`. Boş-iz CTA'sı da bu öğeye yöneltildi.

### 3.3 Durum hapı
- `.browser-workspace` grid'i `auto auto minmax` → `auto minmax` (2 satır). `.browser-signal` artık `position:absolute; left:10px; bottom:10px; height:32px` süzülen hap; `signal-collapsed`/`signal-quiet` display:none kuralları korunur; `setBrowserSignal` API'si değişmedi.
- `#browserSignalMenu` (⋮) hap içinde; popover `#browserSubtitleHealth` + `#browserVideoSubtitles` + `.browser-signal-body`'yi sarıyor, hap üstünde açılır. `initializeSettingsPages` reparent'ı etkilenmeden çalışıyor (iç içe geçmiş donor bloklar sorunsuz).
- Hap metni `white-space:nowrap`+ellipsis; tam metin `browserSignalText.title`'da (hover).

### 3.4 Gruplu "Daha fazla"
- Popover başında `#browserMoreSearch` (menü içi arama — tr-locale lowercase eşleşme, isabetli alt menü otomatik açılır).
- `.browser-more-quick` hızlı satır: yeni sekme (proxy→browserTabNew), `#browserReopenTab` (Ctrl+Shift+T), `#browserFindOpen` (Ctrl+F), zoom üçlüsü.
- Alt menüler: Sayfa ▸ / Video ▸ / Site ▸ / Görünüm ve yardım ▸ (`<details>` summary + caret).
- `disabled` menu öğeleri `display:none` ile gizlenir; **tüm öğe id'leri korundu**.

## Menü yaşam döngüsü
`browserToolbarMenuIds` → `[browserTranslateMenu, browserPageQuickMenu, browserMoreMenu, browserSplitMenu, browserSiteChipDetails, browserSignalMenu, appModeMenu, appMenu]`; dış-tık `closest` seçicisi ve native-view okluzyon ifadesi üç yeni menüyü de kapsıyor (popover'lar WebContentsView arkasında kalmıyor).

## Kabul ölçümü (gerçek Electron, CDP, Emulation 1440×900)
- `.player-head` yüksekliği **38px**, `.browser-toolbar` top 39, `.browser-view-slot` top **88 ≤ 90** ✓
- appMode/appMenu görünür ve popover viewport içinde (sağ kenar 1282 ≤ 1440)
- Birleşik düğme: medya yok → gizli; `browserTracks` ×3 → "Subtitles · 3" + `ready`; çeviri sürüyor → `busy` görünür
- Hap: 32px, sol-alt (top 847, left 21); ⋮ popover hap üstünde açılıyor (108px, görünür)
- Menü araması "zoom" → 22 öğe süzüldü; temizlenince geri yüklendi
- `aria-checked` senkronu browser modunda doğru
- Görsel kanıt: `.uiprev/browser-chrome-design/chrome-*.png` (smoke çıktıları)

## Testler
- `node tests/player-ui.test.js` 148/148 · `browser-toolbar-clickability` · `browser-report120-regressions` 6/6 · `design-system` · `browser-report117-regressions` 15/15 · `ui-locale` 14/14 (11 yeni EN çeviri) · `css-hardcoded-colors` 467/467 (gölgeler `--shadow-float`/`--shadow-popover` token'ına taşındı) · `browser-tabs` 12 · `browser-adblock` · `browser-parti7` hepsi yeşil
- `electron-browser-chrome-design.smoke.js` gerçek Electron'da **geçti** (hap 32px, popover 100-132px, dar 520px'de viewport içinde) — smoke yeni yapıya güncellendi
- `electron-ui-scale-matrix.smoke.js` **geçti** (değişiklik gerektirmedi — popover'lar tüm ölçeklerde viewport içinde)
- `node --check` renderer.js + smoke dosyaları temiz
- Tam `npm test` koşulmadı; CI koşuyor.

## Doğrulanamayan sınırlar (Windows'ta sende)
- `titleBarOverlay` 38px'in Windows'ta gerçek görünümü (kaptan düğmeleri/sekme hizası) — Linux'ta overlay yok.
- Sayfada bul "Aranıyor…" askısı — R123 açık bulgusu, Ctrl+F'i Windows'ta elle doğrula.
- `electron-browser-experience-smoke` bu kutuda koşamıyor (iç içe Electron zygote FATAL) — eşdeğer assertion'lar CDP ile doğrulandı; CI'da koşacak.

## Windows etkisi
`install.bat`/`start.bat` etkilenmez. titleBarOverlay yalnız win32 dalında. Hiçbir Windows yolu/cmd davranışı değişmedi.

## Commit'ler
- Kod+test: `d3850bd`
- Docs+changelog: bu notla birlikte
- Başlangıç HEAD: `fda2272` (Adım 2 ucu)
