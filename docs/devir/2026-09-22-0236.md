# Devir Notu — 2026-09-22 02:36 (Europe/Istanbul)

Repo: `dorukakindev/whisper-browser`. Altı görevli paketin ikinci bacağı (T2 — tam yakalama eksiksizlik kanıtı) `devin/t2-capture-completeness` dalında tamamlandı. Başlangıç tabanı `origin/master@4453ae0` (PR #16/R97 merge'i sonrası). Kod commiti `f8af322`; belge commiti bununla birlikte.

## Ne yapıldı

- **F-99-1 (gerçek hata, düzeltildi):** `ceaCaptureSegmentIdentity` `disc:seq` kimliği kullanıyordu. Canlı playlist yenilemesinde araya `#EXT-X-DISCONTINUITY` eklenince tamamlanmış girdiler sahipsiz kalıyordu: defter 10→15 şişiyor, eksikler yanlış kalıyor, aynı parça yeniden indirilip ikinci kez CEA çözümleniyordu. Kimlik artık `seq:`-ankerli (MEDYA-SEQUENCE tabanı); `sequence` yoksa `disc:url:` yedeği korunuyor.
- **F-99-2 (latent sözleşme düzeltmesi):** renderer `normalizeBrowserCeaCaptureState` `complete` alanını düşürüyordu; eklendi.
- **Node testi** `tests/browser-capture-completeness.test.js`: 30 dk'lık simülasyon — açık→büyüyen→kapalı liste, DISC insertion, imza yenileme (a→b→c), GAP, BYTERANGE üçlüsü tek bundle URL'de, 403, iptal+kurtarma, varyant çökmesi, 304 değişmezliği.
- **Gerçek Electron smoke** `tests/electron-capture-completeness.smoke.js`: izole profil + gerçek HTTPS fixture. Açık liste → dürüst `partial` (`open-playlist`, dosya yazılmadı) → kullanıcı retry'ıyla resume+manifest refresh → `complete`; GİRDİ SRT bağımsız orakla bellek/UI cue kümesiyle birebir karşılaştırıldı; 403 → `partial` (missing=2, dürüst mesaj) → düzelince resume → `complete`.
- **`npm test`** tamamen yeşil; `npm run test:electron-bridge` yeşil.

## Sınırlar (raporda ayrıntılı)

Aynı-içerikli fMP4 segmentleriyle resume sınırında boundary-spanning caption kaybı kanıtlanamadı (tek CEA fixture); BYTERANGE kanıtı node katmanında; UI mesajları bu akışta TR — EN yerelleştirme kapsam dışı.

## Sonraki görevler

T3 (TR çeviri kalite/maliyet korpusu) → T4 (SmartTube OAuth gauntlet) → T5 (çökme/bütünlük/perf) → T6 (temiz-makine repro). Her biri güncel `origin/master`'dan yeni dal + ayrı PR; PR'lar merge edilmez.
