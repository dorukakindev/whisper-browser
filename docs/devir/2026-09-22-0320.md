# Devir — 2026-09-22 03:20 (T4: SmartTube/YouTube OAuth uçtan uca dayanıklılık)

**Repo/dal:** `dorukakindev/whisper-browser` · `devin/t4-smarttube-oauth`
**Başlangıç kod commit'i:** `4453ae003ee092b33d06a802535b65b460169e31` (origin/master)
**Bitiş kod commit'i:** bu notun kod commit'i ile aynı daldaki son `fix`/`test` commit'i (docs ayrı commit).

## Değişen dosyalar

- `backend/youtube.py` — `_endpoint()` test kancası: `WHISPER_YT_TEST_BASE` yalnız loopback host'a yönlendirir; 8 uç nokta kullanımına uygulandı (device/token/revoke/channels/browse/home). Üretim davranışı env yoksa birebir aynı.
- `backend/test_youtube.py` — `TestEndpointOverride`: loopback kabul, uzak/şema-sızıntı redleri, env yokken prod.
- `src/main.js` — **F-101-1 düzeltmesi:** `youtube:browse` handler'ı `_ytBrowseTail` ile serileştirildi + `mediaJobs.youtube` için ≤10 sn bekleme. Uzun işler (poll) için anlık red korunuyor.
- `tests/electron-youtube-oauth.smoke.js` — **yeni.** Gerçek main+renderer, sahte OAuth/YouTube loopback sunucusu, PKCE loopback'e elle state enjeksiyonu, device-code QR akışı, iptal/geç-cevap/red, refresh sayımı, abonelik feed'i, kart→player handoff, istemci değişimi, logout/revoke. 8 ekran görüntüsü + `report.json`.

## Hata kök nedeni (F-101-1)

`mediaJobs.youtube` tek proses slot'u; iki `youtube:browse` (veya browse+refresh) çakışınca ikincisi anında `{ok:false,'Bir YouTube işi zaten çalışıyor.'}` → renderer bölümü "feed alınamadı" ile boş bırakıyordu. Kısa+idempotent browse'lar zincirleme serileştirilip sınırlı bekleme eklendi.

## Kanıtlar

- RED: smoke Faz-4 eşzamanlı browse — düzeltme öncesi `'already running'` hatası; GREEN: aynı koşu iki browse da `ok:true`, sunucuda iki `browseId`.
- Sayımlar: `deviceCalls=3`, `tokenGrants={device:3,code:1,refresh:1}`, `revokeCalls=1`, `browseCalls=[7 kayıt]`, `meCalls=2`.
- Görüntüler `.uiprev/youtube-oauth/*.png` (gitignore'lu scratch — rapora gömülmedi).

## Testler

Koşuldu: `electron-youtube-oauth.smoke.js` (gerçek Electron, DISPLAY=:0), `npm test`, `npm run test:electron-bridge`, `python3 -m unittest test_youtube`. Koşulmayanlar: gerçek Google hesabı akışı (kullanıcı katılımı gerekir), 600sn gerçek timeout, Windows platformu.

## Sınırlar / sonraki adımlar

- Mock OAuth başarısı gerçek hesap kanıtı değildir — Google tarafında consent/scope reddi/refresh-rotation doğrulanmadı.
- `slow_down` grant'ı ve InnerTube continuation ayrı senaryo olarak eklenebilir.
- PR açık bırakılacak (merge kullanıcıda).

## Bağımlılık / şema

Yeni bağımlılık yok. Yeni env: `WHISPER_YT_TEST_BASE` (yalnız loopback; test kancası).
