# Devir notu — 2026-09-20 15:48 (Europe/Istanbul)

## Kapsam
Parti 5a — SmartTube iyileştirmeleri, katalog E-maddeleri:
- **A01** Kart bağlam menüsü
- **A03** Video / kanal gizleme (yerel)
- **A08** İçerik türü filtreleri (Shorts / izlenenler / canlı / Trend rayı)

A22 (yerel çalma listeleri) ve kalan A38 alt maddeleri (radyo/ilgili zinciri, karıştır, en çok oynatılan) bilinçli olarak 5b'ye ertelendi — tek pakette çok büyüktü.

## Commit'ler
- Kod: `dc0ea54` — `SmartTube: kart baglam menusu + yerel gizleme + icerik turu filtreleri (A01/A03/A08)`
- Önceki kod commit'i: `46f1d80` (Parti 4)

## Değişen dosyalar
- `src/renderer/renderer.js`
  - `ST_HIDDEN_VIDEOS_KEY`/`ST_HIDDEN_CHANNELS_KEY` (`localStorage`, `ST_HIDDEN_MAX=500`), `loadStHidden*`/`saveStHidden`
  - `stIsShort` (type/URL/isShort sinyalleri — süre tek başına short sayılmıyor), `stVideoHidden` (gizli id → gizli kanal → Shorts → canlı → izlenen [kütüphane `completed` veya ≥%95])
  - `stFilterVideos`, `applyStContentFilters` (mevcut kartları yerinde söker; refetch yok), `renderStHiddenList` + geri alma
  - `openStCardMenu` — tek sablon menü, `role=menu`/`menuitem`, ok tuşu + Esc gezinmesi, dış-tık kapanma, viewport kıskacı; `card._stVideo` ve `data-video-id` meta verisi; kartlarda `contextmenu` + `⋯` düğmesi
  - Filtre çağrı noktaları: ana feed, Trend rayı, genel bölüm, `renderSmartTubeGrid`, `stAppendSearchResults`
  - `PERSIST_CHECKBOX_CONTROLS` += `stHideShorts/stHideWatched/stHideLive/stHideTrending`
- `src/renderer/index.html` — stDisplay paneline "İçerik filtreleri" grubu + 4 checkbox + "Gizlenenler" listesi
- `src/renderer/styles.css` — `.st-card-menu*` (hover'da görünen ⋯ düğmesi dahil), `.st-display-sep/group/check`, `.st-hidden-*`
- `src/renderer/ui-locale.js` — 17 yeni EN/TR çifti
- `src/settings-security.js` — aynı 4 checkbox id'si persist listesine
- `tests/smarttube-filters-parti5.test.js` — yeni (~30 assertion): `stIsShort`/`stVideoHidden`/`stFilterVideos` vm-sözbirimi, gizle/geri-al akışı, kalıcılık, menü/panel bağlantı regex'leri

## Test
- `node tests/smarttube-filters-parti5.test.js` — OK
- `node tests/report67-smarttube-wiring.test.js` — 72/72
- `npm test` (tüm süit) — **Tüm testler geçti**
- `node --check src/renderer/renderer.js` — OK
- Electron smoke'ları bu ortamda koşulmadı (bilinen headless/desktopCapturer/Windows-fixture sınırları önceki notlarda).

## Bu oturumda yakalanan gerilemeler
- `report67` davranış testi sahte DOM'unda `dataset` yok → `qBtn.setAttribute('data-video-id', …)`'e çevrildi.
- Aynı test `renderSmartTubeGrid(grid, dedupe(videos)` literal'ini kesiyor → çağrı biçimi korunarak `dedupe(videos).slice(0,24)` yapıldı (filtre zaten `renderSmartTubeGrid` içinde).
- `design-system` testi `:focus-visible { outline: none }` yasaklıyor → menü öğesinde focus görünür bırakıldı.

## Notlar / sınırlar
- Menüde bilerek "Watch Later" yok (katalog koşulu: hesap gerektiren sahte öğe eklenmez).
- Gizleme tamamen yerel (localStorage); Invidious/YouTube'a bildirim gitmez — paneldeki ipucu metni bunu söylüyor.
- Sonraki: Parti 5b (A22 + kalan A38), sonra Parti 6.
