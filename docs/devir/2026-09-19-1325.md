# Devir notu — 2026-09-19 13:25 — R83 browser denetimi (kod değişikliği yok)

## Özet

Browser tarafının tamamı 8 kümeli paralel salt-okunur denetimle tarandı;
ajan bulguları tek tek kaynakta yeniden doğrulandı. Sonuç
`BROWSER_BUG_REPORT_83.md` (repo kökü). **18 tekil bulgu:** 6×P2, 12×P3.

- Repo: https://github.com/dorukakindev/whisper-browser, dal `master`
- Başlangıç ürün commit'i: `f586e28617d7fa23c3af5479e3e84731d3d0273a`
- Bitiş ürün kodu: aynı (bu tur kod değiştirmez — yalnız rapor + not)

## Bulgu özeti (detay raporda)

- **P2:** CEA checkpoint-resume cue çiftlenmesi (checkpoint `normalizeCue`
  `captionMode`/`provenance` düşürüyor); runtime-state probu doldurulmuş/
  gizli/select input'u kirli form sayıp sekme boşaltmayı engelliyor;
  `browser:open-link` userinfo URL'sini policy'siz yüklüyor +
  credential'ı `restoredUrl`/oturum dosyasına yazıyor (M-04'ün
  "policy engelliyor" sonucu düzeltildi — `loadURL` programatik olduğu için
  `will-navigate`/`will-frame-navigate` ateşlenmez, Electron dokümanıyla
  teyit); browser modunda `naturalAdvance < 1` kapılı oynatma
  politikaları (loopCue/autoPause/shadowing) 1 sn'lik medya poll'u yüzünden
  hiç tetiklenmiyor; `applyAudioPreference` createMediaElementSource
  hatasında AudioContext sızıntısı + sonsuz yeniden deneme; dinamik-blok
  devamı eski işi değiştirirken kuyruktaki apply batch'leri sessizce
  düşüyor (DOM'a yazılmayan çevrilmiş bloklar).
- **P3:** negatif kalibrasyon ofsetinde phantom `[0,0.001]` cue; arama
  sonucu fileId/fileName farklı `files[]` girdilerinden; ölü
  `already_unloaded` filtresi; `normalizeContext(null)` TypeError;
  cross-origin frame'de link-hints Escape'siz kalıyor; tanı `recent`
  taşması; `session.blocks` append-only → `partial` kilitlenmesi;
  `ref.originals` bayatlığı geri yüklemede site metnini eziyor; duplicate
  `cue.id` → `sentenceIdFor` çöküşü → `completed<total`; exclusions
  rescan emit/handler çift-iş yarışı; manga `layout()` null-region
  TypeError; annotations unsave sessiz no-op.

## Doğrulama

- Tüm bulgular bu oturumda `file:line` ile kaynakta tekrar okundu; CEA
  çiftlenmesi ve phantom-cue için Node repro'su çalıştırıldı.
- B83-03'ün kritik varsayımı (programatik `loadURL`'de `will-navigate`
  ve `will-frame-navigate` tetiklenmez) electron/electron resmi
  `docs/api/web-contents.md` ile teyit edildi.
- Baseline: bu Linux VM'de `npm test` 13 dosyada düşer — hepsi ortam
  nedeni (Windows ffmpeg.exe/venv/genanki/yt_dlp/fts5 yokluğu ve
  Windows-yol semantiği testleri); ürün regresyonu yok. Ürün kodu
  değişmediği için test paketi bu tur için ayrıca koşulmadı.

## Sınır/araç notları

- 8 ayrı-VM ajan kullanıldı; SWE-2 ücretsiz sınırı (~6-7 eşzamanlı
  oturum) yüzünden 3 küme ilk koşuda başlamadı — biten oturumlar
  uyku durumuna alınıp workflow `run_id` ile devam ettirildi.
- `renderer browser-UI` ve `IPC/webview` kümeleri yeni bulgu üretmedi
  (temiz kanıtları raporun "Doğrulanıp temiz çıkan yüzeyler" bölümünde).
- Node bu VM'de elle kuruldu: `~/node22` (v22.14.0, nodejs.org tarball;
  `export PATH=~/node22/bin:$PATH`).

## Sonraki adımlar

- P2'ler için düzeltme turu (önerilen yönler raporda). Özellikle
  B82-03 (policy baypası) ve B82-04 (ölü oynatma politikaları)
  kullanıcı-görünür etkisi en yüksek olanlar.
- İsteğe bağlı: B82-01/02 için hedefli Node repro testleri `tests/`
  altına kalıcılaştırılabilir.
