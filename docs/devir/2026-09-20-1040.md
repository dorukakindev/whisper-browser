# 2026-09-20 10:40 — SmartTube süreklilik 1. adım: kart ilerlemesi + devam rayı

## Bağlam
`FEATURE_RECOMMENDATION_REVIEW_2026-09-20.md` 1. öncelik: SmartTube
kartlarında izleme ilerlemesi + "izlemeye devam et", ardından kullanıcı
yönetimli oynatma kuyruğu ("progress/history first; queue second").
Bu oturum ilk yarıyı teslim eder.

- Repo / dal: https://github.com/dorukakindev/whisper-browser — `master`
- Başlangıç HEAD: `6db0be8d24d37b23c2e41c55b6ab216fcc2a1b9b`
- Ürün+test commit'i: `fc42b40d3935edb5927a23da6e9d087045b74600`

## Değişen dosyalar
- `src/renderer/renderer.js`
  - `buildSmartTubeCard`: `mediaKeyFor('youtube', videoId)` →
    `watchItemByKey` ile kütüphane kaydı; varsa `.st-card-thumb` altına
    `.watch-progress` çubuğu (tamamlanmış = %100). Yarım kayıtta tık
    `player.pendingLibrarySeek` kurar — `loadedmetadata`/`stream open`
    tarafında zaten uygulanan mekanizma (renderer.js ~24062/20814).
  - `stContinueWatchingVideos()` (yeni): `watchLibraryCache`'den
    `type==='youtube' && !completed && progress>0` kayıtları lastWatched
    desc sıralar, kanonik `youtube:<id>` anahtarıyla tekilleştirir, max 12.
    Küçük resim yalnızca `lastInvidiousInstance/vi/<id>/mqdefault.jpg`
    vekili üzerinden; instance yoksa thumbnail basılmaz.
  - `renderSmartTubeSection('home')`: render öncesi
    `window.api.listWatchLibrary()` ile cache tazelenir (stale-seq korumalı),
    "İzlemeye devam et" başlığı + kartlar ızgaranın başına prepend edilir;
    ray kimlikleri `dedupe`'ye dahil edilir (akışta çift kart yok).
- `src/renderer/styles.css`: `.st-card-progress` (thumb alt kenarında tam
  genişlik çubuk; `.watch-progress` token'ları yeniden kullanılır).
- `src/renderer/ui-locale.js`: `İzlemeye devam et` → `Continue watching`.
- `tests/report67-smarttube-wiring.test.js`: kart testi `buildCardHarness`
  paylaşımlı vm+mini-DOM harness'ına taşındı; yeni davranış testleri —
  ilerleme çubuğu + `pendingLibrarySeek` (300/600→%50+seek), tamamlanmış
  kart (%100 çubuk, seek yok), sıfır ilerleme (çubuk yok), ray
  sıralama/tekilleştirme/elemeler ve vekil thumbnail.

## Doğrulama
- `node tests/report67-smarttube-wiring.test.js` → **68/68**
- `node --check` renderer.js / ui-locale.js / main.js → temiz
- `npm test` → tek başarısız dosya `watch-index-assets.test.js`:
  `node:sqlite` fts5 modülü bu Linux Node build'inde yok (belgeli ortam
  sınırı, ürün regresyonu değil).

## Kalan sınırlar / sonraki adım
- Canlı Electron UI koşusu bu VM'de Invidious feed gerektirir — görsel
  doğrulama Windows host üzerinde yapılmalı (ray görünümü, kart çubuğu).
- Öncelik 1'in ikinci yarısı (kullanıcı yönetimli Play Next kuyruğu) ayrı
  turda: `ended`/manuel-sonraki yarışı ve kuyruk kalıcılığı tasarımı gerekir.
