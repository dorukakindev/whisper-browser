# Devir Notu — 2026-09-18 23:55 — YouTube OAuth girişi oynatıcı panelinde

## Özet

Kullanıcı geri bildirimi: YouTube OAuth cihaz-kodu girişi yalnız SmartTube sidebar'ındaydı (`stYtLoginBtn`); oynatıcı panelinde ("player kısmı") yalnız cookie-tarayıcı "YouTube oturumu" alanı vardı. Aynı oturum akışı oynatıcı paneline de bağlandı.

- Repo: `https://github.com/dorukakindev/whisper-browser` · Dal: `master`
- Başlangıç HEAD: `b89a76b0d0ab3fab8aa803e70b4bdb05ba53b865`
- Bitiş kod commit'i: `49d76988696ea379b198594233c9c2ec9a9f894a` (doküman commit'i ayrı)

## Değişen dosyalar

| Dosya | Amaç |
|---|---|
| `src/renderer/index.html` | `playerSourcePanelYoutube` içine `playerYtOAuthBtn` (YouTube ile giriş — Google hesabı) + `playerYtOAuthStatus` (bağlı kanal adı) — `playerCookieBrowser` alanının hemen altına |
| `src/renderer/renderer.js` | `refreshYoutubeAuthUI()`: st-* düğmelerinin yanında oynatıcı düğmesi/durumunu da senkronlar (bağlıyken "YouTube bağlı" + kanal adı, çıkışta gizli); `initSmartTube` içinde `playerYtOAuthBtn` → `openYoutubeLogin` bağlantısı |
| `src/renderer/styles.css` | `.player-yt-oauth` / `.player-yt-oauth-status` (mevcut player-yt-info kalıbı) |
| `tests/electron-smarttube-boot.smoke.js` | `ytPlayerBtnSynced` + `ytPlayerCleanup` probları |

## Davranış

- Oynatıcı → YouTube sekmesinde artık "YouTube ile giriş (Google hesabı)" düğmesi var; aynı `youtubeLoginModal` cihaz-kodu akışını açar (Client ID/Secret kurulumu → `google.com/device` onayı → bağlı durum + çıkış).
- Oturum tek kaynaktan yönetilir: `youtubeLoggedIn`/`youtubeUserName` → `refreshYoutubeAuthUI()` her iki yüzeyi de günceller (sidebar ve oynatıcı paneli aynı anda senkron).
- Çıkış modalın "logged" görünümünden yapılır; oynatıcı düğmesi çıkışlıyken de modalı açar.

## Test kanıtı

- `electron-smarttube-boot` smoke: **geçti** — `ytPlayerBtnSynced: true` (girişliyken düğme + 'MockYT' durumu), `ytPlayerCleanup: true`
- `ui-locale` 14/14 · `design-system` OK · `browser-experience` 7/7 · `electron-bridge` smoke OK
- `node --check renderer.js` temiz
- `npm test` tam paketi bu küçük renderer değişikliği için koşulmadı (hedefli testler + smoke yeşil)

## Sınırlar

- Oynatıcı düğmesi metni `refreshYoutubeAuthUI` çağrılarında güncel yerel dile döner; dil değişimi sonrası ilk senkrona kadar önceki dilde kalabilir (mevcut st-* kalıbıyla aynı).
- `playerCookieBrowser` (yt-dlp cookie yolu) OAuth'tan bağımsız kalır — ikisi ayrı oturum mekanizmaları; OAuth abonelik/feed'i, cookie yt-dlp bot/yaş doğrulamasını besler.
