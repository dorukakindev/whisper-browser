# R110-01 SDH işaret kaybı düzeltmesi

Tarih: 2026-09-23. Depo: `dorukakindev/whisper-browser`; dal: `codex/catalog-sync`. Başlangıç ürün kodu `8eee398a13eaf523630eabbbb055d6997ba9c908`, bitiş ürün kodu `d8dd093`.

`backend/subtitle_sdh.py`: Çıkarılan SDH işaretleri artık yalnız toplam sayıyla değil, parantez türü ve doğrulanmış müzik/alkış eşdeğerlikleriyle eşleşiyor. Bilinmeyen hedef etiket kaynak işaretinin yerine sessizce geçemiyor. Güvenilir eşleşme yoksa kaynak işaretleri korunuyor. `backend/test_subtitle_sdh.py`: ilk/ikinci işaretin eksilmesi, karışık parantez, diyalog parantezi ve farklı efekt için regresyonlar eklendi.

`docs/raporlar/BROWSER_BUG_REPORT_110.md`: Önceki `[GUNFIRE] (glass shatters)` örneğinin geçersiz olduğu açıkça düzeltildi; gerçek `[music] [applause]` → `[müzik]` üretimi kaydedildi. `DEVIR-NOTU.md` indeksine bu not eklendi.

Doğrulama: `backend/venv/Scripts/python.exe -m unittest discover -s backend -p test_subtitle_sdh.py` 8/8; mevcut `test_sdh_markers_restored_after_strip` doğrudan çağrısı geçti; `py_compile` ve `git diff --check` temiz. `pytest` bu venv'de kurulu olmadığı için kullanılmadı. Kullanıcı tercihiyle tam `npm test` ve Electron/gerçek sağlayıcı koşulmadı. Yeni bağımlılık, kurulum veya şema değişikliği yok.

Sınır: Genel SDH çeviri eşdeğerlik sözlüğü sınırlıdır; bilinmeyen çeviri etiketlerinde anlam kaybı yerine özgün kaynak etiketi kullanılır. Gerçek video çıktısı bu turda doğrulanmadı. Başlangıçta mevcut ilgisiz çalışma ağacı değişiklikleri commit'e eklenmedi.
