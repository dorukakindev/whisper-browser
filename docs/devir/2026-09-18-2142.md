# Devir Notu — 2026-09-18 21:42

## Kapsam

`BROWSER_BUG_REPORT_69.md`'deki 36 bulgunun mevcut koda karşı doğrulanması ve doğrulanan GERÇEK bug'ların düzeltilmesi. Önceki devir: `docs/devir/2026-09-18-1947.md` (R70 YouTube OAuth).

- Repo/dal: `whisper-browser` / `master`
- Başlangıç HEAD: `8879958d4262dcb3928b77f6353b19f0548d2b15`
- Bitiş kod HEAD: (bu notun doküman commit'inden önceki kod commit'i — aşağıda)

## Sınıflandırma özeti (rapor §9'da tam tablo)

- **GERÇEK → düzeltildi: 12** — K1, K2, K4, K5, K7, O11, O14, O15, O16, O17, D3, D4
- **Zaten doğru / FALSE POSITIVE: 11** — K3, K6, O1, O4, O8, O9, O12, O18, D2, D6, D7
- **Ertelenen iyileştirme: 13** — O2, O3, O5, O6, O7, O10/D10, O13/D9, D1, D5, D8, D11

## Değişen dosyalar

| Dosya | Değişiklik |
|---|---|
| `src/browser-sensitive-keys.js` (YENİ) | UMD ortak sözlük: `SENSITIVE_KEY_NAMES`, `isSensitiveKey`, `startsWithSensitivePrefix` (x-amz/x-goog/x-api/aws/google prefix'leri). CJS + renderer (`window.BrowserSensitiveKeys`) |
| `src/browser-place-url.js` | UMD fabrikası ortak sözlüğe bağlandı; `redactUrlSensitiveParams` eklendi (kullanıcıinfo + query + hash parametreleri; değişiklik yoksa ham URL aynen korunur → sondaki `/` normalizasyonu kapalı-sekme geri yüklemesini bozmuyor); hash'te ayrı `hashChanged` bayrağı (güvenli parametre yeniden-kodlaması önlendi) |
| `src/browser-adapters.js` | `persistentBrowserMediaUrl` + `sanitizeManifestPreview` ortak sözlük; `MANIFEST_SECRET_KEY_RE` NAMES + `key` + prefix alternasyonları |
| `src/browser-adblock.js` | `safeFilterRule` ortak sözlük + `key` (bearer satırı korundu) |
| `src/browser-playback-diagnostics.js` | `redactDiagnosticText`: `=` ayracı önce, `:` ayracı değerde `=` yasak (`error: idToken=x` çift-yutma bug'ı çözüldü); `code/state/exp` gibi tanı değerleri korunur |
| `src/browser-tab-history.js` | `normalizeClosedBrowserTab` → `redactUrlSensitiveParams` (OAuth callback parametreleri diske yazılmaz) |
| `src/browser-manga.js` | IPv6: `2002::/16` (6to4) ve `2001:0000::/32` (Teredo) reddi |
| `src/subtitle-sentence-layout.js` | `SENTENCE_PROTOCOL_VERSION = 4` + `SUPPORTED_SENTENCE_PROTOCOL_VERSIONS {2,3,4}` + `foldLocale` (Türkçe İ/i/I/ı tek kanon) |
| `src/browser-translation-scheduler.js` | `cancelAll()` sonunda `generation += 1` (abort'u yoksayan sağlayıcının geç yanıtı yeni kuşağa yazamaz) |
| `src/browser-translation-integrity.js` | `submittedSentences` += `queuedSentences` |
| `src/translation-endpoints.js` | 429 `sameProviderAliases` koluna taşındı (varsayılan failover değişmedi) |
| `src/browser-overlay-controller.js` | Observer 128 sınırında LRU tahliyesi (document kökü muaf) + warn; fullscreen restore yalnız bizim değer duruyorsa geri yazar |
| `src/browser-subtitles.js` | `HLS_LANGUAGE_LABELS` (35 dil); `findSubtitleUrls` truncated bayrağı + warn |
| `src/persistent-schema-inventory.js` | `sentence-protocol-js` → v4 |
| `src/renderer/ui-locale.js` | Eksik 10 çeviri (R70 YouTube metinleri + Arama sonuçları) |
| `src/renderer/index.html` | `browser-sensitive-keys.js` script etiketi (place-url'den önce) |
| `tests/browser-translation-integrity.test.js` | `queued` yeni submitted semantiğine uyarlandı |
| `BROWSER_BUG_REPORT_69.md` | §9 doğrulama sonuçları eklendi |

## Doğrulanmayan/FP bulguların kök kanıtları

- K3: `browser-foundation` 24 test geçiyor; init sırası doğru — test regex'i diğer restore çağrılarını hesaba katmıyor
- K6: flush `version` snapshot + yeniden-zamanlama zaten var
- O1: `browserSessionFinalizedForQuit` guard'ı mevcut (main.js:3261,3284)
- O8: `cloudflareProbeState` saf fonksiyon — Map leak yok
- O12: mp4Tfhd/trex `box.start + N <= box.end` guard'ları mevcut
- D2/D6/D7: redirect-hop doğrulama ve çağıran-katman try-catch'leri mevcut

## Test kanıtı

- **`npm test` TAM GEÇTİ — "Tüm testler geçti", EXIT:0** (önceki turun 3 WIP kırığı report62/report63-secret/report63-ssrf bu düzeltmelerle yeşillendi; ui-locale ve persistent-schema-inventory tracked kırıkları da onarıldı)
- WIP regresyonları: report62 ✓ · report63-secret 19/19 ✓ · report63-ssrf 7/7 ✓ · report64-* (bilinen-bug) ✓
- Enjekte script: `buildBrowserOverlayScript` çıktısı `new Function` ile sözdizimi-denetlendi — template-içi backtick yorum hatası bu yolla yakalanıp düzeltildi
- Electron smoke: `smarttube-boot` ✓ · `ui-locale` ✓ · `browser-video-e2e` bu makinede flaky (~%40-50, değişiklikten bağımsız; temiz HEAD'de de aynı, son koşu geçti)

## Sınırlar / notlar

- `_repro/` ve `tests/report6x-*.test.js` dosyaları başka iş akışının WIP'i olarak commit'siz bırakıldı; yalnızca doğrulama için kullanıldı — kapsam dışı.
- Ertelenen 13 iyileştirme rapor §9.3'te; ayrı önceliklendirme ister.
- `browser-video-e2e` smoke'un flake'i (satır 197 overlay zaman aşımı / satır 500 senkron) ayrı bir takip konusu.
