# Devir Notu — 2026-09-20 15:09 (Europe/Istanbul)

- Repo: `dorukakindev/whisper-browser` · dal: `master`
- Başlangıç HEAD: `f3b3a9d` (Parti 1 kod commit'i sonrası) · Bitiş kod commit'i: `40e9567`
- Kapsam: `FEATURE_RECOMMENDATION_CATALOG_2026-09-20.md` E-maddeleri Parti 2 (küçük UI paketi). Katalog toplu uygulama yetkisi değildir; paket sınırlı tutuldu. K/B/R maddelerine dokunulmadı.

## Uygulanan maddeler

- **E01 — Okuma görünümü TOC affordance** (`src/browser-reader.js`): mevcut TOC'ye klavye odağı (`:focus-visible` outline), scrollspy (`syncToc`, rAF-throttled `host` scroll dinleyicisi; aktif başlık `aria-current="true"` + • işareti, TOC listesini otomatik aktif bağlantıya kaydırır) ve `prefers-reduced-motion` uyumu (`scrollIntoView({behavior})` instant'a düşer).
- **E02 — 180 karakter eşiği mikro metni**: okuma görünümü bulunamadığında gösterilen hata artık eşiği açıklıyor: "…en az 180 karakterlik metin ister."
- **A09 — Son kullanılan altyazı izleri üstte** (`src/renderer/renderer.js`): `rememberBrowserTrackUse` başarılı iz yüklemesinde `localStorage`'a `whisper.browserRecentTracks` (dil|etiket anahtarı, en yeni başta, 8 kayıt üst sınır) yazar; `syncOptions` seçenekleri recency sırasına göre dizer ve `↺` işareti ekler. Katalogdaki `lastCaptionPrefs` iddiası güncel kodda yoktu — kendi deposu kuruldu. İz model dizisi (`player.browserTracks`) değişmez; yalnız gösterim sırası değişir.
- **A10 — SmartTube kart görünüm ayarları**: üst çubuğa "Kart görünümü" paneli (`stDisplayToggle`/`stDisplayPanel`, `role="dialog"`, `aria-expanded`, dışarı tıklama + Esc ile kapanır). İki range: `stCardScale` (60–160%, grid `minmax(calc(255px * var(--st-card-scale,1)), 1fr)`) ve `stCardFontScale` (75–150%, başlık/süre/kanal yazıları `calc(... * var(--st-card-font,1))`). İkisi de `PERSIST_VALUE_CONTROLS` + `src/settings-security.js` şemasına (sayısal aralık kayıtlı) eklendi → settings.json'da kalıcı.

## Değişen dosyalar

- `src/browser-reader.js` — TOC scrollspy/focus/reduced-motion + hata mikro metni.
- `src/renderer/index.html` — `stDisplayToggle`, `stDisplayPanel`, `stCardScale`, `stCardFontScale`, `stCardScaleVal`, `stCardFontScaleVal`.
- `src/renderer/renderer.js` — recency yardımcıları (`browserRecentTrackKeys`, `browserTrackRecencyKey`, `rememberBrowserTrackUse`), `syncOptions` sıralama + `↺`, `useBrowserTrack`'ta kayıt, SmartTube panel wiring (`setStDisplayOpen`, `syncStCardDisplay`), PERSIST listesi.
- `src/renderer/styles.css` — `.st-display-panel` stilleri, grid/kart değişkenleri.
- `src/renderer/ui-locale.js` — 6 yeni TR→EN çifti.
- `src/settings-security.js` — `stCardScale`/`stCardFontScale` şema + `UI_NUMERIC_RANGES` aralıkları.
- `tests/settings-security.test.js` — round-trip fixture'ına iki yeni sayısal değer.
- `tests/browser-parti2.test.js` (yeni) — 30+ assert: reader regex kontrolleri, recency fonksiyonlarının vm ile deterministik davranış testi (tekilleşme, sıralama, bozuk JSON, 8 kayıt sınırı, boş iz), HTML/CSS/renderer wiring, ui-locale çiftleri.

## Testler

- `node --check` değişen JS dosyalarında temiz.
- `node tests/browser-parti2.test.js` → OK.
- `npm test` → **Tüm testler geçti** (Node + Python süitleri).
- `node tests/run-electron-smokes.js --all` → 4 smoke başarısız, hepsi ortam kaynaklı ve değişiklikle ilgisiz:
  - `electron-a3-acceptance`: `node_modules/electron/dist/electron.exe` spawn ediyor — Linux'ta Windows ikilisi yok (ENOENT).
  - `electron-browser-menu-visual` + `electron-browser-extras`: `desktopCapturer` pencere listesi Xvfb altında boş (Parti 1'de temiz HEAD'de doğrulanmıştı).
  - `electron-browser-video-e2e`: `.uiprev/flower.mp4` fixture'ı repoda yok (`WHISPER_E2E_VIDEO` ile verilebiliyor).
  - İlgili yüzeylerin smoke'ları geçti: `electron-smarttube-boot` (grid probe: cardCount=10, minCardWidth render doğrulandı), `electron-ui-locale`.

## Bilinen sınırlar / sonraki paketler

- Kalan E paketleri: Parti 3 oynatıcı (A19, A36, A38), Parti 4 sayfa araçları (B22), Parti 5 SmartTube (A01/A03/A08/A22), Parti 6 sekme/PiP (B06/B14/B10), Parti 7 büyükler, Parti 8 a11y+kalanlar.
- Harici API/lisans gerektiren maddeler (mpv/VLC, moviehash vb.) ayrıca doğrulanacak.
