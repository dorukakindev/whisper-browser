# Devir notu — 2026-09-18 14:21

## Konu

Invidious (SmartTube tarzı) entegrasyon denetimi: 6 salt-okunur ajan,
bulgular `BROWSER_BUG_REPORT_67.md` dosyasında. Kod değişikliği yok.

## Repo / dal / commit kimlikleri

- Repo: `https://github.com/dorukakindev/whisper-browser` — dal `master`
- Başlangıç commit: `ea4f386` (denetlenen kod `9ac31a4` + `9f4d3b8`
  Invidious teslimlerini içeriyor)
- Ürün kodu bitiş commit: `ea4f386` — değişmedi (salt-okunur audit)
- Dokümantasyon commit'i: bu notun bulunduğu commit

## Yapılanlar

- 6 ajan: `backend/invidious.py`, `backend/media.py download_stream`,
  `main.js`/preload IPC, renderer oynatma yolu, SmartTube UI, çapraz-kesim.
- En kritik bulgular güncel ağaçta satır satır doğrulandı; API alan
  adları upstream Invidious kaynağı ve timedtext/srv3 spec ile
  karşılaştırıldı.
- `BROWSER_BUG_REPORT_67.md` yazıldı, `DEVIR-NOTU.md` güncellendi.

## Genel hüküm

Entegrasyon uçtan uca bozuk: 9 backend komutundan yalnız `probe` başarı
raporlayabiliyor (eksik şemayla). En ağır bulgu **mevcut işlevsellikte
regresyon**: TDZ çökmesi renderer'ın ~1200 satırını öldürüyor — yt-dlp
oynatıcısı dahil.

## Bulgu özeti (detay: BROWSER_BUG_REPORT_67.md)

**P0/P1 (8):**
- R67-01 — `initPlayerSource()` `let playerSource`'dan önce çağrılıyor
  → uncaught TDZ ReferenceError → `playerProbe`/`playerStream`/
  `playerDownload`/`onMediaEvent`/`setupRovingTablists` dahil tüm kuyruk
  ölü (4 ajan bağımsız doğruladı).
- R67-02 — `runInvidiousCommand` yalnız `probe`/`subs` yakalıyor →
  feed/search/channel/login/logout her zaman `{ok:false}`.
- R67-03 — login modalı `<script>`'ten sonra → butonlar hiç bağlanamaz,
  uygulama modalda kilitlenir; şifre DOM'da kalır.
- R67-04 — CSP `connect-src`/`img-src` instance hostlarını ve
  thumbnail'leri blokluyor (tek oynatma yolu hlsUrl de engelli).
- R67-05 — tek `mediaJobs.invidious` slot'u paralel feed'leri
  deterministik çaktırıyor.
- R67-06 — auth mimarisi ölü: SID hiçbir subprocess'e gitmiyor +
  `/api/v1/feed/subscriptions` yerine `/api/v1/auth/feed` gerekli +
  login `email_or_user` yerine `email` bekliyor.
- R67-07 — `invidious:downloadStream` `invidious.py`'ye gidiyor ama
  `download_stream` `media.py`'de; +`--url` eksik +`downloaded`
  yakalanmıyor +45s timeout +renderer'da çağırıcı yok (İndir hep yt-dlp).
- R67-08 — `fmt=srv1` isteniyor ama parser srv3 bekliyor → sahte
  "No subtitles available." SRT başarı diye yazılıyor.

**P2 (16):** probe şema sürüklenmesi (`dashUrl`/`videoThumbnails`/
`resolution`/`liveNow` vs okunan eski adlar; `heights`/`stream`/
`audioLangs`/`isLive` hiç verilmiyor) · fetch_subs doğrudan youtube.com
(gizlilik vaadi boşa) + instance zorunluluğu çelişkisi · HLS kurtarma
probeYoutube'a sabit · `opts.instance` doğrulamasız SSRF yüzeyi ·
`invidious:cancel` sahiplik ihlali · sabit 45s timeout · SID renderer'a
sızma riski · download_stream iç kusurları (kör -c copy→.mp4, kısmi
dosya, tmp sızıntısı, yanlış kanal progress, ID'siz dosya adı çakışması)
· arama gizli konteynere yazıyor · home hata dalı abonelik bölümünü
siliyor · feed-cache (yenileme no-op, çıkışta bayat) · bayat-yanıt
koruması yok · auth UI desync · başlangıçta opt-in'siz ağ çağrısı ·
i18n eksikleri + `Giriş`→"Intro" yanlış çevirisi.

**P3 (14):** extract_video_id instance URL reddi, süreç-başına instance
taraması, trending→popular mislabel, çift kart, authorThumbnails eksik,
ham err.message, TypeError yüzeyleri, ölü preload/markup, sibling-subs
kök-ad uyumsuzluğu, ffmpeg decode maskesi, kaynak-değişim staleness,
modal a11y, yan-panel render kusurları.

## Doğrulama / sınırlar

- Salt-okunur audit: ürün kodu değiştirilmedi, test koşulmadı.
- R67-01/02/03/04/05/07/08/17 güncel ağaçta bizzat doğrulandı.
- Audit sırasında renderer/index.html 3 kez yeniden yazıldı (3 UI nesli);
  P1/P2'ler tüm nesillerde invariant; varyant-bağımlı P3'ler işaretli.
- Gerçek API alan adları upstream Invidious kaynağına dayanıyor; canlı
  instance doğrulaması yok (deklare sınır).
- Paralel workstream'in commit'lenmemiş dosyaları ağaçta; bu teslim
  yalnız rapor + devir notu + indeks commit'ledi.

## Sonraki adım önerisi (rapordaki handoff)

Önce R67-01 (TDZ — uygulama zaten her açılışta yarı ölü), sonra R67-02
(olay beyaz listesi), R67-07 (downloadStream'i media.py'ye yönlendir —
timeout/progress/iptal bedava gelir), R67-06 (email alanı + auth/feed
endpoint + SID env), R67-08 (srv3 iste veya srv1 parse et), R67-05
(feed serileştirme/slot), R67-04 (CSP), R67-03 (modal konumu).
