# 2026-09-23 01:01 — R111 uçtan uca çeviri çıktı kalitesi denetimi + BUG-R111-01 düzeltmesi

Depo: `dorukakindev/whisper-browser` · Dal: `codex/r111-qa-output` · Başlangıç: `origin/master` @ `f624e3876eaa857fc2a2d07363a005a19510e472` (R110 dahil)

## Görev

Altyazı çevirisinin gerçek çıktı kalitesini uçtan uca denetle: kaynak SRT → cümle gruplama → sağlayıcı yanıtı → cue dağıtımı → SDH geri koyma → son SRT → oynatıcı. Adversarial anonim fixture'lar, deterministik kırmızı ölçüm önce, yalnız doğrulanmış kusura minimal düzeltme; gerçek anahtar/hesap YOK.

## Bulunan ve düzeltilen kusur (BUG-R111-01)

Yalnız-SDH cue'unda (`[music playing]`) sağlayıcının işareti aynen — doğru biçimde — döndürmesi `kaynak_yankisi`/`source_echo`/`untranslated_source` sayılıp reddediliyordu:

- **Etki (düzeltme öncesi, ölçüldü):** 8 cue'luk dosyada 1 yerine 3 istek (2 kurtarma yakıldı); kurtarmanın kabul ettiği farklı metin `restore_sdh_markers`'ta çift işaret üretti (`[music playing] [müzik çalıyor]`); kalite raporu cue'u `untranslated` sayıp dosyayı `.tr.partial.srt` + "1 kaynak metinli cue" uyarısıyla yazdı.
- **Kırmızı kanıt:** `test_lone_sdh_marker_echo_is_not_rejected` → `3 istek: kurtarma tetiklendi` FAIL.
- **Düzeltme (`backend/transcribe.py`, 4 nokta):** echo kontrollerine `not is_structural_sdh_cue(source)` muafiyeti — toplu kabul, refine kabulü, kalite-raporu untranslated ölçümü, `--translate-existing` devralma (2 dal). Diyalog echo kapısı korunuyor (`test_dialogue_echo_still_rejected` koruyucu test: ret → 3 istek → kurtarma).
- **Yeşil kanıt:** aynı fixture koşusu `1 toplu + 0 kurtarma = 1 istek`, `untranslated: 0`, `status: complete`, cue5 `[music playing]`.

## Ölçümler (fixture sunucu + gerçek transcribe alt süreci, `qa-111/run_e2e.py` — 65/65)

- 9 adversarial fixture'da zaman kodu + cue sırası birebir; echo sızıntısı ve boş cue yok; ≤20 cue → tam 1 toplu istek.
- `a-split34`: 4 cue'ya bölünmüş cümle → tek grup `[0,1,2,3]`, parçalı akıcı dağıtım.
- `b-twosent`: "Wait. Are you sure / you want to come with me?" → çıktı cue'sunda gerçek `\n` (`Bekle.`⏎`Emin misin`).
- `e-sdh`: satır-içi `(crowd cheering)` model girdisinden çıkarılıp çıktıda geri konur; çoklu `[applause] [music playing]` ikisi de korunur.
- `f-speaker`: `- ` önekli cue'lar korunur, tekli grup.
- Kapılar: `Emin|misin` bölünmesi, anchor-swap, echo, join-mismatch → ret → kurtarma; kurtarma **yalnız başarısız grubu** yeniden gönderir (sunucu logu kanıtlı).
- Maliyet: aynı dosya 2. koşu **0 istek** (önbellek); 10 cue'da 1 düzenleme → bağlam-anahtarlı ±4 geçersizleşme → 9/10 grup yeniden (belgelendi, tasarım); `--translate-existing` → **0 istek + elle düzeltme korunur**.

## Gerçek Electron kanıtı

Ayrı profil (`qa-111/profile`), `clip.mp4` (testsrc 22 sn) gerçek GTK dosya seçiciyle açıldı → kardeş `clip.en.srt`/`clip.tr.srt` otomatik bulundu → TR iz yüklendi → overlay video yüzeyinde doğrulandı (`qa-111/shots/`): iki-satırlı `Bekle./Emin misin`, `[music playing]`, `[alkış] [music playing] Ayağa kalktı.`, `Bana 2.4 milyon`.

## Testler

`test_sentence_distribution.py` 36/36 (2 yeni) · `test_transcribe.py` 192/192 · `test_subtitle_sdh.py` 8 · `test_golden_corpus.py` 6 · `test_translation_memory.py` 6 · `test_turkish_native_refine.py` 3 · `test_translation_quality_corpus.py` 4 · `test_output_newlines.py` 1 · E2E sürücü 65/65 · `py_compile` temiz. Tam `npm test` kasıtlı koşulmadı (hedefli set yeterli görüldü; CI koşar).

## Sınırlar

- Fixture = deterministik sağlayıcı; ücretli-model kalite iddiası yapılmıyor.
- `restore_sdh_markers` eşleşmeyen hedef işaretini düşürüp kaynağı korur (R110 politikası) — tasarım.
- GTK seçici bilgisayar aracıyla; eski dialog pencereleri ilk denemede ölçümü kirletti, insan yoluyla tekrar doğrulandı.

## Dosyalar

- Kod: `backend/transcribe.py` (4 echo-kapısı muafiyeti), `backend/test_sentence_distribution.py` (2 regresyon testi)
- Rapor: `docs/raporlar/BROWSER_BUG_REPORT_111.md`
- Kanıtlar: `/home/ubuntu/qa-111/` (run_e2e.py, fixtures, out/, shots/, logs/e2e-results.json)

Bitiş kod commit'i: kod commit'inin tam hash'i PR açıklamasında; push sonrası uzak doğrulaması ayrı yapıldı.
