# Devir notu — 2026-09-18 18:52

## Konu

**BROWSER_BUG_REPORT_68 (84 bulgu) tek-tek doğrulaması + yalnız gerçek bug'ların düzeltilmesi.**
Kullanıcı talimatı: rapordaki her iddiayı kod'a karşı doğrula; false positive'lere dokunma;
mantıklı iyileştirmeleri seçerek uygula. Rapor §9'a tam sınıflandırma tablosu yazıldı.

## Repo / dal

- Repo: `https://github.com/dorukakindev/whisper-browser` · dal `master`
- Başlangıç HEAD: `70d6ea2ff5ec49839e76e176075a9432cb7ab61e`
- Bitiş kod HEAD: **bu commit** (aşağıda `git rev-parse HEAD` çıktısına bak)

## Karar özeti (84 bulgu)

- **GERÇEK → düzeltildi: 30** · **İyileştirme uygulandı: 6** · **Deferred: 6**
- **FALSE POSITIVE: 36** · **Zaten doğru/çözüldü: 6**
- Tam tablo: `BROWSER_BUG_REPORT_68.md` §9.1–9.5

## Değişen dosyalar ve amaçları

| Dosya | Değişiklik |
|---|---|
| `src/renderer/renderer.js` | closePlayer ölü-kaynak mediaKey sıfırlama + SmartTube restore (D-K1/K2); `setSmartTubeVisible` arama geri getirme (D-K5); `playerSourceSelect` → SmartTube (D-K4); kanal `lastInvidiousInstance` senkronu (K4/D-K7); `_invLoginBusy` (D-K6); klavye-seek `updateSeekVisuals` (Y9); V-tuşu menü önceliği (Y10); `lastSubtitleMode` persist (Y11); `volumechange→syncMuteIcon` (Y12); ambient `document.hidden` (D-Y3); `stSearchGrid` temizliği + `ST_SEARCH_MAX` (D-Y5/D-O1); `ST_COMMENTS_MAX` (D-O6); `absThumb` http(s) beyaz liste (D-D7); `formatCount`→`Intl` compact (D-D3); 450ms canlı-arama debounce (Y3); eski inv-* render/init fonksiyonları silindi (Y2); birleşik `feed_home` tüketimi (D-K3/Y5) |
| `src/renderer/index.html` | Eski inv-* paneli silindi (Y2); `stSearchClear` × + `invLoginPassToggle` (O6/O7); ikonlara `aria-hidden` (O5); `stStatusLine` `role=status aria-live` (D1) |
| `src/renderer/styles.css` | `.st-grid-row` (D4); 1100/720px breakpoint'ler (O2/D10); reduced-motion (D5); token renkleri (O14/O15); placeholder + side-item focus-visible (O10/D3); `.st-card` `content-visibility` (D-Y10); clear/pass-toggle stilleri |
| `src/main.js` | `downloadStream` işi `'invidious-stream'` jobTag; `invidious:cancel` etiketli download'u öldürür (K1); `isLocalInvidiousInstance` — http'de SID yalnız loopback/`.local`/RFC1918 (K7); `invidious-session.safe.json` + safeStorage kalıcılık, login/logout persist, `whenReady` restore (K2); `'home'` feed kind'ı whitelist'te |
| `src/preload.js` | (önceki turdan `invidiousComments/Playlist`; bu turda ek yok — dosya diff'te yoksa değişmedi) |
| `backend/invidious.py` | `feed_home`: tek süreçte `ThreadPoolExecutor(2)` popular+trending, tek `feed` emit'i (D-K3); `_emit_lock` — NDJSON satır karışması önlemi; `_popular_payload`/`_trending_payload` dict dönüşü |
| `backend/media.py` | `ENOSPC`/`EACCES`/`EPERM` → dostça Türkçe hata (D-O9) |
| `tests/report67-smarttube-wiring.test.js` | 27 yeni R68 regresyon assertion'ı; eski kalıplar `st-grid-row`/jobTag/dict-payload ile uyumlu hale getirildi → **66/66** |
| `tests/report65-invidious-bridge.test.js` | Ölü inv-* assertion'lar SmartTube eşleniklerine taşındı → 21/21 |
| `tests/adversarial-ipc.test.js` | init-sandbox mock'una `restoreInvidiousSessions` eklendi → 15/15 |
| `tests/electron-smarttube-boot.smoke.js` | mock `invidious:feed` `kind:'home'` birleşik payload'a güncellendi; `sepFullRow`/`channelHead` computed-style probuna çevrildi |

## Doğrulama

- `report67-smarttube-wiring`: **66/66** ✓
- `report65-invidious-bridge`: **21/21** ✓ · `adversarial-ipc`: **15/15** ✓
- `electron-smarttube-boot.smoke`: **GEÇTİ** — `{"ok":true, cardCount:10, sepFullRow, channelHead, chipCount:5, musicTabLoaded, degradedStatus, rovingRight/End, upNextItems, commentCount:2, commentNoHtml, hintInStage}` hepsi true
- `backend/test_invidious.py`: **59/59** · `design-system`: OK
- `node --check` main/renderer/preload + `py_compile` invidious/media: **ALL-OK**
- `npm test` kalan kırıklar: `report62-translation-deep-fix`, `report63-secret-redaction`, `report63-ssrf-observer` — **başka iş akışının commit'siz WIP dosyaları** (untracked), bekledikleri ürün kodu yazılmamış; bu kapsamda dokunulmadı. `report64-*` untracked testleri geçiyor.

## Bilinçli yapılmayanlar (deferred)

O4 manuel sidebar toggle (720px'te otomatik ikon rayı yeterli) · D6 kanal açıklama genişletme ·
D-D2 RTL kapsamlı destek · D-D8 IPC payload sınırı · D-O7 backdrop-filter birleştirme ·
D-Y1 listener delegation refactor'ı (leak yok — `innerHTML` topluyor; `content-visibility` asıl maliyeti çözdü).

## Sınırlar / sonraki adımlar

- Canlı Invidious davranışı (feed_home gerçek instance, SID restore döngüsü, kalıcı oturum)
  mock/fixture seviyesinde doğrulandı — `start.bat` ile gerçek ağda denenmeli.
- SID kalıcılık dosyası `userData/invidious-session.safe.json` — safeStorage yoksa
  (şifreleme kapalı platformlar) oturum bellekte kalır; bu platformlarda test edilmeli.
- Diğer AI için geri-dönüş noktası: bu commit `git revert <hash>` ile geri alınabilir;
  tek dosya için `git checkout <hash>~1 -- <dosya>`.

## Commit / push

- Kod commit ve doküman commit ayrı; kesin hash'ler `git log --oneline -3` ile doğrulanır.
- Push hedefi: `origin/master` — uzak hash `git ls-remote` ile doğrulandı.
