# Catalog and master integration — 2026-09-24 04:20 (Europe/Istanbul)

- Repository: `dorukakindev/whisper-browser`; integration branch: `codex/integration-20260924`.
- Start: remote `master` `634b772e9e91eb7f25fbac9390477fecdc8510ca`; local `codex/catalog-sync` `3336734ac37f2fbed46b5ee9fb689487d5a5c426`.
- Integration product-code commit: `52b61768ba96e5951a543c442d21d3b226070b46` (two-parent merge; includes both histories).
- Final product-code commit: `abb9fee` (post-push strict UI contrast-audit visibility fix).

## Scope

The already-merged GitHub PRs #35–#43 were retained. The remaining open catalog PR #33 was merged into their current base in an isolated worktree. Its browser/translation/SmartTube TV enhancements and the migration of historical reports to `docs/raporlar/` were retained. Merge conflicts in `CHANGELOG.md`, `DEVIR-NOTU.md`, `docs/raporlar/BROWSER_BUG_REPORT_110.md`, `src/browser-omnibox.js`, `src/renderer/renderer.js`, `src/renderer/styles.css`, and `src/renderer/ui-locale.js` were resolved by preserving both sides' non-overlapping behavior. The report conflict keeps both historical accounts in one file.

The merge exposed three concrete regressions, documented in [BROWSER_BUG_REPORT_120](../raporlar/BROWSER_BUG_REPORT_120.md): duplicate/missing locale entries, modal `Can` misclassified as a proper name, and an over-limit CSS literal-color count. Relevant fixes and a focused regression were added to `backend/sentence_translation.py`, `backend/test_sentence_distribution.py`, `src/renderer/ui-locale.js`, and `src/renderer/styles.css`; the CSS limit was not raised. No new dependency, installer, or schema change was introduced by this integration beyond the incoming branch's already-committed `qrcode` devDependency move.

The first GitHub CI run exposed a fourth issue in `tools/audit-ui.mjs`: it measured unpainted text inside closed `<details>` menus and falsely reported 49 1:1 contrast failures per browser-home matrix run on Ubuntu. A scoped visibility exclusion fixed that audit without changing UI colors or thresholds. The same strict UI matrix now passes locally with zero failures.

## Verification

- `node --check` on main, preload, omnibox, renderer, SmartTube TV, and locale modules: passed.
- Direct Node checks: `audit-2026-09-22-browser-translate` 18/18; `browser-address-model` 6/6; `browser-recovery-tools` 3/3; `browser-report117-regressions` 15/15; `css-hardcoded-colors` 470/470; `i18n-coverage` 0 missing; `player-ui` 148/148; `report64-address-listbox`, `translation-review-tools` 6/6, `youtube-tv-mode` 7/7, and `report104-youtube-browse`: passed.
- Python: `backend/test_sentence_distribution.py` 37/37; `backend/test_translation_audit_2026_09_22.py` 11/11; `backend/test_subtitle_sdh.py` 8/8; `py_compile` for changed backend modules: passed.
- Real Electron `electron-browser-trusted-bridge.smoke.js --disable-gpu`: passed with temporary media fixture data; no personal browser tabs were opened.
- `git diff --cached --check`: passed after removing inherited report whitespace.
- Additional focused checks after local synchronization: browser download/foundation/omnibox/page-translate/translation scheduler suites and SmartTube error suites passed; backend transcribe 198/198, translation quality 5/5, golden corpus 6/6 passed. `node --check tools/audit-ui.mjs` passed; strict `node tools/audit-ui.mjs --strict` passed across the generated dark/light × TR/EN × viewport screenshot matrix after the visibility fix.

`npm test` was not run locally at the user's request to avoid the full suite's cost. The multi-file Node test launcher initially failed with sandbox `spawn EPERM`, not a product assertion; direct file invocation passed. No live YouTube account or paid provider acceptance was performed. The first GitHub Ubuntu UI-audit check failed and was reproduced/fixed as above; the first Windows `Run test suite` check also failed, but detailed logs were not available through the public check API, so its cause remains unknown pending fresh CI or user-provided log. The original checkout's untracked files (including reports and `_repro/`) were preserved and not committed. Remote push and local branch synchronization are recorded by the final delivery status, not presumed here.
