# Devir notu — 2026-09-26 08:52

## R128: Çok daha derin bug turu — uzak-veri yolları ve IPC sınırları (3 doğrulanmış bug)

**Dal:** `codex/r128-deeper-audit-1790410240` · **Taban:** master `1d0d83e` · **Rapor:** `docs/raporlar/BROWSER_BUG_REPORT_128.md`

Başlangıç kod hash'i: `1d0d83e` (R127 squash merge'i). Bitiş kod hash'i: aşağıdaki kod commit'i.

### Kapsam

Kullanıcı talimatı: "çooook daha derin devam et". R127 desen süpürmesi üzerine bu tur uzak-API-türevli veri yollarına yoğunlaştı: redirect zincirleri, uzak XML/JSON parser'ları, LLM yanıt doğrulayıcıları, main.js'teki tüm `fetch`/`session.fetch` çağrılarının URL-kaynağına göre sınıflandırılması, `new RegExp` dinamik kalıpları.

### Düzeltilen bug'lar (kod commit'i)

1. **BUG-128-01 — `downloadStremioSubtitle` redirect zinciri host-kontrol aşımı (HIGH — SSRF):** ilk URL `approvedStremioUrl` ile doğrulanıp sonra `redirect:'follow'` izleniyordu → onaylı `*.strem.io` adresi iç ağa/`http:` düşüşüne zincirlenebilirdi. Elle döngü (`redirect:'manual'`, ≤3 hop) + her hop yeniden `approvedStremioUrl`; `AbortError` bozulmadan yükselir. Aynı örüntü OpenSubtitles yolu ve `fetchMangaImageSource`/`main.js:8927`'de zaten vardı — Stremio atlamıştı.
2. **BUG-128-02 — `invidious._convert_timedtext_to_srt` sayısız/NaN nitelikte ham `ValueError` (MEDIUM):** srv1 `float(t.get("start"))` ve srv3 `int(p.get("t"))` doğrudan çağrılıyordu; uzak XML `abc`/`nan`/boş üretebilir; `int(float("nan"))` ayrıca çöker. `_num()` yardımcısı (parse + `math.isfinite`) + satır atlama; tamamı-bozuk → mevcut "boş transkript" `RuntimeError`.
3. **BUG-128-03 — `sentence_translation.sentence_reply_issue` boş `ids` `IndexError` (LOW — sağlamlık):** `wholes.get(str(ids[0]))` — canlı yol tetiklenemez (`sentence_groups` hiç boş grup üretmez) ama export edilen doğrulayıcıya `[]` geçilmesi sözleşme dışı çöküştü. `if not ids: return "bos_kimlik_listesi"` kapısı eklendi; `accept_sentence_reply` erken `None` döner. 30k property-fuzz: öncesi 829/20k `IndexError`, sonrası 0/30k.

### Değişen dosyalar

- `src/browser-subtitle-search.js` — Stremio indirme elle-redirect + hop-başına allowlist
- `backend/invidious.py` — `import math` + `_num()` + srv1/srv3 satır-atlama
- `backend/sentence_translation.py` — `bos_kimlik_listesi` kapısı
- `tests/browser-subtitle-search.test.js` — +6 redirect zinciri regresyonu
- `backend/test_invidious.py` — `test_non_numeric_timing_attrs_skipped`
- `backend/test_transcribe.py` — `[]` → `bos_kimlik_listesi` + `None` iddiaları

### Testler

- `node tests/browser-subtitle-search.test.js` — PASS (yeni 6 vaka dahil)
- `backend/venv/bin/python test_invidious.py` — 75/75 · `test_transcribe.py` — 198/198
- `python3 -m py_compile invidious.py sentence_translation.py` — OK · `node --check src/browser-subtitle-search.js` — OK
- 30k property-fuzz (sentence_translation tüm export'ları, rastgele-derin veri) — 0 çökme
- `parse_llm_json_object` 15 adversaryal girdi — nesne-olmayan her şey `RuntimeError`, kirli-JSON kurtarma nesne doğrular
- Koşulmadı: tam `npm test` paketi (hedefli-test kuralı), Electron smoke (renderer UI'a dokunmuyor)

### Temiz çıkan yüzeyler

main.js'teki 7 bare-fetch çağrısı URL-kaynak sınıflandırmasıyla temiz (kullanıcı-ayarlı uçlar + mime-kapılı indirmeler; altyazı-metni yolu zaten manual+allowlist'li); `new RegExp` dinamik kalıpları escape'li veya sabit girdili; `sentence_groups`/`normalize_timings`/`_vtt_to_srt`/`parse_llm_json_object` sağlam; `decideUrlPolicy` renderer-external şema modeli tasarıma uygun.

### Windows etkisi

Platform-nötr düzeltmeler (Node fetch + Python parse). `start.bat`/`install.bat`/file-picker/GPU etkilenmez. Windows'ta doğrulanacak: Stremio sağlayıcısından gerçek altyazı indirme (redirect'li CDN'de `UNSAFE_URL` yerine normal indirme görülmeli).

### Bilinçli bırakılan / doğrulanmamış

- `saveBrowserContextImage` + favicon redirect'leri iç IP'ye dönebilir — Chromium "görseli kaydet"/favicon davranışıyla aynı; `image/*` mime kapısı + kullanıcı-seçimli hedef. Altyazı-metni etki sınıfından farklı.
- `turkishNumberPhrasePattern` yalnız boşluk escape eder — `phrase` bugün yalnız `turkishIntegerWords` çıktısı; ham metin geçilirse kırılır (raporda not).

### Sonraki adım

PR açılır → Windows+Ubuntu CI beklenir → kullanıcı izniyle squash-merge (bu turda izin verildi: "kendin squash and merge veya direk merge edersin keyfine göre").
