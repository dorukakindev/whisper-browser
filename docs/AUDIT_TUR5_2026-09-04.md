# Tur 5 raporu — ilk doğrulama ve düzeltme paketi

> Bu dosya ilk paketin tarihsel kaydıdır. Kalan tüm maddelerin nihai kararları ve yeni düzeltmeler: [150 maddelik tam sonuç](AUDIT_TUR5_2026-09-04_TAM.md). Aşağıdaki “134 açık” ifadesi artık güncel durum değildir.

Başlangıç: `34f17c8`. Kaynak: kullanıcının `911f99d1-158b-41d5-90c5-d9e59488dcf3/pasted-text.txt` eki, BUG-241–390.

Raporun 150 maddesinin tamamı doğrulanmış değildir. Tekrarlar, daha önce giderilmiş sorunlar ve geliştirme tercihleri içerir. Bu paket 9 rapor maddesini kapsayan 7 kök nedeni giderir. Aşağıda durumu belirtilmeyen 134 ID açık inceleme kapsamıdır; gerçek hata olarak kabul edilmemiştir.

## Bu pakette giderilenler

| ID | Kaynak ve ulaşılabilir yol | Düzeltme / davranış testi |
| --- | --- | --- |
| 241, 242 | `backend/media.py:download`, indirme sonrası dosya seçimi | İlk `requested_downloads` bileşeni veya başlığı benzeyen başka dosya seçilmiyor. yt-dlp'nin post-process/taşıma sonrası `post_hooks` yolu, yedek olarak nihai `info.filepath` kullanılıyor. Boş başlık, aynı ilk 40 karakter, mevcut video-only bileşen ve tamamlanmış çıktı sentetik testte ayrıldı. Raporun birleştirme hatasının yt-dlp tarafından mutlaka yutulduğu iddiası kanıtlanmadı; yanlış yol seçme dalı doğrulandı. |
| 254 | `renderer.js`, `playerSeek` input olayı | Canlı/henüz bilinmeyen `Infinity`, `NaN`, sıfır süre için `currentTime` atanmıyor. Sonlu süre/fraction kontrolü ve 0–1 sınırı var. |
| 266, 355 | `transcribe.py:write_json` | Eksik olmayan fakat sonlu da olmayan güven/süre ölçümleri JSON `null` olur. Geçersiz zamanlar `allow_nan=False` ile reddedilir; atomik yazıcı eski çıktıyı korur. Kaynak kelime verisi değiştirilmez. Strict JSON okuma ve eski dosyanın korunması test edildi. |
| 267 | `transcribe.py:_wrap_whisperx_segment`, score/probability uyarlaması | Gerçek sıfır güven artık 1.0'a dönüşmez. Yalnız `None` için varsayılan uygulanır. Her iki alan ayrı test edildi. |
| 321 | `renderer.js:clearPreview`, yeni iş başlangıcı | Eski arama metni, temizleme düğmesi ve bekleyen arama zamanlayıcısı temizlenir. |
| 327 | `renderer.js:closePlayer` → bekleyen shadowing callback | Kapanış shadow-resume ve canlı cue-render zamanlayıcılarını iptal eder. Gizli oynatıcıda gecikmiş otomatik play/render önlenir. |
| 332 | `renderer.js:timelineNudge` | Sıfır veya başlangıçta sıfıra kısılan hareket undo/dirty üretmez; NaN/Infinity reddedilir. Gerçek hareket hâlâ undo oluşturur. |

Ek küçük düzeltme: `renderFinalPreview` yalnız start/end/text kopyalamak yerine segment metadata'sını korur (konuşmacı/güven dahil; önceki turun BUG-163 iddiasıyla ilişkili). **Kullanıcı düzenlemelerinin `preview_refresh` sırasında korunması (322) bu değişiklikle çözülmüş değildir.**

## Güncel kodda zaten giderilmiş / geçersiz iddialar

| ID | Güncel kanıt |
| --- | --- |
| 282 | `resetMediaBoundState` içinde `player.cueQualitySource = []` zaten var. |
| 294 | `dialog:openVideo` filtresi `[...MEDIA_EXTS]` kullanıyor; Tur 4 testinde doğrulanıyor. |
| 296 | Roving-keydown işleyicisinde `event.defaultPrevented` kısa devresi var; Tur 4 testinde doğrulanıyor. |
| 331 | `renderSeekMarkers` yalnız `.seek-marker` düğümlerini kaldırıyor; A-B düğümlerini koruyor. |
| 338 | `resetMediaBoundState` artık `renderAbMarkers()` çağırıyor ve eski A-B işaretlerini temizliyor. |

## Reddedilen iki iddia

- **313:** `-0.15 <= gap <= max_gap`, büyük negatif aralıkları kabul etmez. Örneğin -10 bu koşulu sağlayamaz.
- **314:** Aynı şekilde `-0.05 <= gap <= max_gap` büyük negatif aralıkları reddeder. Mevcut küçük tolerans tek başına hata değildir.

## Doğrulama ve sınırlar

- `node tests/audit-tur5.test.js`: gerçek renderer fonksiyonları/olay işleyicileri VM içinde çalıştırıldı; yalnız kaynak metin araması yapılmadı.
- Python'a üç yeni test eklendi: nihai indirme yolu, sıfır güven, strict/atomik JSON.
- `node --check src/renderer/renderer.js` ve iki değişen Python dosyası için `py_compile` başarılı.
- `npm test`: bütün Node test dosyaları ve Python **133/133** başarılı.
- Gerçek API/indirme/Whisper modeli/DRM oturumu çalıştırılmadı; kullanıcı verileri ve kimlik bilgileri okunmadı.
- `BUG_REPORT.md` ve `b-search.txt` kullanıcı dosyalarına dokunulmadı.

Sonraki yüksek değerli inceleme: 322 (önizleme düzenlemeleri), 288 (kuyruk geri yükleme), 318 (ham cue temizliği), 368/380 (oturum yazım yaşam döngüsü). Bunlar henüz yeni davranış testiyle doğrulanmış veya düzeltilmiş sayılmamalı.
