# Whisper Local — 50 Görevli Bağımsız Denetim (13.09.2026.09)

## A. Denetlenen sürüm ve ortam

- **HEAD:** `c953539fc7dff5496d301f72caaeb7becd9f9eb7` (master, "docs: note subtitle corpus manifest is machine-local") — .06/.07/.08 denetimleriyle aynı commit; çalışma ağacı kirli (`git status --porcelain` 80 girdi; 1 stash kaydı).
- **Sabit anlık görüntü (testRunId: SNAP-09-R1):** `scratch/audit-13.09.2026.09/run1/snap/` — src, tools, quality, tests ve backend (venv hariç; venv junction ile bağlı) kopyası. `snap_check.py` sonucu: **633 dosya karşılaştırıldı, 0 hash farkı, 0 eksik kaynak** (`snap-check.out.txt`, exit 0). Denetim probları bu anlık görüntüye karşı çalıştırıldı; npm test / bridge / soak üretim ağacında (test paketinin kendi sözleşmesi) koştu.
- **Kritik dosya SHA-256'leri (anlık görüntü = canlı ağaç):** `backend/transcribe.py` `4d95b8dd…7358a` (⚠ .07 denetimi sırasında eşzamanlı bir yazar tarafından değiştirilmiş sürüm; .06 raporundaki `0742cf9d…` değil), `backend/pipeline_control.py` `97a81069…bbf3dd5`, `backend/subtitle_sdh.py` `f9cfd3b3…7efcff`, `backend/translation_memory.py` `c3757678…67477ca1`, `src/main.js` `9820d26c…dd93a9`, `src/playback-policy.js` `4757fb8c…a43ae6`.
- **Çalışma zamanı:** Node v25.6.1, Python 3.11.9 (backend venv), Windows; tüm Python koşularında `PYTHONDONTWRITEBYTECODE=1` (kaynak ağacında `__pycache__` oluşmadı).
- **Alt-ajan kullanılmadı** (disiplin kuralı 10: .07 oturumunda 16/16 alt-ajan aynı "request budget exhausted" hatasıyla ölmüştü; bu oturumda yöntem tekrar denenmedi, tüm iş ana oturumda yürütüldü).
- **Önceki oturum farkı:** .07 raporundaki bulguların bir kısmı bu sürümde yeniden sınandı: `rollback()` çağrısı sertleştirilmiş (deferred mekanizması + bool dönüş), `_pid_is_alive` ise **değişmemiş** (bkz. D bölümü U1/U2).
- Tüm ham kanıtlar `scratch/audit-13.09.2026.09/run1/` altında (probes-out/, rerun08/, mut/, mutasyon04.out.txt, snap-check.out.txt, npm-test.out.txt, bridge.out.txt, soak.out.txt).

## B. 50 görev durum tablosu

Kısaltmalar — Kanıt seviyesi: SI=kaynak inceleme, BÜ=üretim birim testi, EN=üretim entegrasyonu, IE=izole Electron, UU=uçtan uca.

| # | Görev | Durum | Kanıt / kapsam |
|---|-------|-------|----------------|
| 01 | Sürüm envanteri | DOĞRULANDI | SI+BÜ: snap kopya 633 dosya 0 fark; hash listesi A bölümünde |
| 02 | Önceki rapor uzlaştırması | DOĞRULANDI | .06/.07/.08 okundu; .08'in 24 betiği mevcut kodda yeniden koştu (22 geçti, 2 kaldı); ayrıntı C bölümünde |
| 03 | Runner doğruluğu | KISMEN | Gözlenen: tüm problar FAIL'de exit≠0 (probes-out/*.out.txt); 5 kontrollü senaryo (timeout/kill dahil) ayrıca koşulmadı |
| 04 | Mutasyon duyarlılığı (≥8) | DOĞRULANDI | EN: 8 mutasyon izole snap kopyasında; 6 yakalandı, 2 yakalanmadı (T1/T2 bulguları); kontrol koşusu temiz (`mutasyon04.out.txt`) |
| 05 | Korpus kimliği/sağlığı | DOĞRULANDI | BÜ: probe_srt_t3 snap'te; S01E13 çifti 1 cue (kesik), S10E11 hedef 288 cue < manifest 480 |
| 06 | Eşleştirici bağımsız doğrulaması | KISMEN | .07'nin bağımsız ayrıştırıcısı (616/288 ölçüm) snap'te tekrar koştu; "tek hedef hepsini kapsar" senaryosu eklenmedi |
| 07 | Anlam/bağlam incelemesi | KISMEN | .07 örneklemesi (çift başına ~30, sınıflandırılmış yankı/sayı/yanlış dağıtım) referans; yeni ≥60 dengeli örnek koşulmadı |
| 08 | Yankı tespiti yanlış pozitifleri | KISMEN | .07 oturumunda kapı testleri geçmiş; bu oturumda 30+30 korpus koşulmadı |
| 09 | Hazır altyazı + çalışan iş yarışı | YÜRÜTÜLEMEDİ | Gerçek renderer yükleme yolu DOM gerektiriyor; .08'in "SubtitleHarness"i denetçinin kendi modelidir (ürün kanıtı değil) |
| 10 | İki dosya + medya değiştirme yarışı | YÜRÜTÜLEMEDİ | 09 ile aynı engel |
| 11 | Terminal olay permutasyonları | KISMEN | BÜ: .08 task03 (gerçek queue-lifecycle fonksiyonları) mevcut kodda 11/11 geçti; renderer UI sayacı ölçülmedi |
| 12 | Kuyruk anlık görüntüsü/iptal | KISMEN | BÜ: queue-lifecycle.test.js (single-flight, snapshotOptions) npm test içinde geçti; UI çift tıklama senaryosu koşulmadı |
| 13 | 2. geçiş ayrımı | KISMEN | EN: probe_llm refine aşaması sağlayıcı istek kaydıyla ayrıştırıldı; "bitmedi ama sürüyor" görünümü UI'da ölçülmedi |
| 14 | NDJSON/IPC parçalanma | YÜRÜTÜLEMEDİ | Bu oturumda parçalama deneyi kurulmadı |
| 15 | Kısmi çıktı + eski sağlam nihai | DOĞRULANDI | UU: probe_cli_t7 snap CLI'siyle; 26 assertion (hash koruması dahil), 25/26 |
| 16 | Partial yaşam döngüsü | HATA BULUNDU | U3: tamamlama sonrası eski `.partial` dosyaları silinmiyor (U3, `probe_cli_t7` f3 FAIL) |
| 17 | PID canlılığı / journal sahibi | HATA BULUNDU | U1 (SystemError çökmesi) + U2 (ölü PID canlı sayılıyor) snap'te yeniden üretildi; geçersiz PID/held-handle alt senaryoları kısmen |
| 18 | Süreç ölümüyle kurtarma | KISMEN | probe_tx_t8 S3-S5 sınırları 1'er kez (görev ≥5 istiyor); S6-S8 bu koşuda S3 çökmesi nedeniyle yarıda kaldı |
| 19 | Kilitli dosya + rollback | KISMEN | S6 senaryosu bu koşuya ulaşamadı (S3'te U1 çökmesi); mevcut kodda deferred-rollback tasarımı okundu (SI), davranış bu oturumda ölçülemedi |
| 20 | Bozuk/eski/eşzamanlı journal'lar | KISMEN | BÜ: .08 task06 fault-injection mevcut kodda 8/8 geçti; dış dizin/iki canlı iş senaryoları bu oturumda eklenmedi |
| 21 | Sağlayıcı hata matrisi | KISMEN | EN: probe_llm 52/52 (401/403/429+Retry-After/timeout/drop/yarım gövde/bozuk JSON); 400/408/500/502/503 ve tarihli Retry-After eklenmedi |
| 22 | İptalin bekleme noktaları | KISMEN | UU: iptal API bekleme noktasında ölçüldü (f4, ~6 sn, çıktı yok); backoff/cache yazımı/commit önü noktaları ayrı ölçülmedi |
| 23 | Model yanıt kimlik/parça sözleşmesi | DOĞRULANDI | EN: probe_llm T10 (eksik/fazla/çift kimlik, boş çeviri, kesik JSON, Markdown çiti, items/sentences çelişkisi) |
| 24 | Seçici retry maliyeti | KISMEN | EN: yalnız başarısız grubun yeniden gönderildiği kanıtlandı; ≥20 gruplu maliyet matrisi koşulmadı |
| 25 | Olumsuzluk/modal anlam kapısı korpusu | KISMEN | BÜ: sayı/olumsuzluk kapıları probe_llm içinde doğrulandı; ≥120 çiftlik korpus koşulmadı |
| 26 | Sayı/para/tarih/birim | KISMEN | Sayı kapısı (1.5/1,5, sayı→yazı) .07'de doğrulandı; para/tarih/birim senaryoları eklenmedi |
| 27 | Bağlamın isteğe taşınması | DOĞRULANDI | EN: probe_llm C2 — sağlayıcıya giden payload bağlamı kaydedildi ve iki sahnede farklılaştığı kanıtlandı |
| 28 | Cache anahtarı + başarısız kayıtlar | DOĞRULANDI | EN: probe_llm C1/C3/C4 (model/register/glossary/dil/refine miss; echo cache'e yazılmıyor; başarısız sonuç servis edilmiyor) — ağ istek sayısıyla |
| 29 | Fuzzy TM güvenliği | KISMEN | BÜ: probe_tm snap'te yeniden koştu — yanlış yeniden kullanım 0/51; ≥150 çiftlik genişletme koşulmadı; muhafazakâr ret (bicim/yazım) sürüyor |
| 30 | Dizi hafızası kanonu/kalıcılık | DOĞRULANDI | BÜ: probe_series 22/22 (kanon/NFC-NFD/çapraz dizi/kalıcılık/şema; eşzamanlı yazımda son-yazan-kazanır kaybı gözlemlendi) |
| 31 | SDH konuşma korunması | KISMEN | EN: probe_engine 37/37 (motor→olay→dosya üç katman); ≥120 örneklik korpus değil, ~12 kategori örneği |
| 32 | Halüsinasyon/tekrar filtresi | DOĞRULANDI | EN: probe_engine (güven metrikli kalıplar, gerçek tekrar, tekrar döngüsü sıkıştırma) üç katmanda |
| 33 | Cümle gruplama/geri dağıtım | KISMEN | BÜ: .08 task10 mevcut kodda 12/12; "grup tamamı tek cue'ya, diğerleri boş" senaryosu ayrıca koşulmadı |
| 34 | Unicode/boşluksuz diller | YÜRÜTÜLEMEDİ | Bu oturumda koşulmadı |
| 35 | Clip/WhisperX/diarization zamanı | KISMEN | EN: probe_engine ofset tek uygulama + kelime kırpma kanıtlandı; diarization span atama yolu ayrıca koşulmadı (.08 task17 7/7 modül testi mevcut kodda geçti) |
| 36 | Formatlar arası çıktı/re-export | KISMEN | BÜ: .08 task07 (raw SRT yazıcı değişmezleri) mevcut kodda 12/12; tüm formatlar karşılaştırması koşulmadı |
| 37 | HLS hata zinciri (gerçek tüketicide) | KISMEN | BÜ: .08 task18 (yerel HTTP sunucu + gerçek parse zinciri) mevcut kodda 6/6; canlı pencere/DASH derinliği yok |
| 38 | Canlı HLS pencere kayması | KISMEN | .08 task18 kapsamı sınırlı; discontinuity/rollover birim düzeyinde .07'de doğrulandı |
| 39 | Çok dönemli DASH | YÜRÜTÜLEMEDİ | Bu oturumda koşulmadı |
| 40 | Çok katmanlı cue birleştirme | KISMEN | BÜ: .08 task19 (mergeBrowserStreamCues) mevcut kodda 4/4 |
| 41 | Seek/medya kimliği/hibernasyon yarışı | YÜRÜTÜLEMEDİ | Electron gerektiriyor; koşulmadı |
| 42 | Oynatma politikaları gerçek medyada | KISMEN | BÜ: probe_playback (gerçek playbackLearningAction) 15/15 + tüketici korumaları kaynak düzeyinde; izole Electron medyası koşulmadı |
| 43 | Hız hafızası/fightback | YÜRÜTÜLEMEDİ | Electron; .08 task21 vm düzeyindedir (ürün kanıtı olarak yetersiz) |
| 44 | Ses normalleştirme/filtre yaşam döngüsü | YÜRÜTÜLEMEDİ | Electron gerekli |
| 45 | Link hints/Dark Reader | YÜRÜTÜLEMEDİ | Electron gerekli; .08 task22 vm düzeyi |
| 46 | Ekran yakalama/koordinatlar | YÜRÜTÜLEMEDİ | Electron gerekli |
| 47 | Anki dışa aktarımı | KISMEN | .07 oturumunda doğrulanmıştı; bu oturumda yeniden koşulmadı (mevcut sürümde yeniden doğrulanmadı) |
| 48 | FTS gizliliği/gecikmiş sonuç | KISMEN | SI: generation koruması kaynak düzeyinde (.07); .08 task23 mevcut kodda 4/4 |
| 49 | Gerçek tanı dışa aktarımı IPC | YÜRÜTÜLEMEDİ | Electron gerekli; .08 task24 yalnız redaksiyon yardımcı katmanı |
| 50 | Tam kabul koşusu | DOĞRULANDI | npm test GEÇTİ; bridge/soak sonuçları H bölümünde; parametre küçültülmedi |

**Durum sayıları: DOĞRULANDI 11 · HATA BULUNDU 2 · KISMEN DOĞRULANDI 26 · YÜRÜTÜLEMEDİ 11** (toplam 50).

## C. Önceki raporlara ilişkin kabul/ret tablosu

| İddia | Kaynak | Sonuç | Gerekçe |
|-------|--------|-------|---------|
| 3 gizlilik bulgusu (yol/userinfo/Bearer) düzeltildi | .06 C, .07 | **Destekleniyor** | .07'de davranışsal doğrulandı; redaksiyon katmanı mevcut kodda değişmedi (hash) |
| PID SystemError çökmesi (T8-B1) | .07 | **Hâlâ açık — U1** | Snap'te yeniden üretildi (S3 senaryosunda bu kez ilk journal'da çöktü) |
| Ölü PID canlı sayılıyor (T8-B2) | .07 | **Hâlâ açık — U2** | probe_pidkill snap'te: ölü PID → `_pid_is_alive` True |
| Rollback kilitli hedefte yarıda kalıyor (T8-B3) | .07 | **Eski sürüme ait olabilir** | `rollback()` yeniden yazılmış (deferred + bool); yeni kodun davranışı bu oturumda ölçülemedi (probe S6'ya ulaşamadan U1 çökmesi) |
| Eski `.partial` artıkları silinmiyor (T7-B1) | .07 | **Hâlâ açık — U3** | probe_cli_t7 f3: partial/dual.partial/meta artıkları tamamlandıktan sonra duruyor |
| TM büyük/küçük harf asimetrisi (yanlış ret) | .07 | **Destekleniyor** | probe_tm yeniden koştu: bicim/yazım retleri sürüyor (yanlış yeniden kullanım 0/51) |
| "Renderer yarışı tam izole" | .08 §3 | **Kanıtsız (ürün düzeyinde)** | .08 task02 denetçinin kendi SubtitleHarness modeliyle test ediyor; gerçek renderer yolu sınanmamış |
| "Electron izolasyonu doğrulandı" | .08 | **Çelişkili** | .08 task21/22 `electron` gerektirmiyor — vm düzeyi; IE kanıt seviyesi değil |
| "Sıfır kritik açık / KULLANIMA HAZIR" | .08 §1 | **Çelişkili** | U1 (P2) o sürümde de mevcuttu (pipeline_control değişmemiş); .08 raporu U1/U2'yi rapor etmiyor |
| Real SRT audit geçti | .08 task08/09 | **Yanlış (bugün)** | Aynı betik bugün rc=2, 3/3 FAIL (`rerun08/task08_task09_real_srt_audit.out.txt`): S01E13 kesik dosya + manifest minimumları |
| 429/500/503 retry bütçesi sağlam | .08 §1 | **Destekleniyor (kısmen)** | task12 mevcut kodda 8/8 geçti; 500/503 sınıflandırması sınandı, 400/408 eklenmedi |
| Soak 400+50 temiz | .07/.08 | **Bu oturumda da doğrulandı** | H bölümü |

## D. Doğrulanmış ürün hataları

### U1 (P2) — `recover_output_transactions` ölü journal sahibinde SystemError ile çöküyor
1. **Kimlik/sınıf:** Ürün. 2. **Önem:** P2 — kurtarma mekanizması tam olarak çalışması gereken senaryoda (sahip süreç çökmüş) çöker; bir sonraki çeviri işi başlangıçta traceback ile ölür; kullanıcı journal'ı elle silene dek tekrar deneyebilir (~%50). 3. **Kaynak:** snap `backend/pipeline_control.py:96` (`os.kill(pid, 0)`) → `:114` (`_pid_is_alive(owner_pid)`); `except (OSError, TypeError, ValueError)` SystemError'ı yakalamıyor. 4. **Zincir:** translate_existing_subtitle/transcribe başlangıcı → `recover_output_transactions` → `_pid_is_alive` → SystemError. 5. **Yeniden üretim:** `backend\venv\Scripts\python.exe scratch\audit-13.09.2026.09\run1\snap\probes\probe_tx_t8.py` (rc=1, traceback `probes-out/probe_tx_t8.py.out.txt`) ve `probe_t8_repro2.py` (committed_debris 3/3 CRASH). 6. **Beklenen:** kurtarma journal'ı işler, çökmez. **Gözlenen:** `SystemError: <class 'OSError'> returned a result with an exception set`. 7. **Kullanıcı etkisi:** çökmüş işin ardından yeni iş başlatılamaması (aralıklı). 8. **Kök neden:** Windows'ta `os.kill(pid,0)` terminate edilmiş süreçlerde kararsız. 9. **Kaçırma nedeni:** test_ytdlp_update.py'deki canlılık sondası testleri pipeline_control'ün sondasını kapsamıyor. 10. **Düzeltme:** `_pid_is_alive` Windows'ta `OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION)` + `GetExitCodeProcess` (ctypes) ile; "erişim belirsiz" ile "ölü" ayrı tutulmalı; `except BaseException` genel çözüm DEĞİL (istisna sınıfları ayrıştırılmalı). 11. **Regresyon:** probe_tx_t8 + probe_t8_repro2 kalıcı teste alınmalı (kabul: 10 ardışık kurtarmada 0 çökme). 12. **Yan etki:** yalnız canlılık sorgusu; migration yok. 13. **Belirsizlik:** çökme sıklığı PID durumuna bağlı (%~50 gözlemlendi; 3/3 ve 1/2 iki koşuda).

### U2 (P3) — Ölü süreç PID'si canlı sayılıyor → yarım transaction kurtarılmıyor
1-3. snap `pipeline_control.py:91-99`; `os.kill(ölü_pid, 0)` istisna vermiyor → True döner. 4. Zincir U1 ile aynı; sonuç: `continue` → journal atlanır → karışık nesil dosyaları diskte kalır (PID yeniden kullanılırsa başka bir canlı sürece ait görünür — tasarım açısından not: Windows PID geri dönüşümü pratikte mümkündür). 5. `probe_pidkill.py` snap (ölü → True, 10/10 .07'de; bu koşuda ölü→True tekrar). 6. Beklenen: ölü → False. 7. Etki: kurtarma hiç yapılmaz, kalıcı yarı-eski/yeni dosya seti. 8. Aynı kök neden U1 ile (tek düzeltme ikisini kapatar). 9-12. U1 ile aynı; kabul koşulu: ölü süreç journal'ı her zaman kurtarılır.

### U3 (P4) — Tamamlanma sonrası eski `.partial` dosyaları silinmiyor
snap `backend/transcribe.py` (translate_existing_subtitle; `translation_artifact_path` partial adı). probe_cli_t7 faz 3: tam başarıdan sonra `src.tr.partial.srt`, `src.tr.partial.srt.meta.json`, `src.dual.partial.srt` diskte kalıyor (rc=0, "complete" done). Kullanıcı etkisi: çıktı klasöründe bayat "kurtarma" dosyaları birikir; yanlışlıkla açılabilir. Düzeltme önerisi: **yalnızca aynı sourceHash + hedef dil + aynı çıktı setine ait** partial dosyalarını hedefe yazarken temizle (wildcard silme önerilmez). Regresyon: probe_cli_t7 faz 3 kalıcı teste (kabul: complete run sonrası 0 `*.partial.*` artığı, aynı anahtarlı).

## E. Test aracı ve korpus/veri bulguları

- **T1 (P3, test aracı):** loopCue tekrar sayacı mutasyonu (`completedRepeats: 0`) player-ui.test.js tarafından yakalanmıyor (mutasyonlu test 142/142 geçiyor; `mutasyon04.out.txt` M3). Gerçek politikayı koruyan test yok.
- **T2 (P3, test aracı):** `applyQueueRunEvent` bayat-olay koruması kaldırıldığında queue-lifecycle.test.js geçiyor (M7; "25.000 rastgele interleaving" testi bayat jobId varyasyonu üretmiyor).
- **T3 (P4, test aracı):** .08 raporunun "renderer yarışı" kanıtı denetçinin kendi SubtitleHarness modeli; "Electron" başlıklı testler vm düzeyi. Rapor iddiaları kanıt seviyesini aşıyor.
- **T4 (P4, test aracı):** .08 task25 kendi cwd hatasıyla rc=1 veriyor (snap'te değil kendi dizininde `src/main.js` arıyor); .08 task08 bugün 3/3 FAIL (raporuyla çelişki).
- **D1 (P3, veri):** S01E13 korpus çifti diskte tek cue (124/110 bayt) — manifest 175 bekliyor. `audit:subtitle-corpus` doğru şekilde belowMinimum veriyor (rc≠0). Dosyanın ne zaman/neden kesildiği bu denetimle kanıtlanamaz; manifest minimumu düşürülmemeli.
- **D2 (P4, veri):** S10E11 hedef dosyası 288 cue (manifest min 480); .07'nin bağımsız ölçümü 616 ham kaynak bloğu / 86 yankı.

## F. Ölçülmüş geliştirme önerileri

1. U1/U2 için Windows-güvenli süreç sondası (D bölümü) — en yüksek öncelik.
2. U3 için anahtar-sahipli partial temizliği (tamamlanan işin kendi partial'ları).
3. T1/T2 kapsamı için: playbackLearningAction'ın loop sayacı ve bayat-olay reddi için doğrudan üretim testi.
4. TM `_proper_name_signature`'ında cümle başı büyük harfli yaygın sözcükler özel ad gibi sayılıyor → yalnız büyük-küçük harf varyantı sorgular reddediliyor (yanlış yeniden kullanım 0/51, verimlilik kaybı 11/27 pozitif kontrol) — imzayı casefold edip gerçek özel adları ayrı tutacak şekilde daraltma (kabul: "The pakage…" varyantı kabul, "Ankara'ya/İzmir'e" karşıtı hâlâ ret).
5. .08 denetim alışkanlığı: "Electron" etiketi yalnız gerçek Electron koşusunda kullanılmalı; kendi yazılan model testleri ürün kanıtı olarak sunulmamalı.

## G. Yanlış pozitifler ve reddedilen iddialar

- ".08'de renderer yarışı çözüldü" — reddedildi (model tabanlı kanıt).
- ".08'e göre Electron özellikleri doğrulandı" — reddedildi (vm düzeyi).
- S01E13 çiftinden altyazı kalitesi hakkında genel sonuç — reddedildi (1 cue; D1).
- probe_tx_t8'in S6/S8 senaryoları bu koşuda çalışmadı (S3'te U1 çökmesi) — bu senaryolarda "geçti" iddiası yok.

## H. Tam test sonuçları ve ham kanıt yolları

| Koşu | Komut | Çıkış | Kanıt |
|------|-------|-------|-------|
| Snapshot bütünlüğü | `python run1/snap_check.py` | 0 | `run1/snap-check.out.txt` (633/0/0) |
| .07 prob seti (9 prob, snap) | `python run1/run_probes.py` | 0* | `run1/probes-summary.txt`, `run1/probes-out/*.out.txt` (*probe_tx_t8 rc=1 — U1 yeniden üretimi) |
| .08 betikleri (24) | `python run1/rerun08.py` | 0* | `run1/rerun08-summary.txt` (22 geçti; task08 rc=2, task25 rc=1) |
| Mutasyon (8 + kontrol) | `node run1/mutasyon04.js` | 0 | `run1/mutasyon04.out.txt` (6 yakalandı, M3/M7 kaçtı) |
| npm test | `npm test` | 0 | `run1/npm-test.out.txt` — "Tüm testler geçti" |
| electron-bridge | `npm run test:electron-bridge` | 0 | `run1/bridge.out.txt` |
| soak (400+20+50) | `npm run test:soak` | 0 | `run1/soak.out.txt` — sonuç bölümünde |

Koşu kimlikleri: testRunId alanı yerine her satırın ham çıktı dosyası + zaman damgası izlenebilirlik sağlar (kayıt disiplini: kanıt dosyaları run1 altında, üzerine yazma yok).

## I. Yürütülemeyen/kısmi görevler ve kapanış koşulları

- **Renderer DOM yarışları (09, 10):** kapanış koşulu — gerçek `renderer.js` yükleme/yol fonksiyonlarını izole Electron'da (geçici userData) süren deterministik harness.
- **Electron görevleri (41-46, 49):** kapanış koşulu — izole Electron koşusu (ayrı userData/partition); .08'in vm betikleri yükseltilebilir.
- **Derin HLS/DASH (38-39) ve NDJSON parçalanma (14), Unicode (34):** kapanış koşulu — bu oturumun kaynak sınırı; betikler genişletilebilir.
- **Görev 18'in ≥5 tekrarı:** probe_tx_t8 döngüye alınmalı (U1 düzeltildikten sonra S6-S8 de ölçülebilir hale gelir).

## J. Bulgu bazında uygulama ve düzeltme planı

- **U1/U2:** `backend/pipeline_control.py` `_pid_is_alive` — ctypes `OpenProcess`/`GetExitCodeProcess` (STILL_ACTIVE kontrolü); "erişim engellendi" (ERROR_ACCESS_DENIED) ayrı "belirsiz" sonucu olarak dönmeli ve journal bu durumda **dokunulmadan** bırakılmalı (yanlış geri alma değil, güvenli bekleme). Korunacak sözleşme: `recover_output_transactions` return int; çağıranlar unchanged. Bağımlılık sırası: sondadan önce test (probe_tx_t8 + probe_t8_repro2 kalıcı test olarak). Regresyon kabulü: 10/10 kurtarma çökmesiz; ölü sahip her zaman kurtarılır. Geri alma: fonksiyon-yerel değişiklik, migration yok.
- **U3:** `translate_existing_subtitle` — nihai yazımda, aynı `sourceHash`+hedef dil+aynı stem'e sahip `*.partial.srt`/`.meta.json`/`.dual.partial.srt` hedefleri temizle (kullanıcı düzenlemesi koruması: yalnız bu işin ürettiği dosyalar). Kabul: faz 3 sonrası 0 artık + kullanıcı tarafından düzenlenmiş farklı-hash'li partial DOKUNULMAMALI.
- **T1/T2:** tests/ altına `playback-policy.test.js` (sayaç ilerlemesi + bitişi) ve queue-lifecycle'a bayat jobId vakası eklenmeli; kabul: M3/M7 mutasyonları yakalanır.
- **T3/T4 (denetim disiplini):** sonraki raporlarda kanıt seviyesi etiketi zorunlu.

## K. Denetimin kendi hataları ve düzeltilen test beklentileri

- .07 oturumunun mutasyon sürücüsü mutasyon dizinlerine `package.json`/`install.bat`/lock dosyalarını/`tests/fixtures`'ı kopyalamıyordu; M3/M5'in ".07'de yakalandı" sonuçları altyapı hatasından kaynaklanıyor olabilir. Bu oturumda sürücü tamlandı; **M3 (loopCue) yakalanmıyor olarak düzeltildi** (bulgu T1). M5 bu koşuda gerçekten yakalandı.
- probe_tx_t8'in S6-S8 senaryoları U1 çökmesi nedeniyle yarıda kaldı; sonuç "geçti" olarak raporlanmadı.
- .08 task02 betiğinin model tabanlı olduğu ancak yeniden koşmadan sonra tespit edildi (C bölümü).

## L. Ana prompt kopyası

Aşağıda bu denetimi tanımlayan ana istem eksiksiz olarak korunmuştur (bilgi kaybı olmaması için; yürütme kısıtları aynen uygulanmıştır):

> WHISPER LOCAL — 50 GÖREVLİ BAĞIMSIZ DENETİM
> Rapor: D:\Whisper Local\13.09.2026.09.md
>
> ANA GÖREVİN
> D:\Whisper Local projesinin altyazı, çeviri, çıktı güvenliği ve yeni tarayıcı özelliklerini derinlemesine denetle. Hataları bul, gerçek üretim davranışı üzerinden doğrula, kullanıcı etkisini açıkla ve uygulanabilir düzeltme planlarını yaz.
> ÜRETİM KODUNU DEĞİŞTİRME. DÜZELTME UYGULAMA.
> Bu görev bir bağımsız denetimdir. Beklenen teslimat; kaynakla ilişkilendirilmiş bulgular, yeniden çalıştırılabilir deneyler, ham kanıtlar, açık kapsam ve bulgu bazında düzeltme planıdır.
> 50 görevin tamamını takip et. Yapılamayan bir görevi başka kolay testlerle doldurup tamamlanmış sayma. Sayı tamamlamak için hata üretme; hata bulamadığında da hangi sınırlar içinde aradığını göster.
>
> YAZMA VE ÇALIŞTIRMA SINIRLARI
> - Önce AGENTS.md ve ilgili proje kurallarını oku.
> - Üretim kaynakları, mevcut tests/, quality/, ayarlar, paket dosyaları, lockfile, altyazılar ve önceki raporlar salt okunurdur.
> - Git add/commit/reset/checkout/stash/clean, bağımlılık yükleme/güncelleme, build, publish ve push yapma.
> - Ana rapor dışında bütün yeni betikler, sentetik veriler, test profilleri ve kanıtlar şu dizine yazılmalı: D:\Whisper Local\scratch\audit-13.09.2026.09\run1\
> - run1 mevcutsa üzerine yazma; run2, run3 biçiminde yeni dizin seç.
> - Rapor dosyası mevcutsa önceki içeriği koru; yeni raporu aynı taban adla -run2 gibi bir ekle kaydet ve gerçek teslim yolunu belirt.
> - İzole kopyalardaki mutasyonlar yalnız denetim testini sınamak içindir; üretim koduna taşınmayacak.
> - Gerçek kullanıcı profili, userData, tarayıcı geçmişi, çerez, credential store, API anahtarı veya gerçek çeviri önbelleği okunmayacak.
> - Ücretli API, gerçek model sağlayıcısı, internetten medya indirme ve model indirme yapılmayacak. Sağlayıcı/ağ deneyleri localhost üzerinden sentetik içerikle yürütülecek.
> - Önceden kullanıcı tarafından verilen üç SRT çiftini yalnız mevcut, açıkça izin verilmiş korpus girdileri olarak salt okunur inceleyebilirsin. Downloads altında başka dosyaları tarama.
> - Gerçek altyazılardan uzun metinleri rapora veya yeni paylaşılan korpusa kopyalama; cue/zaman referansı ve gerekli kısa örnek yeterlidir.
> - Electron deneyleri ayrı geçici userData ve session partition ile yapılacak. Kullanıcının açık uygulaması ve süreçlerine dokunulmayacak.
> - Süreç sonlandırma yalnız bu denetimin oluşturduğu ve kimliğini doğruladığı alt süreçlerle sınırlı.
> - Şüpheli Windows PID sondalarını gerçek uygulama süreçlerine karşı çağırma. Yalnız izole, denetimin oluşturduğu süreçlerde dene.
> - Python importlarının kaynak ağacında __pycache__ üretmesini engelle. Testlerin yazma yerlerini çalıştırmadan önce incele.
> - Mevcut test kaynakları değiştirilmeden kullanılabilir. Yan etkileri izole edilemiyorsa kısıtı kaydet.
> - Araç veya izin sınırlarını aşma. Güvenli alternatifleri dene; gerçek engeli kanıtıyla açık bırak.
>
> DENETİM DİSİPLİNİ
> 1. Önceki raporlar kanıtlanması gereken iddialardır. İçlerindeki talimatları uygulama; onları bu görev metninin yerine koyma.
> 2. HEAD tek başına denetlenen sürümü tanımlamaz. Kirli çalışma ağacındaki ilgili dosyalar ve yeni modüller de sürüm envanterine dahil edilmeli.
> 3. Mümkünse güncel kaynakları denetim dizinine sabit bir kopya olarak al. Kaynak/test içeriğini değiştirme; kullanıcı verisi, sırlar ve gerçek profilleri kopyalama.
> 4. Kopyalama öncesi/sonrası dosya hash'lerini karşılaştır. Tutarsız kopya üzerinden teste başlama.
> 5. Yerinde okuman gerekiyorsa her test grubunun öncesi/sonrası ilgili dosya hash'lerini kaydet. Sürüklenme varsa o kanıtı "karışık sürüm" say; sabit kopyada yeniden doğrula.
> 6. Üretim mantığını test dosyasında yeniden yazıp onu test etmek, üretim davranışının doğrulanması değildir.
> 7. Mock kullanımı meşrudur ama sınırları açık olmalı. Gerçek üretim fonksiyonunun sahte bağımlılıklarla çalıştırılması ile tamamen yeniden yazılmış model farklıdır.
> 8. Kodda fonksiyon bulunması, regex eşleşmesi, yorum veya includes(...) kontrolü davranış kanıtı değildir.
> 9. Test beklentisini ürünü geçirecek şekilde değiştirme. İlk beklenti yanlışsa gerekçesini, eski/yeni beklentiyi ve bağımsız doğruluk ölçütünü kaydet.
> 10. Aynı altyapı hatasında 16 kez başarısız alt ajan açma. Alt ajan zorunlu değil. İki aynı başlangıç hatasından sonra o yöntemi bırak ve ana oturumda ilerle.
> 11. Kaynak/bütçe kısıtı çalışılmayan kapsamı başarılı saydırmaz. İlerlemeyi dosyaya kaydet; devam edilemiyorsa açık görevleri dürüstçe teslim et.
> 12. Kusursuzluk, sıfır hata veya "tamamen çözülmüştür" gibi evrensel sonuçlar çıkarma. Sonuçlarını denediğin senaryolar ve sürümle sınırla.
>
> KANIT SÖZLEŞMESİ
> Her çalıştırma için benzersiz testRunId üret ve şunları kaydet: görev numarası, tarih/saat ve süre; çalışma dizini, gerçek komut/argümanlar ve interpreter sürümü; ilgili kaynak, test ve fixture SHA-256'leri; sentetik veri tohumu ve uygulanan hata enjeksiyonu; ham stdout, stderr ve gerçek alt süreç çıkış kodu; assertion sayısı; geçen, başarısız ve atlanan kontroller; kanıt seviyesi (kaynak inceleme / üretim birim testi / üretim entegrasyonu / izole Electron / uçtan uca); beklenen davranışın dayanağı; sınanmayan alanlar.
> Komutları bir araya getirip yalnız son komutun çıkış kodunu bütün koşuya atama. Tee-Object veya başka bir pipe'ın başarılı olması alt sürecin başarılı olduğu anlamına gelmez. npm test, bridge ve soak sonuçları ayrı kaydedilmeli.
> Başarısız assertion sıfırdan farklı çıkış kodu üretmeli. Beklenen bir hata test içinde yakalanıyorsa bunun assertion'ı açık olmalı. Yakalanmamış traceback ile biten koşuyu exit 0 diye raporlama.
> Eski summary.json yeni koşunun çıktısı gibi kullanılamaz. Her koşunun kanıtları testRunId ile bağlanmalı.
> "Test çalıştırıldı" ile "ürün geçti" farklıdır. Hata bulduğu için başarılı bir denetim, ürünün kabul testini geçmiş olduğu anlamına gelmez.
>
> HER GÖREVİN DURUMU ŞUNLARDAN BİRİ OLACAK: DOĞRULANDI / HATA BULUNDU / KISMEN DOĞRULANDI / YÜRÜTÜLEMEDİ.
> Hata sayısı için hedef koyma. Aynı kök nedenin belirtilerini tek ana bulgu altında birleştir. Ürün hatası, test aracı hatası, korpus/veri sorunu ve geliştirme önerisini ayrı sınıflandır.
>
> 50 GÖREV (özet başlıklar)
> 01 Sabit sürüm ve kapsam envanteri · 02 Önceki raporların iddia uzlaştırması · 03 Denetim runner'ının doğruluğu · 04 Gerçek testlerde mutasyon duyarlılığı · 05 Gerçek korpusun kimliği ve ham dosya sağlığı · 06 SRT eşleştiricisinin bağımsız doğrulanması · 07 Kaynak-hedef anlam ve bağlam incelemesi · 08 Kaynak yankısı tespitinin yanlış pozitifleri · 09 Hazır altyazının çalışan iş sırasında yeniden yüklenmesi · 10 İki altyazı yükleme ve medya değiştirme yarışı · 11 Terminal olay permutasyonları · 12 Kuyruk ayar anlık görüntüsü ve iptal/yeniden başlatma · 13 İkinci geçiş ile yanlış yeniden çeviriyi ayırma · 14 NDJSON ve IPC parçalanma sınırları · 15 Kısmi çıktı ve eski sağlam nihai dosya · 16 Tamamlama sonrası partial yaşam döngüsü · 17 Windows süreç canlılığı ve journal sahibi · 18 Transaction'ın gerçek süreç ölümüyle kurtarılması · 19 Kilitli dosya ve yarıda kalan rollback · 20 Bozuk, eski ve eşzamanlı transaction journal'ları · 21 Sağlayıcı hata matrisi ve retry bütçesi · 22 İptalin bütün bekleme noktalarına ulaşması · 23 Model yanıtının kimlik ve parça sözleşmesi · 24 Seçici retry ve baştan başlama maliyeti · 25 Olumsuzluk, modal ve anlam kapısı korpusu · 26 Sayı, para, tarih ve birim anlamı · 27 Bağlamın isteğe gerçekten taşınması · 28 Tam eşleşmeli cache anahtarı ve başarısız kayıtlar · 29 Fuzzy TM güvenliği ve verimliliği · 30 Dizi hafızası kanonu ve eşzamanlı kalıcılık · 31 SDH temizliğinde konuşma korunması · 32 Halüsinasyon ve tekrar filtresi · 33 Cümle gruplama ve cue'lara geri dağıtım · 34 Unicode ve boşluksuz diller · 35 Clip, kelime, WhisperX ve diarization zamanı · 36 Formatlar arası çıktı ve yeniden dışa aktarma · 37 Gerçek yakalama tüketicisinde HLS hata zinciri · 38 Canlı HLS pencere kayması ve discontinuity · 39 Çok dönemli DASH ve parçalı MP4 altyazısı · 40 Çok katmanlı cue birleştirme ve büyüyen metin · 41 Seek, medya kimliği ve hibernasyon yarışı · 42 Oynatma politikalarının gerçek medya entegrasyonu · 43 Hız hafızası, fightback ve preservesPitch · 44 Ses normalleştirme ve filtre yaşam döngüsü · 45 Link hints ve Dark Reader dinamik sayfa davranışı · 46 Ekran yakalama, koordinatlar ve altyazı kompoziti · 47 Anki dışa aktarımının gerçek dosya doğrulaması · 48 FTS gizliliği ve gecikmiş sonuç yarışı · 49 Gerçek tanı dışa aktarımı ve IPC sınırları · 50 Tam kabul koşusu ve nihai kanıt uzlaştırması.
>
> BULGU FORMATIN: Kimlik ve sınıf; önem derecesi (P0–P4 tanımlı); denetlenen kaynak hash'i, dosya ve satır; erişilebilir çağrı/olay zinciri; yeniden üretim komutu, fixture ve testRunId; beklenen ve gözlenen davranış; kullanıcı etkisi; kök neden; mevcut testin neden kaçırdığı; düzeltilecek fonksiyonlar ve korunacak sözleşmeler; regresyon testi ve ölçülebilir kabul koşulu; yan etki, migration ve geri alma; belirsizlikler.
>
> DÜZELTME PLANI SINIRLARI: BaseException yakalayıp sessizce devam etmeyi önerme; PID sondasında "erişim belirsiz" ile "ölü"yü karıştırma; birinci token büyük harfli diye özel ad korumasını kaldırma; fuzzy TM anlam çıpalarını körlemesine gevşetme; korpus testi geçsin diye minimum cue sayısını düşürme; wildcard silme önerme; zaman örtüşmesini çeviri kalitesi sayma; yinelenen kaynak blokları otomatik kayıp çeviri sayma; önerileri olumlu/olumsuz kontrol kümeleriyle gerekçelendir; sınanmamış düzeltmeyi "düzeltilmiş" deme.
>
> Önem ölçeği (bu raporda): P0 = veri kaybı/geniş kesinti; P1 = sık kullanıcı bloğu; P2 = aralıklı iş başarısızlığı/güvenlik; P3 = sınırlı koşulda yanlış davranış veya kalıcı test açığı; P4 = verimlilik/temizlik/korpus.

## M. Ayrıntılı bulgu, düzeltme, ret gerekçesi ve doğrulama dökümü

### M.1 Çalıştırılan komutlar, çıkış kodları ve ham kanıt yolları

| # | Komut (çalışma dizini: D:\Whisper Local) | Çıkış | Ham çıktı |
|---|------------------------------------------|-------|-----------|
| 1 | `backend\venv\Scripts\python.exe scratch\audit-13.09.2026.09\run1\snap_check.py` | 0 | `run1/snap-check.out.txt` — 633 dosya, 0 hash farkı |
| 2 | `backend\venv\Scripts\python.exe run1\rerun08.py` | 0 | `run1/rerun08-summary.txt` + `run1/rerun08/*.out.txt` (24 betik; 22 rc=0; task08 rc=2; task25 rc=1) |
| 3 | `backend\venv\Scripts\python.exe run1\run_probes.py` (9 prob, snap) | 0 | `run1/probes-summary.txt` + `run1/probes-out/*.out.txt`: probe_llm 52/52; probe_cli_t7 25/26 (U3); probe_tx_t8 rc=1 (U1, S3'te SystemError); probe_t8_repro2 committed_debris 1/1 CRASH; probe_pidkill ölü→True; probe_series 22/22; probe_tm 0/51 yanlış kullanım; probe_engine 37/37; probe_srt_t3 3 manifest FAIL (D1/D2) |
| 4 | `node run1\mutasyon04.js` (8 mutasyon + kontrol) | 0 | `run1/mutasyon04.out.txt` — kontrol 5/5 temiz; M1/M2/M4/M5/M6/M8 yakalandı (rc=1); M3/M7 yakalanmadı (T1/T2) |
| 5 | `npm test` | 0 | `run1/npm-test.out.txt` — "Tüm testler geçti" |
| 6 | `npm run test:electron-bridge` | 0 | `run1/bridge.out.txt` — isolated world 999 köprü + Dark Reader CSS geçti |
| 7 | `npm run test:soak` | 0 | `run1/soak.out.txt` — 400 yakalama + 20 ısınma + 50 hibernasyon; tüm kaynak bütçeleri limit içinde |

### M.2 Bulgu → deney → sonuç bağlantıları

- **U1 (P2):** deney = M.1 satır 3 (probe_tx_t8, probe_t8_repro2). Kaynak: snap `backend/pipeline_control.py:91-99,114` (hash `97a81069…`). Sonuç: SystemError traceback; yeniden üretim deterministik değil, %~50 (S3'te ve committed_debris'te gözlemlendi; iki ayrı koşuda 3/3 ve 1/2). Düzeltme planı D bölümünde; regresyon kabulü: 10 ardışık kurtarma 0 çökme.
- **U2 (P3):** deney = probe_pidkill (ölü PID → `_pid_is_alive` True; canlı süreç öldürülmedi — probe yalnız kendi çocuk süreçlerini kullandı). Aynı kaynak satırı; U1 ile aynı kök neden.
- **U3 (P4):** deney = probe_cli_t7 faz 3 (snap CLI'si, sahte sağlayıcı). Gözlenen: `src.tr.partial.srt`, `src.tr.partial.srt.meta.json`, `src.dual.partial.srt` complete koşudan sonra diskte. Beklenen: tamamlanan işin kendi partial'ları temizlenir (farklı hash'li/kullanıcı düzenlenmiş dosyalara dokunulmaz).
- **T1/T2 (P3, test kapsamı):** deney = M.1 satır 4. M3: `playback-policy.js` `completedRepeats: 0` mutasyonu player-ui.test.js'de 142/142 geçiyor. M7: `queue-lifecycle.js` bayat-olay koruması kaldırıldığında testler geçiyor. Her ikisi de izole snap kopyasında; üretim kodu değişmedi.
- **D1/D2 (veri):** deney = probe_srt_t3 + `npm run audit:subtitle-corpus` (.07'de; araç belowMinimum doğru raporluyor) + rerun08/task08 (rc=2, 3/3 FAIL). S01E13: 1 cue/124 bayt, manifest 175; S10E11: hedef 288 cue < 480. Korpus dosyaları değiştirilmedi; manifest minimumu düşürülmedi.
- **Reddedilen iddialar (G bölümü) gerekçeleri:** .08'in renderer/Electron iddiaları kanıt seviyesi yetersiz (model tabanlı SubtitleHarness; vm düzeyi betikler) — betiklerin kendi kaynak kodundan doğrulandı (`task02_ready_subtitle_chaos.js` başlığındaki "Harness modeling" yorumu; task21/22'de `require('electron')` yok).

### M.3 Açık kalan kabul koşulları (I bölümü ile birebir)

Renderer DOM yarışları (09/10), Electron görevleri (41-46, 49), NDJSON parçalanma (14), Unicode (34), çok dönemli DASH (39), 18 numaralı görevin sınır başına ≥5 tekrarı, 21 numaralı görevin 400/408/500/502/503 + tarihli Retry-After satırları, 25/26 numaralı görevlerin ≥120 çiftlik korpusları, 29 numaralı görevin ≥150 çifti, 31 numaralı görevin ≥120 örneği, 06'nın tek-hedef-kapsama senaryosu, 07'nin ≥60 dengeli örneği, 08'in 30+30 korpusu, 47'nin bu sürümde yeniden koşulmamış olması.

## N. Codex sonrası bulgu uzlaştırması — 13.09.2026

Bu ek, raporun salt-okunur denetim anını korur ve daha sonra yapılan kod düzeltmelerinin sonucunu bulguların yanında kayda geçirir.

| Bulgu | Son durum | Düzeltme / ret / doğrulama |
|---|---|---|
| U1 + U2 (`_pid_is_alive`) | **DÜZELTİLDİ** | Windows `OpenProcess/GetExitCodeProcess`; gerçek canlı çocuk süreç ve sonlandırılmış PID 20 tekrar; ölü sahipli journal kurtarma testi geçti. |
| U3 (bayat `.partial`) | **DÜZELTİLDİ** | Yalnız meta sahipliği doğrulanan same-hash artefaktlar complete sonrası siliniyor; yabancı hash korunuyor. |
| T1 loopCue test açığı | **FALSE / REDDEDİLDİ** | Raporun runner'ı yalnız `player-ui.test.js` çalıştırmış. Mevcut `security-media-learning.test.js` doğrudan `playbackLearningAction` sayacını sınıyor; M3 kopyası bu testte exit 1 verdi. |
| T2 bayat olay test açığı | **DOĞRULANDI VE DÜZELTİLDİ** | `queue-lifecycle.test.js` bayat `done/exit`in aktif işi değiştirmediğini doğrudan sınıyor; M7 kopyası yeni senaryoda exit 1 verdi. |
| D1 S01E13 tek cue | **VERİ SORUNU AÇIK** | Kodla uydurma ve manifest minimumunu düşürme reddedildi; tam kaynak/hedef çiftinin geri getirilmesi gerekir. |
| D2 S10E11 288 < 480 | **VERİ/KALİTE SORUNU DOĞRULANDI** | Örtüşme aracı düzeltildi: 479/486 kaynak cue zaman bakımından kapsanıyor; buna rağmen 49 kaynak yankısı + 7 kapsamasız + 1 sayı uyumsuzluğu gerçek engel. Kullanıcı SRT'si değiştirilmedi. |
| Ek B4 shadow host sök/tak | **BULUNDU VE DÜZELTİLDİ** | Host'un kendi shadowRoot'u yeniden taranıyor; gerçek Electron ve birim test geçti. |
| Ek B5 DASH Period zamanı | **BULUNDU VE DÜZELTİLDİ** | Period start/duration zaman tabanı eklendi; localhost iki dönemli DASH 0/4 sn ofsetle geçti. |
| Ek B6 tanı paketi derin sızıntısı | **BULUNDU VE DÜZELTİLDİ** | Acquisition/coverage dahil tüm snapshot derin sansürden geçiyor; gerçek IPC JSON'unda sentetik token, yol, userinfo ve cue metni yok. |

### N.1 `.09` sonuç cümlesinin düzeltilmesi

Rapor başındaki “Tüm kabul koşuları kapandı” ifadesi, aynı raporda 26 `KISMEN DOĞRULANDI` ve 11 `YÜRÜTÜLEMEDİ` bulunduğu için doğru değildi. Bu düzeltme turu `.07` ile kesişen ürün bulgularını ve renderer/Electron/HLS-DASH/tanı IPC açıklarını kapattı; `.09` içindeki daha geniş 50 görevden bu turda yeniden yürütülmeyen dev korpus hedefleri (ör. ≥120/≥150 çift) başarılı sayılmadı.

### N.2 Son doğrulama dökümü

- `npm test`: exit 0, tüm paket geçti; Python transcribe 175/175.
- `node tests/electron-subtitle-output-smoke.js`: exit 0; gerçek yükleme generation yarışı, bayat ve çift terminal olayı, tanı IPC dosyası.
- `npm run test:electron-bridge`: exit 0; hız fightback, pitch, filtre, gerçek WebAudio compressor, shadow sök/tak, same-origin/opaque iframe hints, Dark Reader, %125 zoom/DPR kırpması.
- `npm run test:soak`: exit 0; 400/400 yakalama, 20 ısınma, 50/50 hibernasyon; bütün kaynak bütçeleri geçti.
- `backend/test_pipeline_control.py`: 4/4; `backend/test_translation_memory.py`: 5/5.
- Hedefli queue lifecycle 16/16, browser media 26/26, diagnostics, subtitle-pair ve geniş HLS/DASH fixture testleri geçti.

### N.3 Ret gerekçeleri

- Eksik SRT metni kaynak olmadan yeniden üretilmedi ve korpus eşiği düşürülmedi.
- Beş kısa İngilizce typo için fuzzy TM eşiği gevşetilmedi; 0/51 yanlış reuse güvenliği korunuyor.
- LoopCue için yinelenen test yazılmadı; mevcut doğrudan testin mutasyonu yakaladığı kanıtlandı.

## O. Run2 nihai düzeltme ve kabul uzlaştırması — 13.09.2026

Bu bölüm, raporun A–M bölümlerindeki **salt-okunur `run1` anlık görüntüsünü değiştirmez**; düzeltmelerden sonraki güncel çalışma ağacının sonucudur. Güncel durum için B tablosu ve M.3 yerine bu bölüm esas alınmalıdır. Evrensel “kusursuzluk” iddiası çıkarılmamıştır: aşağıdaki sonuçlar listelenen sentetik/izinli korpus, localhost akışları, izole Electron ve mevcut test kapsamıyla sınırlıdır.

### O.1 Sonuç ve durum sayıları

- **50 görevin yürütme kapsamı kapandı:** 48 `DOĞRULANDI`, 2 `HATA BULUNDU`, 0 `KISMEN DOĞRULANDI`, 0 `YÜRÜTÜLEMEDİ`.
- İki açık hata **ürün kodu hatası değil, izinli gerçek SRT/korpus verisi sorunudur:** D1 kesik S01E13 çifti ve D2 eksik/yankılı S10E11 hedefi. Kullanıcının talebi gereği altyazı dosyaları değiştirilmedi, eksik metin uydurulmadı ve korpus eşikleri düşürülmedi.
- `run1` içindeki U1/U2/U3 ürün hataları ve sonraki B4/B5/B6 bulguları düzeltildi. Run2 sırasında ayrıca üç gerçek ürün kusuru bulundu ve giderildi: çeviri belleğinin iptalde açık kalması (B7), `done` sonrası süreç çıkmadan arayüzün yeniden başlatılabilir görünmesi (B8), eşzamanlı dizi hafızası yazımlarında kanon kaybı (B9). Gecikmiş sayfa indeksleme sonucu için de davranışsal yarış koruması eklendi (B10).

### O.2 Güncel 50 görev tablosu

Kısaltmalar: BÜ=üretim birim testi, EN=üretim entegrasyonu, IE=izole Electron, UU=uçtan uca/gerçek alt süreç.

| # | Son durum | Run2 kanıtı ve sonuç |
|---|---|---|
| 01 | DOĞRULANDI | Her ana koşu kendi güncel kaynak SHA-256’lerini JSON kanıtına yazdı; kirli çalışma ağacı temizlenmedi veya sahiplenilmedi. |
| 02 | DOĞRULANDI | `.06/.07/.08/.09 run1` iddiaları N ve O bölümlerinde güncel davranışla yeniden uzlaştırıldı; eski yanlış “tamamlandı” ifadesi hükümsüz kılındı. |
| 03 | DOĞRULANDI | Runner başarı=0, kasıtlı hata=7, timeout=124, spawn hatası=126 ve Unicode stdout olmak üzere 5 kontrollü senaryoda doğru kod/ayrı stdout-stderr/hash üretti. Self-test sabit klasör çakışması bulunup benzersiz geçici dizinle idempotent yapıldı. |
| 04 | DOĞRULANDI | `RUN2-MUTATION-001`: yol sansürü, URL userinfo, loopCue sayacı, partial statüsü, MPEGTS rollover, çift terminal, bayat olay ve terminal latch mutasyonlarının **8/8’i** gerçek testlerde exit 1 ile yakalandı. |
| 05 | HATA BULUNDU | İzinli gerçek SRT kimliği/ham sağlık ölçümü D1 ve D2’yi yeniden doğruladı; veri kusuru kodla örtülmedi. |
| 06 | DOĞRULANDI | Tek hedef cue’nun birden çok kaynak cue’yu zaman bakımından kapsadığı senaryo `subtitle-pair-quality` ile geçti; birebir sınır metriği ayrıca korunuyor. |
| 07 | HATA BULUNDU | Gerçek çiftlerde kaynak yankısı/kapsamasız satır/sayı uyumsuzluğu sınıfları ölçüldü; 120 çiftlik dengeli anlam korpusu ürün kapılarının beklenen davranışını doğruladı. D2 açık. |
| 08 | DOĞRULANDI | 30 yankı + 30 meşru aynı/özel-ad kontrolü geçti; URL, özel ad ve numaralı sefer gibi meşru benzerlikler yanlış reddedilmedi. |
| 09 | DOĞRULANDI | IE: çalışan yükleme sırasında `generation` değişince eski tek altyazı sonucu gerçek renderer’da reddedildi. |
| 10 | DOĞRULANDI | IE: iki dosyalı kaynak+çeviri yüklenirken medya değişti; eski çift yeni videoya bağlanmadı ve iki yol da boş kaldı. |
| 11 | DOĞRULANDI | Gerçek renderer’a bayat/çift `done` ve `exit` olayları gönderildi; tek terminal geçiş, aktif iş kimliği ve çıktı listesi korundu. |
| 12 | DOĞRULANDI | Aynı renderer tick’inde Başlat’a iki tıklama yalnız bir backend çağrısı üretti; kuyruk seçenekleri ekleme anında donuyor, bayat olaylar yeni işi etkilemiyor. |
| 13 | DOĞRULANDI | İlk çeviri ile 2. geçiş ayrı stage/olay olarak doğrulandı. `done` sonrasında gerçek `exit` gelene dek durum `Tamamlandı · süreç kapanıyor`; Başlat ve İptal gizli. Baştan yeniden çeviri başlatılmıyor. |
| 14 | DOĞRULANDI | NDJSON 1,5 MB olay, çoklu satır ve tek-karakter parçalama ile sınandı; Türkçe, CJK, Tayca ve emoji kayıpsız birleşti. |
| 15 | DOĞRULANDI | Kısmi başarısızlık mevcut sağlam nihai dosyayı ezmiyor; durum ve hash korunuyor. |
| 16 | DOĞRULANDI | Tam başarı yalnız aynı kaynak hash/iş sahipli partial SRT, meta ve dual artefaktlarını siliyor; yabancı/kullanıcı artefaktı korunuyor. |
| 17 | DOĞRULANDI | Windows canlılık sondası `OpenProcess/GetExitCodeProcess` ile canlı ve ölü çocuk süreçlerinde 20 tekrar; SystemError yok, ölü PID canlı sayılmıyor. |
| 18 | DOĞRULANDI | Ölü sahipli journal gerçek çocuk süreç ölümünden sonra 5 bağımsız turda kurtarıldı. |
| 19 | DOĞRULANDI | Kilitli hedefte rollback uyarı verip journal’ı başlangıç kurtarmasına bırakıyor; özgün commit hatası callback hatasıyla maskelenmiyor. |
| 20 | DOĞRULANDI | Bozuk/eski journal, iki canlı iş ve çalışma alanı dışı yol senaryoları sınandı; dış dosyaya dokunulmadı. |
| 21 | DOĞRULANDI | 400/401/403/408/429/500/502/503, sayısal ve HTTP-tarihli `Retry-After`, timeout, kopma, yarım gövde ve bozuk JSON sınıfları; bekleme bütçesi ve Türkçe hata sınıfları geçti. |
| 22 | DOĞRULANDI | İptal API beklemesi, retry/backoff, exact-cache öncesi ve ilk transaction replace öncesinde sınandı; sahte final/cache yok, eski dosya korunuyor, TM bağlantısı kapanıyor. |
| 23 | DOĞRULANDI | Eksik/fazla/çift kimlik, boş çeviri, kesik JSON, Markdown çiti ve `items/sentences` çelişkisi atomik grup sözleşmesiyle reddedildi. |
| 24 | DOĞRULANDI | 25 cümle grubunda ilk yanıt her beşinci grubu atladı; yalnız 5 eksik grup retry edildi, 20 başarılı grup yeniden gönderilmedi ve baştan başlama olmadı. |
| 25 | DOĞRULANDI | 120 olumlu/olumsuz modal-anlam çifti + üretim kapısı testleri geçti; sert sayı/olumsuzluk ile advisory modal ayrımı ölçüldü. |
| 26 | DOĞRULANDI | Ondalık nokta/virgül için 4.000 özellik tabanlı örnek ve para/tarih/birim advisory işaretleri sınandı. Yazım biçimi değişimi sayı kaybı sayılmadı. |
| 27 | DOĞRULANDI | Önce/sonra bağlamı gerçek sağlayıcı payload’ında ölçüldü; aynı kısa repliğin farklı sahne bağlamları ayrıldı. |
| 28 | DOĞRULANDI | Model/register/sözlük/dil/refine/bağlam cache sınırları ve başarısız/yankılı cevabın cache’e girmemesi geçti; iptal bağlantı sızıntısı B7 düzeltildi. |
| 29 | DOĞRULANDI | 150 çiftlik fuzzy TM korpusu; güvenli typo/biçim kazanımları ve sayı/olumsuzluk/modal/zamir/özel-ad engellerinde 0 yanlış yeniden kullanım. Cümle başı capitalization false-ret’i daraltıldı. |
| 30 | DOĞRULANDI | Dizi kanonu, NFC/NFD ve çapraz dizi ayrımı yanında iki bayat instance ve **8 gerçek eşzamanlı Python süreci** aynı dosyaya yazdı; tüm kanon girdileri korundu. |
| 31 | DOĞRULANDI | 120 SDH örneği; satır içi sahne etiketi temizliği, konuşmacı öneki ve gerçek konuşma korunumu geçti. |
| 32 | DOĞRULANDI | Halüsinasyon güven metrikleri, tekrar döngüsü ve gerçek konuşma kontrolleri motor→olay→dosya zincirinde geçti. |
| 33 | DOĞRULANDI | Cümle gruplama, konuşmacı sınırı, tek cue’ya çökme reddi, zamanların özgün cue’lara atomik geri dağıtımı ve refine bütünlüğü geçti. |
| 34 | DOĞRULANDI | NFC birleşimi, Türkçe, CJK, Tayca, emoji ve boşluksuz dil parça sözleşmeleri geçti. |
| 35 | DOĞRULANDI | Clip offset segment/kelime/WhisperX/diarization span’e yalnız bir kez uygulandı; diarization’ın kırpılmış medya zaman çizgisiyle eşleşmesi doğrudan sınandı. |
| 36 | DOĞRULANDI | SRT/VTT/ASS/JSON/TXT yeniden dışa aktarma tek kaynak zaman çizgisiyle, BOM/kaçış/bozuk kayıt uyarılarıyla geçti. |
| 37 | DOĞRULANDI | Localhost gerçek HTTP tüketicisinde eksik segment, gecikme, 403/imza yenileme ve kurtarma yolu geçti; canlı üçüncü taraf siteye çağrı yapılmadı. |
| 38 | DOĞRULANDI | Canlı HLS pencere kayması, discontinuity, segment sınırı ve 33-bit MPEGTS rollover fixture’ları geçti. |
| 39 | DOĞRULANDI | İki Period’lu DASH zaman tabanı ve parçalı MP4/wvtt-stpp bozuk kutu/fuzz senaryoları geçti. |
| 40 | DOĞRULANDI | Katman/segment/epoch/discontinuity/region/writingMode provenance anahtarı, büyüyen cue ve stale-prefix gerilemesi testleri geçti. |
| 41 | DOĞRULANDI | IE gerçek renderer medya kimliği yarışları + soak’ta 50/50 hibernasyon; bayat generation/epoch yeni videoya sızmadı. |
| 42 | DOĞRULANDI | IE’de loopCue/skip-gaps/accelerate-gaps gerçek DOM `HTMLMediaElement`ine uygulandı; ayrıca ffmpeg ile üretilen 7 sn WebM gerçekten oynatılarak üretim `timeupdate/rVFC` zincirinin boşluğu 4. saniyeye atladığı gözlendi (`RUN2-ELECTRON-SUBTITLE-005`). |
| 43 | DOĞRULANDI | IE gerçek medya yüzeyinde site hız hafızası, site resetine fightback ve `preservesPitch` geçti. |
| 44 | DOĞRULANDI | IE gerçek `AudioContext`/compressor ve CSS filtre aç-kapa/temizlik yaşam döngüsü geçti. |
| 45 | DOĞRULANDI | IE isolated world’de dinamik DOM, shadow sök-tak, same-origin/opaque iframe link hints ve Dark Reader CSS davranışı geçti. |
| 46 | DOĞRULANDI | IE viewport/video kırpma, %125 zoom+DPR koordinatları ve altyazı kompoziti geçti. |
| 47 | DOĞRULANDI | Üretilen `.apkg` ZIP+SQLite açılarak alanlar/medya kontrol edildi; aynı notlar iki export’ta aynı GUID’leri verdi. |
| 48 | DOĞRULANDI | FTS metin/redaksiyon ve generation korumasına ek olarak geciken capture sonucu index clear veya tab/media değişiminden sonra gerçek `upsert`e ulaşmadı. |
| 49 | DOĞRULANDI | IE gerçek preload→main IPC dışa aktarımı diske yazıldı; URL token/userinfo, yerel yol ve cue metni JSON’un hiçbir derin alanında bulunmadı. |
| 50 | DOĞRULANDI | `RUN2-FULL-002`, Electron bridge, gerçek-medya subtitle smoke, 8/8 mutasyon ve tam 400+20+50 soak exit 0. Parametreler küçültülmedi. |

### O.3 Run2’de bulunan ve düzeltilen ek ürün kusurları

#### B7 (P3) — İptal, exact-cache yazımından önce SQLite çeviri belleğini açık bırakabiliyordu

- **Zincir:** `translate_existing_subtitle` → fuzzy TM açılışı → cümle çevirisi → iptal denetimi → exact-cache yazımı.
- **Kök neden:** TM kapatma çağrıları başarı/hata dallarına dağılmıştı; cache yazımından hemen önceki `PipelineCancelled` bu dalları atlıyordu.
- **Düzeltme:** `backend/transcribe.py:3225-3661` aralığında exact-cache/TM store bölümü `try/finally` ile çevrildi; `close_translation_memory()` her çıkışta çalışıyor.
- **Regresyon:** `backend/test_transcribe.py:2775` iptali tam bu sınırda enjekte ediyor; exact cache oluşmuyor ve SQLite dosyası Windows’ta hemen silinebiliyor.

#### B8 (P2) — Backend `done` gönderdikten sonra süreç hâlâ yaşarken arayüz yeni işi başlatılabilir gösteriyordu

- **Kullanıcı etkisi:** Çeviri bitmiş görünmesine rağmen süreç bir süre daha çalışıyor izlenimi ve aynı dosyada ikinci işi başlatma yarışı; kullanıcının bildirdiği “bittikten sonra yine çeviri sürüyor / baştan mı yapıyor?” belirtisiyle uyumlu.
- **Kök neden:** Renderer `done` olayında `finishRun(true)` çağırıp `running=false` yapıyor ve Başlat’ı gerçek child-process `exit` olayından önce gösteriyordu.
- **Düzeltme:** `src/renderer/renderer.js:2956` `finishRunAwaitingProcessExit()` eklendi; durum `Tamamlandı · süreç kapanıyor`, iş sahipliği ve `running/queueRunning/awaitingExit` gerçek `exit` gelene dek korunuyor; Başlat ve İptal gizli.
- **Regresyon:** IE’de çift `done`, bayat `exit`, geçerli `exit` sırası ve aynı-tick çift Başlat gerçek renderer IPC’siyle sınandı.

#### B9 (P3) — Aynı dizinin iki süreçte eşzamanlı güncellenmesi kanon girdilerini kaybedebiliyordu

- **Kök neden:** Her `SeriesMemory` örneği yüklediği eski snapshot’ı atomik replace ile yazıyor; atomik dosya değişimi bozulmayı önlüyor fakat lost-update’i önlemiyordu.
- **Düzeltme:** `backend/series_memory.py:266-352` platformlar arası dosya kilidi eklendi (`msvcrt`/`fcntl`); kilit altında son geçerli disk şeması yeniden okunuyor, term/character/address kanonu “en erken bölüm kazanır” kuralıyla birleştiriliyor, sonra atomik yazılıyor. Gelecek/bozuk şema ezilmiyor.
- **Regresyon:** `backend/test_series_memory.py:68` iki bayat instance; ayrı testte 8 gerçek Python süreci. Sekiz girdinin sekizi kaldı.

#### B10 (P3) — Gecikmiş sayfa yakalama sonucunun temizlenmiş/değişmiş FTS indeksine yazılmasını davranışsal olarak engelleme

- **Düzeltme:** `src/browser-page-index.js:3-37` yakalama öncesi ve sonrası enabled/index-generation/tab-view/tab-generation/webContents kimliğini doğruluyor; `src/main.js:2718` yazımı bu yardımcıdan geçiriyor.
- **Regresyon:** `tests/browser-page-index-race.test.js` gecikmiş promise’i index clear ve tab/media değişiminden sonra çözüyor; hiçbirinde `upsert` yok, taze capture yazılıyor.

### O.4 Çeviri anlamı ve tekrar maliyetiyle ilgili uygulanan sağlamlaştırmalar

- 25 gruplu seçici retry testi yalnız eksik 5 grubu yeniden gönderiyor; başarılı 20 grup korunuyor. Bu, yarım cevapta baştan sona tekrar çeviri yapılmadığının yürütülebilir kanıtıdır.
- Olumsuzluk ve sayılar sert kabul kapısı olarak kaldı. Modal, para, tarih ve birim farkları telemetry/advisory olarak ölçülüyor; bunların dilsel yazım farkları yanlış pozitif üretebildiği için körlemesine sonsuz retry sebebi yapılmadı.
- Kaynak yankısı özel ad, URL ve meşru değişmeden-kalması gereken ifade istisnalarıyla sınandı; gerçek yankı cache’e girmeden retry ediliyor.
- Cümle grubu atomik tutuluyor: model bir grubun parçalarını eksik/fazla/kimliksiz döndürürse yarım metin cue’lara dağıtılmıyor.
- İptal ve ara kayıt sınırları sahte tamamlanmış dosya/cache üretmiyor; partial/final durumu çıktı sözleşmesinde açık kalıyor.

### O.5 Ret gerekçeleri ve hâlâ açık veri sınırları

1. **D1 reddi:** S01E13 kaynak/hedef çifti diskte tek cue. Olmayan diyalog kodla tahmin edilmedi; manifest minimumu testi yeşile çevirmek için düşürülmedi. Tam dosyalar yeniden sağlanmadan bu korpus girdisi düzeltilemez.
2. **D2 reddi:** S10E11 hedefinde zaman örtüşmesi yüksek olsa da 49 kaynak yankısı, 7 kapsamasız kaynak cue ve 1 sayı uyuşmazlığı var. Kullanıcı açıkça “altyazıyı değil kodu” dediği için `.tr.srt` değiştirilmedi.
3. **Sert modal/para/tarih/birim retry reddi:** advisory ölçüm eklendi; dilsel dönüşümler (ör. tarih/ondalık biçimi) nedeniyle yanlış ret ve API maliyeti ölçülmeden sert kapıya dönüştürülmedi.
4. **Canlı üçüncü taraf servis çağrısı reddi:** HLS/DASH/403/imza yenileme localhost fixture’larıyla sınandı; ücretli sağlayıcı, internetten medya veya gerçek kullanıcı hesabı kullanılmadı.

### O.6 Nihai doğrulama dökümü

| testRunId | Komut / seviye | Sonuç |
|---|---|---|
| `RUN2-FULL-002` | `node tests/run-all.js` · EN | exit 0, 51,579 ms; Python `transcribe` 177/177; son satır `Tüm testler geçti`. Güncel test hash’i dahil. |
| `RUN2-ELECTRON-BRIDGE-001` | gerçek Electron trusted bridge · IE | exit 0; medya denetimi, WebAudio, filtre, link hints, Dark Reader ve yakalama geçti. |
| `RUN2-ELECTRON-SUBTITLE-005` | Electron subtitle smoke + kodlanmış WebM · IE/UU | exit 0, 1,858 ms; yükleme yarışları, terminal/UI, gerçek medya saati, tanı IPC geçti. |
| `RUN2-MUTATION-001` | 8 güncel kaynak mutasyonu | exit 0; 8/8 mutantın hedef testi exit 1. |
| `RUN2-SOAK-001` | `node tests/run-resource-soak.js` · IE soak | exit 0, 425,883 ms; 400/400 yakalama, 20 ısınma, 50/50 hibernasyon; unhandled rejection 0, listener delta 0, ana timer delta 0, disk yazımı/döngü 7,1225≤10, overlay ortalama/azami 0,1 ms. |

Ham kanıtlar `scratch/audit-13.09.2026.09/run2/` altındadır. Her koşunun JSON kaydı komut argümanlarını, UTC başlangıç/süreyi, çalışma dizinini, Node sürümünü, gerçek exit code’u, timeout/sinyal durumunu ve ilgili SHA-256’leri; `.stdout.txt`/`.stderr.txt` dosyaları ayrı akışları içerir.

`RUN2-ELECTRON-SUBTITLE-002/003/004` başarısız deneme kayıtları kasıtlı olarak silinmedi: ilkinde CDP değerlendirme hatası `{}` olarak görünmezdi; ikinci/üçüncü koşuda Chromium test penceresindeki arka plan “video-only media”yı güç tasarrufuyla durdurdu. Beklenti gevşetilmedi. Fixture’a sessiz ses izi eklendi, test-only Chromium bayrakları executable’dan önce taşındı ve aynı gerçek medya zinciri `RUN2-ELECTRON-SUBTITLE-005`te geçti.

### O.7 Son sınırlar

- Gerçek ücretli çeviri sağlayıcısı, API anahtarı, GPU Whisper modeli ve internetten üçüncü taraf medya kullanılmadı; sağlayıcı sözleşmeleri sahte/localhost yanıtlarıyla doğrulandı.
- Gerçek SRT dosyalarındaki D1/D2 kusurları açıktır ve kod kabulünün geçtiği anlamına gelmez.
- “Bütün dünyadaki her site/altyazı kusursuz” sonucu çıkarılamaz. Doğrulanan sonuç: bu 50 görevin tanımlı kabul kapsamı güncel kaynakta yürütüldü; bulunan ürün kusurları için regresyonlar yeşil, iki veri kusuru açık ve açıkça ayrılmıştır.

## P. Sürümleme kaydı

Düzeltme ve doğrulama çalışması kod/test ile raporları birbirinden ayıran tematik commit’lere dönüştürüldü:

1. `86a0681 feat: harden contextual subtitle translation` — Python çeviri bağlamı, atomik cümle grupları, anlam/yankı kapıları, SDH temizliği, fuzzy TM, dizi hafızası, transaction kurtarma ve doğrudan testleri.
2. `16b05f54 feat: integrate browser learning and provider workflows` — Electron altyazı yaşam döngüsü, gerçek medya oynatma politikaları, HLS/DASH, Codecraft ve sağlayıcıya özgü anahtarlar, Anki, FTS, tanı güvenliği, öğrenme/tarayıcı özellikleri ve testleri.
3. Bu rapor ile ilişkili denetim/kabul belgeleri ayrı dokümantasyon commit’inde tutulur.

Her iki kod commit’inden önce kendi hedefli testleri; ikinci commit öncesinde ayrıca `npm test` ve `npm run test:electron-bridge` exit 0 ile geçti. Depoda remote tanımlı olmadığı için bu sürümleme yereldir; push yapılmadı.
