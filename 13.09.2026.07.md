# Bağımsız Denetim Turu 2 — 13.09.2026 (Rapor 07)

## 1. İncelenen sürüm, hash'ler, zaman ve ortam

- **Git HEAD:** `c953539fc7dff5496d301f72caaeb7becd9f9eb7` (master) — çalışma ağacı kirli (önceki turların commit edilmemiş çalışması); denetlenen sürüm budur.
- **Başlangıç:** 13.09.2026 07:27 · **Bitiş:** 13.09.2026 (aynı gün, bu oturum).
- **Yazma alanı:** `scratch/audit-13.09.2026.07/run1/` — 24 prob betiği + ham çıktılar + `tmp/` çalışma dizinleri. Kaynak/test/ayar/lockfile/önceki raporlar değiştirilmedi; git state'e dokunulmadı.
- **Kritik dosya SHA-256'leri (denetim başı, `run1/hashes-start.txt`; denetim sonunda değişiklik yok):**
  `src/main.js` `9820d26c…dd93a9` · `src/browser-adapters.js` `6cd1f16e…82ef01` · `backend/transcribe.py` `0742cf9d…60384` · `backend/sentence_translation.py` `784d3ccb…81e253` · `backend/translation_memory.py` `79330b45…1a349` · `backend/series_memory.py` `2a6dc60d…c00df` · `backend/subtitle_sdh.py` `280e258b…40dcaa8` · `src/subtitle-output-contract.js` `9e9082bb…18065d` · `src/playback-policy.js` `4757fb8c…43ae6` · `src/browser-media-controller.js` `f9a565ed…98f74d` · `src/watch-index.js` `463bab83…771c719` · `src/browser-subtitles.js` `eb0fdf32…0ad9f8` · `src/browser-diagnostics-export.js` `11cdcc55…bc69f`
- **Ortam:** Node v25.6.1, Python 3.11.9 (venv, openai 2.37.0, pyannote/whisperx kurulu), ffmpeg `C:\ffmpeg\bin`. Gerçek kullanıcı profili/çerez/API anahtarı okunmadı; ücretli model çağrısı yapılmadı (tüm sağlayıcı çağrıları yerel sahte sağlayıcıya: 47731–47734). İzole Electron yalnızca projenin kendi test:soak/test:electron-bridge komutlarında (kendi geçici userData'ları) çalıştı.
- **Alt-ajan durumu:** 16 alt-denetçi denendi (14 + 2); TÜMÜ "request budget is exhausted after 150 model requests" hatasıyla ilk araç çağrısından önce öldü; tek artifakt üretilmedi (`scratch/audit-13.09.2026.07/t*/` klasörleri boş). Denetim tümüyle ana oturumda yürütüldü.
- **Denetim sırasında kaynak sürüklenmesi (izleme kaydı):** Denetim sonunda `backend/transcribe.py` SHA-256 `0742cf9d…60384` → `4d95b8dd…7358a`, `backend/subtitle_sdh.py` `280e258b…40dcaa8` → `f9cfd3b3…7efcff`, `backend/translation_memory.py` `79330b45…1a349` → `c3757678…67477ca1` olarak değişti (`run1/hashes-start.txt` / `run1/hashes-end.txt`). Bu denetim bu dosyalara yazmadı (tüm yazmalar `scratch/audit-13.09.2026.07/` ve bu raporla sınırlı; hiçbir git durumu komutu çalıştırılmadı) — çalışma ağacı paylaşımlı, eşzamanlı başka bir yazar etkin. Sonuçlar, her problanın çalıştığı anda diskte bulunan sürüme aittir; sürüklenme, gözlemlenen davranışların yeniden üretilebilirliğini etkileyebilir (kapanış koşulu: sabit sürüm üzerinde probların yeniden koşulması).

## 2. Önceki raporun düzeltilmiş kapsam beyanı

Önceki rapor (13.09.2026.06.md) 19 görevi "tamamlandı" saymıştı. Bu turda yapılan düzeltmeler:

| Önceki iddia | Gerçek durum | Düzeltme |
|---|---|---|
| G2 ölçümleri (S10E11: 486/288, eşleşen 202, engel 421, eksik 284, fazla 86) | Kısmen doğru — aracın **exact-timestamp-key** eşleşmesinden geliyor | Bağımsız örtüşme bazlı ölçüm: 616 ham blok (131 ardışık aynı-metin YouTube ikilemesi araçta birleşiyor → 486), örtüşen 608, örtüşmeyen yalnız 8. "Eksik 284" konsolide çeviri yapısının yanlış yorumlanması (bkz. T3 bulgusu) |
| G17/G18 "tamamlandı" (saf fonksiyon probu) | Yalnız filtre fonksiyonu düzeyi | Bu turda gerçek transcribe() boru hattı üzerinden üç katman (motor→ekran→dosya) 37 assertion ile doğrulandı; önceki tur "tamamlandı" etiketi düşüktü |
| G21/G22 "tamamlandı" (probe_hls.js = manifest string parser) | Parser düzeyi | Bu turda gerçek HTTP fetch zinciri eklendi (T16, kompakt kapsam) |
| G24 "tamamlandı" (12 saf politika senaryosu) | Politika düzeyi | Bu turda aynı akışta entegrasyon + sayaçlı tüm-döngü senaryosu eklendi (T17) |
| Önceki probların "10/10 test geçti" dili | 12 problanın 11'i **assert'siz, print-tabanlı** (`find /c "assert"`: yalnız 1 dosyada 1 eşleşme) | "OK/FAIL" satırları elle sayılmış; exit kodu açıkça ayarlanmış olsa bile assertion'sız prob zayıf kanıttır (T2 bulgusu) |
| "Soak test çakışması" gerekçesi | Bu turda çalıştırıldı | Tam soak GEÇTİ (400+50) |

## 3. Yirmi görevin kabul koşulu/kanıt/durum tablosu

| # | Görev | Durum | Kanıt (komut → çıktı) |
|---|---|---|---|
| 1 | Sürüm sabitleme + önceki 3 bulgunun yeniden kontrolü + düzeltme tablosu | **Doğrulandı** | `node -e ...redactBrowserDiagnosticsText/redactCaptureUrl...` → `run1/t1-prevbugs.out.txt` (3/3 OK, fails=0): yerel yol `[yol gizlendi]`, userinfo temiz, `authorization=[gizlendi]`. Düzeltme tablosu: Bölüm 2 |
| 2 | Denetim araçlarının kendi doğruluğu + mutasyon testi | **Doğrulandı** | (a) Önceki 12 probun 11'i assert'siz/print-tabanlı (grep kanıtı). (b) `node run1/probe_mutasyon_t2.js` → `run1/probe_mutasyon_t2.out.txt`: kontrol 5 orijinal test geçti; **5/5 mutasyon (yol sansürü, userinfo, loopCue sayacı, partial-status, MPEGTS rollover) izole kopyada ilgili gerçek testi exit 1 ile düşürdü**; üretim dosyalarına dokunulmadı |
| 3 | Gerçek SRT çiftlerinde bağımsız bütünlük | **Kısmen** | `backend\venv\Scripts\python.exe run1/probe_srt_t3.py` → `run1/probe_srt_t3.out.txt` + `t3_srt_summary.json`: bağımsız ayrıştırıcıyla S10E11 616/288 (örtüşen 608, örtüşmeyen 8, çökme 216 hedef←536 kaynak, yankı 86, sayı 2), S13E04 290/290 (yankı 27), S01E13 1/1 (bkz. T3-B2). Mevcut araç karşılaştırması: `node tools/audit-subtitle-pair.js … --json` → `run1/t3_tool_s10e11.json` (486/288/202/284/86) + `npm run audit:subtitle-corpus` → `run1/t3_corpus_tool.out.txt`. Uyuşmazlık nedenleri açıklığa kavuşturuldu (Bölüm 4) |
| 4 | Anlam ve komşu bağlam incelemesi | **Kısmen** | Örneklem: her çiftte başlangıç/orta/son %10 (deterministik indeks; S10E11/S13E04 30+30, S01E13 1 örnek). Bulgular: S10E11'de kaynak yankısı örnekleri (ör. ilk cue İngilizce kalmış), en az 2 "yanlış dağıtım" örneği (kaynak sorusunun yerine komşu cümlenin çevirisi yazılmış); S13E04'te 1 yankı örneği, sayılar korunmuş ("on numara", "sekiz çanta"); S01E13 tek cue doğru. Uzun telifli içerik rapora taşınmadı |
| 5 | Hazır altyazı yükleme ile çalışan işin yarışı | **Kısmen** | Renderer DOM düzeyi yarış harness'i kurulamadı (renderer.js 20.161 satır, vm yüklemesi için tam DOM stub'ı gerekli; oturum bütçesi tükendi). Kod düzeyi: epoch/generation modeli (`resetBrowserCaptureState`, `browserStateGeneration`) + mevcut modül testleri (npm test kapsamında geçti). Açık kalan: Bölüm 6 |
| 6 | done/exit/error ve ikinci geçiş ayrımı | **Kısmen** | Gerçek arka uç olay zinciri `probe_cli_t7.py` ile doğrulandı: done olayı roller/status/temiz çıktı; kısmi durum `partial` + 6/2 sayaçları; iptal (WHISPER_CANCEL_FILE) 6,0 sn'de sonlandı, rc=1, hiçbir çıktı yazılmadı. Renderer kuyruk mantığı (çift done, exit→done sırası) davranışsal sürülmedi |
| 7 | Kısmi çeviri ve nihai çıktı koruması | **Doğrulandı** | `backend\venv\Scripts\python.exe run1/probe_cli_t7.py` → `run1/probe_cli_t7.out.txt` (26 assertion): faz1 tam koşu (nihai+dual+meta, partial yok); faz2 sağlam nihai dosyalar hash'te değişmedi, `.tr.partial.srt` + 6/2 meta yazıldı, done çıktısı `partial`; faz3 eksikler tamamlandı → nihai yayınlandı, **eski partial artıkları silinmedi (T7-B1)**; faz4 iptal 6,00 sn, çıktı yok |
| 8 | Çok dosyalı çıktı transaction'ında hata enjeksiyonu | **Hata bulundu** | `backend\venv\Scripts\python.exe run1/probe_tx_t8.py` + `probe_t8_repro.py` + `probe_t8_repro2.py` → ilgili `.out.txt` (34+assertion): S1 stage hatası rollback ✓, S2 boş stage düşer ✓, S3/S4/S5 gerçek süreç ölümü (os._exit) sonrası kurtarma tek nesile döndürdü ✓, S7 replace enjeksiyonu ✓, S8 committed-artık temizliği ✓. **S6 kilit: rollback yarıda patladı (karışık nesil + artık)**; **kurtarma fonksiyonu ölü sahipli journal'da ~%50 SystemError ile çöktü** (T8-B1/B2) |
| 9 | Yerel sahte sağlayıcıyla retry ve iptal | **Doğrulandı** | `backend\venv\Scripts\python.exe run1/probe_llm.py` (T9 bölümü) → `run1/probe_llm.out.txt`: 429+Retry-After (istek=2, bekleme=1.0sn kayıtlı), 401/403 (tek istek, retry yok, None + reason=authentication), bağlantı kopması/yarım gövde (toparlanma ve kalıcı başarısızlıkta None+dürüst ret), kalıcı kaynak yankısı (grup bazlı 7 retry, cache'e yazılmadı), sayı kapısı reddi, timeout sınıflandırması (gerçek APITimeoutError, 1 retry). İptal gecikmesi gerçek CLI yoluyla 6,00 sn |
| 10 | Model çıktısının kimlik/parça sözleşmesi | **Doğrulandı** | `probe_llm.py` (T10): eksik kimlik reddedildi+yalnız eksik grup yeniden istendi (istekler [[0..7],[0]]), fazla kimlik zararsız, çift kimlik tutarsızlığı reddedildi (yalnız o grup), boş çeviri reddedildi, bozuk/yarım JSON tamamlanmadı, Markdown çiti **kabul edildi** (parse_llm_json_object çit soyuyor — doğrulanmış davranış), items/sentences çelişkisi reddedildi, karışık koşuda yalnız hatalı grup yeniden gönderildi |
| 11 | Bağlam ve tam eşleşmeli cache izolasyonu | **Doğrulandı** | `probe_llm.py` (T11): C1 ikinci koşu 0 istek (hit); C2 aynı kısa replik farklı komşuda miss + sağlayıcıya giden `context_before` farklı (kayıtlı payload kanıtı) + aynı bağlam tekrar hit; C3 model/register/sözlük/dil/refine tek tek değişince miss (taban hit iken); C4 başarısız sonuç cache'ten servis edilmedi, başarı sonrası alındı |
| 12 | Fuzzy TM yanlış yeniden kullanım oranı | **Doğrulandı (P4 bulgusu)** | `backend\venv\Scripts\python.exe run1/probe_tm_t12.py` → `run1/probe_tm_t12.out.txt` + `t12_summary.json`: 51 karşıt örnek (7 kategori×~8) **yanlış yeniden kullanım 0/51**; olumlu 27 kontrolde güvenli eşleşme 16/27 — yanlış ret 11 (büyük/küçük harf 6/8, kısa kelime yazım hatası 5/8; Bölüm 4 T12-B1) |
| 13 | Dizi hafızası kalıcılık ve kanon sınırları | **Doğrulandı** | `backend\venv\Scripts\python.exe run1/probe_series_t13.py` → `run1/probe_series_t13.out.txt` (22 assertion): 5 kanon biçimi aynı dosyaya; NFC/NFD aynı slug; benzer diziler ayrı; çapraz-dizi sızma yok; unvan/genel-ad/yanlış-register filtreleri; geç bölüm ezmedi/erken bölüm geçti; **yeni süreçte kalıcılık**; bozuk JSON ve gelecek şema devre dışı + dosya korunarak; eşzamanlı yazımda dosya geçerli JSON kaldı ama **son-yazan kazanır** (tek anahtar — gözlem) |
| 14 | SDH ve halüsinasyon filtresi yanlış pozitif ölçümü | **Doğrulandı** | `backend\venv\Scripts\python.exe run1/probe_engine_t14_t15.py` (T14 bölümü) → `run1/probe_engine_t14_t15.out.txt`: sahte motor (gerçek WhisperModel arayüzü) + **gerçek transcribe() boru hattı**; 12 vaka üç katmanda (motor→segment olayı→SRT/JSON): saf etiket/garbled atıldı, konuşmacı/eser adı/şarkı/dengesiz parantez/gerçek tekrar korundu, satır içi SDH ayıklandı, tekrar döngüsü 2'ye sıkıştırıldı, YT kalıbı güçlü metrikte korundu/zayıf metrikte atıldı; halüsinasyon sayacı done uyarısında göründü |
| 15 | Clip offset, WhisperX, diarization zaman zinciri | **Doğrulandı (diarization span dışı)** | `probe_engine_t14_t15.py` (T15): gerçek ffmpeg + sahte motor; taban koşuya karşı `--clip-start 10.5` → SRT cue başları **tam 10.5sn** (bir kez), JSON kelimeleri ofsetli, segment sınırını aşan kelime kırpıldı, ekran olayları aynı eksende; kısa segment + normalize_timings birleşiminde zamanlar geçerli ve JSON/SRT aynı eksende. Diarization span zinciri davranışsal sürülmedi (açık kalan: Bölüm 6) |
| 16 | Yerel HLS/DASH sunucusunda segment kaybı | **Kısmen** | `node run1/probe_hls_t16.js` → `run1/probe_hls_t16.out.txt`: yerel HTTP sunucu + **gerçek fetch + gerçek parseSubtitlePayload zinciri**; taban 5/5 cue; 404/403/yanlış MIME/yarım gövde → cue üretmedi, retry istekleri sayıldı, sonraki segment sağlıklı. Kapsam dışı kalan: playlist canlı yenilenme, çok dönemli DASH manifest, MPEGTS rollover'un HTTP akışında uçtan uca sürülmesi (parser düzeyi mevcut testlerde) |
| 17 | Seek/reklam/oynatma politikalarının entegrasyonu | **Doğrulandı (tüketici model düzeyi)** | `node run1/probe_playback_t17.js` → `run1/probe_playback_t17.out.txt` (15 assertion): gerçek `playbackLearningAction` ile aynı akışta loopCue tüm-döngü (2 geri seek + bitiş, 60 adım sınırı, sonsuz döngü yok), kullanıcı seek'i otomatik komuttan ayrıştı (büyük sıçramada loop/auto-pause yok), skipGaps+A-B etkileşimi (A-B aktifken komut yok — renderer.js:11427 koruması), skipGaps zinciri tek seek, shadowing/autoPause çakışan konuşmacı koruması, accelerateGaps, medya değişimi. Renderer tüketicisi (applyPlaybackLearningPolicy, renderer.js:11425-11470) kod düzeyinde doğrulandı; DOM'da yürütülmedi |
| 18 | İzole Electron'da yeni medya/görünüm özellikleri | **Yürütülemedi** | Denendi: 16 alt-denetçi denemesi başarısız; ana oturum bütçesi bu oturumda T1-T17+T20 kanıtlarıyla tükendi. İzole Electron smoke deseni mevcut (`tests/electron-browser-trusted-bridge.smoke.js` geçti — T20). Kapanış koşulu: Bölüm 6 |
| 19 | FTS temizleme yarışı + gerçek tanı dışa aktarımı | **Kısmen** | FTS: koruma zinciri kod düzeyinde doğrulandı (main.js:2710-2727 `scheduleBrowserPageIndex` — await **sonrası** generation/etkinlik/tabla yeniden kontrol; main.js:11659-11668 temizleme generation++ + zamanlayıcı iptali) → geç sonuç yeniden yazılamaz. Tanı paketi: sansür katmanı davranışsal olarak doğrulandı (T1 probu, 3/3) + `browserDiagnosticsExportSnapshot`'ın sansör çağrı noktaları okundu (main.js:3060-3121); **main.js'in tamamı üzerinden gerçek Electron dışa aktarım sürülmedi** |
| 20 | Tam kabul koşusu | **Doğrulandı** | `npm test` → `run1/npm-test.out.txt` (exit 0, "Tüm testler geçti"); `npm run test:electron-bridge` → `run1/bridge.out.txt` (geçti); `npm run test:soak` → `run1/soak.out.txt`: **400 yakalama çevrimi (400/400) + 20 ısınma + 50 hibernasyon çevrimi, sonuç GEÇTİ**; unhandled-rejection 0, tüm kaynak bütçeleri (RSS/heap/timer/GPU/disk/overlay) limit içinde; parametre küçültülmedi |

## 4. Güncel kodda doğrulanmış yeni hatalar

### T8-B1 (P2) — **DÜZELTİLDİ VE DOĞRULANDI** — Kurtarma yolu ölü işlem journal'ında ~%50 olasılıkla çöküyordu
- **Konum:** `backend/pipeline_control.py:96` `_pid_is_alive` → `os.kill(pid, 0)`; `pipeline_control.py:114` çağrı zinciri; `except (OSError, TypeError, ValueError)` SystemError'ı yakalamıyor.
- **Zincir:** Çöken transkripsiyon/çeviri işi → `.whisper-output-transaction-*.json` journal kalır → sonraki her iş başlangıcında `recover_output_transactions` (transcribe.py:6391, translate_existing_subtitle) → `_pid_is_alive(ölü PID)` → `SystemError: <class 'OSError'> returned a result with an exception set`.
- **Yeniden üretim:** `backend\venv\Scripts\python.exe scratch\audit-13.09.2026.07\run1\probe_t8_repro2.py` → committed_debris 3 denemeden 0./2.'si CRASH SystemError (toplam 6 denemede 3 çökme; `probe_t8_repro.py` 3/3). Deterministik değil, ~%50; iki modda da görüldü.
- **Beklenen:** Kurtarma journal'ı temizler/geri alır, asla istisna fırlatmaz. **Gözlenen:** iş başlangıcında traceback ile ölüm; kullanıcı sonraki çeviri denemesini tekrar denemek zorunda.
- **Etki:** Önceki iş çökmüşse kullanıcının bir sonraki işi ~%50 olasılıkla başlangıçta patlar; tekrar deneme sonunda kurtulur (journal silinene dek tekrar eder).
- **Testlerin kaçırdığı neden:** Mevcut testler journal durumlarını canlı aynı süreçte üretiyor (owner_pid = kendi PID); ölü sahipli journal + Windows os.kill tuhaflığı modellenmemiş.
- **Düzeltme planı:** Windows'ta `os.kill(pid, 0)` yerine `ctypes` `OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION)` + `GetExitCodeProcess != STILL_ACTIVE` (veya mevcut `test_ytdlp_update.py`'deki güvenli sonda); ayrıca `_pid_is_alive` çağrısını `try/except BaseException` ile sarmak.
- **Regresyon kabulü:** `probe_t8_repro2.py` senaryoları (after_staging/mid_commit/committed_debris × 3) 10/10 çökmesiz kurtarma; journal temizlenmiş.

### T8-B2 (P3) — **DÜZELTİLDİ VE DOĞRULANDI** — Ölü PID canlı sayılıyordu
- **Konum:** `backend/pipeline_control.py:92-103` (`os.kill(pid, 0)` Windows'ta yeni ölmüş PID için istisna vermeyebilir).
- **Yeniden üretim:** `backend\venv\Scripts\python.exe scratch\audit-13.09.2026.07\run1\probe_t8_repro.py` → `_pid_is_alive(ölü)` **10/10 True** (0.05 sn ve 1.5 sn beklemede de).
- **Beklenen/Gözlenen:** ölü → False. Gözlenen: True → `recover_output_transactions` journal'ı atlıyor (`continue`) → karışık nesil + artık dosyalar diskte kalıyor.
- **Etki:** Kesilen işin yarım çıktı seti kullanıcıda süresiz görünür (PID yeniden kullanılana kadar her koşu atlar).
- **Düzeltme planı:** T8-B1 ile aynı sonda; regresyon: ölü-PID tespiti 10/10 False.

### T8-B3 (P3) — **DÜZELTİLDİ VE DOĞRULANDI** — rollback kilitli hedefte yarıda kalıyordu
- **Konum:** `backend/pipeline_control.py:217-240` `rollback` — `self._replace(backup, final)` PermissionError'u yakalamıyor; `commit`'in `except BaseException: self.rollback()` zinciri kırılıyor.
- **Yeniden üretim:** `probe_tx_t8.py` S6 → `run1/probe_tx_t8.out.txt`: kilitli `c.srt` üzerinde commit → a/b GEN2, c GEN1, 5 artık dosya; `recover_output_transactions` (kilit kalkınca) tek eski nesile döndürdü (doğrulandı).
- **Etki:** Başka program tutarken yazım → karışık eski/yeni altyazı dosyaları + iş hata olarak görünür; kilit kalkıp bir sonraki koşu kurtarana dek sürer.
- **Düzeltme planı:** rollback döngüsünde giriş-başına try/except; başarısız girişleri journal'da bırakıp kalanları geri almaya devam; regresyon: S6 senaryosu (kilit + kurtarma tek nesil).

### T7-B1 (P4) — **DÜZELTİLDİ VE DOĞRULANDI** — Tamamlanma sonrası eski .partial dosyaları silinmiyordu
- **Konum:** `backend/transcribe.py:6395-6530` (partial çıktı yazımı; temizlik kodu yok — grep `partial.*unlink` boş).
- **Yeniden üretim:** `probe_cli_t7.py` faz3 → `run1/probe_cli_t7.out.txt`: tamamlama koşusu `src.tr.srt` yazdı; `src.tr.partial.srt`, `src.tr.partial.srt.meta.json`, `src.dual.partial.srt` diskte kaldı.
- **Etki:** Çıktı klasöründe hem nihai hem eski "kurtarma" dosyası; kullanıcı bayat partial'ı açabilir.
- **Düzeltme planı:** Başarılı `complete` transaction'ında aynı stem+hedef için `*.partial.*` artefaktlarını (yalnız bu işe ait sourceHash meta'sı doğrulanarak) sil; regresyon: faz3 senaryosu artık yok.

### T3-B1 (P4, araç) — **DÜZELTİLDİ VE DOĞRULANDI** — Kalite aracının "eksik çeviri" sayımı konsolide çeviride şişiyordu
- **Konum:** `src/subtitle-pair-quality.js:26-41` `alignByTimestamp` — eşleşme yalnız birebir zaman anahtarıyla; S10E11'de 216 hedef 536 kaynak cue'u kapsıyor.
- **Kanıt:** Araç 284 eksik/86 fazla bildirirken bağımsız örtüşme ölçümü 8 örtüşmeyen/0 fazla (çakışma ≥0.15 sn). "missingTarget 284" çoğunlukla tek hedef cue'unda birleşmiş çevriler.
- **Etki:** Aracın raporu içeriği kayıp gibi gösteriyor; gerçek içerik kaybı T4 örneklemindeki yankı/yanlış dağıtım vakalarıdır.
- **Düzeltme planı:** Eşleşmeyi örtüşme-oranlı yap veya "missingTarget"ı "birebir eşleşmeyen (konsolide olabilir)" olarak yeniden adlandır + örtüşme bazlı ayrı sinyal; regresyon: S10E11 ölçümü bu raporun bağımsız sayılarıyla.

### T3-B2 (P4, veri) — **DOĞRULANDI; KOD DEĞİŞİKLİĞİ REDDEDİLDİ** — S01E13 korpus çifti diskte 1 cue'a düşmüş
- **Kanıt:** `probe_srt_t3.py` + `npm run audit:subtitle-corpus`: kaynak/hedef 83/69 karakter, 1/1 cue; manifest `expectedMinSourceCues/Target: 175`. Araç doğru biçimde `sourceBelowMinimum/targetBelowMinimum (actual 1, expectedMinimum 175)` raporluyor ve başarısız çıkıyor.
- **Etki:** Korpus ölçümü bu çift için anlamsız; manifest beklenenleri dosyalar silinip değiştirilse de yakalamaya devam ediyor (olumlu), ancak çift denetim kapsamı dışı.
- **Düzeltme:** Dosyaları geri getir veya manifestte bu çifti tek-cue beklentisiyle güncelle.

### T12-B1 (P4) — **DÜZELTİLDİ VE DOĞRULANDI** — Özel ad imzası büyük/küçük harf asimetrisi
- **Konum:** `backend/translation_memory.py:74-83` `_proper_name_signature` — büyük harf testi orijinal üzerinde, karşılaştırma casefold edilmiş; cümle başı "The" imzaya girer, küçük "the" girmez.
- **Kanıt:** `probe_tm_t12.py`: büyük/küçük harf varyantlarında 6/8 ret; yazım hatası varyantlarında 5/8 ret (kısa kelime eşiği + modal çıpası); **yanlış yeniden kullanım 0/51**.
- **Etki:** Yalnız verimlilik kaybı (gereksiz API çağrısı); güvenlik yönü doğru.
- **Düzeltme planı:** Cümle başı büyük harfini imzadan muaf tut (ilk token yalnız sözcük uzunluğu > 1 ve hepsi büyük değilse özel ad say); regresyon: büyük/küçük harf varyantlarında yeniden kullanım ≥ 6/8, karşıt örneklerde hâlâ 0.

## 5. Önceden düzeltilmiş bulgular ve güncel doğrulama sonucu

| Bulgu | Durum |
|---|---|
| BUG-01 tanı paketinde yerel yol sızıntısı | **Düzeltildi, davranışsal olarak doğrulandı** (`t1-prevbugs.out.txt`: `C:\Users\K\Videos\gizli.srt` → `[yol gizlendi]`) |
| BUG-02 URL userinfo sızıntısı | **Düzeltildi, doğrulandı** (`https://user:pass123@…` → `https://example.com/path`) |
| BUG-03 Bearer iki-token sızıntısı | **Düzeltildi, doğrulandı** (`authorization: Bearer eyJ…` → `authorization=[gizlendi]`) |
| M3/T2: loopCue sayacı mutasyonu yakalanıyor | Doğrulandı (mutasyon testi exit 1) |
| M1/M2: yol/userinfo sansür mutasyonları yakalanıyor | Doğrulandı |
| M4: partial-status mutasyonu yakalanıyor | Doğrulandı |
| M5: MPEGTS rollover mutasyonu yakalanıyor | Doğrulandı |

## 6. Doğrulanamayan riskler ve açık kalan alanlar

| Alan | Durum | Kapanış koşulu |
|---|---|---|
| T5/T6 renderer DOM yarışları (yeniden yükleme/temizleme/geç sonuç; çift done, exit→done sırası, kuyrukta tek başlama) | Kod düzeyi + modül testleri + arka uç olay zinciri doğrulandı; DOM düzeyi yürütülemedi | renderer.js'i yükleyen tam DOM stub'lı vm/Electron harness (izole userData): senaryolar görev metnindeki 5+6 sıra listesiyle |
| T15 diarization (pyannote) span ofset zinciri | Kelime/segment ofset zinciri doğrulandı; konuşmacı span'ları davranışsal sürülmedi | Sahte diarization span'larıyla gerçek atama yolu (pyannote kurulu) |
| T16 canlı playlist yenileme, çok dönemli DASH, MPEGTS rollover'un HTTP uçtan uca akışı | Parser/fixture düzeyinde mevcut testler + kompakt HTTP fetch zinciri doğrulandı | Görev metnindeki 9 senaryonun tamamı tek sunucu düzeneklerinde |
| T18 izole Electron yeni medya/görünüm özellikleri (hız sıfırlama yarışı, video DOM'dan çıkarma/geri takma, ses normalleştirme, CSS filtresi, AudioContext, link hints iframe/shadow DOM, Dark Reader geç sonuç, ekran yakalama zoom/DPR) | **Yürütülemedi** — alt-denetçi altyapısı 16/16 ölü; ana oturum bütçesi tükendi | `tests/electron-browser-trusted-bridge.smoke.js` deseniyle izole userData'lı Electron düzenek; her alt alan ayrı kanıt |
| T19 tanı paketinin main.js genelinde gerçek dışa aktarım koşusu | Sansür katmanı + snapshot çağrı noktaları doğrulandı; tam zincir Electron gerektirir | `browser:diagnostics:export` IPC'sinin izole Electron'ta sentetik hassas veriyle sürülmesi |
| Alt-ajan altyapısı | 16/16 deneme "request budget is exhausted" ile ilk araç çağrısından önce öldü (ham hata `run1` alt-denetçi raporlarında) | Paralel ajan bütçesi olmadan tek-ajan sıralı yürütme |

## 7. Ölçümlere dayanan geliştirme önerileri

1. **`_pid_is_alive` sondasının değiştirilmesi (T8-B1/B2):** Tek değişiklik, iki P2/P3 bulguyu kapatır; `test_ytdlp_update.py`'deki güvenilir Windows sonda örüntüleri mevcut.
2. **rollback dayanıklılığı (T8-B3):** Giriş başına hata yutma + kalan girişleri geri alma; ~10 satır.
3. **Tamamlanmada partial temizliği (T7-B1):** OutputTransaction commit sonrası aynı işe ait `*.partial.*` artefaktlarını temizle.
4. **Kalite aracı eşleşme yöntemi (T3-B1):** Örtüşme bazlı ikinci sinyal; mevcut exact-key eşleşmesi korunsun.
5. **Özel ad imzası normalizasyonu (T12-B1):** Cümle başı büyük harf muafiyeti.
6. **Önceki tur problarının assert'e taşınması:** 11/12 prob print-tabanlı; kalıcı test korpusuna assert'li biçimde taşınmalı (bu turun tüm probları bu desende yazıldı).
7. **S01E13 korpus çiftinin onarılması (T3-B2).**

## 8. Uygulama ve düzeltme planı (bulgu bazında)

| Bulgu | Değişecek dosya/fonksiyon | Korunacak sözleşme | Bağımlılık sırası | Regresyon senaryosu | Kabul koşulu | Migration/rollback | Yan etki riski |
|---|---|---|---|---|---|---|---|
| T8-B1 | `backend/pipeline_control.py` `_pid_is_alive` | Journal formatı ve `recover_output_transactions` dönüş sözleşmesi değişmez | Tek commit; T8-B2 ile aynı | `probe_t8_repro2.py` senaryoları | 10/10 çökmesiz kurtarma + journal temiz | Geri al: eski fonksiyon | Çok düşük (yalnız liveness sondası) |
| T8-B2 | aynı | aynı | T8-B1 ile birlikte | ölü-PID 10/10 False | aynı | aynı | düşük |
| T8-B3 | `backend/pipeline_control.py` `rollback` | "commit öncesi eski nesil görünür" garantisi | T8-B1 sonrası | `probe_tx_t8.py` S6 | kilit senaryosunda kalan girişler de geri alınır | — | düşük |
| T7-B1 | `backend/transcribe.py` (translate_existing_subtitle sonunda) | Kullanıcı düzenlemesi/farklı iş dosyaları korunmalı (sourceHash doğrulaması) | bağımsız | `probe_cli_t7.py` faz3 | faz3 sonrası `*.partial.*` artığı yok | — | orta: yalnız aynı işe ait partial silinmeli |
| T3-B1 | `src/subtitle-pair-quality.js` `alignByTimestamp`/rapor | Mevcut issue anahtarları ve CLI çıktısı geriye uyumlu | bağımsız | S10E11 çift ölçümü | örtüşme bazlı sayı bu raporunkiyle | — | düşük |
| T12-B1 | `backend/translation_memory.py` `_proper_name_signature` | fuzzy_semantically_compatible'in tüm güvenlik çıpaları | bağımsız | `probe_tm_t12.py` | harf varyantı ≥6/8 kabul, karşıt 0/51 | — | düşük |
| T3-B2 | `quality/subtitle-quality-corpus.json` (veri) | — | — | korpus aracı | S01E13 ya tam ya bilinçli tek-cue | — | yok |

## Ayrıntılı bulgu, düzeltme, ret gerekçesi ve doğrulama dökümü

### Çalıştırılan komutlar ve çıkış kodları (ham çıktılar `scratch/audit-13.09.2026.07/run1/` altında)

| Komut | Çıkış | Ham çıktı | Assertion |
|---|---|---|---|
| `node -e "…redactBrowserDiagnosticsText/redactCaptureUrl…"` (T1) | 0 | `t1-prevbugs.out.txt` | 3 |
| `node scratch\…\probe_llm.py` (T9/T10/T11) | 0 | `probe_llm.out.txt` + `probe_llm_summary.json` | 52 |
| `backend\venv\Scripts\python.exe scratch\…\probe_cli_t7.py` (T7) | 0 | `probe_cli_t7.out.txt` + `probe_cli_t7_summary.json` | 26 |
| `backend\venv\Scripts\python.exe scratch\…\probe_tx_t8.py` (T8) | 0 | `probe_tx_t8.out.txt` + `probe_tx_t8_summary.json` | 34 |
| `backend\venv\Scripts\python.exe scratch\…\probe_t8_repro.py` | 0 | `probe_t8_repro.out.txt` | 23 ölçüm |
| `backend\venv\Scripts\python.exe scratch\…\probe_t8_repro2.py` | 0 | konsol çıktısı (raporda alıntı) | 6 koşu |
| `backend\venv\Scripts\python.exe scratch\…\probe_tm_t12.py` (T12) | 0 | `probe_tm_t12.out.txt` + `t12_summary.json` | 78 örnek kaydı |
| `backend\venv\Scripts\python.exe scratch\…\probe_series_t13.py` (T13) | 0 | `probe_series_t13.out.txt` + `t13_summary.json` | 22 |
| `backend\venv\Scripts\python.exe scratch\…\probe_engine_t14_t15.py` (T14/T15) | 0 | `probe_engine_t14_t15.out.txt` + `t14t15_summary.json` | 37 |
| `backend\venv\Scripts\python.exe scratch\…\probe_srt_t3.py` (T3) | 1 (S01E13 manifest beklentisi) | `probe_srt_t3.out.txt` + `t3_srt_summary.json` | 6 |
| `node tools\audit-subtitle-pair.js <S10E11> --json` | 0 | `t3_tool_s10e11.json` | — |
| `npm run audit:subtitle-corpus` | 2 (S01E13 below-minimum) | `t3_corpus_tool.out.txt` | — |
| `node scratch\…\probe_mutasyon_t2.js` (T2) | 0 | `probe_mutasyon_t2.out.txt` + `mut/` | 5 mutasyon + 5 kontrol |
| `node scratch\…\probe_playback_t17.js` (T17) | 0 | `probe_playback_t17.out.txt` | 15 |
| `node scratch\…\probe_hls_t16.js` (T16) | 0 | `probe_hls_t16.out.txt` | 10 |
| `npm test` (T20) | 0 | `npm-test.out.txt` | proje paketi (tümü geçti) |
| `npm run test:electron-bridge` (T20) | 0 | `bridge.out.txt` | smoke geçti |
| `npm run test:soak` (T20) | 0 | `soak.out.txt` | 400+20+50 çevrim, tüm bütçeler OK |

### Mutasyon sonuçları (izole kopya; üretim dosyaları değişmedi)

| Mutasyon | Bozulan kontrol | Test | Çıkış |
|---|---|---|---|
| M1 yerel yol regex kaldırıldı | `redactBrowserDiagnosticsText` | `browser-diagnostics-export.test.js` | 1 (yakalandı) |
| M2 userinfo temizliği kaldırıldı | `redactCaptureUrl` | `browser-adapters.test.js` | 1 |
| M3 loopCue sayacı sıfırlandı | `playbackLearningAction` | `player-ui.test.js` | 1 |
| M4 partial-status hep 'complete' | `cleanDescriptor` | `subtitle-output-contract.test.js` | 1 |
| M5 MPEGTS rollover eşiği ∞ | `unwrapMpegTs` | `browser-subtitles.test.js` | 1 |

Kontrol: 5 orijinal test mutasyonsuz kopyada 0 ile geçti (aynı sürüm).

### Denetimin kendi hataları (dürüstlük kaydı)

Probların ilk koşularında 40'tan fazla FAIL üretildi; büyük çoğunluğu **prob hatası** çıktı ve kaynak okunarak ayıklandı: boş içerik üreticisi (`src` yerine `source` alanı), Windows `write_text` satır sonu ikiye katlaması (SRT bozulması), konumsal ID eşlemesi (`i` pozisyonu), yanlış senaryo zamanları (minGap/örnekleme), S01E13 beklentisi (manifest), aynı metinli güçlü/zayıf çiftler, büyük/küçük harf araması, T17'de seek-sonrası yeniden oynatma modeli. Ürün hatası olarak kalanlar Bölüm 4'te 8 alanıyla listelendi; hiçbir prob hatası ürün bulgusuna dönüştürülmedi.

### Açık kalan kabul koşulları

Bölüm 6'daki tabloda görev bazında listelenmiştir; en geniş açık alanlar T18 (tamamı), T5/T6 (renderer DOM düzeyi) ve T19'un Electron dışa aktarım koşusudur. Sahte sağlayıcı/port kullanımı: 47731 (T16), 47732 (T7), 47733 (T9-T11). Soak ve bridge kendi geçici userData'larında koştu; ana uygulama ve diğer süreçler sonlandırılmadı.

## 9. Codex doğrulama ve düzeltme uzlaştırması — 13.09.2026

Bu bölüm raporun ilk denetim anını silmez; güncel çalışma ağacında kaynak ve çalıştırılabilir kanıtla yapılan düzeltmelerin yetkili son durumudur. Kullanıcının Downloads altındaki SRT dosyaları değiştirilmedi.

### 9.1 Yirmi görevin son durumu

| # | Son durum | Son kanıt / karar |
|---|---|---|
| 1 | **DOĞRULANDI** | Önceki üç gizlilik düzeltmesi kalıcı testte; ayrıca bütün snapshot için derin sansür eklendi. |
| 2 | **DOĞRULANDI, M3 İDDİASI REDDEDİLDİ** | LoopCue mutasyonu mevcut `security-media-learning.test.js` doğrudan politika testinde exit 1 üretiyor. Yalnız `player-ui.test.js` çalıştıran mutasyon koşucusu yanlış test seçmiş. M7 bayat olay boşluğu gerçekti ve doğrudan test eklendi. |
| 3 | **KOD DÜZELTİLDİ; VERİ EKSİĞİ DOĞRULANDI** | Örtüşme ≥0,15 sn kapsaması eklendi. S10E11: 486/288, exact 202 fakat kapsanan kaynak 479 ve hedef 288; gerçek engeller 57. S01E13 tek cue olduğu için bağımsız kalite kapsamı yok. |
| 4 | **DOĞRULANDI, KAYNAK DOSYA DEĞİŞTİRİLMEDİ** | Kaynak yankısı/yanlış dağıtım içerik kusurudur; bu tur kodu düzeltti, kullanıcının altyazısını otomatik yeniden yazmadı. |
| 5 | **DOĞRULANDI (GERÇEK ELECTRON)** | Video değişirken bekleyen `attachCompletedJobSubtitles` eski generation sonucu olarak reddedildi; yeni `mediaKey` korundu. |
| 6 | **DOĞRULANDI (GERÇEK MAIN→RENDERER IPC)** | Bayat `done/exit` yeni işi değiştirmedi; `done→done→exit` kuyruk öğesini tek `done` terminaline ve tek kapanışa getirdi. |
| 7 | **DÜZELTİLDİ** | Complete yayın sonrası yalnız aynı `sourceHash`/hedef dile ait partial+meta+dual artefaktları temizleniyor; farklı hash'li dosya korunuyor. |
| 8 | **DÜZELTİLDİ** | Windows `OpenProcess/GetExitCodeProcess` sondası; ölü sahipli journal kurtarma; kilitli rollback journal'ı koruyup kalan girişlere devam ediyor. 4 kalıcı transaction testi geçti. |
| 9 | **DOĞRULANDI** | Retry/iptal sözleşmesinde yeni kusur bulunmadı; önceki yerel sağlayıcı kanıtı tam paketle yeniden geçti. |
| 10 | **DOĞRULANDI** | Kimlik/parça sözleşmesi testleri tam pakette geçti. |
| 11 | **DOĞRULANDI** | Bağlam ve tam cache izolasyonu testleri tam pakette geçti. |
| 12 | **DÜZELTİLDİ + DAR RET** | Aynı cümlenin ilk sözcüğündeki case farkı özel ad sayılmıyor; Alice/Alicia ve Ankara/İzmir hâlâ reddediliyor. Sonuç 22/27 güvenli eşleşme, 0/51 yanlış kullanım. Beş kısa yazım hatasını kabul edecek gevşetme anlam güvenliği nedeniyle reddedildi. |
| 13 | **DOĞRULANDI** | Dizi kanonu/kalıcılık testleri tam pakette geçti; last-writer-wins gözlemi yeni kesin hata kanıtı değildir. |
| 14 | **DOĞRULANDI** | SDH/halüsinasyon zinciri ve kalıcı Python testleri geçti. |
| 15 | **DOĞRULANDI** | `offset_diarization_spans` üretim yoluna alındı; 10,5 sn clip ofsetinin span'lara bir kez uygulanıp gerçek `assign_speakers` sonucunu koruduğu test edildi. |
| 16 | **DÜZELTİLDİ VE GENİŞLETİLDİ** | Gerçek localhost fetch ile kayan canlı HLS penceresi, ortak segment tekilleştirme, 33-bit MPEGTS devri, gecikme/404/403/discontinuity ve iki dönemli DASH çalıştı. DASH `Period@start/duration` zaman tabanı parsera eklendi. |
| 17 | **DOĞRULANDI** | Politika entegrasyonu geçti; loopCue test boşluğu iddiası reddedildi. |
| 18 | **DOĞRULANDI (GERÇEK ELECTRON)** | Hız fightback, preservesPitch, CSS filtre, gerçek AudioContext+DynamicsCompressor, shadow host sök/tak, aynı MediaElementSource'un yeniden kullanımı, ana/shadow/same-origin/opaque iframe link hints, Dark Reader isolated world temizliği ve %125 zoom+DPR video kırpması geçti. Sök/tak sırasında host'un kendi shadowRoot'unun taranmaması ayrıca bulunup düzeltildi. |
| 19 | **DÜZELTİLDİ VE DOĞRULANDI (GERÇEK IPC)** | `browser:diagnostics:export` gerçek IPC ile JSON yazdı; sentetik userinfo, imzalı URL, Bearer, yerel yol ve cue metni dosyada kalmadı. `acquisition` ve `coverage.streamKey` derin sansür açığı düzeltildi. FTS generation koruması mevcut testte geçti. |
| 20 | **DOĞRULANDI** | `npm test` exit 0; Electron bridge exit 0; tam soak 400/400 + 20 ısınma + 50/50 hibernasyon GEÇTİ; sözdizimi kontrolleri ve `git diff --check` geçti. |

### 9.2 Ayrıntılı bulgu, düzeltme, ret gerekçesi ve doğrulama dökümü

- **T8-B1/T8-B2:** `backend/pipeline_control.py::_pid_is_alive` Windows'ta `os.kill(pid, 0)` yerine salt-okunur işlem tutamacı ve çıkış kodu kullanıyor. Erişim reddi belirsizliği canlı kabul edilerek journal yanlış geri alınmıyor; sonda istisnası kurtarmayı çökertmiyor. Raporun eski `probe_t8_repro2.py` betiğindeki `pidcheck=RAISE:AttributeError` ürün sonucu değildir: `subprocess.run()` dönüşü `CompletedProcess` olup `pid` alanı yoktur. Aynı koşuda 6/6 journal yine kurtarıldı; kalıcı test gerçek `Popen.pid` ile 20/20 ölü sonucu doğruladı.
- **T8-B3:** Rollback her girişte `OSError`ı ayrı ele alıyor; kilitli girişin backup/journal'ı korunurken diğer girişler geri alınabiliyor. Uyarı callback'i bozulsa bile asıl commit hatası maskelenmiyor.
- **T7-B1:** Temizlik wildcard kullanmıyor. Dosya adı kökü, hedef dil, izinli format, meta `sourceHash`, `targetLanguage` ve varsa `sourceId` birlikte doğrulanmadan silme yapılmıyor.
- **T3-B1:** Exact timestamp ölçümü geriye uyumluluk için `matchedCues/nonExactTimestamp` olarak korundu; kayıp/fazla kararı örtüşme kapsamasından üretiliyor. 100 ms tesadüfi temas 150 ms eşiğini geçemiyor.
- **T3-B2 ve D1/D2 ret gerekçesi:** Eksik telifli SRT içeriği kaynak olmadan kodla uydurulamaz; manifest minimumlarını düşürmek kanıtı saklayacağı için reddedildi. Kullanıcı tam dosyayı geri getirirse korpus yeniden çalıştırılmalıdır.
- **T12-B1:** Yalnız iki cümlenin casefold edilmiş ilk semantik token'ı aynıysa ilk-token kapitalizasyonu muaf. Gerçek özel ad değişiklikleri güvenlik çıpasında kalıyor. Kısa typo eşiğini topluca düşürmek yanlış fuzzy reuse riski nedeniyle reddedildi.
- **Yeni B4 — shadow host yeniden takma:** `browser-media-controller.js::scan` doğrudan eklenen host'un kendi `shadowRoot`unu taramıyordu. Gerçek Electron testinde yeniden takılan video `null` kaldı. Host shadow kökü gözlenip taranıyor; birim test 26/26 ve Electron testi geçti.
- **Yeni B5 — DASH Period zamanı:** İkinci Period URL olarak yakalansa da cue ofseti 0 kalıyordu. ISO-8601 `Period@start/duration` çözümü ve çıkarımsal dönem başlangıcı matcher'a taşındı; iki dönem ofsetleri 0 ve 4 sn oldu.
- **Yeni B6 — derin tanı paketi sızıntısı:** `acquisition` ve `coverage` ham iç içe nesnelerdi. Bütün snapshot özyinelemeli sır/yol/URL sansüründen, sonra cue metni sansüründen geçiyor. Gerçek IPC çıktısı sentetik sırların hiçbirini içermedi.

### 9.3 Son kabul komutları

| Komut | Sonuç |
|---|---|
| `npm test` | exit 0, **Tüm testler geçti**; `backend/test_transcribe.py` 175/175 |
| `node tests/electron-subtitle-output-smoke.js` | exit 0; yükleme yarışı, main→renderer terminal sırası ve gerçek tanı IPC geçti |
| `npm run test:electron-bridge` | exit 0; gerçek medya/shadow/iframe/link hints/Dark Reader/zoom-DPR geçti |
| `npm run test:soak` | exit 0; 400/400 yakalama, 50/50 hibernasyon, 24 kaynak bütçesi geçti |
| `python -m unittest test_pipeline_control test_translation_memory` | 9/9 geçti |
| Hedefli Node paketleri | subtitle-pair, queue lifecycle 16/16, media controller 26/26, diagnostics, stream fixtures geçti |
| Python/Node sözdizimi + `git diff --check` | geçti |
